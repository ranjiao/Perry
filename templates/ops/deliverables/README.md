# Deliverables

One file per piece of work. Required sections: **Summary**, **Detail**,
**Sources** — plus a `Sign-off: <name>, YYYY-MM-DD` line before anything goes
out.

Check with `bin/deliverable-lint`.
