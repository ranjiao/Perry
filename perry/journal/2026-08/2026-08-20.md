# 2026-08-20

## Status changes

- [TASK-105] in_progress → done · closed · evidence: `evidence/2026-08/TASK-105-v3-verification.md` · verification: V3
- [TASK-093] in_progress → done · closed · evidence: `evidence/2026-08/TASK-093-final-v4-review.md` · verification: V4
- [TASK-106] not_started → in_progress · started
- [TASK-106] in_progress → done · closed · evidence: `evidence/2026-08/TASK-106-final-v4-review.md` · verification: V4
- [TASK-092] next action · Mint the OKR and config stores plus their renderers, mirroring the tasks.jsonl -> BOARD.md projection landed by TASK-088/089/090.
- [TASK-047] blocked → not_started
- [TASK-040] blocked → not_started
- [TASK-102] rung · — → V4
- [TASK-092] next action · Mint the OKR and config stores plus their renderers, reusing the store-and-projection pattern already in bin/perry_store.py.
- [TASK-047] next action · Re-measure the two blockers, then flip the conformance gate to enforce.
- [TASK-040] next action · Risks still read from a markdown table with empty opened/cleared; make them records in the store.
- [USER-004] — → pending · When migration encounters a file the user has chmod-ed read-only, should it refuse to touch that file, or migrate it and name the overridden permission in the plan? Today it migrates silently: write_atomic renames over the target, and a rename needs write permission on the directory, not on the file. · blocks: TASK-079
- [TASK-077] evidence · — → evidence/2026-08/TASK-077-context.md
- [TASK-077] next action · Run the finance-shaped role end to end on a copy of gimegime-pmo, then write the extraction report.
- [TASK-079] evidence · — → evidence/2026-08/TASK-079-context.md
- [TASK-079] next action · List the overridden read-only bit in the migration plan; the refuse-vs-report policy is pending USER-004.
- [TASK-070] evidence · — → evidence/2026-08/TASK-070-context.md
- [TASK-070] next action · Start with journal/ and evidence/ retention, which do not depend on the store; log rotation follows it.
- [TASK-066] evidence · — → evidence/2026-08/TASK-066-context.md
- [TASK-066] next action · Re-size the split after the markdown reader, row renderer and cell escaping are gone.
- [TASK-079] not_started → in_progress · started
- [TASK-079] next action · dispatched 13:27 via claude-subagent; awaiting completion
- [TASK-086] not_started → in_progress · started
- [TASK-086] next action · dispatched 13:27 via codex; awaiting completion
- [TASK-047] not_started → in_progress · started
- [TASK-047] next action · dispatched 13:35 via claude-subagent; awaiting completion
- [TASK-079] evidence · evidence/2026-08/TASK-079-context.md → evidence/2026-08/TASK-079-dispatch-2026-08-20-1345.md
- [TASK-079] in_progress → review
- [TASK-086] evidence · — → evidence/2026-08/TASK-086-dispatch-2026-08-20-1346.md
- [TASK-086] in_progress → review · wrong-base: worktree branched from main (f3c6712), 279 commits behind feat/work-modes; verification measured the wrong tree
- [TASK-086] review → in_progress
- [TASK-047] evidence · — → evidence/2026-08/TASK-047-dispatch-2026-08-20-1416.md
- [TASK-047] in_progress → review
- [TASK-086] evidence · evidence/2026-08/TASK-086-dispatch-2026-08-20-1346.md → evidence/2026-08/TASK-086-dispatch-2026-08-20-1429.md
- [TASK-086] in_progress → review
- [TASK-086] next action · user confirms the strict-flag contract change (the namespace warning is no longer promoted to a failure), then merge PR #9; PR #7 is superseded
- [TASK-107] — → not_started · the dispatch safety gate matches its fragments as bare substrings, so ordinary English trips it · owner: Coding Agent · priority: P1
- [TASK-107] not_started → in_progress · started
- [TASK-107] in_progress → review
- [TASK-107] evidence · — → evidence/2026-08/TASK-107-spec.md
- [TASK-108] — → not_started · LOAD-03 counts prose about a decision, so documenting an open question makes the count go up · owner: Coding Agent · priority: P1
- [TASK-108] not_started → in_progress · started
- [TASK-047] next action · PR #8 merged and the gate enforces on this branch; awaiting the user's V5 sign-off text (name, date, what was checked)
- [TASK-085] not_started → in_progress · started
- [TASK-047] review → done · closed · evidence: `evidence/2026-08/TASK-047-dispatch-2026-08-20-1416.md` · verification: V5
- [TASK-108] evidence · — → evidence/2026-08/TASK-108-dispatch-2026-08-20-1547.md
- [TASK-108] in_progress → review
- [TASK-109] — → not_started · a V5 sign-off is composed by selection from measured facts, not authored from memory · owner: Coding Agent · priority: P1
- [TASK-109] not_started → in_progress · started
- [TASK-085] evidence · — → evidence/2026-08/TASK-085-dispatch-2026-08-20-1607.md
- [TASK-085] in_progress → review
- [TASK-092] not_started → in_progress · started
- [TASK-109] evidence · — → evidence/2026-08/TASK-109-dispatch-2026-08-20-1623.md
- [TASK-109] in_progress → review
- [TASK-070] P2 → P1 · reprioritized
- [TASK-110] — → not_started · measure what Perry state costs and propose a retention policy, deleting nothing · owner: Coding Agent · priority: P1
- [TASK-070] next action · holds the execute half only; TASK-110 measures and proposes first, and nothing is deleted until the user approves that proposal
- [TASK-110] not_started → in_progress · started
- [TASK-100] not_started → in_progress · started
- [USER-003] pending → answered · Already decided in ADR-007 decision 2 on 2026-08-19 (Deciders: Ran Jiao): BOARD.md becomes rendered output and a hand edit becomes drift. This row was minted the same day and duplicates that decision; recorded here so the queue matches the record. TASK-038 is unblocked, and still needs its V5 signature, which is a different act from this permission.
- [TASK-038] blocked → review
- [TASK-111] — → not_started · a test reads two files outside the repository, so it is green here and red on CI forever · owner: Coding Agent · priority: P1
- [TASK-111] next action · CI red on every run since the corpus silently shrinks; see tests/test_goals_writer.py ELSEWHERE
- [TASK-107] review → done · closed · evidence: `evidence/2026-08/TASK-107-spec.md` · verification: V5 · signed off: Ran Jiao 2026-08-20 — 2 checked, 5 accepted on report
- [TASK-085] review → done · closed · evidence: `evidence/2026-08/TASK-085-dispatch-2026-08-20-1607.md` · verification: V2
- [TASK-086] review → done · closed · evidence: `evidence/2026-08/TASK-086-dispatch-2026-08-20-1429.md` · verification: V2
- [TASK-038] review → done · closed · evidence: `evidence/2026-08/TASK-038-v5-signoff-request.md` · verification: V5 · signed off: Ran Jiao 2026-08-20 — 10 checked
- [TASK-109] review → done · closed · evidence: `evidence/2026-08/TASK-109-dispatch-2026-08-20-1623.md` · verification: V5 · signed off: Ran Jiao 2026-08-20 — 2 checked, 8 accepted on report
- [TASK-112] — → not_started · the sign-off drafting guard cannot describe itself, so a true statement about it is refused · owner: Coding Agent · priority: P2
- [TASK-092] evidence · — → evidence/2026-08/TASK-092-dispatch-2026-08-20-1654.md
- [TASK-092] in_progress → review
- [TASK-113] — → not_started · three checks read the project living around them, so ordinary progress turns them red · owner: Coding Agent · priority: P1
- [TASK-114] — → not_started · aiMark reads Perry through the current contracts instead of a pin nine versions old · owner: Coding Agent · priority: P1
- [TASK-114] not_started → in_progress
- [TASK-114] evidence · — → evidence/2026-08/TASK-114-delegation-prompt.md
- [TASK-113] not_started → in_progress · started
- [TASK-079] review → done · closed · evidence: `evidence/2026-08/TASK-079-dispatch-2026-08-20-1345.md` · verification: V4
- [TASK-115] — → not_started · two guards on the read-only report have a hole beside them, found by mutation not by review · owner: Coding Agent · priority: P2
- [TASK-108] review → done · closed · evidence: `evidence/2026-08/TASK-108-dispatch-2026-08-20-1547.md` · verification: V4
- [TASK-116] — → not_started · the mention list is write-only, mislabelled, and two of its carve-outs are unpinned · owner: Coding Agent · priority: P2
- [TASK-110] evidence · — → evidence/2026-08/TASK-110-dispatch-2026-08-20-1725.md
- [TASK-110] in_progress → review
- [TASK-070] next action · decide the retention proposal in TASK-110's evidence; nothing is deleted until then
- [TASK-117] — → not_started · two tools disagree about whether the board has drifted when the event log is absent · owner: Coding Agent · priority: P2
- [TASK-118] — → not_started · the id minters read three sources and the canonical store is not one of them · owner: Coding Agent · priority: P2
- [TASK-115] not_started → in_progress · started
- [TASK-100] evidence · — → evidence/2026-08/TASK-100-dispatch-2026-08-20-1730.md
- [TASK-100] in_progress → review
- [TASK-092] next action · V4 review re-dispatched after the first reviewer stalled at 600s; PR #16 is merged and nine rows wait on this row closing
- [TASK-115] in_progress → done · closed · evidence: `evidence/2026-08/TASK-115-dispatch-2026-08-20-1751.md` · verification: V3
- [TASK-119] — → not_started · the linkage graph is documented as machine-written and no tool writes it · owner: Coding Agent · priority: P1
- [TASK-120] — → not_started · the linkage edges are read but never folded into KR progress · owner: Coding Agent · priority: P1
- [TASK-113] in_progress → done · closed · evidence: `evidence/2026-08/TASK-113-dispatch-2026-08-20-1813.md` · verification: V3
- [TASK-121] — → not_started · the sweep that found four more live-state assertions runs once and then is thrown away · owner: Coding Agent · priority: P1
- [TASK-092] review → done · closed · evidence: `evidence/2026-08/TASK-092-dispatch-2026-08-20-1654.md` · verification: V4
- [TASK-122] — → not_started · the repair path the tools advertise leaves the file needing a whitespace fix · owner: Coding Agent · priority: P1
- [TASK-123] — → not_started · the goals writer takes the file as truth and derives the store, which is the opposite direction from the KR · owner: Coding Agent · priority: P1
- [TASK-110] review → done · closed · evidence: `evidence/2026-08/TASK-110-dispatch-2026-08-20-1725.md` · verification: V3
- [TASK-070] not_started → dropped · reason: The measurement this row asked for was made (TASK-110) and it argues against the row. The whole proposal recovers 560,089 bytes — 1.4 days of growth at the measured 405,349 bytes/day — once, and nothing in it keeps pace. The cost is also not paid where the bytes are: perry-lint reads all 135 evidence files in 8ms and no agent loads evidence/ per turn. Rule B's selector decays; rule A deletes the argument history a project that reviews this hard depends on. The lever the measurement does point at is how much prose a V4 round emits, which is a product decision, not this row. Dropped on the evidence, not for lack of interest.
- [USER-004] pending → answered · Migrate and name the override; do not refuse. Reasoning recorded because the row was minted for it: refusing would block a whole migration for a reason unrelated to shape, and migration is the one road ADR-004 gives an undeclared project — a refusal there is the wall with no door that this project rejects everywhere else. The override is also reversible: the restore point carries the file's original bytes, verified under TASK-079. The 'at least name it' half of the ADR-004 posture is already satisfied by what shipped in PR #6, and TASK-115 added the guard that keeps that wording an observation rather than advice. The read-only bit stays a signal Perry reports and does not act on.
- [TASK-094] not_started → in_progress · started
- [TASK-111] not_started → in_progress · started
- [TASK-111] in_progress → review
- [TASK-111] evidence · — → evidence/2026-08/TASK-111-dispatch-2026-08-20-1930.md
- [TASK-124] — → not_started · the conformance corpus reads a project outside the repo and has no committed substitute · owner: Coding Agent · priority: P2
- [TASK-125] — → not_started · the Anti-Goals-inside-a-version insert case runs only on the author machine · owner: Coding Agent · priority: P2
- [TASK-094] in_progress → review
- [TASK-094] evidence · — → evidence/2026-08/TASK-094-dispatch-2026-08-20-1958.md
- [TASK-050] next action · unblocks on PR #20; re-scope to the adoption reader (parse_board/parse_okr with no store, parse_tracks, read_conformance, parse_phase/parse_decisions) — the fifth hardening round should be a mutation harness, not another regex
- [TASK-067] next action · unblocks on PR #20 but does not become empty: perry-decide still writes DECISIONS.md, perry-goals still writes OKR.md § Commitments in place, perry-migrate still rewrites a stranger files, and ragged-row is still the only catch
- [TASK-126] — → not_started · closing the dangling-id row requires writing the record that re-dangles it · owner: Coding Agent · priority: P1
- [TASK-126] depends on · — → TASK-112
- [TASK-127] — → not_started · the contract docs and the payloads they describe are never diffed against each other · owner: Coding Agent · priority: P1
- [TASK-128] — → not_started · the Agent entity has no writer because the signature that would give it one was never taken · owner: User · priority: P0
- [TASK-129] — → not_started · Agent is five strings that do not join, and role has never once been written · owner: Coding Agent · priority: P1
- [TASK-129] depends on · — → TASK-128
- [TASK-128] next action · user decision: which lane writes .perry/roles/, or the card stays owner user — then the V5 signature
- [TASK-129] next action · blocked on TASK-128; the writer cannot be chosen before the signature names the lane
- [TASK-127] evidence · — → evidence/2026-08/TASK-127-spec.md
- [TASK-127] not_started → in_progress · started
- [TASK-128] evidence · — → evidence/2026-08/TASK-128-proposal.md
- [TASK-128] not_started → review
- [TASK-128] review → done · closed · evidence: `evidence/2026-08/TASK-128-proposal.md` · verification: V5 · signed off: Ran Jiao 2026-08-20 — 8 checked
- [TASK-129] next action · unblocked: work owns .perry/agents.jsonl → .perry/roles/ as of the 2026-08-20 signature; needs a spec, then dispatch
- [TASK-127] in_progress → review
- [TASK-127] evidence · evidence/2026-08/TASK-127-spec.md → evidence/2026-08/TASK-127-dispatch-2026-08-20-2045.md
- [TASK-130] — → not_started · schema README says three contracts and pins goals at a version that shipped two ago · owner: Coding Agent · priority: P2
- [TASK-131] — → not_started · seventeen emitted contract keys are documented nowhere, and now there is a number for it · owner: Coding Agent · priority: P2
- [TASK-132] — → not_started · the parity check cannot see 23 keys because Perry own state leaves four collections empty · owner: Coding Agent · priority: P2
- [TASK-133] — → not_started · declare the first non-project track on Perry itself, and measure what a mixed spine costs · owner: User + Agent · priority: P1
- [TASK-133] not_started → in_progress · started
- [TASK-134] — → not_started · probe row for the TASK-133 track experiment · owner: PMO Agent · priority: P2
- [TASK-134] not_started → dropped · at stage: triaged · reason: probe row for the TASK-133 experiment; it proved a queue-track row carries a KR edge with no friction, and that is recorded in the evidence rather than left as a live row
- [TASK-135] — → not_started · a track can be declared but no existing row can be moved onto it · owner: Coding Agent · priority: P1
- [TASK-136] — → not_started · a queue track SLA is parsed, stored and never measured against anything · owner: Coding Agent · priority: P1
- [TASK-137] — → not_started · a new queue row is born in the second stage, not the first · owner: Coding Agent · priority: P2
- [TASK-133] evidence · — → evidence/2026-08/TASK-133-track-experiment.md
- [TASK-133] in_progress → review
- [TASK-138] — → not_started · stat -f succeeds on GNU with a different meaning, so the mtime fallback is never reached · owner: Coding Agent · priority: P0
- [TASK-138] not_started → in_progress · started
- [TASK-138] in_progress → done · closed · evidence: `evidence/2026-08/TASK-133-track-experiment.md` · verification: V3
- [TASK-126] evidence · — → evidence/2026-08/TASK-126-spec.md
- [TASK-126] next action · spec written; dispatch pre-flight REFUSED on the fragment `diagnose` — awaiting per-task clearance
- [TASK-139] — → not_started · a design back-reference lives in a cell the close path clears, so a finished design reports as never handed off · owner: Coding Agent · priority: P2
- [TASK-139] depends on · — → TASK-102

## Notes

- Triage (project mode, track `main`). Zero stale rows: every open row had been
  updated within 1–2 days, against P0≥3d / P1≥7d / P2≥14d. Zero drift, zero
  unrecorded rows across 407 events. `rows_with_no_status`, `off_enum_status`,
  `sections_skipped`, `depends_on_unknown` and `dependency_cycles` all empty.
- TASK-040 survives rather than being dropped. Its own cell said to drop it once
  TASK-089 landed "and risks are records". TASK-089 has landed; risks have not
  become records — `perry-task list` still reports `risks[].source: table` with
  `opened` and `cleared` empty on all three. Half a condition is not the
  condition, so the row was unblocked and re-scoped to the remaining half.
- TASK-092 is the structural blocker: it gates TASK-037, TASK-094, TASK-097 and
  TASK-102 directly, and TASK-050, TASK-067, TASK-095, TASK-099 transitively —
  9 of 20 open rows — and it was carrying no next step at all.
- Three rewritten `Next action` cells were rewritten a second time. The first
  version justified the change by naming the completed row that unblocked it,
  which re-fires `conformance.next_action_cites_closed` at every future triage.
  The cells now name only the next step; the history is in this journal and the
  event log, which is where the reference says it belongs.
- Not actioned this round, carried to the next batch: TASK-077's cell claims
  "BLOCKED ON phases B, D and E" while TASK-073, TASK-075 and TASK-076 are all
  done and the tool computes `startable: true`; five inflated cells (TASK-079 at
  1253 chars, TASK-070 994, TASK-077 589, TASK-066 467, TASK-040 414) whose
  detail belongs under `evidence/2026-08/`; and the `## Cadence` register, which
  holds no rows at all while `weekly/` has never been written.
- TASK-038 is reported by `blocked_without_dependency` because it waits on
  USER-003, which `depends_on` cannot express. The row is correct; the field
  cannot represent a task blocked on a User Input Queue row.
- Triage, second batch. Four inflated `Next action` cells moved off the board
  into `evidence/2026-08/<TASK-ID>-context.md`, each preserving the original
  text verbatim under a header naming why it moved: TASK-079 (1253 chars),
  TASK-070 (994), TASK-077 (589), TASK-066 (467). The board now carries Status +
  Next action + Evidence path for all four, and the longest open cell is 124
  characters.
- The suffix is `-context.md`, not the `-spec.md` that 29 files in this
  directory use. These are not specs in that template's shape — they are the
  reasoning a row accumulated before anyone started it — and filing them as
  specs would misreport what they are.
- USER-004 was minted out of TASK-079's cell. The cell said in its own words
  that the read-only-file policy was deliberately not decided, and an undecided
  question sitting in a Next action is invisible to the User Input Queue, which
  is the one surface a standup reports it on. TASK-079 stays `not_started`
  rather than `blocked`: making the migration plan list an overridden permission
  is required under either answer, so half the row can proceed.
- TASK-077's cell had claimed "BLOCKED ON phases B, D and E" since 2026-08-18.
  TASK-073, TASK-075 and TASK-076 are all done and the tool computes
  `startable: true` — the claim had been false for two days and nothing on the
  board could contradict it.
- Post-batch, every conformance array is empty for open rows:
  `next_action_cites_closed`, `evidence_not_found`, `rows_with_no_computable_age`
  and blank rungs all at zero, drift still 0 across 415 events. The one
  remaining entry is `blocked_without_dependency: [TASK-038]`, which is the
  field's limitation and not the row's.
- Still not addressed: `## Cadence` holds no rows and `weekly/` has never been
  written. Raised, not actioned — registering a recurrence is a decision about
  how the project runs, not a triage correction.
- TASK-086 was re-routed from `codex` to `claude-subagent` and re-dispatched.
  The project's codex quota is not to be spent on dispatch, and every automated
  dispatch goes to `claude-subagent` from now on. The spec's `Executor` line and
  its `## Changes` log record the swap; nothing about the task's scope changed.
- The stopped codex run had already executed `git switch -c
  coding/task-086-lint-ns01` in the main checkout, moving the session off
  `feat/work-modes` while the TASK-079 dispatch was live in the same working
  tree. Both branches pointed at the same commit and `bin/perry-lint` was
  untouched, so nothing was lost; the branch was deleted and `feat/work-modes`
  restored. The re-dispatch runs in its own git worktree, which is what the
  concurrent case needed from the start — `work/reference/dispatch.md` says a
  subagent shares the parent cwd and `git-boundaries.md` tells every dispatched
  agent to create a branch, and those two sentences are a collision whenever
  more than one dispatch is in flight.
- TASK-047 dispatched after the user explicitly cleared the high-stakes gate.
  The scan's match on `state-schema.json` was substantive, not a substring
  artifact — the task really does edit that file — so it was put to the user
  with the consequence stated: the flip changes upgrade behaviour for every
  existing Perry project, not only this repository. The clearance and its scope
  limit (the conformance default value only, no `claims[]` entry) are recorded
  in the spec's own `## Changes` block, and the dispatch prompt carries the same
  limit as a constraint the agent must honour.
- `PERRY_MAX_DISPATCH_SUBAGENT` raised from 2 to 3 at the user's instruction, to
  free the third concurrent slot once all dispatch was routed to a single
  executor. Passed per-invocation; it is not exported in this session's shell.
- TASK-086 re-dispatched on the correct base. The failed run's worktree came off
  `main` (f3c6712), 279 commits behind `feat/work-modes`, because the Agent
  tool's worktree isolation does not take a base and picked the default branch.
  The re-run uses a worktree created explicitly with `git worktree add -b
  coding/task-086-lint-ns01-v2 <path> feat/work-modes`, verified before dispatch
  by asserting `f58394a` is an ancestor and that `tests/parallel` is present —
  the two facts whose absence produced the wrong-base result last time.
- The first run's three findings were carried into the new prompt rather than
  left to be rediscovered: the `--strict` contract change, the two
  false-positive collisions in the shipped `sample-project` fixture, and the
  diagnose-only wording of the catalog entry. Its PR (#7) is left open and
  unmerged against the wrong base; retiring it is the user's call.

## New tasks added

### TASK-107 — the dispatch safety gate matches its fragments as bare substrings, so ordinary English trips it

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: escalation fragments match on ASCII-word edges rather than bare substrings, in one shared matcher both perry-lint and the dispatch pre-flight call, with the three docs that specify substring changed in the same edit
- **Verification**: a regression fixture in which TASK-079 origin/original, TASK-086 adopt/adopted and TASK-105-106 main/remains stop matching while git push origin main, state-schema.json, ~/.claude/skills, design/ and an ASCII fragment inside Chinese prose still match; python3 tests/parallel no redder than baseline
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-108 — LOAD-03 counts prose about a decision, so documenting an open question makes the count go up

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: LOAD-03 counts distinct open decisions, not occurrences of decision-words in prose. A pending row in the User Input Queue counts once no matter how many files discuss it, and prose that describes an already-recorded decision does not add to the count. Perry own repository reports a number that matches its own queue.
- **Verification**: a fixture with one pending USER row discussed in three files reports 1, not 3; a fixture with two pending rows and no prose reports 2; an answered row still reports 0; the existing exemptions for declared TBD fields and for Implementation plan sections keep working; on this repository the reported number equals the pending count in the User Input Queue, and test_diagnose TestUserLoadFindings passes
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked
- The user ran `perry-conform declare --all`; the repository is 16/16 declared
  with the gate enforcing. Note the side effect: `--all` re-stamps every row,
  so the thirteen files declared on 2026-08-17 now read 2026-08-20 and the
  original acceptance dates survive only in git history. Behaviour is unchanged.
- `bin/README.md § The switch-over checklist` corrected. Its cost 2 said "a
  brand-new project asks for one declaration before its first write", which is
  true and too narrow: every file Perry creates after the last declaration is
  born undeclared, in a project declared for weeks as much as in a new one —
  which is why `phase/002`, `DESIGN-007` and one knowledge card were undeclared
  here while nothing about them was malformed. Measured on a scratch project
  rather than asserted: creation is NOT gated (`perry-decide bootstrap` wrote
  the file), and the next write to it IS. Both halves matter — a gate that
  refused creation would be the wall the checklist exists to avoid.
- That measurement is what a V5 sign-off is for. Reading the documents confirms
  they are internally consistent; it does not surface a scope error in one of
  them, because the document reads correctly either way.

### TASK-109 — a V5 sign-off is composed by selection from measured facts, not authored from memory

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: close-task on a V5 row offers one selection prompt built from what Perry actually measured, each item labelled with whether Perry verified it independently; name and date are filled automatically; every item not selected is recorded as accepted-on-report rather than dropped; and no option Perry drafts is ever a claim about what the user did
- **Verification**: a fixture close where three measured items and one unverified statement are offered, two are selected, and the written record distinguishes checked from accepted-on-report; a fixture where the user adds free text Perry did not offer; an assertion that no drafted option asserts a user action; an assertion that a V5 close with nothing selected and no free text is refused rather than written blank; the existing V5 signatures in this repository still read correctly
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked
- PR #6's reported conflicts were not a code conflict. Its base was `main`,
  which trails `feat/work-modes` by ~280 commits, so GitHub was diffing against
  a tree that predates the whole task-store cutover. The cause is a PMO defect:
  the first dispatch prompt of the day did not name a base branch, so the agent
  used the repository default. Every prompt from the TASK-086 re-dispatch onward
  says "open a PR against `feat/work-modes` (NOT `main`)", which is why only
  #6 was affected.
  Verified before touching anything: merging `feat/work-modes` into the branch
  in a scratch worktree produced **zero conflicts** — `tests/test_migrate.py` is
  the one file both sides touched, and the two edits are in different regions,
  so git resolved it unaided. That merge was then discarded and the branch reset
  to `a09a4a2`, identical to origin, because retargeting the base needs no merge
  commit at all. `gh pr edit 6 --base feat/work-modes` → MERGEABLE.

### TASK-110 — measure what Perry state costs and propose a retention policy, deleting nothing

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: a measurement of every Perry-written directory by size, file count and growth rate over the tracked history, and a retention proposal per directory naming what would be kept, what would be moved and what would be deleted, with the byte figure each rule recovers — as a document under evidence/, with no file deleted, moved or rewritten
- **Verification**: the measurement is reproducible by a command recorded in the document and re-running it gives the same figures; the proposal covers every directory the measurement names, with no directory silently omitted; every rule states the bytes it recovers and what becomes unrecoverable if it runs; a reviewer can reach a different retention decision from the same document
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked
- USER-003 was answered by pointing at the record rather than by a new decision.
  It asked whether Perry may make `tasks.jsonl` the authoritative Task record
  with `BOARD.md` becoming a generated view. ADR-007 decision 2 answers exactly
  that — *"Yes — it becomes rendered output, and a hand edit becomes drift"* —
  dated 2026-08-19, the same day the row was minted, with the user named as
  decider. The row was a duplicate nobody swept, and it spent two days reported
  as a person waiting.
  Worth keeping: the standup counted it as blocking work that had **already
  shipped**. TASK-088/089/090 closed with V4 reviews, and every dispatch today
  ran on the store model. A queue row is only as true as its last sweep, and
  nothing reconciles a `USER-` row against a decision that answers it.
- TASK-038 unblocked and moved to `review`. Its implementation is complete; what
  it still lacks is the V5 signature, which is a different act from the
  permission USER-003 asked for — the sign-off request says so itself: "this
  file is not a signature". Deferred until PR #12 lands, because that PR is the
  selection-based signature flow and TASK-038 is its first real use.
- The three safety-gate rows were worked through with the user. TASK-070 was
  split rather than cleared or refused: TASK-110 measures and proposes, deleting
  nothing, and TASK-070 keeps the execute half pending approval of what TASK-110
  produces. TASK-070 went P2 → P1 on a re-measurement — Perry's own state is now
  31.3% of tracked bytes, not the 19.5% the row was opened against, and
  `evidence/` grew 5.6x in two days. TASK-100 was cleared, bounded to two added
  `claims[]` entries. TASK-077 stays untouched at the user's decision.

### TASK-111 — a test reads two files outside the repository, so it is green here and red on CI forever

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: the byte-identity corpus no longer changes strength with the machine: the shapes it needs are either committed as fixtures or the test skips loudly with the reason named, and no test in the suite silently narrows its own assertion when an optional path is absent
- **Verification**: the failing CI assertion passes on a checkout with no home directory projects present; a mechanical check that no test in the suite reads a path outside the repository root without either committing the file or skipping with a stated reason; the corpus still exercises the multi-version and objective-heading shapes it was written to cover, proven by asserting it fails when those shapes are removed
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked
- PR #12's CI failure was an artifact of the branch base, not a defect. GitHub
  tests the branch, and TASK-109's was cut at 15a7e3c — before c9018ae, which is
  where `SKILL.md` went from 21030 to 20410 bytes and the blank-marker rendering
  was fixed. Two of CI's four reds are exactly those; a third is the documented
  `test_host_support` flake. Merged locally and run: 62 modules, 1819 tests,
  **all green**.
  The general shape is worth naming: every PR branch this session was cut from a
  point that `feat/work-modes` has since passed, so each one's CI reports
  failures that are the base's, not the change's. A red check on a PR here means
  "compare against the merge result" before it means anything else.
- CI's fourth red is real, and is now TASK-111. `test_goals_writer`'s corpus
  reads `~/proj/gimegime-pmo/OKR.md` and `~/proj/aimark/perry/OKR.md` — files
  outside the repository — under `if p.exists()`. On CI they are absent, the
  corpus silently shrinks, and the assertion that it holds at least two distinct
  version counts fails. The test's own docstring says it exists so the corpus
  "cannot quietly become uniform"; `if p.exists()` is how it quietly becomes
  uniform on any machine but this one. It is green here and red on CI forever,
  which is the state in which a real regression stops being visible.
- PR #7 was merged into `main`. It is the stale-base TASK-086 implementation,
  superseded by PR #9 on `feat/work-modes`, so one feature now has two different
  implementations — `NS-01` appears 9 times in `main`'s `bin/perry-lint` and 13
  times in this branch's, differing by 2,311 lines, with a test-merge producing
  4 conflict blocks. The hazard is that the conflict is semantic: a textual
  resolution can leave both in place and no test asserts there is only one.
  PR #13 reverts it, verified two ways — `bin/perry-lint` returns byte-identical
  to `f3c6712`, and the test-merge conflict count goes 4 to 0. Opened as a PR
  rather than pushed, because `git-boundaries.md` permits a direct push to
  `main` only for low-risk documentation commits and this is a code revert.

### TASK-112 — the sign-off drafting guard cannot describe itself, so a true statement about it is refused

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: a sign-off option that describes the drafting rule itself is not refused by that rule, while an option asserting what a person did still is — the guard distinguishes a claim about the user from a sentence naming the concept
- **Verification**: the exact line refused today is accepted: "check_no_user_claim is enforced at the option builder rather than by review, so Perry cannot draft a claim about what the user did"; the lines correctly refused today stay refused, including one naming an act of judgement and one asserting what a person typed; a fixture proves the guard still fires on a real user-claim after the change
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-113 — three checks read the project living around them, so ordinary progress turns them red

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: the V5-close test asserts the property rather than a count of three; the flag-refusal test uses a fixture row instead of a live board row that can be closed; and an id appearing inside a signed record or inside prose reporting a check result is not counted as a reference, so writing that a code is gone does not resurrect it
- **Verification**: closing any further row leaves all three green; a fixture proves each check still fires on the defect it was written for, so none is weakened into passing; the signed V5 records in the journal are not edited by the fix; and a mechanical sweep names any remaining test that asserts a count taken from live project state
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-114 — aiMark reads Perry through the current contracts instead of a pin nine versions old

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: aiMark reads tasks through the current perry-task/list rather than 1.2, and its reads of goals and decisions go through perry-goals/list and perry-decide/list; the version it pins is recorded in one place in aiMark so the next drift is visible; no markdown parsing is removed and no write path is added by this change
- **Verification**: aiMark renders the same task set it renders today against a real project, before and after; a field the older contract did not carry is visibly read through the new one; the pinned version appears in exactly one place in aiMark, proven by a search; and the change is limited to the read path, proven by the diff touching no rendering of the OKR chain view
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-115 — two guards on the read-only report have a hole beside them, found by mutation not by review

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: the wording guard covers the machine-readable plan as well as the rendered note, so a policy word cannot enter through the JSON surface; and the task-store path that reports an overridden read-only bit has a test behind it
- **Verification**: the exact two mutations that survived are caught: injecting "you should chmod it" into read_only_override.observed fails a test, and deleting read_only_mode=owner_read_only(store_path) from _plan_task_store fails a test; the ten existing tests stay green and none is weakened to achieve it
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-116 — the mention list is write-only, mislabelled, and two of its carve-outs are unpinned

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: the unrecorded-question list reaches a reader — a finding or the text output, and the catalog documents it; it contains what its name says by excluding prose that names an already-recorded id anywhere, not only on the record own line; and the Implementation-plan and code-span carve-outs each have a test that goes red when the carve-out is deleted
- **Verification**: deleting the Implementation-plan skip fails a test; deleting the code-span quote filter fails a test; a fixture where prose elsewhere cites a recorded id reports that id in neither the count nor the mention list; the mention list is visible without --json; and on this repository the list contains only genuinely unrecorded questions, which today would be 2 of the 5 currently listed
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-117 — two tools disagree about whether the board has drifted when the event log is absent

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: with the event log removed, perry-lint and perry-tasks verify agree about drift; whichever is wrong is corrected, and the created field resolves from a source that survives the log being disposable as its own documentation claims
- **Verification**: on a tree with no event log, perry-lint and perry-tasks verify report the same drift count; a fixture pins the agreement so a future divergence fails; and the documentation that calls the log disposable either holds or is corrected in the same change
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-118 — the id minters read three sources and the canonical store is not one of them

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: USER, CAD and RX ids are minted from a source set that includes the canonical store, so an id cannot be reissued when a row leaves the board while its journal record is absent; and bin/perry-task header no longer claims TASK ids come from three sources when the implementation reads the store
- **Verification**: a fixture where a USER row is absent from the board and from the journal but present in the store mints the next id rather than reissuing one; the same for RX and CAD; the existing minting behaviour is unchanged in every case where the board still carries the maximum
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-119 — the linkage graph is documented as machine-written and no tool writes it

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: perry-goals gains the link subcommand its own reference documents — appending a task edge to a KR, registering a project alias, and declaring a task unlinked — with the three refusals that reference already specifies: an unknown KR id, a task already under a different KR, and an alias another project claims
- **Verification**: each of the three refusals is exercised by a test; a task moved between KRs leaves no duplicate edge; declaring a task unlinked removes it from any KR tasks list in the same write; every write stamps updated with a full ISO datetime because a day-only value is dropped by both readers; and phase/002-linkage.md as it stands today round-trips through the tool unchanged
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-120 — the linkage edges are read but never folded into KR progress

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: a KR reports how many of its linked tasks are done, so phase progress is computed from the graph rather than assessed by hand; the snapshot renders it and the number excludes declared-unlinked work rather than hiding it
- **Verification**: with phase/002-linkage.md as it stands, each of the eight KRs reports a done-over-linked count that matches what the board says about those task ids; a KR with no edges reports no progress rather than zero progress, since the two mean different things; declared-unlinked tasks appear in neither numerator nor denominator and are still surfaced separately
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-121 — the sweep that found four more live-state assertions runs once and then is thrown away

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: the two-stage AST sweep lands under tests/ as a standing check, so a test that asserts a literal over live project state is caught when it is written rather than when the project moves; the four instances it already found are either fixed or carry a written exemption naming why the assertion is deliberate
- **Verification**: the sweep runs in the suite and passes on the tree as it stands; adding a test that asserts an exact count over perry/BOARD.md makes it fail; the deliberate exemption in test_one_primitive stays green through its written reason rather than by being skipped; and the risks and md_store hits either move to the property they actually want or record why the count is the property
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-122 — the repair path the tools advertise leaves the file needing a whitespace fix

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: render --write restores a hand-edited non-table line byte-faithfully, so the command both tools name in their refusal messages does not itself leave trailing or doubled whitespace; the padding rule that is correct for a table cell stops applying to a slot on a line with no cell boundaries, in the same way escape was already parameterised for that split
- **Verification**: repairing a hand-edited State root bullet and a bullet-form KR each restore the file byte-identically, proven by cmp rather than substring matching; git diff --check is clean after the repair; the round-trip test covers a bullet case as well as a table cell and asserts full text equality; and the table-cell padding behaviour is unchanged, proven by the board round-trip staying byte-identical
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-123 — the goals writer takes the file as truth and derives the store, which is the opposite direction from the KR

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: either perry-goals writes the store and renders OKR.md from it, so the file is the projection the phase KR describes — or the KR is amended to say what the project actually decided, with the reason recorded; the two must agree, and which way they agree is the user decision this row exists to force
- **Verification**: whichever direction is chosen, a hand edit to OKR.md and a write through the tool both end with the store and the file agreeing, and the drift report says which one moved; the perry-goals list payload does not change shape either way; and phase 002 P-O1.2 can be answered yes or no by a command rather than by reading the writer source
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-124 — the conformance corpus reads a project outside the repo and has no committed substitute

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: tests/test_conformance.py:440 stops depending on ~/proj/gimegime-pmo for its only coverage: either a trimmed fixture carrying the shapes those four tests need is committed and they run everywhere, or the row records why the shape cannot be committed and the skip names that reason
- **Verification**: with HOME pointed at an empty directory and PERRY_TEST_CORPUS unset, the four tests in those two classes run rather than skip; mutating the committed fixture reproduces each of their failures; reverting the fix breaks the new test
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-125 — the Anti-Goals-inside-a-version insert case runs only on the author machine

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: a purpose-built OKR fixture carrying `### Anti-Goals` nested inside a version WITHOUT a `## Commitments` section, so test_a_declared_queue_track_makes_the_section_land_cleanly and its sibling insert test run from the repository rather than from ~/proj
- **Verification**: with HOME at an empty directory both tests run rather than skip; removing the nested Anti-Goals heading from the new fixture reddens them
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-126 — closing the dangling-id row requires writing the record that re-dangles it

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: LOAD-02 can reach zero on a project that has documented its own fix: the three structural marks (signed record, blockquote, paragraph naming a check by code or test name) are extended so that a dispatch record whose subject IS the check does not count as a live reference, or the check reports a separate reason when its only live mentions are in documents about itself
- **Verification**: measured now: every live mention of REL-00 and DESIGN-900 is in perry/evidence/2026-08/TASK-113-dispatch-2026-08-20-1813.md lines 11/16/18/35, and every other mention in the repository already classifies as a report; after the fix that file stops being a live reference without its prose being edited, and a genuinely live reference in an ordinary evidence file still counts
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-127 — the contract docs and the payloads they describe are never diffed against each other

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: a mechanical two-way diff, per contract file under schema/, between the field paths the document declares and the field paths the tool actually emits: documented-but-not-emitted and emitted-but-not-documented, each reported with a count and a per-contract baseline that lives in a file rather than in a memory. Covers every contract present, not the three the KR text was written against
- **Verification**: removing one documented key from its emit site turns the check red; adding one emitted key the document does not declare turns it red; the per-contract counts are printed; and the run names how many contract files it found, so a contract added later cannot be silently skipped
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-128 — the Agent entity has no writer because the signature that would give it one was never taken

- **Owner**: User
- **Priority**: P0
- **Track / mode**: main / project
- **Deliverable**: DESIGN-007 step 2 resolved: either a new signed row in SKILL.md § The hand-off contract naming which lane writes .perry/roles/, or a signed decision that the card stays owner user and the Agent store lives elsewhere. Recorded at the precision of the 2026-08-16 signature — what was checked, not that it was reviewed
- **Verification**: V5 — the signer names the date and what they checked; tests/test_ownership.py accepts the path afterwards rather than refusing it; DESIGN-007 steps 3, 4, 12 and 14 stop citing step 2 as their blocker
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-129 — Agent is five strings that do not join, and role has never once been written

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: DESIGN-007 step 3: Agent becomes a store with an id and typed may_touch[] / must_escalate[]; the card under .perry/roles/ is rendered from it rather than being its definition; the task store role field becomes a foreign key or is deleted; events actor carries the id
- **Verification**: measured 2026-08-20: role is empty on all 120 task records, owner is empty on 73 of 120, and the event actor field holds six distinct free-text values across 505 events (agent 442, codex 28, PMO Agent 26, Coding Agent 4, Review Agent 4, coding agent 1). After: one join key, those six resolve to declared ids, and reverting the change breaks the test
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-130 — schema README says three contracts and pins goals at a version that shipped two ago

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: schema/README.md § The three read contracts names all five contracts and reads the version out of each contract file rather than restating it, so perry-goals/list/2.0 cannot be listed as 1.0 again
- **Verification**: the section lists five; bumping any contract version in its own file and re-running the check reports the README as stale; TASK-127 parity numbers are unchanged by the edit
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-131 — seventeen emitted contract keys are documented nowhere, and now there is a number for it

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: the 12 undeclared keys on perry-task/list/1.11 (semantics plus its three children, and the six nested conformance entry shapes) and the 5 on perry-goals/list/2.0 are declared in their contract documents, taking the parity count from 17 to 0
- **Verification**: the TASK-127 parity check reports 0 emitted_not_documented across all five contracts, and its baseline file records the drop from 17
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-132 — the parity check cannot see 23 keys because Perry own state leaves four collections empty

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: a fixture project the parity check can be pointed at with --root that populates intake.rows, asks.items, roles.cards and expired_sunsets, so the 23 unplaced keys are compared rather than reported unplaced
- **Verification**: the check run against the fixture reports 0 unplaced keys where Perry own repo reports 23; removing one documented key from an entry shape reddens it there
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-133 — declare the first non-project track on Perry itself, and measure what a mixed spine costs

- **Owner**: User + Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: this repository declares one queue track in .perry/config.md § Tracks, at least one row sits on it carrying a kr: edge, and the experiment reports three measured numbers: files changed, state files rewritten, and where the mode-specific triage question diverges from what a project-mode spine needs
- **Verification**: git diff --stat names every file the declaration touched; perry-state --json before and after shows which state files changed; the queue triage step runs and its output is quoted verbatim, including any step it reports it cannot run
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-134 — probe row for the TASK-133 track experiment

- **Owner**: PMO Agent
- **Priority**: P2
- **Track / mode**: intake / queue
- **Deliverable**: exists only to test whether a queue-track row can carry a KR edge and appear in queue triage
- **Verification**: deleted at the end of the experiment
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-135 — a track can be declared but no existing row can be moved onto it

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: an existing task row can change track: --track is accepted by a subcommand that operates on a row that already exists, not only by add at creation and route from an intake row. Moving a row onto a queue-mode track stamps Arrived and the first post-intake Stage the same way route does, and moving it off does not silently strand those fields
- **Verification**: measured 2026-08-20: perry-task has no track subcommand and status --track is not accepted, so a project declaring a second track starts it empty and cannot populate it from work already on the board; after the fix, six existing rows move to a declared queue track and the board renders them with Arrived and Stage, and moving a row to a track that is not declared is refused by name
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-136 — a queue track SLA is parsed, stored and never measured against anything

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: the queue breach step exists: today − Arrived is computed per row against its track SLA and surfaced in perry-state --json, so triage can ask what breached rather than being told it cannot
- **Verification**: measured 2026-08-20: the intake track carries sla 5d and perry-state computes only stage_counts and wip_breaches for it; grep finds no consumer of a track SLA outside classify_due, which governs a Commitments Due cell rather than a row clock. After: a row whose Arrived is older than its track SLA appears in the payload, and one inside it does not
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-137 — a new queue row is born in the second stage, not the first

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: perry-task add --track <queue-track> sets Stage to the first declared stage, or the default is documented as deliberate with the reason
- **Verification**: measured 2026-08-20: intake declares new→triaged→in_progress→resolved and TASK-134 was created with stage triaged, skipping new; after the fix a row created on that track reads new, and a track declaring a different first stage gets that one
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-138 — stat -f succeeds on GNU with a different meaning, so the mtime fallback is never reached

- **Owner**: Coding Agent
- **Priority**: P0
- **Track / mode**: intake / queue
- **Deliverable**: bin/perry-dispatch-limit mtime() returns an integer on both BSD and GNU stat: it tries the GNU form first, which fails cleanly on BSD, and rejects any output that is not all digits rather than trusting the exit code. A regression fixture with a fake stat on PATH reproduces the GNU behaviour so the defect is catchable on a mac
- **Verification**: with a fake stat that mimics GNU (-f prints a filesystem block and exits 0, -c %Y prints digits), the current code produces the CI error "syntax error in expression" and the fixed code produces a clean count; reverting the fix reddens the new test
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-139 — a design back-reference lives in a cell the close path clears, so a finished design reports as never handed off

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: intake / queue
- **Deliverable**: a task carries its design id in a field the lifecycle does not destroy, and walk_design counts that field; DESIGN-001 stops appearing in pending_handoff without any evidence file being edited to mention it
- **Verification**: measured 2026-08-20: DESIGN-001 is the only design with zero matches in both perry/tasks.jsonl blobs and .perry/events.jsonl, while its six implementation tasks TASK-001..006 are all done at V3 with real evidence, and the 2026-08-16 journal shows every one of them was defined with Next action naming a DESIGN-001 phase. After the fix DESIGN-001 reports handed off, and a design that genuinely has no implementation tasks still reports pending
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

## V5 sign-off

**TASK-107 — V5 sign-off. Ran Jiao, 2026-08-20.**

**checked**

- perry-state --escalation-scan on TASK-079-spec.md exits 0; the fragment origin no longer matches inside the word originally *(Perry verified)*
- the reworded rule in .perry/hook.md and work/state/hook_TEMPLATE.md describes what the matcher now does *(restated — Perry did not verify this)*

**accepted on report**

- the same scan on TASK-086-spec.md exits 0; adopt no longer matches inside adopted *(Perry verified)*
- tests/test_escalation_boundaries.py passes 22/22 *(Perry verified)*
- the dangling-id check reports [] — TASK-107 resolves and REL-00 is gone *(Perry verified)*
- python3 bin/perry-lint is clean and python3 tests/parallel is no redder than the baseline *(Perry verified)*
- escalation_pattern guards ASCII fragments by their own edges rather than by \b, because \b does not exist in Chinese *(restated — Perry did not verify this)*

**TASK-038 — V5 sign-off. Ran Jiao, 2026-08-20.**

**checked**

- perry-lint reports 108 store records with 0 rows drifted on this repository right now *(Perry verified)*
- every mutating command today wrote store + journal + BOARD.md + event, and BOARD.md was never hand-edited *(Perry verified)*
- TASK-088, TASK-089 and TASK-090 are closed, each with its own V4 review evidence file *(Perry verified)*
- the full suite is green on this branch: 62 modules, 1819 tests *(Perry verified)*
- the write side passed V4 against seven written criteria including recovery, duplicate refusal, validation, locking and transaction behaviour (evidence/2026-08/TASK-089-v4-review-r4.md) *(restated — Perry did not verify this)*
- the read side passed V4 with the board and event log independently removed or changed, plus malformed-store and dependency checks (evidence/2026-08/TASK-090-v4-review.md) *(restated — Perry did not verify this)*
- migration and restore safety is NOT covered here and remains TASK-044 *(restated — Perry did not verify this)*
- comprehensive projection-drift diagnostics is NOT covered here and remains TASK-093 *(restated — Perry did not verify this)*
- moving the two external projects is NOT covered here and remains TASK-097 *(restated — Perry did not verify this)*
- Windows, network filesystems, real process kills and live external-project migration were not part of these V4 reviews *(restated — Perry did not verify this)*

**TASK-109 — V5 sign-off. Ran Jiao, 2026-08-20.**

**checked**

- check_no_user_claim runs at the option builder rather than in review *(restated — Perry did not verify this)*
- the prompt degrades to a numbered free-text fallback on a host with no selection UI, and both spellings mint the same record *(restated — Perry did not verify this)*

**accepted on report**

- the mechanism signed two real closes today — TASK-107 with 2 of 7 items selected and TASK-038 with all 10; both records are in today's journal *(Perry verified)*
- an offer built from nothing is refused: signoff-offer with no --measured returned 'nothing to offer ... a prompt that teaches the reader to press return' *(Perry verified)*
- TASK-107's journal record carries two separate disposition headings, where the free-text format it replaces has none *(Perry verified)*
- the signature block's name and date came from git config, not from a command-line argument *(Perry verified)*
- the drafting guard refused three lines written by the PMO itself while preparing this very offer *(Perry verified)*
- tests/test_v5_signoff.py passes 32/32 and the full suite is green on this branch *(Perry verified)*
- three dispositions rather than two, because the existing corpus already writes the third by hand and not-looked-at is never a default *(restated — Perry did not verify this)*
- the CJK half of the drafting guard initially never fired because backslash-w matches CJK, found by the test corpus and fixed *(restated — Perry did not verify this)*
- Two dispatches stalled at the 600s watchdog within minutes of each other — the
  TASK-092 V4 review at "Now the new module", TASK-113 at "Now the fourth
  instance". Neither failed on its problem. Both were starved: `tests/parallel`
  runs 8 workers, the dispatch cap counts **agents**, and the cap had been raised
  to 3 that afternoon, so three suite-running agents put 24 workers on a 14-core
  machine plus whatever the PMO was running itself.
  The cap is not the wrong number; it is the wrong unit. Dropped to one other
  dispatch and resumed TASK-113 rather than re-dispatching it — its worktree
  still carried all four modified files, uncommitted, and `SendMessage` resumes
  an agent from its transcript. Recorded in memory so the next raise of that cap
  asks what the agents will be running.
- The stalled TASK-092 review is a dispatch failure, not a verdict. It banked one
  criterion before stopping — out-of-scope diff clean — and the re-run gets a
  fresh context rather than resuming, because a reviewer told what it had already
  concluded is no longer the fresh-context reviewer V4 requires. That is the one
  place where resuming is the wrong move and re-dispatching is right.
- TASK-070 dropped on its own evidence. TASK-110 measured what the row asked for
  and the measurement argues against it: the whole retention proposal recovers
  560,089 bytes against a measured growth of 405,349 bytes/day — **1.4 days,
  once** — and `perry-lint` reads all 135 evidence files in 8ms, so the cost is
  not being paid where the bytes are. The measurement's own strongest argument
  was against itself: the handoff document that reopened the `.perry/` question
  would have been deleted by the age-based rule it weakly proposed.
  Recorded as dropped **on the evidence, not for lack of interest** — the
  distinction is what a future reader needs, because "nobody got to it" and
  "we measured it and it was not worth doing" look identical on a closed row.
  The lever the measurement does point at — 65% of `evidence/` is review
  transcripts from one dispatch campaign — is how much prose a V4 round is asked
  to emit. That is a product decision and was left where it belongs.
- USER-004 answered: migrate and name the override, do not refuse. Two things
  changed since it was minted that made the answer easier. The report half
  shipped in PR #6, so the choice was no longer "silent overwrite versus
  refusal" but "report versus refusal"; and TASK-115 added the guard keeping
  that wording an observation rather than advice, which is what lets Perry
  report a signal without acting on it. The deciding argument is ADR-004's own
  shape: migration is the one road an undeclared project has, and a refusal
  there is the wall with no door this project rejects everywhere else.

**TASK-128 — V5 sign-off. Ran Jiao, 2026-08-20.**

**checked**

- the edit is one table row in SKILL.md § The hand-off contract; goals and decide are byte-identical across it *(Perry verified)*
- SKILL.md goes 20410 → 20457 bytes against the 20480 cap in tests/test_router_budget.py — 23 bytes left *(Perry verified)*
- schema/state-schema.json line 2102 currently reads OWNER IS user, NOT A LANE for .perry/roles/*.md, and becomes wrong on this commit *(Perry verified)*
- tests/test_ownership.py refuses a lane-owned path the contract does not list; it refused this one *(Perry verified)*
- DESIGN-007 § 5.3 already names .perry/agents.jsonl → .perry/roles/*.md; no path was chosen here *(Perry verified)*
- the paragraph under the table still reads Two changes from the previous contract and gains no mention of this one; there is no room *(Perry verified)*
- DESIGN-007 decision #2 — the store is the definition, the card is rendered output — carries a 2026-08-19 signature and is not reopened here *(restated — Perry did not verify this)*
- a hand edit to a role card will read as drift, the same behaviour BOARD.md has had since ADR-007 decision 2 *(restated — Perry did not verify this)*
