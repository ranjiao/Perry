# 2026-09-04

## Status changes

- [TASK-335] — → not_started · Two contract-parity tests fail because the witness cannot make a collection observable · owner: Coding Agent · priority: P1
- [TASK-335] next action · MEASURED BY THE PMO 2026-09-04. Both failures are in tests/test_contract_key_parity.py and both name the same key: test_without_the_witness_the_four_are_unobservable expects 'conformance.in_progress_with_no_live_run[].means' in the blind run's not-observable set and it is ABSENT -- that set holds only depends_on_unknown and review_idle keys -- while test_the_same_mutation_is_silent_without_the_witness finds it present where it expects absence. PRE-EXISTING, NOT CAUSED BY TONIGHT'S WORK: reproduced identically in a detached worktree at 5601e45, which is before TASK-331, TASK-332 and TASK-334 landed. TASK-330's V4 reviewer hit the same two reds independently in their own worktree and chased them down rather than assuming: they reproduce on a clean tree, reproduce with the pre-TASK-330 predicate, and the module contains the word 'summary' zero times, so they are unrelated to the summary work. WHY IT MATTERS NOW: with TASK-334 fixed this is the ONLY module making tests/run red, so every future baseline has to carry it as a known red or it will be misread as a regression -- which is exactly what happened to me twice tonight in the other direction. WHAT THE ROUND MUST DECIDE FIRST: whether the witness fixture stopped producing the state (fixture defect) or Perry's own board now populates the collection so the blind run is no longer blind (premise defect). Perry currently has two in_progress rows, TASK-256 and TASK-263. Do not change the assertion before answering that, because the two answers need opposite fixes.
- [TASK-335] depends on · — → TASK-132
- [TASK-336] — → not_started · summary_tokens' CJK behaviour lost its pin when the fragment floor was removed · owner: Coding Agent · priority: P1
- [TASK-337] — → not_started · Two procedure pages still tell authors the writer refuses summaries it now accepts · owner: Coding Agent · priority: P1
- [TASK-336] depends on · — → TASK-330
- [TASK-337] depends on · — → TASK-331
- [TASK-325] review → done · closed · evidence: `evidence/2026-09/TASK-325-round1-v4-review.md` · verification: V3
- [TASK-330] rung · — → V4
- [TASK-330] review → done · closed · evidence: `evidence/2026-09/TASK-330-round1-v4-review.md` · verification: V3
- [TASK-256] evidence · — → evidence/2026-09/TASK-256-round2-result.md
- [TASK-256] in_progress → review
- [TASK-335] next action · MECHANISM FOUND, and it is not what I guessed. TASK-256's round-2 agent diagnosed it independently while measuring its own baseline, and I confirmed the gate: bin/perry-task:6303 fires when 'idle >= in_progress_limit', where in_progress_limit = threshold('in_progress_idle_hours', 4) at :6278 and idle_hours is computed against REAL TIME. So conformance.in_progress_with_no_live_run is EMPTY while an in_progress row is fresh and becomes NON-EMPTY once the event log ages past four hours -- and the two parity tests assert that the collection is empty in the blind run so that the witness fixture is what makes its keys observable. THE TEST THEREFORE BREAKS WITH THE PASSAGE OF TIME ALONE, on an unchanged tree. That is why it reproduced for me at 5601e45 in a detached worktree: that checkout's board also carries in_progress rows whose events are frozen at their commit and keep aging. MY EARLIER NOTE ON THIS ROW OFFERED TWO CANDIDATE CAUSES -- fixture defect or premise defect -- AND BOTH WERE WRONG. It is neither: the fixture is fine and the premise was true when written. The defect is that an anti-vacuity control was written against a value that decays. The agent's proof is better than mine: it re-ran the identical stub tree as a real repo at the same moment and got the same two failures with zero of its edits present. WHAT THE FIX MUST DECIDE: whether the blind run should be measured against a board frozen in time (a fixture, or a pinned clock) rather than against Perry's own live state. Any fix that just widens the assertion re-creates the vacuity TASK-132 built this class of test to remove. Perry currently has two in_progress rows, TASK-256 and TASK-263, and closing them will make the symptom disappear WITHOUT fixing anything -- do not read a green suite after that as the row being done.
- [TASK-338] — → not_started · A test's tree guard names another agent's worktree, so runs are not isolated from each other · owner: Coding Agent · priority: P2

## New tasks added

### TASK-335 — Two contract-parity tests fail because the witness cannot make a collection observable

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: Both tests pass on a clean tree, or the witness fixture is corrected so the collection is non-empty under it and empty without it -- whichever the measurement says. If the premise itself is wrong because Perry's own board now populates the collection, say so and change the premise rather than the assertion.
- **Verification**: Reproduce both failures first and quote them. After the fix, tests/run is green in 3 consecutive runs. Mutation: revert the fix and show both named tests go red. perry-lint --root . 0 errors.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-336 — summary_tokens' CJK behaviour lost its pin when the fragment floor was removed

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: A test that fails when summary_tokens stops counting CJK, reached through summary-repeats-title rather than through the removed floor. The removed rules do not come back.
- **Verification**: Mutation: revert summary_tokens to str.split() and show a named test go red where all 30 currently stay green. Control: a Chinese summary that legitimately restates its title is still caught. perry-lint --root . 0 errors; suite no redder than the measured baseline.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-337 — Two procedure pages still tell authors the writer refuses summaries it now accepts

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: Both pages describe what the writer actually refuses, and TASK-331's ast guard is widened to cover all three surfaces so a fourth divergence cannot open silently.
- **Verification**: Mutation: reintroduce a removed rule name into each page and show the widened guard go red on each. Control: the Changelog entry naming the removed rules stays green. perry-lint --root . 0 errors.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-338 — A test's tree guard names another agent's worktree, so runs are not isolated from each other

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: A tree guard reports only on the tree its own run owns, and a stale worktree from a finished round is removed or is provably invisible to every walking guard.
- **Verification**: Reproduce the cross-naming first and quote it. After the fix, run two agents' suites concurrently and show neither guard names the other's path. Mutation: revert the scoping and show a named test go red. perry-lint --root . 0 errors.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked
