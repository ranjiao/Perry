# 2026-09-08

## Status changes

- [TASK-155] next action · DEFERRED TO PHASE 004 BY USER DECISION 2026-09-08, during the phase 003 mid-phase review. The row is blocked on a CLAIM-SURFACE AUTHORIZATION, not on another row: the fix is a per-KR assertion date, a new field in schema/state-schema.json, which sits on .perry/hook.md High-stakes operations under the claim surface and cannot be dispatched without the user saying so. What forced the decision: re-asserting P003-O1-KR2 and P003-O2-KR1 would have bumped phase/003-linkage.md updated:, re-dating all 115 linkage records (declared_at, bin/perry-tasks:1612) and marking all four asserted current values fresh (asserted_at, bin/lib/__init__.py:1066) including the two being corrected. The user chose to carry the measured numbers to score-phase instead. Full account and both measurements: evidence/2026-09/2026-09-08-kr-remeasurement.md. The 2026-09-07 live reproduction this row already carried is preserved in its Summary and in that file.
- [TASK-394] in_progress → review
- [TASK-383] blocked → not_started
- [TASK-280] depends on · TASK-278 → —
- [TASK-280] next action · STILL BLOCKED, BUT NOT ON TASK-278 — that edge was satisfied (TASK-278 done, V4 PASS) and was cleared 2026-09-08 by the phase 003 mid-phase triage, which is why this row read as blocked_by_closed_rows. The round stopped 2026-09-08 and the stop is the deliverable; nothing shed. The remaining blocker is the CLAIM SURFACE: schema/state-schema.json marks the linkage document's krs[].title required:true and perry-lint enforces it, so shedding it is 6 hard errors against the spec's own 0-errors bar. Flipping only that flag in a sandbox copy took the errors 6 to 0, so the schema is precisely and solely that blocker. schema/state-schema.json is on .perry/hook.md High-stakes operations under the claim surface, so this cannot be dispatched without the user authorizing it. Third row in this state: see TASK-155 and the mid-phase review's structural-blocker finding. Second blocker recorded separately: _linkage_drift_rows does the opposite of its own docstring.
- [TASK-217] next action · DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. CONSEQUENCE ACCEPTED: phase 003's retro will be written without the four pages agreeing on whether it comes before or after score-phase, so that ordering is decided by hand once, for this phase only, and the disagreement is still there for phase 004 to resolve.
- [TASK-218] next action · DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. CONSEQUENCE ACCEPTED: the closing phase id is not threaded through the close stages, so whoever closes phase 003 passes it by hand at each step and nothing checks that all four stages got the same one.
- [TASK-220] next action · DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. CONSEQUENCE ACCEPTED: there is no close-phase router subcommand, so closing phase 003 means running the four existing commands separately, in an order nobody has written down (which is TASK-217, deferred with it).
- [TASK-231] next action · DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. THIS ROW IS FINDING 2 OF THE MID-PHASE REVIEW, FILED ON DAY 1: a measured KR number has no way into the register that does not re-date everything else. The workaround already taken: the 2026-09-08 re-measurement of P003-O1-KR2 and P003-O2-KR1 was written to evidence/2026-09/2026-09-08-kr-remeasurement.md instead of the register, and score-phase cites that file. Same claim-surface blocker as TASK-155 and TASK-280.
- [TASK-396] — → not_started · perry-task has no published write contract, so a newly required flag reaches a consumer as a refusal at runtime · owner: Coding Agent · priority: P1
- [TASK-397] — → not_started · ADR-017 changed an identifier's value space and perry-goals/list stayed at 2.3, so a consumer joining on a stored KR id gets a silent miss · owner: Coding Agent · priority: P1
- [TASK-398] — → not_started · an advisory a program must act on is a payload field, not a stderr line, and add still writes one under --json · owner: Coding Agent · priority: P2
- [TASK-399] — → not_started · tests/durations.json describes no run that has happened, so no speedup can be attributed and no regression can be seen · owner: Coding Agent · priority: P1
- [TASK-400] — → not_started · the suite's most expensive module tests the suite's own guard, at 5.1s per test, and it sets the makespan floor · owner: Coding Agent · priority: P1
- [TASK-401] — → not_started · every bin/ entrypoint is recompiled on every invocation because an extensionless __main__ is never bytecode-cached · owner: Coding Agent · priority: P1
- [TASK-402] — → not_started · class-A tests build every fixture row by spawning the CLI, so 386 CPU-seconds go to process startup before any assertion runs · owner: Coding Agent · priority: P1
- [TASK-403] — → not_started · four test modules use a regex to judge meaning, which ADR-007 decision 3 already ruled the Python layer never does · owner: Coding Agent · priority: P2
- [TASK-404] — → not_started · 36 test modules assert against live project state, pinning 614 dates and 191 ids that go red when the board moves rather than when code breaks · owner: Coding Agent · priority: P2
- [TASK-399] next action · VERIFICATION AMENDED 2026-09-08, with the measurement in evidence/2026-09/TASK-399-result.md. The original asked for two --times runs with no module's x column exceeding 1.5 in either direction. Measured: 0 modules over 1.5, but 17 under 0.67 -- and that is a systematic 0.775 scale factor from load, not dispersion. The same suite on the same commit measured 122.2s, 128.4s and 165.7s inside twenty minutes on a machine carrying resident load ~20. The criterion tests absolute seconds, and this file is a sort key: test_durations_provenance.TestTheHintIsStillOnlyAHint asserts exactly that. Replaced with an order criterion, measured on the same run: Spearman rho 0.990 over 120 modules, top-10 overlap 10/10, largest rank movement 19 places mid-pack. Makespan is set by the longest modules and none of them moved.
- [TASK-399] not_started → done · closed · evidence: `evidence/2026-09/TASK-399-result.md` · verification: V3
- [TASK-402] next action · PREMISE FALSIFIED TWICE BEFORE THIS ROW WAS WORKED, 2026-09-08 -- read evidence/2026-09/TASK-402-premise.md BEFORE converting anything. W4 opens by assuming the process boundary is the cost. On the two modules at the head of the ranking it was 12 percent and both were product defects instead; fixing those took the suite from 128.4s to 106.4s with no test file edited. Subprocess-bound does not mean boundary-bound. W4 remains right where a fixture row is built by spawning perry-task -- boundary measured at 54 percent there -- which is test_task_writer_*, test_track_move and test_purge, mid-ranking rather than the head.
- [TASK-402] next action · V5 SIGNED 2026-09-08 by Ran Jiao, scope recorded verbatim in evidence/2026-09/TASK-402-v5-signoff.md. The signature covers that the conversions did not change what the tests report -- five id-set comparisons, every diff empty over 3393 ids. It explicitly does NOT cover that these tests still catch regressions: no mutation was run in any of the four rounds, and that gap is filed separately rather than buried in a closed row. All four of the row's own verification criteria are answered in the sign-off file, including the --serial check the earlier rounds substituted a reverse-order run for.
- [TASK-402] not_started → done · closed · evidence: `evidence/2026-09/TASK-402-v5-signoff.md` · verification: V3
- [TASK-405] — → not_started · four in-process conversion rounds shipped without a single mutation, so nothing shows those tests would still go red · owner: Coding Agent · priority: P1
- [TASK-400] next action · Three self-test modules moved behind --slow: test_tree_guard, test_parallel_runner, test_durations_provenance, 52.4s. The guard itself did not move -- tests/run step 0 still runs tests/tree_guard.py on every path, proved by planting a file (exit 0 / 1 / 0). Default is the old set minus exactly 88 ids with none added; --slow restores it with a 0-line diff. Awaiting V4: a reviewer against evidence/2026-09/TASK-400-result.md. Read its last section first -- this change broke six of the self-tests it was moving, and the default run reported clean because it had just moved them.
- [TASK-400] not_started → review
- [TASK-400] evidence · — → evidence/2026-09/TASK-400-criteria.md
- [USER-917] pending → answered · 决定 2026-09-08：选 A —— 新增 --compact payload，从完整 payload 在进程内派生。--json 保持已发布的形状不变，所以 aiMark 和任何现有消费者不受影响，而窄模式可以作为一个严格投影来测试。重新测量后这个决定比立项时更紧迫：payload 从 09-04 的 186,856 bytes 涨到今天的 258,989 bytes（约 65k tokens），+39%，其中 board 一个 section 占 159,648 bytes = 72%。涨的是本会话加的十几行 task。第一手证据：今天开场跑 perry-state --json 输出 248.8KB 被系统直接转存到文件，因为塞不进上下文，此后整个会话都在从那个文件里抠字段。选项 C（只改 SKILL.md 用 --section）明确排除：board 单独就是 72%，达不到目标。
- [USER-918] pending → answered · 决定 2026-09-08：选 B —— 论证搬进 bin/README.md，--help 只留 Usage 和子命令。重新测量：perry-task --help 169 行 10,690 字节，Usage 在第 51 行；perry-goals 107 行，Usage 在第 58 行；perry-decide 62 行，Usage 在第 41 行。而 perry-lint 同样长（136 行）Usage 却在第 8 行，说明这不是论证太多，是排版顺序。PMO 提出的风险，用户已知并接受：论证离开它解释的代码之后两边会各自漂移，而那正是 ADR-019 今天删掉的那一类重复。缓解办法应与搬迁同批交付，不是后补：bin/README.md 目前覆盖 19 个工具中的 19 个，但没有任何测试引用它，覆盖是手工维持的。搬迁应带一个机械检查——每个 bin/ 工具在 README 里有对应小节，且其 Usage 块与工具实际的 --help 一致——把漂移从静默变成被报告，这是 perry-lint 对其它投影已经在用的模式。

## New tasks added

### TASK-396 — perry-task has no published write contract, so a newly required flag reaches a consumer as a refusal at runtime

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: perry-task <verb> --describe --json returning required[], optional[], mutually_exclusive[] and its own contract version, for the six verbs aiMark drives
- **Verification**: delete --summary from add's required[] in the describe payload and a test that compares it against add's real refusal must go red
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-397 — ADR-017 changed an identifier's value space and perry-goals/list stayed at 2.3, so a consumer joining on a stored KR id gets a silent miss

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: perry-goals/list bumped to 2.4 with a semantics[] entry for the three id-bearing fields, saying both grammars are live while OKR_TEMPLATE.md mints the old one
- **Verification**: aiMark bumps CONTRACT_TESTED.goals to 2.4 and its changed-detector goes quiet; reverting the semantics entry makes the parity test red
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-398 — an advisory a program must act on is a payload field, not a stderr line, and add still writes one under --json

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: under --json, add emits no advisory on stderr; anything a program should act on is a key on stdout
- **Verification**: run add --json with no KR and assert stderr is empty; restore the warning line and the test goes red
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-399 — tests/durations.json describes no run that has happened, so no speedup can be attributed and no regression can be seen

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: A tests/durations.json in which every module carries a sec and a stamped source — ref, date, worker count, load average
- **Verification**: python3 tests/parallel --times run twice: no module's x column exceeds 1.5 in either direction, and test_durations_provenance reports 0 unstamped and 0 unmeasured
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-400 — the suite's most expensive module tests the suite's own guard, at 5.1s per test, and it sets the makespan floor

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: A bash tests/run --slow path or equivalent selector carrying the harness self-tests, and a default run that does not
- **Verification**: --ids set diff: default-run ids equal today's set minus exactly the 25 test_tree_guard ids, and --slow restores the full set byte for byte; step 0's snapshot/verify still fires on every default run, proved by planting a file
- **Dependencies**: TASK-399
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-401 — every bin/ entrypoint is recompiled on every invocation because an extensionless __main__ is never bytecode-cached

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: bin/<tool> reduced to a shim with the body in an importable .py module shipping main(argv) -> int, for perry-task, perry-tasks, perry-lint, perry-goals, perry-state and perry-decide
- **Verification**: second and later invocations of each tool are >=10ms faster than the first at median of 21, so a __pycache__ entry exists and is used; tests/run step 3 still passes --help for every script; full --ids set unchanged
- **Dependencies**: TASK-399
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-402 — class-A tests build every fixture row by spawning the CLI, so 386 CPU-seconds go to process startup before any assertion runs

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: One shared tests/inproc.py that loads a tool once per process and invokes main(argv) with captured stdout and a return code, plus the ten highest-cost class-A modules converted to it
- **Verification**: python3 tests/parallel --ids before.txt then --ids after.txt and the pass/fail id SETS are identical, not merely the same size; measured wall drops against the TASK-399 baseline; every converted module also passes under --serial, proving no shared-process order dependence was introduced
- **Dependencies**: TASK-399
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-403 — four test modules use a regex to judge meaning, which ADR-007 decision 3 already ruled the Python layer never does

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: An ADR recording which prose checks move to an agent step and which stay mechanical, then the retirement of test_procedures_call_the_tool, test_heading_defines, test_heading_title and test_shipped_vocabulary and the review step that replaces them
- **Verification**: the replacement step catches every defect currently in each retired module's planted corpus — run the plants against it and report the fraction, in the shape those modules already use
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-404 — 36 test modules assert against live project state, pinning 614 dates and 191 ids that go red when the board moves rather than when code breaks

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: A rule in AGENTS.md plus a check that fails a test module asserting a literal date or id read out of live state, and the existing 36 modules moved onto fixtures
- **Verification**: the check fires on a planted assertion and stays silent on the converted modules; full --ids set unchanged apart from renames
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)

### TASK-405 — four in-process conversion rounds shipped without a single mutation, so nothing shows those tests would still go red

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: A mutation pass over the converted modules and the ADR-019 linkage half: for each behavioural change, revert the implementing line and record whether a test goes red, naming every green
- **Verification**: each mutation is listed with the file, the line reverted and the test that caught it; a mutation that stays green is reported as a finding rather than fixed silently, and perry-restore-check confirms every restore against the branch base
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: declared unlinked (`--unlinked`)
