# 2026-09-03

## Status changes

- [TASK-247] review → done · closed · evidence: `evidence/2026-09/TASK-247-round1-v4-review.md` · verification: V4
- [TASK-181] review → done · closed · evidence: `evidence/2026-09/TASK-181-round1-v4-review.md` · verification: V4
- [TASK-293] review → done · closed · evidence: `evidence/2026-09/TASK-293-round2-v4-review.md` · verification: V4
- [TASK-323] — → not_started · TASK-067's population decayed under it — census the write and read paths on today's HEAD and produce the `## Bound` its criteria file never had · owner: Coding Agent · priority: P1
- [TASK-323] not_started → in_progress
- [TASK-285] not_started → in_progress · started
- [TASK-285] in_progress → review
- [TASK-323] in_progress → review
- [TASK-139] next action · SPEC WRITTEN 2026-09-03 after 14d at breach; startable. THE FILED TITLE IS NOW MISLEADING AND THE ROW IS NOT WHAT IT LOOKS LIKE. Its 'a cell the close path clears' half was already fixed when the row was written -- ae505b3 landed 2026-08-18, two days BEFORE the row arrived on 2026-08-20, and viewer/parsers.py:3284-3295 documents it by name. But the row's DELIVERABLE was never that half: it asks for a task to carry its design id in 'a field the lifecycle does not destroy', counted structurally. MEASURED 2026-09-03 on 5c76aa2, and the state is WORSE than when the row was filed: DESIGN-001 reports impl_refs=11 and so is absent from pending_handoff, but all 11 are incidental prose mentions (store blobs TASK-212/282/292/297/258; event lines adding 293/284), while its six real implementation rows TASK-001..006 -- all done at V3 with evidence -- contribute ZERO. The signal flipped from a false POSITIVE, which was visible, to a false NEGATIVE, which is not. Sibling of TASK-282, same family: a literal matcher standing in for intent. Two shapes open, (a) a schema field -- escalated claim surface -- or (b) a typed design-link event alongside link-edge; the round picks one and records why. evidence/2026-09/TASK-139-spec.md carries the bound.
- [TASK-155] next action · SPEC WRITTEN 2026-09-03 after 13d at breach; startable. ONE WORD OF THE TITLE IS FALSE: not 'silently'. bin/perry-goals:2055-2062 already warns on every write that would re-date an asserted KR, naming each one. The defect is disclosed at THREE layers and fixed at none -- the contract emits asserted_scope: register beside the date so it cannot be misread (goals-list-contract.md:144, lib/__init__.py:705), the writer warns, and perry-state reports asserted/stale/stale_ids. The two-facts half is real and unfixed: one updated: field at phase/003-linkage.md means both 'when the register changed' and 'when the KR numbers were asserted', written at one site (perry-goals:2063) and read as freshness at perry-state:1971. Consequence: stale is computable only register-wide, so today's kr_currents 'stale 0' against 3 asserted KRs is not evidence any of them is fresh. THE CHEAPEST WRONG OUTCOME HERE IS A FOURTH DISCLOSURE. Hand-off warning in the spec: if the fix needs a field in phase/<NNN>-linkage.md, that is goals-owned, perry-goals krs is read-only, there is no KR write path (TASK-264), and the last hand edit is recorded in USER-912's answer -- so the round stops and hands off rather than editing it. evidence/2026-09/TASK-155-spec.md carries the bound and a control.
- [TASK-285] next action · ROUND 1 FAILED 2026-09-03 and both findings were mine; fixed on main at db12fa4, AWAITING ROUND 2. F2 was the serious one -- SEVEN GREEN MUTATIONS, decisively: soften the rule to 'A dispatched agent MAY work in its own git worktree where convenient' and all 7 guards report OK. The headline deliverable inverted from mandatory to optional without reddening its own guard, because every assertion pinned the PRESENCE of words and none pinned MODALITY. Reproduced before fixing. Guard now pins the imperative whole and rejects 8 hedges inside the rule block; the push NOT-in became a property check over every CLAUSE that commands a push, which took two tries -- a line-level check was still rescued by 'hook' elsewhere in the same bullet, and a bolded lead sentence ends '.**' where a plain sentence split does not cut. Eight mutations, eight red. F1: dispatch.md still listed PR URL first among REQUIRED RESULT fields with 'n/a -- direct push' as its only alternative, so on a project whose hook escalates a push a compliant agent had no truthful value for a field that step 4 gates the review transition on -- an inconsistency THIS CHANGE created across three files. Branch is now the unconditional field, PR URL conditional, template agrees with prose, one test pins all three. The round also verified the three things it was told to be sceptical about were honest (M6 really was the harness not the guard; TASK-318 really is filed and predates this row; the test_role_cards edit really was the test being wrong) and recorded an instrument failure of its own. evidence/2026-09/TASK-285-round1-v4-review.md
- [TASK-285] review → in_progress · round 1 FAILed; both findings fixed at db12fa4, awaiting round 2
- [TASK-323] review → in_progress · round 1 FAILed; both blocking findings fixed at db12fa4, awaiting round 2
- [TASK-323] next action · ROUND 1 FAILED 2026-09-03 on two blocking findings, both fixed on main at db12fa4, AWAITING ROUND 2. F1 WAS MINE, NOT THE CENSUS AGENT'S: the ## Bound said '21 rows -- 13 caught, 8 uncaught'; the delivered table has 20 rows and 12 caught. I copied that figure out of the agent's RESULT block and never counted it, which is knowledge/verification/numbers-migrate-between-sentences committed into the file that decides this row's own PASS. Corrected with the provenance stated rather than quietly. F2: the ragged-row exhibit quotes perry/BOARD.md:12 at 5720730, where that line is prose and no table has a 7-column header (widths present: 3, 4, 6, 11, 15) and TASK-900 is not a row that exists -- the claim is TRUE and the reviewer reproduced it against the real P0 table at 16-vs-15 cells, but the exhibit was synthetic and presented as read off the named baseline. Corrected, AND the limit the round surfaced is now recorded because it is load-bearing for reading (B): ragged-row fires ONLY inside a schema-recognised table; the identical rows in an unrecognised section produce 0 errors, so a project filing work under its own headings via add --group is outside the backstop entirely. THE BOUND ITSELF SURVIVED THE ATTACK -- the reviewer diffed the script's output against the hand-written member list including multiplicities, exact match, and planted six constructs that are members by the rule's own wording; the bound stayed at 89 and it swept the 21-file domain to confirm none is instantiated today. It did find the prose claim about NAME bindings is false for the f-string clause, and filed that as a new row per the bound's own rule rather than as the verdict. Non-blocking F3: tables.py:156, a 'caught' row, refuses no cell value -- 208,420 planted inputs never reached it.
- [TASK-324] — → not_started · TASK-067's bound claims names resolve through `NAME = "…"` bindings, and that is false for its own f-string clause · owner: Coding Agent · priority: P2
- [TASK-324] next action · FILED BY TASK-323's round-1 reviewer as F4, deliberately as a new row rather than as its verdict — the bound's own rule says a 90th member is a new row, and the reviewer followed it rather than widening the round. NOT URGENT, AND THAT IS MEASURED: none of the six shapes exists in the tree today, so the bound is correct about the current tree and wrong only about what it promises for a future one. The risk is the next round trusting the prose. The same reviewer filed F5, two citation slips in the same file: 'bin/perry-lint:252-256' for a block that is 253-256, and 'the one hit is a shell pipeline' for a grep returning three, two of them prose messages. Fix those in the same pass. Source: evidence/2026-09/TASK-323-round1-v4-review.md, sections F4 and F5.
- [TASK-182] next action · SPEC WRITTEN 2026-09-03 and the row is NOT what its title implies -- measured before dispatch, on 77e4ac4. The byte-for-byte match ALREADY HOLDS: perry-okr render reproduces perry/OKR.md at md5 5f400212ba724adb6246b91ab60857e4, diff reports identical true with lines_verbatim [] and cells_verbatim {}, 51 records (objective 10, kr 38, version 3). TASK-181 landed the objective records and render already existed, so it arrived without anyone building it. BUT THE GATE CANNOT FAIL. On a git-archive copy with all 10 objective records deleted, diff still reports identical: true -- the renderer passes through verbatim any line it has no record for, and the only thing that moves is lines_verbatim going 0 to 10. So diff exiting 0 does not mean the records rebuild the file, it means the output matches whether the records did the work or the file did. DESIGN-009 section 7 risk 2 already names the correct bar and nothing implements it: 'cells_verbatim must be {}'. The real deliverable is making step 2's gate able to fail, plus the control that goes red when the records are removed. ORDERING IS LOAD-BEARING: the target is the CURRENT OKR.md with its tables, and TASK-236 deletes them -- run before it or the gate passes against a file with nothing left to rebuild. Escalation scan exit 0. SLOT-BLOCKED: claude-subagent is 2/2 with both V4 round 2 reviews in flight. evidence/2026-09/TASK-182-spec.md
- [TASK-323] in_progress → done · closed · evidence: `evidence/2026-09/TASK-323-round2-v4-review.md` · verification: V4
- [TASK-285] in_progress → blocked · awaiting USER-913 — review-rounds-exhausted after 2 FAILs
- [USER-914] — → pending · TASK-285 has FAILed two V4 rounds and perry-lint says another round is not the next step. UNUSUALLY, THE PRINCIPLE HAS ALREADY BEEN NAMED -- by the round 2 reviewer, not by me -- and I have already implemented it. This ask exists to have you confirm or overrule that, because a PMO who has just fixed something is the worst judge of whether the principle is settled. WHAT HAPPENED. The row writes one rule into three shipped files: a dispatched agent works in its own git worktree, the primary checkout is never switched by an agent and merges the branch afterwards. Round 1 defeated the guard by softening the rule to 'MAY ... where convenient' -- every assertion pinned the presence of words, none pinned modality. I fixed that with a denylist of eight hedge words. Round 2 then kept the pinned imperative VERBATIM, used none of the eight, and added one sentence inside the rule's own blockquote: 'Where a worktree is impractical, sharing the primary checkout instead is an acceptable alternative.' All nine tests reported OK with the headline deliverable retracted. It also walked past the push check with two synonyms (publishes / raises a pull request). Its summary: A DENYLIST OVER ENGLISH HAS NOW LOST THIS ARGUMENT TWICE. THE OPTIONS. (a) IMPLEMENTED AND MY RECOMMENDATION -- an allowlist. The rule block is compared literally and four normative regions are pinned by sha256; any addition, removal or rewording reddens. Rationale prose is deliberately NOT pinned, so the observed-failure account and the local-merge argument can still be improved without touching a test; what an agent is TOLD TO DO cannot change silently. Thirteen mutations, thirteen red, including both that defeated the old design and a single extra space. Cost: changing the rule now requires re-pinning a digest in the same commit, which is friction on purpose. This is the same move you chose in USER-904 (one header_index over a smarter detector) and USER-906 (one invariant over a fourth predicate). (b) Keep a denylist and widen it again. The record says this is the third widening and the first two each looked principled. (c) Accept the guard as advisory, close at a lower rung, document the limit. (d) Drop the guard entirely and rely on the fact that the rule is now in three files a reader sees. STATED LIMIT OF (a), because it is real: an allowlist pins what EXISTS and cannot cover a fifth normative region someone adds later in a file it does not read. Evidence: evidence/2026-09/TASK-285-round2-v4-review.md and TASK-285-result.md. · blocks: TASK-285
- [TASK-325] — → not_started · Perry has a plain-language field for every task, 90 of 114 open rows leave it empty, and nothing asks for it · owner: Coding Agent · priority: P1
- [USER-915] — → pending · TASK-067 has FAILed two V4 rounds and needs a principle chosen, not a third round. THE CENSUS THAT MAKES THIS ANSWERABLE NOW EXISTS: TASK-323 delivered a bound and a census and PASSED V4 round 2 on 2026-09-03, so unlike the last two rounds this choice is against measured numbers rather than a recollection. Both failed rounds carried criteria-unbounded because the criteria file had no bound; it now has one -- 89 members (27 W1 / 18 W2 / 38 R1 / 6 R2), last element viewer/tables.py:280, generated by a runnable script at evidence/2026-09/TASK-323-bound.py. Census: 20 rows, 12 caught, 8 uncaught, plus 13 shape rows with 4 red controls. THE TWO READINGS, with what each would actually have to cover. (A) EVERY WRITE PATH MUST REFUSE AN UNRENDERABLE CELL. Population is all writers. To close it must cover all 27 W1 members, already refusing, PLUS the 7 uncaught separator-row builders -- perry-task:985, :1034, :1112, :5174, perry_md_store.py:774, perry-goals:327-328, perry-goals:3093 -- PLUS bin/perry-decide:332, which guards with a weaker third spelling that misses a trailing newline, PLUS perry-task:7431. It would NOT cover the five uncaught read shapes, which are not writes. Its cost is visible in the bound itself: the population has no last element other than the current line count, which is exactly why this file had no bound for two rounds and why each round found a member the previous one never reached. (B) ONE CHOKE POINT PLUS ONE DETECTOR, MY RECOMMENDATION. To close it must cover ONE SYMBOL -- nothing outside render_row builds a row -- with ragged-row as the backstop. The census says precisely what that buys and what it does not: it covers the 7 separator builders by construction IF separator rows are routed through the choke point too, which they are not today; its backstop is MEASURED to work on w4-w7 and MEASURED not to exist for r2/r3/r4/r7/r8; ragged-row cannot cover the read side at all and .split('|', 6) proves no count-based check ever will; its detector has four demonstrated blind spots on the read half because SPLIT_RE matches none of maxsplit, re.split, a SEP constant, or .rsplit/.partition; and it does not reach perry-decide:332, which builds no rows. TWO MORE FACTS THE ROUNDS DID NOT HAVE. First, ragged-row fires ONLY inside a table the schema recognises -- round 1 confirmed with a control that the identical 8-cell row is silent in an unrecognised section -- so on a project filing work under its own headings via add --group, which Perry explicitly supports, (B)'s backstop does not exist. Second, both readings leave perry-task:7431 open; it is a message-quality defect rather than a corruption path. PRECEDENT, OFFERED AS PRECEDENT AND NOT AS AN ARGUMENT: you chose shape (B) in USER-904 after seven rounds and USER-906 after three. The census neither confirms nor contradicts that -- (B)'s one-symbol surface is real and measured, and so is the fact that its detector is green on nine shapes, five of which have no backstop of any kind. Evidence: evidence/2026-08/TASK-067-finding.md sections Bound, Census and The two readings; evidence/2026-09/TASK-323-round2-v4-review.md. · blocks: TASK-067
- [TASK-182] not_started → review
- [TASK-283] not_started → in_progress · started
- [TASK-325] not_started → in_progress · started
- [TASK-326] — → not_started · test_project_root_resolution reddens under the parallel runner while agent worktrees are live — TASK-258's disease in a second module · owner: Coding Agent · priority: P1
- [TASK-283] in_progress → review
- [TASK-326] next action · REPRODUCED TWICE MORE 2026-09-03, after the TASK-182 and TASK-283 merges: 3 of 23 red under tests/parallel -j 4, 23/23 green under python3 -m unittest on the same tree seconds later, with 34 nested agent worktrees present under .claude/worktrees/. So the correlation now has three observations and the mechanism still has zero -- that gap is the row. TASK-258 fixed the same disease in tests/test_tree_guard.py and is done, so the technique probably already exists in that file's fixture. DO NOT ACCEPT THE CORRELATION AS THE CAUSE: the verification requires removing the worktrees (or pointing the walk away from them), showing the parallel run goes green, restoring them, and showing it goes red again. A control that does not flip both ways has not identified anything.
- [USER-914] pending → answered · 决定 2026-09-03（用户拍板，Perry 推荐 a）：选 (a) —— 白名单。规则块逐字比对，四个规范性区域按 sha256 钉死；任何增删改写都变红，包括没人预料到的那种。理由散文故意不钉：观察记录和本地 merge 的论证可以继续改进而不动测试，不能悄悄变的是 agent 被要求做什么。代价接受：以后改这条规则必须在同一个提交里重新钉一次摘要，这个摩擦是有意的。不选 (b) 再拓宽黑名单 —— 那已经输了两次，第三次也只是猜下一个没被列进去的词。已实现，13 个突变 13 红。声明的限制保留在记录里：白名单只能钉住已存在的规范性文字，日后有人在它读不到的文件里新加第五处，它看不见。
- [USER-915] pending → answered · 决定 2026-09-03（用户拍板，Perry 推荐乙/B）：选 (B) —— 一个收口点。除 render_row 外没有东西构造表格行，守卫变成一个符号的检查面。这是本项目第三次选这个形状（USER-904 的 header_index、USER-906 的 shrink 不变量）。THE THIRD OPTION WAS OFFERED AND NOT TAKEN: 我另外提过「(B) 收口 + 把甲那 9 处里真正会写坏数据的 8 处也做掉」，用户选的是 (B) 本身，所以那 8 处不在这次范围内。因此实现时必须明确以下三点，它们是 (B) 买到的和没买到的，普查已经量过：(1) 7 个分隔行构造点只有在分隔行也被路由进收口点时才被覆盖 —— 今天没有，所以路由分隔行是 (B) 的一部分而不是附加项；(2) bin/perry-decide:332 不构造行，(B) 够不着它，它仍然开着；(3) bin/perry-task:7431 两种读法都不覆盖，是消息质量缺陷不是损坏路径。最重要的一条，按 Perry 的推荐记录：不要把 ragged-row 当成 (B) 的卖点。普查测出它在 w4-w7 有效、在 r2/r3/r4/r7/r8 不存在，读取侧有四个已证实盲点（maxsplit、re.split、SEP 常量、.rsplit/.partition），而且只在 schema 认得的表格里触发 —— round 1 用对照确认，同样的 8 格行放在不被识别的 section 里报 0 错误，所以用 add --group 的项目上这张网根本不存在。这些洞要写成声明的限制，不能让下一轮以为有兜底。
- [TASK-285] blocked → review · USER-914 answered (a); the principle is settled, so a further round is no longer forbidden
- [TASK-067] next action · UNBLOCKED 2026-09-03 by USER-915: reading (B) chosen -- one choke point, nothing outside render_row builds a row. STILL NEEDS A SPEC; the choice is made, the work is not. THREE THINGS THE SPEC MUST CARRY, all measured by TASK-323's census and none of them optional. (1) Routing the 7 separator-row builders through the choke point IS PART OF (B), not an extra: perry-task:985/:1034/:1112/:5174, perry_md_store.py:774, perry-goals:327-328, perry-goals:3093. (B) covers them by construction only if they are routed, and today they are not. (2) bin/perry-decide:332 builds no rows, so (B) does not reach it -- it stays open and needs its own row. (3) bin/perry-task:7431 is covered by neither reading; message quality, not corruption. AND THE RECOMMENDATION THE USER'S ANSWER CARRIES: do NOT sell ragged-row as (B)'s backstop. Measured present for w4-w7, measured absent for r2/r3/r4/r7/r8; four demonstrated read-side blind spots (maxsplit, re.split, a SEP constant, .rsplit/.partition); and it fires ONLY inside a schema-recognised table -- confirmed with a control, the same 8-cell row is silent in an unrecognised section, so on a project using add --group the net does not exist. Those holes are declared limits in the deliverable, not caveats to discover later. The third option -- (B) plus fixing the 8 corruption sites from reading (A) -- was offered and NOT taken.
- [TASK-067] not_started → in_progress · started
- [TASK-066] summary · — → bin/perry-task is to be split into one file per subcommand group. The split is deliberately left unsized for now: the markdown reader, the row renderer and the cell escaping are all on their way out of that file, and re-measuring once they are gone is cheaper than splitting it twice.
- [TASK-129] summary · — → Perry represents an agent as five separate strings that never join into one record, and the `role` field has never once been written on any row. Who owns the agent register was settled on 2026-08-20 — work owns `.perry/agents.jsonl` and `.perry/roles/` — so what the row still needs is a spec before it can be dispatched.
- [TASK-172] summary · — → Four of Perry's six document collections cannot be reached through any published contract. Deferred on 2026-08-21 by the user, with the cost recorded rather than waved through: aiMark reads those directories directly for now, which knowingly makes it a second reader of Perry's LAYOUT, so a consumer holding `perry/design/` breaks silently the first time a project moves its state root.
- [TASK-198] summary · — → Perry keeps six registers as JSONL stores that tools read and write through. `BOARD.md § Cadence` is not one of them and is still a markdown table nothing owns. This row converts it.
- [TASK-204] summary · — → Perry records every other kind of event through a writer that owns the event's shape. A migration event has no writer at all, so TASK-180 appended hand-written JSON straight into the append-only event log.
- [TASK-206] summary · — → A Perry write returns no sequence number. So a consumer that polls has no way to tell whether what it just read already reflects its own write or predates it, and a stale read is indistinguishable from a fresh one.
- [TASK-207] summary · — → Nothing lets a caller say 'write this only if the board is still what I read'. There is no compare-and-set on a Perry write, and the board demonstrably does move between a read and the write that follows it, so two writers can silently overwrite each other.
- [TASK-208] summary · — → perry-diagnose answers 'has this ask been answered?' by searching for words in free prose rather than by reading the ask register. It disagrees with the store in both directions — reporting answered asks as pending and pending ones as answered.
- [TASK-173] summary · — → An Objective has no id, so nothing can point at one durably: `okr.objectives[].id` is empty for all five. The phase level already solved exactly this — `phase/002-linkage.md` states `id: O1` and the payload carries it — and the contract already accepts a STATED id where it refuses a derived one. DESIGN-009 was drafted for it on 2026-08-21 with four user decisions still open.
- [TASK-177] summary · — → Setting an OKR is currently a ten-field checklist to be filled in, where it should be an elicitation that draws the answers out. DESIGN-011 was drafted for it on 2026-08-21 with four user decisions open. Its step 2 is the real gate: run the question bank against a project that has no OKR.md and score the output with the rubric, or the claim that the rubric surfaces zero issues is not measured at all.
- [TASK-077] summary · — → The first role card for work that is not software — a finance-shaped role, run end to end on one real task. All four dependencies are done, so it is startable; the real blocker is that every role card shipped so far is software-shaped, so this is the first honest test of whether the role model generalises. The run itself and its V5 signature stay with the user.
- [TASK-179] summary · — → Writing about a task id in prose costs a dangling-reference entry, and three records did on one night. The row is to decide the rule for the five dangling ids that resulted — widen the report's mark beyond the paragraph, exempt evidence records wholesale, or accept the cost in writing — and then make the reconcile test assert whichever was chosen.
- [TASK-266] summary · — → ADR-012 decided that an agent should detect when a projection is behind the store it is derived from, and nothing in Perry surfaces that. This is the whole implementation of the detection half: a per-pair mtime comparison replacing about 3,100 lines of drift census. Perry's own history is the argument for building the surface rather than writing the instruction — an instruction with nothing behind it does not get run.
- [TASK-267] summary · — → Perry reports six stores and names five of them. The tasks store alone prints `store: 225 record(s)` and `drift against the store`, where the other five say risks store, OKR store, config store, intake store and ask store — so the one line a reader is most likely to be looking at is the one that does not say what it is about.
- [TASK-268] summary · — → The top-risks reader still builds its answer from `BOARD.md` even though `perry/risks.jsonl` exists and is canonical. The task and OKR readers sitting next to it in the same file already prefer their stores, so this one reader is the odd one out rather than the norm.
- [TASK-269] summary · — → A test module passes when run alone and fails three identity assertions when run under `unittest discover`, because of a module double-import artifact. Two reviewers observed it independently on 2026-08-29. The defect worth fixing is not the assertions: it is that the suite's answer depends on which runner invoked it and nothing anywhere says so.
- [TASK-270] summary · — → One command can put a project into a state only that same command is offered to repair, and it cannot. Run against a config file carrying no settings, `perry-config write --from-file` writes a zero-record store and exits 0; every subsequent task and goals write is then refused forever, while verify, diff and lint all report zero drift and so report nothing wrong.
- [TASK-271] summary · — → `tracks_source` appears on two published payloads with four possible values and has no entry in the schema or in any reference page — so a consumer reading it has to guess what the values mean. Two consecutive reviewers raised it before it was filed.
- [TASK-272] summary · — → A host-support test is flaky under the parallel runner: red once on 2026-08-29 with an empty in-flight cache, green in isolation and green on two consecutive re-runs. Same class as the queue-reconcile and scratchpad-baseline races already on the board — a concurrency defect that only appears under load.
- [TASK-273] summary · — → Duplicate ids destroy records silently, in two different directions. A duplicate on the BOARD deletes a stored risks or asks record on an ordinary write, because the record builders skip an id they have already seen; a duplicate IN THE STORE leaks one record's cleared state onto the other and then persists it. Both predate TASK-203 and neither was reachable before it.
- [TASK-274] summary · — → Re-ordering the tracks table in `.perry/config.md` by hand reads as config-store drift to perry-lint and as nothing at all to every other tool. The behaviour is defensible under the principle as written, but it is neither documented nor guarded. The reviewer who found it also notes that two neighbouring behaviours were each verified against only one fixture.
- [TASK-275] summary · — → After an intake sweep empties the canonical intake store, perry-lint reports `perry/intake.jsonl` as foreign state Perry did not write — even though the intake contract says an empty file is still a store. The fix has to recognise Perry's own declared empty store without also excusing a foreign file that happens to share the name.
- [TASK-265] summary · — → perry-state exists because `BOARD.md` was canonical and counting anything meant parsing a markdown table. With six JSONL stores the counting is a loop, so what is left worth keeping is the payload SHAPE it publishes, not its reader. DESIGN-014's user decision 1 was answered on 2026-09-01: thin it to a query over the stores rather than keeping or deleting it, and the published shape does not change.
- [TASK-262] summary · — → A render has to show which of the things it prints are canonical records and which are projections of them, or a reader cannot tell what is safe to edit. Blocked in practice on the two rows that create the render this property has to hold on. It replaces TASK-199, which carried the same scope and was dropped in error on 2026-09-01 in a way that could not be reversed.
- [TASK-281] summary · — → The sixth row of DESIGN-015. It is what turns phase KR P003-O3-KR2 from a number somebody typed into a number something counted, by computing it from the linkage store and the event log. It should not be allowed to lag the rest of the sequence for that reason.
- [TASK-280] summary · — → The fifth row of DESIGN-015: `phase/<NNN>-linkage.md` sheds the half of itself that is schema'd data once that data lives in the store. Separable and allowed to lag — unlike the add-time writer it does not gate P003-O3-KR2 — and it is DESIGN-013 hygiene rather than work serving that KR.
- [TASK-276] summary · — → The first row of DESIGN-015's implementation plan, locked 2026-09-02: declare `linkage.jsonl` in Perry's claims list and write the three record schemas it will hold. Section 7 of that design carries the risk this creates — a new claimed path means a new namespace-collision warning in every existing Perry project.
- [TASK-277] summary · — → The second row of DESIGN-015: import the linkage records that currently live in `phase/003-linkage.md` into the new store. The count filed with the row is a measurement rather than an estimate, so it is to be re-measured before importing and the difference reported if the register has moved.
- [TASK-278] summary · — → The third row of DESIGN-015, and the one the whole sequence turns on: move the six linkage readers onto the store. It MUST land before the add-time writer, and the ordering is a hard constraint rather than a preference — a writer pointing at a store the readers have not moved to fails silently, with the edge landing while every reader still answers from the old document.
- [TASK-279] summary · — → The fourth row of DESIGN-015: `perry-task add --kr` writes the linkage edge itself and the add event carries it, which is what moves phase KR P003-O3-KR2 off zero. Blocked on the reader migration by the hard ordering constraint that row records. The design flags its own reading of user decision 3 as an interpretation, so if an explicit never-asked record was intended, this row changes with it.
- [TASK-212] summary · — → Two directions of one missing edge, and this row is the design-to-task half: a locked design's plan step has no link to the work that discharges it, so a decision that gets no task row simply does not ship and nothing notices. Its sibling covers the task-to-design direction. Whoever picks either up should first decide whether one declared edge answers both, because two mechanisms for one relationship is how this project acquires a second translation table.
- [TASK-282] summary · — → The task-to-design half: an implementation row need not cite the design it implements, so a finished design can report as never handed off forever. The trap was measured as it fired — filing this row took DESIGN-001 from `impl_refs` 0 to 1 and emptied the pending list, with nothing implemented, because the counter is a literal text match. So the defect is two-sided: six real implementation rows count for nothing, and one row that merely says 'read DESIGN-001' counts as implementing it.
- [TASK-263] summary · — → Measure how bin/perry-task and bin/perry-lint split by CATEGORY, counted at the call site rather than by grep. It is the first step of DESIGN-014's implementation plan and the precondition for that document's central section being a measurement rather than an admission: the two files are 40% of bin/ and neither of them is one thing.
- [TASK-067] summary · — → A writer can destroy the very table it is writing to, and perry-lint cannot see it happen. The row's SCOPE HAS DECAYED and must be re-derived before anyone starts: of the four hazards originally justifying it, two are now measurably false — the decisions index has no write site left, and the migration tool was deleted on 2026-08-31 — one needs confirming rather than inheriting, and one is doubtful. The underlying question may still be a real P0, but its population has to be re-measured before the work can be estimated.
- [TASK-099] summary · — → Sweep bin/, viewer/ and tests/ for document-handling code that ADR-007 made dead, and report what is dead against what is still load-bearing. Round 1 is the CENSUS only: the fence this row is eventually for is blocked on a product decision, and the phase says outright that the measurement is the input the user needs in order to answer it. The round does not fence anything and does not close the row.
- [TASK-283] summary · — → perry-lint reads the track register out of `.perry/config.md` after resolving the project root, so on a project that keeps its config only in the store it returns no tracks rather than that project's own. It is strictly better than reading a FOREIGN register and it is still not reading `.perry/config.jsonl`. Reported by another row's agent rather than folded into that round, which is why it is a row instead of scope creep.
- [TASK-286] summary · — → review.md requires a bounded criterion and perry-lint reports its absence, but add-task — the only place a spec is ever written — never mentions `## Bound`. So specs are authored without one and the omission surfaces rounds later, which is a measured cost rather than a style preference: one row burned five review rounds, another eleven, and a census found the unbounded finding eight times across twelve review files.
- [TASK-287] summary · — → Nothing warns an author against writing a cross-reference to a task id that has not been minted yet, and the id guessed may land on the row doing the guessing. Observed when a row's next action said 'unlike TASK-280' predicting what the NEXT row would be numbered, and that row was itself minted as TASK-280, making the sentence self-referential. The existing rule forbids an example id that resolves to nothing; this one resolves, to the wrong row.
- [TASK-288] summary · — → The git-boundaries page never says how to write a commit message, and Perry's documentation convention is backticked identifiers — so a `-m` string containing them has them executed by the shell and substituted away. It happened: a commit explaining the escalation gate's false positives named two fragments and shipped with both names simply gone. This bites Perry harder than most projects because nearly every honest commit message about its own code contains backticks.
- [TASK-289] summary · — → perry-task's `--actor` is optional and defaults, so a write to a lane-owned file can end up with no owner any session can claim. The actionable core is that optional actor, NOT the incident that exposed it — five link edges were written that the session filing this could not account for, and the edges themselves turned out to be correct. The tool's own help already states the open question: DESIGN-004 asks whether actor should be mandatory and until that is answered an unset actor is recorded as unset.
- [TASK-290] summary · — → The dispatch safety gate matches a path a spec merely CITES as though the spec were going to write it. Both directions are live and one is silent: honest specs are refused for naming the evidence file the round exists to produce, and one spec passed only because an incidental mention elsewhere cancelled a real hit, with no output distinguishing that from a genuinely clean scan. Same defect family as a design-id counter that cannot tell 'implements' from 'mentions' — a literal matcher standing in for intent.
- [TASK-291] summary · — → close-task lets a V4 closure through when its verdict block cannot be parsed, and perry-lint reports it only afterwards — so a rung can be claimed and merely questioned later, which is not a gate. Scoped to the FORWARD half: refuse the close. Whether the ten existing closures in that state should be re-reviewed or restated rewrites this project's own verification record and belongs to the user.
- [TASK-264] summary · — → There is no write path for a KR record at all, so adding, restating or withdrawing one means hand-editing a file the documentation says is never edited by hand. It is now blocking a decided pivot: the user decided to withdraw a phase KR, the pivot procedure offers exactly that operation, and the tooling cannot perform it. The deliverable should cover both levels — the overall KRs in okr.jsonl and the phase KRs in the linkage file.
- [TASK-294] summary · — → Every namespace-collision warning Perry emits is about a file Perry itself wrote, and the remedy each one offers is a no-op — the warnings say 'move Perry's state to its own root' and the state root already IS that root. One of the five is a separate bug: a FILE claim whose warning tells the reader to move files out of a file. This is why every lint run reported warnings that every session skipped as pre-existing; a check that only ever cries wolf gets trained out of the reader.
- [TASK-295] summary · — → A scanner's heading-depth test does not cover the claim its own docstring makes. Reported by a reviewer as a green mutation and correctly not charged as a failure — a green mutation is a finding either way, and here it is the second half of one: the guard does not test what it says it tests.
- [TASK-296] summary · — → A store record's title field is unverifiable: plant garbage in it and `verify` still reports drift 0 and `byte_identical: true`. It matters because that sentence is what every store row in this project rests its acceptance on, and it is silent about any field the renderer does not read. Found while attacking a load-bearing claim and correctly ruled pre-existing rather than that row's defect.
- [TASK-298] summary · — → A dispatched agent's process cleanup matches across the whole machine and killed another agent's test suite. This is what concurrency looks like when the isolation is per-worktree but the process table is shared: the dispatch limit reserves slots and nothing scopes a signal. Worth pairing with the finding that the shared scratchpad is not private either — both are the same shape.
- [TASK-300] summary · — → DESIGN-014's per-tool line counts drifted and were not re-measured when the document was locked. Small on its own, but that document's whole method IS per-tool measurement, so a stale figure in it is the document failing its own standard rather than a typo.
- [TASK-302] summary · — → bin/perry-config's help text names a tool that was deleted. The argument that decides it came from the consumer who reported it: the guidance says to believe the contract table over the help text, but the help text is what a person actually runs — so Perry told a consumer to prefer one surface over another instead of fixing the one people invoke.
- [TASK-301] summary · — → schema/README.md tells a consumer to read the state root out of `.perry/config.md`, which is the projection rather than the register. Measured 2026-09-02, and the measurement makes the fix sharper than 'the page is stale': the tool resolves the state root itself and publishes it, including on a project that has no store, so a consumer that can run the tool never needs to parse the markdown and one that reads the store alone would get it wrong.
- [TASK-299] summary · — → A line count in an evidence document was produced by an instrument that walks the filesystem rather than the git index, so untracked bytecode was counted into it. The lesson this row should carry is about instruments and about propagation: the wrong REASON for the wrong number had already been written into a locked design document, and correcting the board cell did not correct the artifact. A reviewer found it there and failed the round, correctly. A finding already written down somewhere needs the write chased, not just the record amended.
- [TASK-305] summary · — → A design document's entry cites the branch it measured by a moving ref, so its figure stops re-deriving the moment that branch advances. Recorded by two consecutive reviewers and deliberately not charged by either, because it sits outside the round's bound and the review rules forbid widening one except by filing a row. This is that row. The item had already degraded further between the two rounds, which is the argument for it rather than against it.
- [TASK-306] summary · — → One half of a spec-scanning function de-duplicates across sections and the other half does not. Deferred in 2026-08 with a stated condition — the next change to that function — and the condition has now fired, so the deferral discharges into a row rather than expiring silently, which is the whole point of attaching a condition to one. Still cosmetic today: no spec repeats a fragment across two sections.
- [TASK-297] summary · — → A parser's docstring says the walk is skipped when it is handed an exact root, and the code walks four levels anyway. CLOSING IT COSTS MORE THAN THE DOCSTRING GAP SUGGESTS and a correct fix will look like a regression unless it lands with its tripwire: the minimal fix turns another round's deliberate anti-vacuity test red, so whoever takes this must retire or invert that test in the same commit. Whether the minimal form is green suite-wide is unverified and should be established first.
- [TASK-307] summary · — → Three more test fixtures carry a `.perry/` directory with no event log, so the same parent walk escapes them too. Inert today for exactly one reason — none of the three ships a design directory — and adding a single design document to any of them re-opens the defect there with no warning and no test. Sequence it after the walk fix: if that makes the walk honour an explicit root, these three may need nothing at all.
- [TASK-308] summary · — → Almost every spec on this project — 127 of 134 — carries no `## Bound`, and the check that would report that cannot fire until after the round it would have bounded. The check is implemented and correct and it reports two, because it reads the criteria field OF A VERDICT BLOCK — so a spec is only reachable once a review has already been written against it. A post-hoc check on a pre-flight property. The two it does fire on belong to the row that failed two review rounds and hit the schema limit: it found the unboundedness by paying for it twice.
- [TASK-309] summary · — → Nothing a dispatched agent does reaches disk until its single terminal commit, so an interruption at any point before that costs the whole round. Measured on 2026-09-02: five agents were dispatched, the laptop was closed, all five died at the watchdog and git log across all five branches shows zero commits. Between fifteen and thirty minutes of work each, and the only surviving trace was the last sentence each had streamed. The cause was environmental, which is exactly why the row exists — an agent cannot prevent the interruption, but it can refuse to hold a round hostage to a single terminal commit.
- [TASK-310] summary · — → Two comments in a test file assert evidence the round could not have measured and a set it did not enumerate. One claims zero differences where 843 of 846 file modes differ; the other calls a file 'the only other file the copied run executes' where the enumeration finds twenty-four. Both are documentation defects against a round that passed on everything behavioural, and one edit closes both.
- [TASK-311] summary · — → A host-support test admits four registrations through a gate whose global cap is three, and the runner's own docstring calls the module a known flake without naming the mechanism — which is the problem, because a named flake with an unnamed mechanism is indistinguishable from a real concurrency defect, and this one reads like one. Attributed rather than waved away: the module is red on a pristine archive of the base commit, one run in four under load.
- [TASK-312] summary · — → This test module is now the suite's longest at 48-124 seconds, and its own fix is what put it there. It is NOT a regression and should not be read as one — the round that did it just passed review, and both of the things that cost the time are load-bearing. So the question is not whether the cost is justified but whether it is the right size. The 48-124 second spread is itself the first finding: the module's cost varies 2.6x with machine load.
- [TASK-313] summary · — → Perry's limiter for concurrent dispatches has a race in it, found while three review rounds were running at once. Reported by a round that deliberately left it outside its own bound, and filed here so the refusal discharges into a row rather than into a sentence in a result document. The limiter is the only thing coordinating concurrent agents on this machine, and five ran at once the night it was found.
- [TASK-314] summary · — → A test fixture hard-codes the test-id shape produced by Python 3.11 while its docstring claims it was copied line for line from a real run — and the interpreter this project actually runs is 3.9.6, where the ids collapse to the class name. Counts are unaffected, which is why it was not charged. The part worth knowing beyond this fixture is that two interpreters are in play here and they disagree about test identity.
- [TASK-315] summary · — → A module made red only by an unexpected success reports zero failed tests — the exact zero-failures-under-a-red-module shape the fix that shipped argues against, arriving through the one door its guard does not cover. Unreachable today because no test module uses an expected failure. Filed now rather than when somebody writes the first one, because at that point the number will look right and be wrong.
- [TASK-316] summary · — → The header-rule net can see a walk that loses a whole reader DIRECTORY and cannot see one that loses a single reader FILE — and could not before the round that widened it either, so this is the remaining half of an older hole rather than damage from that round. Per-file loss was never expressible in the corpus, whose entries are per-directory.
- [TASK-317] summary · — → A round's docstring and its result document both say eight whole-tree scans where the instrumented count is twelve. The delta is right and the cost claim rests on the delta, which is why it was not charged. The four unaccounted scans are one test scanning inside a loop, so the number counts call SITES where the runtime counts CALLS — and that is the same slip the round already fixed once in a neighbouring docstring while leaving this one standing.
- [TASK-318] summary · — → A markdown code fence disables the paragraph a procedure depends on while the guard checking that paragraph is present stays green. The reviewer filed rather than failed, and the reason is the interesting part: an HTML comment renders as nothing, so a guard staying green over one asserts a reader sees text no reader can see — but fenced prose still renders and is still read. Whoever takes this should decide whether the guard is about BYTES PRESENT or about A READER SEEING, because those two readings diverge here for the first time.
- [TASK-319] summary · — → A parser's docstring still carries the premise its own round measured false, and now contradicts the corrected copy in perry-lint. The round deliberately left the parser alone, because touching it would have broken the byte-identity carrying an earlier parity result forward without re-earning it — the right call for that round, and it leaves the two docstrings disagreeing with the parser's being the wrong one. Cheap to fix now that the parity no longer needs protecting.
- [TASK-320] summary · — → A green mutation, filed rather than charged: map one function's error return to True — so nothing is ever reported stale — and all 24 provenance tests still pass. The reason is exact: the test pins only the True and None answers and never the False case, which is the only answer staleness reporting depends on. The shape is worth more than the instance — a three-valued function whose tests pin two values leaves the third free, and here the free one is the whole point of the feature.
- [TASK-321] summary · — → A result document closes with a false sentence about a lint rule that is deliberate and already recorded. The reviewer checked the round's own alarm and found it half right: the non-monotonicity is real and was reproduced at its source, but it is documented, and the payload DOES record which ids came back clean — so the round's closing claim that nothing records it is false here. Correct the page and open no further row.
- [TASK-322] summary · — → A comment names the wrong file as the guard for its own audit wiring: the file it points at is byte-identical to base and contains no occurrence of the term at all. The guard is real and lives elsewhere, so this is a pointer defect rather than a missing guard. Worth a row because a comment that sends a reader to the wrong file to verify a claim costs the next reader exactly the time the comment was meant to save.
- [TASK-139] summary · — → THE FILED TITLE IS NOW MISLEADING AND THE ROW IS NOT WHAT IT LOOKS LIKE. Its 'a cell the close path clears' half was already fixed two days before the row was even filed. The deliverable was never that half: it asks for a task to carry its design id in a field the lifecycle does not destroy, counted structurally. Re-measured 2026-09-03 and the state is WORSE than at filing — the design now reports eleven implementation references, all of them incidental prose mentions, while its six real implementation rows contribute zero. The signal flipped from a false positive, which was visible, to a false negative, which is not.
- [TASK-155] summary · — → ONE WORD OF THE TITLE IS FALSE: not 'silently' — the writer already warns on every write that would re-date an asserted KR, naming each one. The defect is disclosed at three layers and fixed at none. The real and unfixed half is that one `updated:` field carries two different facts, 'when the register changed' and 'when the KR numbers were asserted', written at one site and read as freshness at another. The consequence is that staleness is computable only register-wide, so a report of 'stale 0' is not evidence that any individual number is fresh. The cheapest wrong outcome here is a fourth disclosure.
- [TASK-324] summary · — → A bound claims that names resolve through simple assignment bindings, and that is false for its own f-string clause. NOT URGENT, and that is measured: none of the six shapes exists in the tree today, so the bound is correct about the current tree and wrong only about what it promises for a future one. The risk is the next round trusting the prose. Two citation slips in the same file are to be fixed in the same pass.
- [TASK-285] summary · — → dispatch.md tells a dispatched agent to branch but never to isolate, so a subagent switches the SHARED working tree and the PMO's own writes land on that agent's branch. Blocked awaiting a user decision after the round exhausted its review budget with two failures.
- [TASK-325] in_progress → review
- [TASK-139] not_started → in_progress · started
- [TASK-155] not_started → in_progress · started
- [TASK-256] not_started → in_progress · started
- [TASK-155] next action · ROUND ONE LOST 2026-09-03 to the 600s stream watchdog, nothing recovered. Its branch coding/task-155-per-kr-assertion-date sat at 061ee7b -- the commit I cut it from -- so ZERO work survived; the agent's last streamed line shows it was still establishing a baseline. This is TASK-309's subject exactly ('a dispatched agent's entire round lives in its context until one final commit'), and it is the second stall of this shape in two days. The brief already said to commit the evidence file before any long run; that was not early enough, because the long run WAS the baseline. Re-dispatched with a first-five-minutes stub commit as step 1 rather than as advice. The spec is unchanged and still correct.
- [TASK-309] next action · SECOND OBSERVATION 2026-09-03, and the first one where the cost is measured. TASK-155's round died at the 600s stream watchdog with its branch still at the commit it was cut from -- zero commits, whole round lost, agent still establishing a baseline when it stalled. Yesterday a batch of five died the same way when the laptop closed. THE MITIGATION THAT DID NOT WORK IS WORTH RECORDING: the dispatch brief already instructed 'commit your evidence file before any long run', and that is too late by construction when the long run IS the baseline suite. Whatever this row builds has to make the first commit happen before the agent does anything expensive, not before its LAST expensive thing.
- [TASK-327] — → not_started · perry-decide guards a line break with a third, weaker spelling · owner: Coding Agent · priority: P1
- [TASK-326] next action · FOURTH AND FIFTH OBSERVATIONS 2026-09-03, and the shape is now much sharper than 'one flaky module'. Modules seen red under tests/parallel -j 4 and green standalone seconds later, one per run and A DIFFERENT ONE EACH TIME: test_project_root_resolution (3 of 23), test_one_primitive (1 of 6), test_one_header_rule (1 of 13, an ERROR not a failure). Every one of them WALKS THE REPOSITORY, and 38 nested agent worktrees sit under .claude/worktrees/, each carrying a full .perry/ and perry/. So the hypothesis is no longer 'that module is flaky' but 'tree-walking modules are unreliable under parallel while nested worktrees exist', which is a class and is testable. STILL UNPROVEN and the verification is unchanged: remove the worktrees, show the parallel run green, restore them, show it red. A control that does not flip both ways has identified nothing. NOTE THE ONE THAT WAS REAL: test_store_drift also went red in this window and was NOT interference -- it was a genuine orphaned record from a merge I got wrong. Do not assume every red in this window belongs to this row.
- [TASK-067] in_progress → review
- [TASK-256] in_progress → review
- [TASK-155] in_progress → blocked · blocked on TASK-264 — no writer of `current` exists, so a per-KR assertion date cannot be written by anything
- [TASK-264] next action · NOW BLOCKS TASK-155, measured 2026-09-03. TASK-155's round proved by exhaustion that a per-KR assertion date cannot be written by anything that exists: no per-KR field in phase/<NNN>-linkage.md, ZERO of 1880 events across 23 kinds carries current/target/asserted/metric, and no store holds phase KRs (okr.jsonl is OKR.md's register, not this). perry-goals:1141 says it outright -- 'Nothing here writes either field' -- and krs refuses --write, so the live values were hand-written, which is USER-912's recorded crossing. AND THE SHAPE OF THE WRITER IS NOW CONSTRAINED BY A SECOND FINDING: TASK-155 found that a genuinely re-measured KR does NOT re-date, so the assertion date must be written BY WHATEVER ASSERTS current, not by the register's writer -- no link write is involved in that direction at all. Sequencing TASK-155 established: this row, then the schema field (behind the TASK-119 section 5 gate, a user decision), then TASK-155's reader and contract change, which is then small and mechanical.
- [TASK-139] next action · ROUND ONE LOST 2026-09-03 to the 600s stream watchdog -- second row to die that way today, and again with ZERO commits: its branch never existed. Its last streamed line shows it had already decided shape (a) was unblocked and architecturally correct and was examining the off-board write path, so the analysis is gone rather than the answer being unknown. Re-dispatching with a stub commit as step 1 rather than as advice, which is the mitigation TASK-309 is about.
- [TASK-290] not_started → in_progress · started
- [TASK-308] not_started → in_progress · started
- [TASK-273] not_started → in_progress · started
- [TASK-308] in_progress → review
- [TASK-290] in_progress → review
- [TASK-139] in_progress → review
- [TASK-273] in_progress → review
- [TASK-139] next action · DELIVERED and merged, awaiting V4. Shape (a), a structural design_refs field, and BOTH COST PREMISES IN MY SPEC WERE FALSE -- verified: schema/state-schema.json does not enumerate task-record fields at all (claims[] declares paths and owners, files[] declares markdown documents); the field list is bin/perry_store.py section STORED and validate_records already skips unknown fields, the same additive path TASK-106 used for summary. No schema file and nothing under claims was touched, so the escalation I warned about never applied, and no migration is needed because an absent field counts zero. Shape (b) rejected on the project's own words: bin/perry-task:42 calls the event log DERIVED AND DISPOSABLE and says anything load-bearing living only there is a bug in the same class. THE TITLE PREMISE IS OBSOLETE TOO: perry-task done removes the row from BOARD.md, a projection, while tasks.jsonl keeps all 321 records including TASK-001..006 at done -- the defect was in the reader, not the storage. DESIGN-001 was at impl_refs=18, not my spec's 11, and TWO OF THE 18 WERE THIS ROW'S OWN DISPATCH EVENTS: the row filed to fix the count was inflating it, and the number drifts up every time the PMO writes about the problem. Now 0 and honestly pending, no edge invented. AND IT CAUGHT AN ERROR IN MY BRIEF: I said the first attempt left no branch and zero commits; the branch carries 5af2111, 'TASK-139 evidence file, committed before the baseline run'. The stub-commit-first instruction I added today WORKED ON ITS FIRST USE and I reported it as having failed -- I never checked this branch and generalised from TASK-155. Twelve mutations, six green across rounds, every green a finding; final round 11 of 11 red. Deleting .perry/events.jsonl no longer moves the count, which before would have collapsed every locked design to pending.
- [TASK-066] depends on · TASK-065, TASK-038 → TASK-065, TASK-038, TASK-263
- [TASK-066] next action · NOW WAITS ON TASK-263, added 2026-09-03, and the reason is this row's own next action: it says re-size the split after the markdown reader, row renderer and cell escaping are gone, and TASK-263 is what MEASURES the category split by call site. Splitting before that measurement is the two-splits outcome this row already argues against. MEASURED 2026-09-03 on c506ca5: bin/perry-task is 7851 lines / 412,790 chars, about 103k tokens if read whole -- but it is already grep-navigable, 135 top-level functions and 26 cmd_*, largest single function 527 lines, so the three agents that changed it today all worked by targeted reads and none read it whole. The saving from splitting is real and second-order; the larger saving available today costs no code at all, which is the PMO naming the function and line in the brief. TASK-067 landed half the precondition: the row renderer and cell escaping moved to viewer/tables.py, and only 36 lines here still reference the table primitives. TIMING, and it is the stronger argument: splitting while review rounds are in flight would make every branch unmergeable and every line number in every spec and criteria file stale, on a board that cites them heavily. Sequence: reviews clear, then TASK-263 measures, then this row splits to that measurement.
- [TASK-263] next action · NOW GATES TASK-066, recorded 2026-09-03. TASK-066's own next action says to re-size the split after the markdown reader, row renderer and cell escaping are gone; this row is the measurement that sizing needs, so 066 waits on it rather than guessing. ALSO: this spec was one of the four rows the dispatch gate was blocking on a CITATION rather than a write. TASK-290 landed 2026-09-03 and it now scans exit 0, verified -- it no longer needs the escalation override that TASK-308 was dispatched under. It is startable.
- [TASK-256] review → in_progress · round 1 FAILed — all/any false PASS on the multi-path interface, and the self-check vouches while unverifiable
- [TASK-256] next action · ROUND 1 FAILED 2026-09-03 on two behaviours in the shipped helper. (1) bin/perry-restore-check:231 is ok = all(r['ok'] for r in results); mutating all to any leaves ALL FIFTEEN TESTS GREEN because no test passes more than one path -- all ten run_helper sites pass exactly one. The mutant exits 0 on a two-file call where one file does not match the ref: a false PASS on the documented multi-path interface, in a tool whose whole job is refusing that. I REPRODUCED THIS MYSELF on a git-archive copy. Two siblings green for the same reason: :122 outside-repo and :129 not-at-ref, and the latter is reachable exactly when a round pins a pre-branch baseline, which the new rule prescribes. (2) self_check() returns three verdicts and the refusal gates on one, so 'unverifiable' answers anyway -- a helper carrying a mutation printed a clean verdict from a git-archive copy, the workflow review-constraints.md itself prescribes, while its own JSON showed differing md5s. I did NOT reproduce this one; my attempt hit a different path and the review carries the proof. FIX BOTH, and the test gap is the real lesson: fifteen tests and not one exercised the multi-path interface the tool documents. NOT PART OF THE FAIL BUT FILED BY THE REVIEWER AND POINTED: FOUR copies of the one-home explanation shipped by a row whose principle is one copy, and four of five attacks on the one-home guards succeeded (paraphrase, partial copy, subdirectory, paraphrased heading; only the exact-heading sibling was caught). The scope call on dispatch.md/delegate.md was CONFIRMED right -- grep -i 'mutat' exits 1 on both.
- [TASK-273] review → done · closed · evidence: `evidence/2026-09/TASK-273-round1-v4-review.md` · verification: V4
- [TASK-328] — → not_started · duplicate_row_ids' reported line number is off by one and nothing pins it, while its store-side sibling is pinned · owner: Coding Agent · priority: P2
- [TASK-067] review → in_progress · round 5 FAILed — _domain() lists two directories while claiming discovery
- [TASK-067] next action · ROUND 5 FAILED 2026-09-03, and the round cleared most of what I told it to attack rather than agreeing with me: the filename-keyed exemption does NOT rot silently (renaming the choke point gives 1 failure + 5 errors, splitting it gives 1 failure), the mutation table reproduces exactly at 9 planted / 9 red / 0 green / 0 anchor misses with M8 and M9 caught ONLY by the two interior tests, and declared limits 1, 3 and 4 are accurate to the measurement, each reproduced independently. It also confirmed the author was right to decline the manufactured corruption, by reading the pre-fix source at all six fresh-separator sites. THE FAIL IS A DOCSTRING THAT ASSERTS THE OPPOSITE OF ITS CODE, verified by me: _domain()'s docstring in tests/test_one_choke_point.py says 'Every shipped Python source file, DISCOVERED, not listed' and line 200 iterates a hardcoded pair of directory names, while this row's own Bound scans the whole repo. A tracked hand-built builder in packs/, templates/, modes/, work/, goals/, decide/, schema/, perry/ or at the repo root leaves the rule test GREEN, and archiving the census script makes even the census-agreement test skip -- 'Ran 13 tests, OK (skipped=1)'. THE PROPERTY IS FREE: it measured that widening to the census's own domain adds ZERO findings. TWO SUPPORTING FINDINGS: a hand-built helper INSIDE viewer/tables.py passes all 13 tests, so TestTheChokePointsOwnInterior closes M8 and M9 as instances rather than as a category; and a SEP constant resolves for concat and join but NOT inside f-strings or as a % argument, under a test asserting the opposite. Two things it recorded and did NOT count against the verdict, both needing their own rows: the 'no plant corrupted a file' sentence is over-broad by two sites (the pre-fix widen turns a 3-cell separator into 2), and a suite race it attributed by measurement rather than assumption -- test_row_integrity's pre-existing probe reproduces at 1 in 12 ALONE, so tests/header_rule.py's unguarded read_text is the defect and it predates this row.
- [TASK-326] next action · A MECHANISM AT LAST, and it is better than my hypothesis -- from TASK-067's round-5 reviewer, which attributed by measurement rather than assumption. tests/header_rule.py reads files during a tree walk with UNGUARDED read_text calls (:153, :886, :973), and test_row_integrity's pre-existing probe reproduces the flake at 1 IN 12 RUNNING ALONE. Alone matters: my hypothesis was 'tree-walking modules are unreliable while nested worktrees exist', and a 1-in-12 solo reproduction means the worktrees are not required -- they are one way to perturb the tree mid-walk, not the cause. tests/header_rule.py was last changed by TASK-244 (8829174), so the defect PREDATES TASK-067 and predates today's worktree load; verified. REVISE THIS ROW ACCORDINGLY: the subject is an unguarded read during a tree walk, and the verification should be that probe at 1-in-12 rather than the worktree add/remove control I wrote. Keep the worktree question as a SECOND, separate observation -- three different tree-walking modules reddened under parallel today and that may still be a distinct effect, but it is no longer the leading explanation.
- [TASK-290] review → in_progress · round 1 FAILed — _PATH_CHAR admits 11 non-filename characters, so a trailing full stop or markdown bold discounts a real hit
- [TASK-290] next action · ROUND 1 FAILED 2026-09-03 AND THE GATE IS WEAKER ON MAIN RIGHT NOW. I reproduced it in four lines: 'schema/state-schema.json — the claim surface' exits 3 (correct); 'schema/state-schema.json.' exits 0; backticked exits 3 (correct); '**schema/state-schema.json**' exits 0. _PATH_CHAR at viewer/parsers.py:4344 admits eleven characters that are not filename characters -- . ~ $ * < > { } @ + - -- and rule 1 measures its enclosing component from that token, so any of them adjacent to the match makes the component longer than the fragment and it is discounted as names-a-longer-file. ENDING A SENTENCE WITH THE PATH IS ORDINARY PROSE, NOT A TRICK, and it silently disarms a safety gate. Passing now needs no rewording at all, only formatting -- the failure .perry/hook.md's own preamble names as the one thing a gate must never reward. NOT REVERTED, and the reason is measured rather than argued: ZERO of the 147 live specs currently hit this discount, while the hole the merge CLOSED was firing on three real specs that declared a claim-surface edit. Reverting trades a hole nobody is in for one three specs were already in. But the margin is thin -- 6 of the 16 refusing specs hold on exactly ONE occurrence of their fragment, so the next author who ends a sentence with the path loses the refusal. THE FIX MUST PIN THE CHARACTER CLASS: two green mutations, both at line 4344 against the full 3258-test suite, mean nothing tests the contents of the set the safety decision hangs on. Everything else in the round HOLDS and was re-derived rather than inherited -- census, all 13 legitimate refusals, the four fragments' discount counts to the unit, the false-PASS closure end to end, all 13 cleared specs judged independently with none wrongly cleared, both author greens confirmed closed. The reviewer also caught and discarded one of its own apparent greens as an equivalent mutant, then re-targeted it red.
- [TASK-099] summary · Sweep bin/, viewer/ and tests/ for document-handling code that ADR-007 made dead, and report what is dead against what is still load-bearing. Round 1 is the CENSUS only: the fence this row is eventually for is blocked on a product decision, and the phase says outright that the measurement is the input the user needs in order to answer it. The round does not fence anything and does not close the row. → Run the repository-wide ADR-007 census by call site. Cover every production and test path that parses document structure or asks a question of prose, not only readers of rendered files that already have canonical stores. Classify each site as typed/deterministic, opaque-document transport, agent-owned prose interpretation, or obsolete representation code; this round measures and changes no behavior.
- [TASK-099] next action · Rewrite and run the existing census spec against a stable commit. Inventory bin/, viewer/, setup and tests/ by call site; name the owning function, downstream callers, replacement boundary (typed store/manifest or agent workflow), and exact deletion dependency. TASK-263 supplies the detailed split for perry-task and perry-lint.
- [TASK-112] summary · — → The V5 safety outcome remains required, but Python must not decide whether prose claims that a user reviewed, approved or signed off. Replace the bilingual word-list/regex guard with an agent drafting rule: tools expose measured facts as typed values, the agent may phrase only those facts, and the human selects or adds what they personally checked.
- [TASK-112] next action · Remove check_no_user_claim and its language-specific regex contract from the task scope. Specify the typed measured/restated inputs and the agent-side drafting/refusal protocol; Python may validate enum membership and selection provenance, not sentence meaning.
- [TASK-172] summary · Four of Perry's six document collections cannot be reached through any published contract. Deferred on 2026-08-21 by the user, with the cost recorded rather than waved through: aiMark reads those directories directly for now, which knowingly makes it a second reader of Perry's LAYOUT, so a consumer holding `perry/design/` breaks silently the first time a project moves its state root. → Publish contracts for Perry's document collections without teaching Python their prose schemas. A contract may enumerate stable ids, paths, kinds and raw content, but the document body is opaque and is interpreted by the consuming agent.
- [TASK-172] next action · Re-scope the four missing collection contracts as locator/transport APIs only. For each collection, declare typed metadata and raw-content fields, then verify that no parser extracts status, decisions, meaning or quality from the body.
- [TASK-186] summary · — → Record spec authorship as typed provenance written by the spec-creation tool, not as an Authored by line later parsed from Markdown. The escalation preflight reads that provenance from the structured task/spec manifest and reports it.
- [TASK-186] next action · Replace the DESIGN-010 step-2 file-field parser with a typed spec manifest/store field that callers cannot override. Preserve the provenance guarantee and update TASK-187's dependency contract to consume the structured field.
- [TASK-187] summary · — → Keep machine-authored high-stakes specs fail-closed, but make the decision from typed authorship and typed write targets. Do not infer intent from Files in scope, Deliverable or Out of scope prose.
- [TASK-187] next action · Depend on TASK-186's structured provenance and define an enumerated write-target manifest. The gate compares those targets with the high-stakes registry; document prose is handed to the agent and cannot green-light or cancel a hit.
- [TASK-219] summary · DESIGN-012 decision 4. With the retro running after scoring, 'cites rather than re-derives' becomes a comparison between two files that both exist, so it is checkable instead of being a convention. → The retro must use the authoritative phase-score records rather than re-derive them, but Python must not compare statuses parsed from two Markdown documents. Persist score provenance as typed KR/status references and have the agent generate the retro from that payload.
- [TASK-219] next action · Replace the proposed cross_file prose check with a structured phase-score/retro provenance contract. Verify that changing a typed KR status changes the generated retro input and that no Python path parses retro.md or phase prose to establish agreement.
- [TASK-221] summary · DESIGN-012 decision 3. Phase 002 was left scored with no retro and a rollover that never ran, and nothing anywhere recorded that. The progress is already legible from what the stages leave behind, so this needs no new file. → An interrupted phase close must remain visible, but its stage is resolved from typed phase-close state plus artifact existence, never by parsing phase Status prose or retro content. The snapshot reports the structured stage and hands any prose artifact to an agent.
- [TASK-221] next action · Rewrite the five-state recovery table around a typed phase-close record/event sequence. Keep artifact existence and CURRENT pointer checks deterministic; remove phase Markdown parsing from the acceptance path and preserve the no-new-dossier decision.
- [TASK-257] summary · Filed 2026-08-30 from the TASK-249 round-4 confirmation, which PASSED the row and merged it — these three are the non-blocking remainder. The '.md' case is the reason this is P1 rather than a tidy-up: a suffix that satisfies the pin makes the guard blind to the project's most-written file. The row can also strike its own 'the skip path is reasoned, not exercised' caveat: the reviewer exercised it on a case-sensitive APFS image, where the case-flipped spelling is correctly refused. → The ignored-path/name set is bounded configuration and belongs in a typed schema or manifest, not in a prose bullet that Python searches. Preserve the foreign-project safety behavior by validating the structured set directly.
- [TASK-257] next action · Move the ignored-name values and their semantics to one typed declaration, render any explanatory bullet from it, and test exact values plus foreign-project refusal. Drop substring, bullet-shape and help-message regex tests.
- [TASK-263] summary · Measure how bin/perry-task and bin/perry-lint split by CATEGORY, counted at the call site rather than by grep. It is the first step of DESIGN-014's implementation plan and the precondition for that document's central section being a measurement rather than an admission: the two files are 40% of bin/ and neither of them is one thing. → Measure bin/perry-task and bin/perry-lint by call site against ADR-007's actual boundary: typed deterministic operations, opaque document transport/rendering, agent-owned prose interpretation, and obsolete document representation. The report is the detailed input to TASK-099 and must not classify by grep.
- [TASK-263] next action · Update the existing spec, then produce a line-accounted call-site census for both files. Every prose/document path must name whether it moves to a typed store/manifest, moves to an agent workflow, or is deleted; keep TASK-066 blocked until this measurement exists.
- [TASK-265] summary · perry-state exists because `BOARD.md` was canonical and counting anything meant parsing a markdown table. With six JSONL stores the counting is a loop, so what is left worth keeping is the payload SHAPE it publishes, not its reader. DESIGN-014's user decision 1 was answered on 2026-09-01: thin it to a query over the stores rather than keeping or deleting it, and the published shape does not change. → Thin perry-state to the published payload over typed stores and deterministic filesystem facts. Preserve the payload shape, but remove reads that parse OKR, phase, design, ADR, role, knowledge or spec prose; expose opaque document references for agents instead.
- [TASK-265] next action · Inventory every perry-state dependency on viewer/parsers.py. Replace state-bearing reads with JSONL/typed records, retain only deterministic existence/count/time computations, and return raw document handles where the caller needs agent interpretation.
- [TASK-291] summary · close-task lets a V4 closure through when its verdict block cannot be parsed, and perry-lint reports it only afterwards — so a rung can be claimed and merely questioned later, which is not a gate. Scoped to the FORWARD half: refuse the close. Whether the ten existing closures in that state should be re-reviewed or restated rewrites this project's own verification record and belongs to the user. → A V4 close still must fail closed without a valid fresh-review record, but the verdict must be a typed review artifact/store entry rather than a block parsed from review Markdown. Evidence prose remains an agent-authored document linked by id.
- [TASK-291] next action · Define the minimal typed V4 verdict contract (task id, reviewer identity, criteria ref, verdict, checked/not-checked, timestamp) and make close-task validate it. Do not add or strengthen regex parsing of review documents; keep historical repairs out of scope.
- [TASK-296] summary · A store record's title field is unverifiable: plant garbage in it and `verify` still reports drift 0 and `byte_identical: true`. It matters because that sentence is what every store row in this project rests its acceptance on, and it is silent about any field the renderer does not read. Found while attacking a load-bearing claim and correctly ruled pre-existing rather than that row's defect. → A typed title is verified by transport completeness, not by judging whether its prose is good. Every stored title must reach the published payload/render verbatim, and verify must fail when a renderer silently ignores the field.
- [TASK-296] next action · Re-scope the mutation to prove field coverage and byte preservation only. Remove garbage title as a quality claim; mutate any title value and assert the corresponding typed output changes without parsing or scoring the text.
- [TASK-116] not_started → dropped · reason: Drop: it asks Python to distinguish recorded from unrecorded questions by interpreting prose mentions. Use explicit typed references or agent review instead; no independent parser fix remains.
- [TASK-179] not_started → dropped · reason: Drop: dangling-id meaning in prose is agent-owned or must be expressed as an explicit typed reference. Widening or exempting the prose scanner would deepen the ADR-007 violation.
- [TASK-232] not_started → dropped · reason: Drop: renaming the shared Markdown parser is churn on a representation layer scheduled for decomposition and deletion. Keep stable imports until TASK-099 and TASK-263 identify the surviving typed helpers.
- [TASK-274] not_started → dropped · at stage: triaged · reason: Drop: reordered rendered Markdown is not state and should not gain another drift guard. The config store is authoritative; Python must not parse the projection to infer meaning.
- [TASK-295] not_started → dropped · reason: Drop: the objective-heading Markdown scanner is superseded by objective records in the OKR store. Strengthening its test preserves a parser ADR-007 says to remove.
- [TASK-306] not_started → dropped · reason: Drop: de-duplication inside scan_spec_escalations is cosmetic work on a prose/spec parser being replaced by typed write-target manifests.
- [TASK-318] not_started → dropped · reason: Drop: deciding whether a Markdown fence changes the normative meaning of procedure prose belongs to an agent or reviewer, not a Python visibility classifier.
- [TASK-319] not_started → dropped · reason: Drop: correcting the docstring of scan_spec_escalations has no standalone value when that prose parser is being replaced; preserve the finding in history.
- [TASK-290] in_progress → review
- [TASK-329] — → not_started · markdown italic still clears the claim surface: a trailing underscore is a word character, so the gate's right-edge guard never matches · owner: Coding Agent · priority: P1
- [TASK-290] next action · RE-EVALUATED AGAINST ADR-007 2026-09-03 AT USER REQUEST. THIS ROW IS THE VIOLATION, not a row that happens to touch prose. It matches hook fragments against the PROSE SECTIONS of evidence/*-spec.md. ADR-007 section 5b partitions by FILE: a file with typed columns and no prose sections is a store Python owns; a file with prose sections is a document, and of documents it says Python 'should not be parsing them at all, NOT EVEN LENIENTLY. It should locate the file and hand it to an agent.' A spec is typed header fields plus prose sections, and this gate reads the prose half. FIVE ROUNDS OF THE SAME CATEGORY: TASK-107 bare substrings, TASK-284 a gate blind to the fields, TASK-290 citation-vs-write, round 1 a full stop, TASK-329 markdown italics -- with TASK-306 queued behind. AND THE DIRECTION IS UNSAFE: a formatting change DISARMS a safety gate. RECOMMENDATION: the merged work stays, because it closed a false pass that was live on three real specs. But further rounds on this matcher should stop in favour of moving the input -- the spec header already carries typed fields Python reads as values (Dispatch mode, Deployed, Touches architecture), and a 'Writes:' field of the same shape makes the prose irrelevant to the safety decision. Its V4 round is dispatched; let it finish, then decide whether round 3 happens at all.
- [TASK-308] next action · RE-EVALUATED AGAINST ADR-007 2026-09-03 AT USER REQUEST, AND MEASURED. It has the SAME disease as TASK-290 but in the SAFE direction. Built a fixture with the correct state-root layout (my first attempt scanned zero specs and reported unbounded=0 for every variant -- the exact TASK-284 failure mode, on myself, caught by adding a control). With the control at scanned=1/unbounded=1: '## Bound' 0, '##  Bound' 0, '##Bound' 0, but '## **Bound**' reports 1 -- BOLDING THE HEADING MAKES THE CHECK CALL THE BOUND MISSING. Formatting changes the answer, same category; the difference is that a false 'missing' report costs a warning, not a disarmed gate. VERDICT: the PURPOSE is valuable and ADR-007-compatible -- telling an author the bound is missing BEFORE the round is exactly what the two failed rounds today needed. What is wrong is the INPUT. If a bound becomes a typed header field of the shape the spec header already carries, or the criteria carry it structurally, the check reads a value and the formatting question disappears. KEEP THE PURPOSE, MOVE THE INPUT. Its V4 round has not been dispatched; hold it until that decision is made rather than reviewing an implementation that is about to be re-based.
- [TASK-325] next action · RE-EVALUATED AGAINST ADR-007 2026-09-03 AT USER REQUEST. THIS ONE IS THE LEAST AFFECTED OF THE THREE and the reason is structural, not a judgement call: tasks[].summary is a TYPED FIELD IN A STORE, not a section of a document. Reading it is ADR-007 rule 1, which is Python's own side of the line. No file is parsed and no heading is matched, so none of the five formatting defects that hit TASK-290 and TASK-308 can reach it. BUT TWO OF ITS FOUR RULES JUDGE LANGUAGE INSIDE THAT FIELD. summary-missing (absent, empty, whitespace) and summary-repeats-title (folded equality or prefix, comparing two typed fields) are deterministic facts and are clean. summary-has-no-sentence ('no sentence terminator anywhere') and summary-is-a-fragment ('fewer than SUMMARY_MIN_WORDS tokens') are claims about prose, in a field whose content is prose. The round already knew the risk and its docstring says so -- 'what it does not judge is part of its contract', with readability, reading-level, vocabulary, hedging and tone all explicitly excluded, and opens-with-a-bare-id rejected after measuring that all 10 such summaries were good. It drew the line one step further out than ADR-007 would. RECOMMENDATION: keep the field, the writer gate, the procedure and the two deterministic rules; re-scope or drop the two language rules. That is a small edit, not a redesign, and it does not touch the 74 backfilled summaries or the coverage number. Its V4 round has not been dispatched; this is small enough to settle first.
- [TASK-330] — → not_started · drop summary-has-no-sentence and summary-is-a-fragment: prose quality is the writing agent's job, not a check's · owner: Coding Agent · priority: P1
- [TASK-330] not_started → in_progress · started
- [TASK-330] evidence · — → evidence/2026-09/TASK-330-result.md
- [TASK-330] in_progress → review
- [TASK-331] — → not_started · The task-list contract still defines summary by two rules that no longer exist · owner: Coding Agent · priority: P1
- [TASK-332] — → not_started · Nothing pins the NOT CHECKED register that records what a predicate declines · owner: Coding Agent · priority: P2
- [TASK-331] depends on · — → TASK-330
- [TASK-332] depends on · — → TASK-330
- [TASK-099] P1 → P0 · reprioritized
- [TASK-263] P1 → P0 · reprioritized
- [TASK-263] not_started → in_progress
- [TASK-067] in_progress → review
- [TASK-333] — → not_started · Give the escalation gate a typed field to read instead of a spec's prose · owner: Coding Agent · priority: P1
- [TASK-333] next action · MEASURED BY THE PMO 2026-09-03 across all 148 specs on disk: 133 pass, 15 refuse, 0 unarmed. Of the 15 refusals, FIVE ARE FALSE POSITIVES where the spec only cites or explicitly declines to touch the thing named -- TASK-099 ('setup and hook code that reads repository documents -- read.', a read-only census), TASK-108 (matches the tool NAMES bin/perry-diagnose and tests/test_diagnose.py, not the /perry diagnose command), TASK-139 ('only if a new field is declared. This is the claim surface; changing it is escalated. If the design points that way, stop and file'), TASK-220 (cites adopt/diagnose as a ROUTER PRECEDENT), and TASK-244 -- whose own spec header already carries a hand-written note saying the gate matched the English noun in 'the harness re-does setup per test'. A human had to annotate around the gate rather than fix it. ONE IS GENUINELY HIGH-STAKES and must keep refusing: TASK-107 (~/.claude/skills, ln -s variants, publish, rm -rf, --force-with-lease). The remaining nine name schema/state-schema.json or claims, and whether editing a non-claims section of that file should escalate at all is a POLICY question for the hook, not a gate defect -- decide it in this row or say it is out. WHY A TYPED FIELD RATHER THAN A BETTER REGEX: this is the fifth round of the same shape. TASK-107 matched bare substrings, TASK-284 was blind to fields, TASK-290 could not tell cite from write, TASK-290 round 1 was beaten by a full stop, TASK-329 is open because a markdown italic's trailing underscore is a word character. ADR-007 decision 3 says the Python layer never parses a document at all, and the user restated it on 2026-09-03. THIS ROW WOULD RETIRE TASK-329 and end TASK-290's future rounds; TASK-306 is already dropped. BLAST RADIUS IS HIGH -- the gate arms every dispatch on this project, so the row needs the user's sign-off on the field's NAME and on what happens to the 148 existing specs that do not carry it (the spec-side answer must be 'report unarmed', never 'assume safe').
- [TASK-329] depends on · — → TASK-333

## New tasks added

### TASK-323 — TASK-067's population decayed under it — census the write and read paths on today's HEAD and produce the `## Bound` its criteria file never had

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: A revised TASK-067-finding.md carrying a `## Bound` — an enumerated set with a last element — plus a census table: every path that can put an unrenderable value into a table row, and every path that splits a row without going through split_row, each with file:line and a caught/uncaught verdict measured on today's HEAD.
- **Verification**: For each census row marked uncaught, plant that exact shape and show the row lands corrupt while perry-lint reports clean. For each marked caught, plant it and show the refusal, naming which check fired. Every plant on a git-archive copy in the scratchpad; the live tree is never written to and is byte-identical afterwards.
- **Dependencies**: —
- **Out of scope**: Fixing anything. This row measures. The fix is TASK-067 and which principle it is fixed under is a user decision that this census exists to inform.
- **KR linkage**: P003-O1-KR2

### TASK-324 — TASK-067's bound claims names resolve through `NAME = "…"` bindings, and that is false for its own f-string clause

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: The bound's prose says what its script does. Either `TASK-323-bound.py` resolves bound names in the clauses the prose claims — `visit_JoinedStr`, `.split(sep=…)`, a compiled-pattern split — or the prose is narrowed to the clauses that actually do it. Whichever is chosen, say why in the file.
- **Verification**: Round 1 constructed six members the rule should catch and does not: an f-string via a `SEP = "|"` binding, `.split(sep="|")`, a compiled-pattern split, unbound `str.split`, a row built in an `re.sub` replacement, and a tuple-bound delimiter. Plant each; the bound must return 91+ where the prose says it should, or the prose must no longer say it. An inline control must be caught at all three lines, as round 1's was.
- **Dependencies**: —
- **Out of scope**: Widening the bound's membership rule to change what TASK-067 is measured against. Round 1 swept the whole 21-file domain and none of the six shapes is instantiated today, so the 89 stands and TASK-323's census is unaffected. This row closes the gap between the prose and the script BEFORE the next round leans on it.
- **KR linkage**: P003-O1-KR2

### TASK-325 — Perry has a plain-language field for every task, 90 of 114 open rows leave it empty, and nothing asks for it

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: Three parts, landing together. (1) work/reference/subcommands.md makes --summary required on every add and states the writing rule concretely enough to be violated: the reader does not read code; no task or design ids; no file paths; no file:line citations; no backticked identifiers; two to three sentences; and it must answer both what is broken now and what the user will see differently once it is fixed. (2) bin/perry-lint gains a check that reports an open row whose summary is empty, and an open row whose summary matches that shorthand blacklist. (3) Backfill: a summary for each of the 90 open rows that has none, and a rewrite of the 33 existing summaries that carry ids, paths or backticks.
- **Verification**: Mutation discipline per work/reference/review.md. The lint check must go red on planted violations and green on corrected text: plant an open row with an empty summary, one whose summary cites TASK-067, one citing bin/perry-lint:252, one wrapping an identifier in backticks, and one that is a single clause restating its own title -- five plants, five refusals, each naming which clause fired. Then soften the rule in subcommands.md to say a summary is RECOMMENDED rather than required, and show the check still reports every empty row, so the check does not depend on the prose that asks for it. Coverage after backfill is one command over perry/tasks.jsonl and must read 114 of 114 open rows carrying a summary and 0 matching the blacklist. Every plant on a git archive copy in the scratch directory; git status --porcelain empty at the end.
- **Dependencies**: —
- **Out of scope**: Closed rows -- done and dropped rows keep whatever summary they have. Rewriting any title. Changing what perry-explain prints: it already prints summary at bin/perry-explain:515 and perry-task list --json already carries the field, so no consumer needs a change and aiMark needs none. Adding a new schema field: summary exists since contract 1.11 at schema/task-list-contract.md:134, and this row uses it rather than replacing it.
- **KR linkage**: unlinked

### TASK-326 — test_project_root_resolution reddens under the parallel runner while agent worktrees are live — TASK-258's disease in a second module

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: The module is green under `python3 tests/parallel -j 4` with agent worktrees present, or the finding is re-attributed with evidence to something other than worktree interference.
- **Verification**: Reproduce first: 3 failures under the parallel runner, 23/23 green under `python3 -m unittest tests.test_project_root_resolution`, with .claude/worktrees/ populated. Then establish the mechanism rather than assuming it — remove the worktrees (or point the walk away from them) and show the parallel run goes green, and restore them and show it goes red again. A fix whose control does not flip both ways has not identified the cause.
- **Dependencies**: —
- **Out of scope**: tests/test_tree_guard.py, which TASK-258 already fixed. Changing where the Agent tool puts worktrees — that is the harness's choice, not Perry's.
- **KR linkage**: unlinked

### TASK-327 — perry-decide guards a line break with a third, weaker spelling

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: bin/perry-decide:332 calls viewer/tables.py line_break_at instead of len(_value.splitlines()) > 1, with a test for the trailing-newline case the current spelling misses
- **Verification**: a mutation reverting perry-decide:332 to len(_value.splitlines()) > 1 turns a named test red; perry-lint --root . stays at 0 errors
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-328 — duplicate_row_ids' reported line number is off by one and nothing pins it, while its store-side sibling is pinned

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: A named test fails when the `+ 1` in `bin/perry_store.py § duplicate_row_ids` is removed, the way the store-side sibling `duplicate_record_ids` already is.
- **Verification**: Delete the `+ 1` and show the named test go red; restore and show it green. The existing assertion matches `on lines \d+, \d+` and therefore cannot see the shift — the replacement must assert the actual line numbers against a fixture whose rows sit at known lines. Anchor by line number with an assert on the old text; verify the restore against `git show <ref>:<path>`, not a snapshot.
- **Dependencies**: —
- **Out of scope**: The refusal itself, which is correct and was PASSed at V4 — it fires, names the id, prints both rows, and leaves the store byte-identical. This is the diagnostic only.
- **KR linkage**: unlinked

### TASK-329 — markdown italic still clears the claim surface: a trailing underscore is a word character, so the gate's right-edge guard never matches

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: `_schema/state-schema.json_` and `schema/state-schema.json_` in a `## Files in scope` section produce exit 3, and the 16 current refusals are unchanged.
- **Verification**: Reproduce first on main: the plain spelling exits 3, both underscore spellings exit 0. After the change all three exit 3 and the refused (spec, fragment) pair list is byte-identical to baseline across all 147 specs. A control: an underscore that is genuinely part of a filename must still match. Mutations anchored by line with an assert on the old text; restores verified against `git show <ref>:<path>`, single-path only.
- **Dependencies**: —
- **Out of scope**: Rule 1 and the two path-component rules, which TASK-290 owns and which are not where this lives. The bold-own-tree false positive, which is safe-direction and gets its own row.
- **KR linkage**: unlinked

### TASK-330 — drop summary-has-no-sentence and summary-is-a-fragment: prose quality is the writing agent's job, not a check's

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: summary_shape reports only summary-missing and summary-repeats-title; both tools stop refusing on the two removed rules; the NOT CHECKED list records the removal and the reason.
- **Verification**: Four counts reproduced before and after: missing 194, repeats-title 0, has-no-sentence 0, is-a-fragment 0 — so the removal is behaviour-neutral on today's corpus and only the writer's future refusals change. perry-task add --summary 'Short.' is refused before and accepted after; a missing summary and a title-restating summary are still refused after. The two tools still agree on one corpus. Mutation: revert the removal, a named test goes red.
- **Dependencies**: —
- **Out of scope**: The 74 backfilled summaries and the coverage figure. summary-missing and summary-repeats-title. Any new check — this row only removes. The escalation gate's prose parsing, which is TASK-290 and TASK-308.
- **KR linkage**: unlinked

### TASK-331 — The task-list contract still defines summary by two rules that no longer exist

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: schema/task-list-contract.md:134 describes only what summary_shape still checks -- missing, and repeats-title -- with the removal dated and attributed, and a guard that fails if the contract and the predicate disagree about which rules exist.
- **Verification**: A test derives the rule names from bin/lib summary_shape and asserts the contract's field definition names those and no others; reverting either half turns it red. perry-lint --root . at 0 errors, full suite no redder than baseline.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-332 — Nothing pins the NOT CHECKED register that records what a predicate declines

- **Owner**: Coding Agent
- **Priority**: P2
- **Track / mode**: main / project
- **Deliverable**: A guard that fails when summary_shape's NOT CHECKED list loses an entry, following the source-text precedent already in the same file (test_perry_lint_binds_the_predicate_from_lib_rather_than_copying_it).
- **Verification**: Re-plant TASK-330's M5 -- delete the NOT CHECKED entry for the two removed rules -- and show a named test go red where it was green. Anchor by line number with an assert on the old text; clear __pycache__; verify the restore against git show <ref>:<path>. Full suite no redder than baseline.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked

### TASK-333 — Give the escalation gate a typed field to read instead of a spec's prose

- **Owner**: Coding Agent
- **Priority**: P1
- **Track / mode**: main / project
- **Deliverable**: A spec declares what it writes in a typed header field (for example '> Writes:' listing paths and operations), perry-state --escalation-scan reads only that field, and no prose section is scanned at all. Specs without the field are reported as unarmed rather than guessed at.
- **Verification**: All five measured false positives (TASK-099, TASK-108, TASK-139, TASK-220, TASK-244) pass; TASK-107 still refuses on host skill installation; a spec that declares a write to the claim surface still refuses. Mutation: remove the field from a refusing spec and show it report unarmed rather than pass. Full suite no redder than the measured baseline; perry-lint --root . 0 errors.
- **Dependencies**: —
- **Out of scope**: —
- **KR linkage**: unlinked
