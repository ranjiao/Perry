# Board — Perry

> Live working memory. Current open work only — closed tasks leave this file.
> History of what happened on a given day: `journal/2026-08/2026-08-16.md`
> Per-task spec / deliverable / audit: `evidence/2026-08/<TASK-ID>-*.md` (P0/P1 always have a `<TASK-ID>-spec.md`)
> Auto-dispatch a task: `/pmo dispatch <TASK-ID>` (requires spec.Dispatch mode = auto)
>
> Last updated: 2026-08-16 (21st pass — DESIGN-004 handed off, 6 tasks)
> Hard cap: ≤200 lines. If you're over, run `/pmo triage`.
>
> **Bootstrapped 2026-08-16** from the hand-off of DESIGN-001 and DESIGN-002, both
> locked the same day. Every row cites its design phase. Perry has no `OKR.md`, so
> every row is **declared unlinked** — not guessed into a KR.

## P0 (must finish this period)

| ID | Title | Owner | Status | Next action | Evidence | Verification |
|---|---|---|---|---|---|---|
| TASK-044 | Migration must be dry-runnable, lossless, recoverable and user-declared | Coding Agent | blocked | V4: FAIL on one blocking finding, and ADR-004 does not reopen — all five guarantees hold. fix_tables recognizes a table by sharing ONE column name, so a legend under ## P0 is widened, lint goes to 0, the marker declares it conformant, and perry-task list returns rows fabricated from the legend's cells. Same class in enum_candidates and field_line. Fixes are TASK-051 and TASK-052. | — | V4 |
| TASK-050 | One normalization for a header cell, not two | Coding Agent | not_started | viewer/parsers.py normalizes header cells with .strip().lower() at 11 sites; bin/perry-task and bin/perry-lint use squash, which also strips backticks and asterisks. On a header whose Risk cell is bolded, the writer says risk-table and the reader says not — so risk-add writes, perry-state reports 0 risks, perry-lint is clean and risk-migrate says already migrated. Four exits, all closed. Task tables survive only via positional fallbacks. Fix: one function, plus the reader/writer agreement test in the shape tests/test_risks.py already uses for placeholders and bullets. | — | V4 |
| TASK-051 | Migration recognizes a table by shape, not by vocabulary | Coding Agent | not_started | `fix_tables` treats a table as the schema's if it shares ONE column name, and ID/Status/Owner are the three commonest words in any markdown table. A legend under `## P0` gets widened; lint goes 6 errors to 0; the marker declares it conformant; `perry-task list` then returns rows fabricated from the legend's own cells through a frozen contract. Same class in two more transforms: `enum_candidates` reads `not yet locked` as `locked`, and `field_line` rewrites body prose that merely mentions `Status:`. All three pass every losslessness assertion. | — | V4 |
| TASK-052 | The losslessness assertions ask whether the bytes survived, never what the file now says | Coding Agent | not_started | Named by two independent reviewers. TASK-049, and B-1/M-1/M-2 of the migration review, all preserve every character, cell, row count and id — and change what the file means. One of the four assertions (`cells`) additionally has no test that can fail on its absence: deleting it leaves 813 tests green. This row is the missing check, not another instance. | — | V4 |
| TASK-053 | route ignores --group, and the refusal that recommends it is mine | Coding Agent | not_started | `--group` parses into args.group and `cmd_route` never reads it, so the intake drain cannot run on a board with no `## P0`/`## P1` — which is gimegime-pmo's actual shape. `check_priority`'s message tells the user to pass the heading to `--group`, a flag route throws away. Round-5's B-3 fixed the narrow-columns half and left the missing-heading half; the guard is named `test_the_intake_drain_runs_on_a_narrow_pre_existing_board` and its fixture contains a `## P1`. | — | V4 |
| TASK-057 | evidence_paths is empty on every closed row, and nothing says so | Coding Agent | not_started | aiMark, 2026-08-17, doc/perry-contract-gaps.md §1. Reproduced here: 32 closed rows, all carrying an evidence cell, all with evidence_paths empty, and the files exist. The same path on the same board resolves while the row is open. conformance.evidence_not_found does not report it either, so a consumer cannot tell 'not resolved' from 'file missing' — aiMark renders the document that justifies the close as a dead link. Fix: resolve the cell for closed rows, or say in the payload that closed rows are not resolved. | — | V4 |
| TASK-063 | A dependency is not queryable — blocked says that, never on what | Coding Agent | not_started | Found by the user calling `perry-task list` and seeing a pile of rows with no way to tell which were advanceable. Three facts. (1) `add --depends` exists and writes `- **Dependencies**: <free text>` into the journal's definition block — written once at creation, never updatable, not on the board, not in the payload. So the dependency graph lives in date-sharded append-only prose, which is verbatim what DESIGN-004 section 1.3 named as the defect for the task set itself. (2) `status --status blocked` REFUSES without `--reason`, on the stated ground that `a blocked row with no named dependency is one nobody can unblock` — and then puts that named dependency in prose. The instinct is right and the answer lands somewhere unreadable. (3) `subcommands.md:87` tells triage to find `the same dependency cited in >= 2 rows`, which requires reading dependencies across rows; nothing can. A rule with no readable input. Blocked on TASK-050's agent, which owns bin/perry-task and schema/task-list-contract.md right now. | — | V4 |
| TASK-067 | The writer can destroy the table it writes to, and perry-lint cannot see it | Coding Agent | review | Fix landed: render_row refuses a value a row cannot carry (one line, reads back as itself); every reader routed through one split_row (parsers 10 sites, perry-lint, perry-state, perry-diagnose — the last two found by the category-shaped test, not by the report); ragged-row finding in both directions, placed above the missing-columns bail-out. 17 new tests, suite 955 to 971, 11/11 mutations red, no existing test edited. gimegime-pmo 59 and PolyForge 13 unchanged, zero ragged-row on either. AWAITING V4 by a fresh reviewer against evidence/2026-08/TASK-067-finding.md. | evidence/2026-08/TASK-067-finding.md | V4 |

## P1

| ID | Title | Owner | Status | Next action | Evidence | Verification |
|---|---|---|---|---|---|---|
| TASK-019 | `modes/pipeline.md` | Coding Agent | blocked | Round-6 V4: FAIL. Two claims no round had tested — the missing-SLA finding is stated in modes/pipeline.md, modes/queue.md and the schema and implemented nowhere (the schema note books it as fixed in round 3), and perry-task and perry-state disagree about the same track's stages and WIP. Review filed at evidence/2026-08/TASK-019-020-round6-review.md. Fix is TASK-056. | `evidence/2026-08/TASK-019-020-v4-review.md` | V4 |
| TASK-020 | `modes/queue.md` + `BOARD.md § Intake` + triage drain | Coding Agent | blocked | Round-6 V4: FAIL. route ignores --group, so the intake drain still cannot run on a board with no ## P0/## P1 — gimegime-pmo's actual shape — and the refusal that recommends --group is one route throws away. The guard's fixture contains a ## P1, so it graded narrow columns and could not fail on a missing heading. Also the overflow signal does not name intake. Fixes are TASK-053 and TASK-062. | `evidence/2026-08/TASK-019-020-v4-review.md` | V4 |
| TASK-027 | Lane rename goals/work/decide + aliases | Coding Agent | blocked | Round-5 V4: FAIL. Round 4 failed this for a category — the rename reached the documents a reviewer reads, not the artifacts a user receives — and the list was worked through while the category was not. bin/ --help still prints /pmo, /okr, /design; the guard it was given sets TOOLS to 2 of 15 tools and discards docstrings, which is what --help prints. Fix is TASK-054. | `evidence/2026-08/TASK-027-spec.md` | V4 |
| TASK-046 | A queue track must declare an SLA at creation — no default | Coding Agent | not_started | User decision 2026-08-17: force the question at creation, give no default. Rejected a default (5d) because a number the user never declared would start producing 'overdue' verdicts — the confidently-wrong-value class this project keeps fixing. Rejected the status quo because every queue user hits the gap after the fact rather than before. Blocked on TASK-044, which owns schema + perry-lint right now. | — | V4 |
| TASK-047 | Flip the conformance gate to enforce | Coding Agent | not_started | User decision 2026-08-17: flip after TASK-044 lands, not before. The advisory default was chosen because enforcing would name a command nobody could run; TASK-044 is what removes that. Blocked on TASK-044. | — | V4 |
| TASK-048 | perry-diagnose's own output does not validate against Perry's schema | Coding Agent | not_started | Found by TASK-044: all 13 of PolyForge's lint errors are Perry's own diagnostic output failing schema/state-schema.json — status 'false_positive'/'declined' are not in finding_status, source 'read'/'read+interview' not in finding_source, ids LOAD-02/CON-03b do not match ^[A-Z]{3}-\d{2}$. Migration deliberately refuses to edit it: rewriting a finding's status to satisfy an enum is editing a diagnostic record. | — | V3 |
| TASK-056 | A claim stated in three places and implemented in none — the missing-SLA finding | Coding Agent | not_started | modes/pipeline.md:147, modes/queue.md:21 and schema/state-schema.json:64 all say triage reports a missing SLA as a finding. `grep -rn no_default bin/ viewer/ tests/` returns nothing and the triage procedure never mentions it. The schema note attributes the fix to round 3 — it was booked as done by writing the note. Either implement it or delete all three claims. Related: perry-state and perry-task disagree about the same track's stages and WIP, and `default_wip` has zero readers. | — | V4 |
| TASK-058 | asks, risks and drift have no contract — the ask from the only consumer | Coding Agent | not_started | aiMark §2. All three are written by perry-task and readable only through perry-state, the one payload with no version. The task contract names them out of scope and invites a 1.x bump if a front-end needs them; this is that ask. User Input Queue is the needs-you list the Projects dashboard sorts on. Two shape problems to fix in the same pass: a bullet-sourced risk arrives with id = the severity letter and severity = 'watch' for every row (the H/M/L a human wrote survives only in meta), and user_input_queue.items[].idle is a rendered string '9d' rather than an integer. | — | V4 |
| TASK-059 | The agents roster: aiMark's ask, as an input to DESIGN-006 | Coding Agent | not_started | Rescoped 2026-08-17 on the user's observation that this is DESIGN-006's work. That design's § 1.1 is titled 'There is no role object — only tasks and a hardcoded agent list' and names the three places the roster lives today: delegate's hardcoded Coding/Research/Review, hook_TEMPLATE's free-text 'Special agents available', and perry-dispatch-limit tracking executors as though they were roles. Adding {id, tasks} to perry-goals/list now would freeze the roster into a shape DESIGN-006 replaces, in a frozen additive contract, and file it under goals — while a roster is a fact about who executes, not about goal attribution. Blocked on DESIGN-006. aiMark keeps its DeclaredAgent path unused and its Agents view says source: owner, which is honest. | — | V5 |
| TASK-060 | mint_id does not adopt the board's own id prefix, and add has no way to ask | Coding Agent | not_started | aiMark section 4. On a board whose seventeen rows all carry that project's own prefix, add minted a TASK- id instead. Legitimate under the contract (ids are opaque, a board may carry several project-declared prefixes) but a user creating a task from a front-end watches their board sprout a second id family with no way to ask for the first. aiMark passes no id and cannot: nothing in the surface would let it. Fix: default to the prefix the board already uses, or accept --prefix. (Prefix examples deliberately not written out here — quoting another project's ids in a board cell puts them in this project's dangling report, which is the third time.) | — | V4 |
| TASK-062 | The board-overflow signal does not name intake, and prescribes the split queue mode forbids | Coding Agent | not_started | TASK-020's round-6 M-4. Built the fixture the rubric names (220 intake rows, board at 244 lines): perry-lint says 'Split the overflow into a sibling file' and perry-state says 'run /perry work triage', while modes/queue.md:66-69 says the correct response is a finding and explicitly not moving the section somewhere it can grow unnoticed. Neither message names intake as the cause. And perry-state carries no intake block at all — viewer/parsers.py dispatches board sections through an allowlist that ## Intake does not match — so the correlation subcommands.md demands is not computable from the payload triage is told to read, while the same document forbids obtaining it by opening the file. No test builds the over-cap fixture. | — | V4 |
| TASK-064 | SKILL.md is 62 KB and loads on every invocation — cut it under 20 KB | Coding Agent | not_started | SKILL.md is 62,398 bytes and is loaded on every single invocation. Target: under 20 KB. Sink the bodies of adopt / diagnose / relocate / config / style / hooks into reference/, and leave the router a dispatch table plus a one-line pointer per entry. MEASURED, so the plan is not a guess. The six sections the ask names total about 19 KB — not enough on their own. The two largest are `Mandatory first move: combined snapshot` at 12,707 bytes and `First-time setup` at 7,009, and getting under 20 KB means the snapshot ritual's body sinks too, leaving the router with the commands to run, the dashboard shape, and pointers for the rest. THE CONSTRAINT THAT MAKES THIS RISKY: 63 places cite `SKILL.md § <section>` by name, 17 of them the hand-off contract. TASK-054's agent declined to rewrite 37 shorthand occurrences for exactly this reason — renaming a heading dangles every `§`-citation to it, silently, because the pointer guard resolves the path half and not the section half. So: every existing citation must still resolve after the move, or be updated in the same commit, and the guard that checks section halves does not exist yet and is part of this task. THE HAND-OFF CONTRACT IS THE ONE TO THINK ABOUT SEPARATELY. It carries a V5 signature naming what the user checked, and 17 citations point at it. Moving the text does not invalidate the signature, but a signed section that moves without saying so reads as a different section. Decide and state: keep it in the router, or move it with the signature and a line recording that the move happened. DELIVERABLE: SKILL.md under 20 KB; a byte-budget test asserting it, and asserting the same for each lane SKILL.md against its own cap; every `§` citation resolving; nothing deleted that is not reachable from a pointer. | — | V4 |
| TASK-065 | bin/lib/ — six primitives, fourteen implementations, no shared module | Coding Agent | not_started | MEASURED, so this is a defect class and not tidiness. Six primitives, fourteen implementations: write_atomic in perry-conform + perry-decide + perry-migrate; load_schema in perry-decide + perry-conform + perry-task; resolve_state_root in perry-diagnose + viewer/parsers.py; project_lock and mint_id each in perry-decide + perry-task; heading_re in perry-lint + perry-task. EVERY REVIEW ROUND THIS SESSION FOUND AN INSTANCE OF EXACTLY THIS. squash versus .strip().lower() across bin/ and viewer/ (TASK-050, still open, silently loses a user's live risks). The writer's is-this-a-risk-table predicate being a second copy of the reader's. split_stages in two files. norm being squash under another name. Each was fixed one at a time; the structure that generates them was not touched. There is no shared module, so a second implementation is the path of least resistance every time. THE PATH HACK IS THE OTHER HALF. Eight sys.path.insert calls: seven tools reverse-import from viewer/, and three of them re-insert bin/ to import each other. viewer/parsers.py is 2,520 lines — the second-largest file in the repo — and it is the read side of every CLI tool while living in a directory named for the web viewer. tests/ imports it the same way. A module nobody can import without a path hack is a module nobody will import. SCOPE: bin/lib/ holding the primitives above; parsers.py becomes a proper module rather than something reached by inserting a sibling directory. Whether it moves out of viewer/ or viewer/ stops being the anchor is the decision to make and state. THE GATE: behaviour-neutral. The suite is the proof and no test may need editing — that is what made the viewer/tables.py extraction safe, and it is the property to reproduce. Fold each duplicate pair only after showing the two bodies are identical; where they are NOT identical, that difference is a finding to report, not a merge to perform quietly. | — | V4 |

## P2

| ID | Title | Owner | Status | Next action | Evidence | Verification |
|---|---|---|---|---|---|---|
| TASK-034 | aimark integration — one call answers both of §1.3's questions | User + Agent | review | lifecycle run and evidenced: evidence/2026-08/TASK-034-lifecycle.md. One call answers both questions; nothing left on Perry's side that the run could find. Waits on your V5 — whether it is enough for aiMark. | — | V5 |
| TASK-028 | diagnose/adopt mode detection + both READMEs | User + Agent | blocked | V4: FAIL on one blocking finding. Stage since and Commitment are attributed to pipeline exclusively at weight 3 each, while modes/inquiry.md and modes/queue.md also own them — so a correctly-declared inquiry track is accused by MODE-01, and one weight-3 signal alone reaches `high`, which MODE-01 is gated on. Fix is TASK-055. | — | V4 |
| TASK-037 | perry-goals writer | Coding Agent | not_started | extraction landed (viewer/tables.py) and the byte-identity gate passes over 5 real OKR.md files incl. gimegime-pmo and aimark. Next: the OKR.md write path, starting with § Commitments (TASK-042). | — | V4 |
| TASK-038 | tasks: event log becomes canonical, BOARD.md becomes a projection | Coding Agent | not_started | Direction accepted by the user 2026-08-17 (DESIGN-005 § 9): the log becomes canonical, a hand edit raises a reconcile prompt rather than being overwritten or silently trusted. Still sequenced last per § 5.5; the acceptance was taken early so TASK-037's writer is built against the right notion of truth. | — | V5 |
| TASK-040 | perry-task: Top risks becomes a table with id / opened / cleared | Coding Agent | blocked | V4 round 2: FAIL. B-1 is not fixed — the writer's `is this a risk table` test is still a second copy of the reader's and they disagree on a decorated header, so risk-add writes, perry-state reports 0 risks, perry-lint is clean and risk-migrate says already migrated. Root cause is two normalizations across bin/ and viewer/. Fix is TASK-050. | — | V4 |
| TASK-042 | OKR.md § Commitments — the half TASK-021 did not do | Coding Agent | not_started | blocked on TASK-037's extraction. Acceptance is goals/reference/phases.md § commit <promise> — the procedure shipped 2026-08-17 states the rules the tool must implement; this row closes when commit stops being prose. | — | V4 |
| TASK-045 | Retire the runtime tolerance branches, behind the conformance marker | Coding Agent | not_started | Blocked: nothing may be removed before the marker exists and the migration can produce it (ADR-004). | — | V4 |
| TASK-061 | Four things the contract docs do not say that a consumer had to discover | Coding Agent | not_started | aiMark §5. (1) stderr is not the failure channel — every successful write prints the advisory conformance line there, and a front-end treating stderr as failure reports every write as an error. (2) event_written is documented nowhere and is the difference between 'the row moved' and 'the row moved and its timeline will have a hole'. (3) two events can share a ts, so a consumer sorting by ts needs to know ties are not duplicates. (4) rows_with_no_computable_age fires per row on a project with no event log — 17 of 17 — restating has_event_log: false seventeen times rather than reporting a finding. | — | V3 |
| TASK-066 | Split perry-task by subcommand group | Coding Agent | not_started | MEASURED: 3,098 lines, 22 subcommands — add start done stage status next retitle rung evidence drop resolve-intake intake ask answer cadence-add cadence-done risk-migrate risk-add risk-clear intake-sweep route list. It is the largest file in the repo and every fix agent this session has had to take a lock on it, which is why three tasks are queued behind whoever holds it. SUGGESTED GROUPS: add/route (the board writers), cadence, risks, intake/ask, and the read path (list). The four cell writers (next/retitle/rung/evidence) are already one function with four configurations and move together. BLOCKED ON the bin/lib/ extraction: splitting first means each new file re-imports the primitives through the same path hack, and the split then has to be redone. Do the shared module first. THE GATE, same as the extraction: behaviour-neutral, no test edited. perry-task/list is a frozen published contract and its payload must be byte-identical across the split — assert that, do not assume it. The three-way atomic write (board row + journal line + event) and the single project lock must stay single; a split that gives two files their own commit path is worse than the file being long. | — | V4 |

## Cadence (recurring; doesn't consume P0 slots)

| ID | Recurring task | Owner | Frequency | Next due | Last evidence |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## User Input Queue

| USER-id | Needed from user | Blocks | Idle | Status |
|---|---|---|---|---|
| USER-001 | Staleness threshold N | TASK-005 | — | **answered 2026-08-16: 30 days** |
| USER-002 | `--claims` vs `--strict` | — | — | **answered 2026-08-16: exempt** |

## Done this period (leaves the board at next triage)

| ID | Title | Evidence |
|---|---|---|

## Top risks (one-line; full list in `PROJECT_STATE.md`)

| ID | Risk | Opened | Status |
|---|---|---|---|
| RX-001 | Perry is half-adopted: `.perry/config.md` exists and flips `is_adopted()`, so lint demands a full state tree it does not have yet. Recorded in ADR-001 as a candidate finding for DESIGN-002. |  | open |
| RX-002 | ~~`LOAD-03` (10 decisions queued on the user)~~ — cleared 2026-08-16 when DESIGN-003's 8 rows were decided and USER-001/002 were answered. `bin/perry-diagnose --root .` now reports 0 errors and no `LOAD-*` finding. |  | cleared 2026-08-16 |
| RX-003 | The V4 review found `OKR.md § Commitments` is written by two modes that disclaim the goals cascade, with no declared owner. That is a hand-off-contract question, so TASK-026 now blocks phase D as well as phase G. |  | open |
| RX-004 | DESIGN-003 phase G rewrites `SKILL.md § The hand-off contract` — the one rule that keeps lanes composable, and `perry-lint` cannot see a bad edit to it. Mitigation is in DESIGN-003 §7: TASK-026 lands first and alone, with V5 sign-off and an ownership-refusal fixture. |  | open |
