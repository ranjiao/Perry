# Board — Perry

> Live working memory. Current open work only — closed tasks leave this file.
> History of what happened on a given day: `journal/2026-08/2026-08-16.md`
> Per-task spec / deliverable / audit: `evidence/2026-08/<TASK-ID>-*.md` (P0/P1 always have a `<TASK-ID>-spec.md`)
> Auto-dispatch a task: `/pmo dispatch <TASK-ID>` (requires spec.Dispatch mode = auto)
>
> Last updated: 2026-08-16 (21st pass — DESIGN-004 handed off, 6 tasks)
> Hard cap: ≤200 lines. If you're over, run `/pmo triage`.
>
> **Bootstrapped 2026-08-16** from the hand-off of DESIGN-001 and DESIGN-002, both
> locked the same day. Every row cites its design phase. Perry has no `OKR.md`, so
> every row is **declared unlinked** — not guessed into a KR.

## P0 (must finish this period)

| ID | Title | Owner | Status | Next action | Evidence | Verification | Depends on |
|---|---|---|---|---|---|---|---|
| TASK-044 | Migration must be dry-runnable, lossless, recoverable and user-declared | Coding Agent | in_progress | V4 ROUND 3: FAIL on guarantee 3, ADR-004 STANDS — fixed 2026-08-18. The reviewer stopped guessing where the third crash was and ENUMERATED ALL FIVE PLACES MIGRATION WRITES TO A PROJECT. Rounds 1 and 2 had guarded two; three were unguarded, including the recovery path itself: do_restore's undo restored BOARD.md and then died unlinking conformance.md, leaving it half restored with the record still claiming conformance for a rolled-back file — the exact thing restore_point's own docstring exists to prevent. C.declare sat outside round 1's try, so a project migrated once and then locked down was FULLY migrated with the restore point on disk and never named. restore_point itself was unguarded. THE AXIS ALL THREE ROUNDS MISSED: a read-only DIRECTORY, not a read-only file — write_atomic renames, and a rename needs write permission on the directory, which is why every existing test, all driving read-only through a file, passed throughout. FIXED all three plus the scratch write in Linter.errors, guarded rather than exempted because an exemption list is where this kind of guard rots. AND THE ENUMERATION IS NOW A STANDING CHECK: tests/test_migrate.py TestEveryWriteSiteIsGuarded walks the AST and fails if any write call sits outside a try — proved by planting a sixth site after the guard was written and watching it go red. That is the fix; the three patches are not. Guarantees 1, 2, 4, 5 all PASS on gimegime-pmo (365 to 380 ids, none lost, 59 to 15, 30 declared, PolyForge refused in one sentence). TASK-079 affects guarantee 4 and not 3 — restore puts the read-only file's bytes back exactly; what the preview never mentions is the mode it is about to override. AWAITING a fresh V4. | — | V4 |  |
| TASK-050 | One normalization for a header cell, not two | Coding Agent | review | NARROWED BY ADR-007 2026-08-19, after its round-4 FAIL. The header rule dies for BOARD.md, OKR.md and .perry/config.md when TASK-094 deletes it — those files stop being tables. What REMAINS in this row's scope is design/*.md and .perry/roles/*.md, and under ADR-007 rule 2 Python stops parsing those entirely rather than resolving their headers correctly. So the four rounds of guard-versus-reviewer on this row are not worth a fifth: the category is being removed, not hardened. KEEP THE ROW OPEN until TASK-094 lands, because until then the five live copies still matter; then re-scope it to whatever adoption still needs, which is the only parsing that survives. | — | V4 |  |
| TASK-067 | The writer can destroy the table it writes to, and perry-lint cannot see it | Coding Agent | review | NARROWED BY ADR-007 2026-08-19, after its round-4 FAIL. Once the three stores land, no markdown cell holds a value — so a writer cannot destroy a table it no longer writes, and a reader cannot mis-split a row that is not a row. The static guard that four rounds could not make category-shaped stops needing to be category-shaped. WHAT SURVIVES: adoption of a foreign project, which parses by definition, and that is where this row's remaining scope goes. The behavioural backstop (tests/test_escaped_pipe_corpus.py) survives too and is the right shape for it. KEEP OPEN until TASK-094/095 land. | evidence/2026-08/TASK-067-finding.md | V4 |  |
| TASK-089 | perry-task writes the store, not the board | Coding Agent | review | Built and verified 2026-08-19. Both decisions measured, not chosen. THE UNREPRODUCIBLE STATUS CELLS ARE REFUSED AT ROW SCOPE: a whole-project refusal was the literal reading of the brief and two pinned tests rejected it correctly — the read and write paths must agree about what a row is, and one fixture row files into three sections so one bad cell refused a command about a different table. Verified here: the bad row refused by name, its clean neighbour writes fine. The gate runs in main() before the subcommand, because inside commit() start walked straight through it — cmd_start overwrites the Status cell, so the check ran on a board where the offending cell no longer existed and approved the write that had erased it. ROW ORDER IS RECORDED, null not zero for a row off the board: 31 on-board rows carry an order, 66 closed carry null, none carries 0, and a write produces a ONE-LINE diff rather than the whole-file diff ADR-004 means when it says a migration must be reviewable. perry/tasks.jsonl is tracked now — it was ignored while derived; it is the write target now, and an untracked truth vanishes on a fresh clone while its projection stays. That flipped three fixtures from no-store to with-store silently and they failed the day it was tracked; each now says which case it builds. AWAITING a fresh V4. | — | V4 | TASK-088 |
| TASK-091 | By when splits into due + by_when_note, and CLOCK_RE is deleted | Coding Agent | review | Built and verified 2026-08-19. CLOCK_RE and every helper gone from bin/ — 0 occurrences, 182 lines deleted — replaced by two ANCHORED patterns with no CJK character in either, so round 5's defect cannot be re-expressed. THE SPLIT STRENGTHENS THE RULE RATHER THAN WEAKENING IT, verified by running it: prose in the typed field is refused and told where to go, and a commitment with NO due at all is refused outright because the note is a note ABOUT a deadline and never a substitute for one. Migration verified lossless by me on a register mixing all five shapes: 5 non-empty cells before, 5 after. Two integration gaps closed, the first re-verified here — perry-migrate would have bolted an empty Due onto a pre-split table, leaving every clock in the retired column with lint reporting zero errors. AWAITING a fresh V4: what I checked is the deletion, the losslessness on one register I built, and the refusal messages; what a reviewer should probe is the shapes I did not construct. | — | V4 |  |

## P1

| ID | Title | Owner | Status | Next action | Evidence | Verification | Depends on |
|---|---|---|---|---|---|---|---|
| TASK-019 | `modes/pipeline.md` | Coding Agent | blocked | TWO OF THREE FINDINGS CLOSED 2026-08-18, evidence at evidence/2026-08/TASK-019-stages-and-wip.md; the third closed as TASK-056. (2) STAGES: perry-state said stage_list [] on a blank Stages cell while perry-task birthed rows at the mode default — the reader denied a stage the writer was using. stage_list is now the EFFECTIVE vocabulary from the same schema field the writer reads, with stages_declared keeping the other question answerable. (3) WIP: not a contradiction — modes/pipeline.md says outright that WIP has no check. What the concession left behind was worse: viewer/parsers.py Task carried NEITHER track NOR stage, the two columns the non-project modes are defined by, and perry-state reads the board through that class — so triage step 2 was doable only by eyeballing a board the procedure forbids eyeballing, while perry-task/list parsed both with its own reader. Both now resolve by header name; perry-state reports stage_counts and wip_breaches per track, at-or-over the declared limit, and silence where no WIP was declared. The mode file now records which of its three conceded checks landed, with a test. 11 mutations, 11 red after three of mine turned out to be no-ops rather than blind guards. AWAITING a fresh V4 on all three fixes together. | `evidence/2026-08/TASK-019-020-v4-review.md` | V4 | TASK-056 |
| TASK-020 | `modes/queue.md` + `BOARD.md § Intake` + triage drain | Coding Agent | blocked | Round-6 V4: FAIL. route ignores --group, so the intake drain still cannot run on a board with no ## P0/## P1 — gimegime-pmo's actual shape — and the refusal that recommends --group is one route throws away. The guard's fixture contains a ## P1, so it graded narrow columns and could not fail on a missing heading. Also the overflow signal does not name intake. Fixes are TASK-053 and TASK-062. | `evidence/2026-08/TASK-019-020-v4-review.md` | V4 | TASK-053, TASK-062 |
| TASK-027 | Lane rename goals/work/decide + aliases | Coding Agent | review | ROUND 5 FINDINGS FIXED 2026-08-18, awaiting a fresh V4. B-1: viewer/ was a shipped user-facing surface in NEITHER the carve-out nor the enforced set — viewer/README.md told non-technical users to run the withdrawn shorthand and called it the recommended path, and viewer/templates/architecture.html printed it into the page every project sees before architecture init is run. THE CAUSE IS STRUCTURAL AND NOW GUARDED: the two lists were pinned TO EACH OTHER so neither could grow without the other, and nothing checked their UNION covers the tree — a path in neither was invisible to both. The partition is asserted now, and running it found a SECOND uncovered path nobody had reported: templates/, copied verbatim into a user's repository, exactly the reason the state templates are exempt, and the carve-out never said so. AGENTS.md is neither, with the reason recorded — a deliberate empty placeholder from when the routing layer was dropped, shipping zero bytes of prose. B-2: bin/perry-lint printed a lane name that no longer exists and router-subcommands.md repeated it inside the help block; both fixed. THE WRITTEN DECISION THE REVIEWER ASKED FOR: the two withdrawn names as HTTP ROUTES in viewer/ are a different namespace — renaming them breaks the running web app and the rename was never about them; what is in scope is the same token used as a chat command, which the README did one section later with the words in chat. Encoded in the check, not left as prose. MY FIRST PARTITION TEST HARDCODED THE EXEMPT SET and reported five paths the carve-out names in prose I had not read — the hardcoded-list defect this file exists to prevent, committed inside the test written to prevent it. It reads the carve-out now. | `evidence/2026-08/TASK-027-spec.md` | V4 |  |
| TASK-047 | Flip the conformance gate to enforce | Coding Agent | blocked | blocked on TASK-044 has not landed; two blockers measured and made executable | — | V4 | TASK-044 |
| TASK-038 | tasks: the task store becomes canonical, BOARD.md becomes a projection | Coding Agent | in_progress | Phase 002 first slice. Building perry/tasks.jsonl and proving it reproduces BOARD.md before anything writes to it. | — | V5 |  |
| TASK-077 | DESIGN-006 F — a finance-shaped role runs one real task end to end | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1. BLOCKED ON phases B, D and E. THIS IS THE PASS CONDITION in DESIGN-003's sense — the abstraction survives contact with a real non-software role, or the extraction report says why not. Candidate project: gimegime-pmo, on a copy. V5 and it cannot be otherwise: the user signs that knowledge was injected, that the escalation union blocked what it should have blocked, and that the output was accepted by the card's own Accepted by clause. An agent cannot sign any of those three. OUT OF SCOPE: cross-project role sharing, deferred in section 8. | — | V5 | TASK-073, TASK-075, TASK-076 |
| TASK-079 | Migration writes a file the user marked read-only, via rename | Coding Agent | not_started | FOUND 2026-08-18 while fixing TASK-044's planning crash, by asserting what I ASSUMED rather than what happens — the test failed and the code was right. write_atomic writes a .tmp and calls Path.replace, and A RENAME NEEDS WRITE PERMISSION ON THE DIRECTORY, NOT ON THE TARGET. So a file the user has chmod-ed read-only is migrated like any other, and nothing in the plan says the bit was there. THE QUESTION IS A POLICY ONE ABOUT SOMEBODY ELSE'S FILES and I deliberately did not decide it mid-fix: (a) the bit is an explicit user signal and ADR-004's whole posture is 'the user declares', so migration should refuse or at least name it; (b) it may be incidental — copied off a read-only medium, or a stale mode from an archive — and refusing would block a migration for a reason unrelated to shape. PINNED BY A TEST TODAY: the file IS migrated, and the restore point carries its original bytes, so the recovery path covers it — which is what makes the current behaviour survivable rather than merely undetected. NOT TRUE TODAY: that the plan mentions the mode at all. Whichever way this is decided, TASK-044-spec says migration is 'not silent — every file it touched, listed, with what changed in each', and a permission it overrode belongs in that list. | — | V4 |  |
| TASK-090 | perry-task reads the store, not the board | Coding Agent | not_started | — | — | V4 | TASK-089 |
| TASK-092 | OKR.md and .perry/config.md become stores with renderers | Coding Agent | not_started | — | — | V4 | TASK-089 |
| TASK-093 | A hand edit to a rendered file is reported rather than honoured | Coding Agent | review | ROUND 1 FINDINGS FIXED 2026-08-19, awaiting a fresh V4. FOUR reachable store states killed the whole lint — a JSON array, a bare scalar, a directory, an unreadable file — because only JSONDecodeError was caught, breaking the rule its own sibling guard states eight lines down. All four report now; two mutations red. THE SEVERITY ARGUMENT'S GROUND (c) WAS FALSE AND I HAD ASSERTED IT IN A COMMIT MESSAGE: an error here cannot reach ADR-004's gate, because shape_errors runs check_file only and this check is called from main(); the reviewer promoted all four to error and still got conformant/errors:0 with a gated write succeeding. The warn conclusion survives on the better argument that was available all along — warnings are quality signals, not shape violations. The message was false for 63 of 95 rows because build() includes closed tasks whose rows done removed. 'No store' and 'clean' printed byte-identically. The recorded limit was stale — TASK-088's renderer landed two commits later. STILL OPEN, split out as TASK-100: it fires on files Perry does not own, and tasks.jsonl is in no claims[] entry so NS-01 can never report a collision on it. | — | V4 |  |
| TASK-094 | Delete the header rule and the row splitter for the three stores | Coding Agent | not_started | — | — | V3 | TASK-090, TASK-092 |
| TASK-095 | Remove the parser for the three stores; keep what adoption needs | Coding Agent | not_started | — | — | V4 | TASK-094 |
| TASK-096 | Lane procedures call the tool before writing prose | Coding Agent | review | Built and verified 2026-08-19: 21 findings on a pristine git-archive tree, 0 after — I counted both myself rather than taking the report's 19. THREE OF THEM WERE WRONG, NOT MERELY UNTIDY: plan-week taught a hand edit citing 'the tool has no priority subcommand yet' when perry-task prioritize has existed since TASK-071; add-task step 2 said the definition block is still written by hand when perry-task add renders the whole thing; and one instructed writing a BOARD column that does not exist in the schema. THE GUARD WALKS THE TREE AND I PROVED IT: planted a lane that never existed plus a page two directories deep, both reported. My first plant was NOT caught and that was correct — it named OKR.md's narrative, which no tool writes, so it is not a target; re-planted against DECISIONS.md and both fired. Six exemptions, each a predicate with a reason: the subtle one is that adoption may transcribe an AUTHORED DOCUMENT but not a PROJECTION, because a projection is regenerated and a transcribed one is drift immediately. AWAITING a fresh V4 on the exemption set — that is where a wrong call hides, since the main assertion sits at 0. | — | V4 |  |
| TASK-097 | Migrate the two real projects to the store, at V5 | Coding Agent | not_started | — | — | V5 | TASK-092 |
| TASK-099 | Sweep bin/, viewer/ and tests/ for document handling that ADR-007 made dead | Coding Agent | not_started | — | — | V4 | TASK-095 |

## P2

| ID | Title | Owner | Status | Next action | Evidence | Verification | Depends on |
|---|---|---|---|---|---|---|---|
| TASK-028 | diagnose/adopt mode detection + both READMEs | User + Agent | review | ROUND 1 FINDING FIXED 2026-08-18, awaiting a fresh V4. The finding: len(tracks)==1 was a proxy for 'no register exists' and is ALSO true when the register declares exactly one track — so every board row, every commitment row and every project-wide file was scored as that track's, ignoring the Track column the scanner already reads and the schema's rule that a blank Track means the implicit main track. A project declaring one pipeline track with its ordinary work in untracked rows scored project 7 / pipeline 4 and got a MODE-01 warn TELLING THE USER TO CHANGE A MODE CELL THAT WAS CORRECT. All four sites addressed: the proxy now asks the declaredness question that was already being computed sixty lines below for the payload; the implicit main track is enumerated when untracked rows put one on the board; and file evidence is split into two buckets because lumping them was the bug in BOTH directions — Intake, answer files and SRC digests are evidence of a WORK SHAPE and reach any track that might have it, while phase/ and OKR objectives describe the REPOSITORY and reach only the project-wide track. TWO WRONG FIXES FIRST, both caught by existing tests that were right: withholding shape evidence made a declared queue track unreadable; and a fourth test asserts deliberately that ONE declared track does get the project-wide signals, which contradicts the finding until you read its board — it has NO ROWS. The finding's scenario is one declared track AND untracked rows beside it. So the rule is not how many tracks are declared, it is whether there is a project-wide track to own them. ALSO CORRECTED: the row previously claimed 8 and 10 mode mentions in the two READMEs; the reviewer measured 8 and 8 and I had written a number without running it. | — | V4 |  |
| TASK-037 | perry-goals writer | Coding Agent | review | NARROWED BY ADR-007 2026-08-19. perry-goals' writer moves to a store under TASK-092, so its open findings about cell escaping and the create/amend split stop applying to OKR.md — a JSON string carries a line break without a guard. WHAT STILL APPLIES: the flag-naming in refusals, and the module-scope handler class of bug, neither of which is about markdown. Re-scope when TASK-092 lands. | — | V4 |  |
| TASK-040 | perry-task: Top risks becomes a table with id / opened / cleared | Coding Agent | blocked | SUPERSEDED BY ADR-007 — PENDING, not dropped yet, 2026-08-19. Making Top risks a table with minted ids is a table-shaped fix to a section of BOARD.md, and BOARD.md becomes a rendered projection of a store. Risks become records with ids by construction, not by adding a column to markdown. NOT DROPPED YET for the same reason as TASK-042: the store has not landed. Drop it when TASK-089 lands and risks are records. | — | V4 | TASK-089 |
| TASK-042 | OKR.md § Commitments — the half TASK-021 did not do | Coding Agent | review | SUPERSEDED BY TASK-091 — PENDING, not dropped yet, 2026-08-19. TASK-091 deletes CLOCK_RE and splits By when into a typed due plus an unvalidated by_when_note. That removes the question this row has failed to answer in five rounds and four shapes, most recently because word boundaries do not exist in CJK. NOT DROPPED YET, deliberately: TASK-091 is in flight and if it fails, this row is what the project falls back to, and the CJK-boundary fix committed today is real and standing. Drop it the day 091 lands; reopen this text if 091 does not. | — | V4 | TASK-091 |
| TASK-045 | Retire the runtime tolerance branches, behind the conformance marker | Coding Agent | blocked | blocked on chain 044 → 047 → 045; switching to the head of it | — | V4 | TASK-044, TASK-047 |
| TASK-066 | Split perry-task by subcommand group | Coding Agent | not_started | RECONSIDER UNDER ADR-007 2026-08-19. Splitting perry-task by subcommand group was sized against a tool that parses and writes markdown. Under ADR-007 that tool loses its board reader, its row renderer and its cell escaping — TASK-090, 094 and 095 between them. The split may be unnecessary at the smaller size, or may want different seams. Do not start this before TASK-095; sizing a refactor against code that is about to be deleted is how a refactor becomes wasted. | — | V4 | TASK-065, TASK-038 |
| TASK-070 | Perry's own state is 19.5% of the tracked repo and grows unbounded | Coding Agent | not_started | SCOPE SHRANK AGAIN 2026-08-18 — ADR-006 settled the tension this row named, and in the direction that helps. The event log is no longer the only record of any task: perry/tasks.jsonl becomes the truth, and .perry/events.jsonl goes back to being history that is genuinely disposable. So the log IS a rotation candidate now, which it could not be while deleting it deleted 35 tasks. THIS ROW IS NOW journal/, evidence/, AND log rotation. Re-measured at the revise: perry+.perry is 642,170 of 3,111,696 tracked (20.6 percent). evidence/ is 174,151 across 36 files and is the LARGEST of the three — larger than journal/ at 127,599 across 3 — which the audit that opened this row did not have; it named the journal. design/ 148,182 and decisions/ stay: architecture record. NOTE the sequencing: rotation cannot ship before TASK-038 builds the store, or rotating the log still deletes closed tasks. Blocked on TASK-038 for that half; journal/ and evidence/ retention are independent and can go first. | — | V3 |  |
| TASK-085 | Decision status has no word for a proposal, and lives in three places | Coding Agent | not_started | — | — | V2 |  |
| TASK-086 | DESIGN-002 decision 4 says lint warns on a collision; lint does not emit NS-01 | Coding Agent | not_started | — | — | V2 |  |
| TASK-100 | tasks.jsonl is in no claims[] entry, so a namespace collision on it cannot be reported | Coding Agent | not_started | — | — | V3 |  |

## Cadence (recurring; doesn't consume P0 slots)

| ID | Recurring task | Owner | Frequency | Next due | Last evidence |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## User Input Queue

| USER-id | Needed from user | Blocks | Idle | Status |
|---|---|---|---|---|
| USER-001 | Staleness threshold N | TASK-005 | — | **answered 2026-08-16: 30 days** |
| USER-002 | `--claims` vs `--strict` | — | — | **answered 2026-08-16: exempt** |

## Done this period (leaves the board at next triage)

| ID | Title | Evidence |
|---|---|---|

## Top risks (one-line; full list in `PROJECT_STATE.md`)

| ID | Risk | Opened | Status |
|---|---|---|---|
| RX-001 | Perry is half-adopted: `.perry/config.md` exists and flips `is_adopted()`, so lint demands a full state tree it does not have yet. Recorded in ADR-001 as a candidate finding for DESIGN-002. |  | open |
| RX-002 | ~~`LOAD-03` (10 decisions queued on the user)~~ — cleared 2026-08-16 when DESIGN-003's 8 rows were decided and USER-001/002 were answered. `bin/perry-diagnose --root .` now reports 0 errors and no `LOAD-*` finding. |  | cleared 2026-08-16 |
| RX-003 | The V4 review found `OKR.md § Commitments` is written by two modes that disclaim the goals cascade, with no declared owner. That is a hand-off-contract question, so TASK-026 now blocks phase D as well as phase G. |  | open |
| RX-004 | DESIGN-003 phase G rewrites `SKILL.md § The hand-off contract` — the one rule that keeps lanes composable, and `perry-lint` cannot see a bad edit to it. Mitigation is in DESIGN-003 §7: TASK-026 lands first and alone, with V5 sign-off and an ownership-refusal fixture. |  | open |
