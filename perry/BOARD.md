# Board — Perry

> Live working memory. Current open work only — closed tasks leave this file.
> History of what happened on a given day: `journal/2026-08/2026-08-16.md`
> Per-task spec / deliverable / audit: `evidence/2026-08/<TASK-ID>-*.md` (P0/P1 always have a `<TASK-ID>-spec.md`)
> Auto-dispatch a task: `/pmo dispatch <TASK-ID>` (requires spec.Dispatch mode = auto)
>
> Last updated: 2026-08-16 (21st pass — DESIGN-004 handed off, 6 tasks)
> Hard cap: ≤200 lines. If you're over, run `/pmo triage`.
>
> **Bootstrapped 2026-08-16** from the hand-off of DESIGN-001 and DESIGN-002, both
> locked the same day. Every row cites its design phase. Perry has no `OKR.md`, so
> every row is **declared unlinked** — not guessed into a KR.

## P0 (must finish this period)

| ID | Title | Owner | Status | Next action | Evidence | Verification |
|---|---|---|---|---|---|---|
| TASK-044 | Migration must be dry-runnable, lossless, recoverable and user-declared | Coding Agent | blocked | V4: FAIL on one blocking finding, and ADR-004 does not reopen — all five guarantees hold. fix_tables recognizes a table by sharing ONE column name, so a legend under ## P0 is widened, lint goes to 0, the marker declares it conformant, and perry-task list returns rows fabricated from the legend's cells. Same class in enum_candidates and field_line. Fixes are TASK-051 and TASK-052. | — | V4 |
| TASK-050 | One normalization for a header cell, not two | Coding Agent | review | viewer/parsers.py normalizes header cells with .strip().lower() at 11 sites; bin/perry-task and bin/perry-lint use squash, which also strips backticks and asterisks. On a header whose Risk cell is bolded, the writer says risk-table and the reader says not — so risk-add writes, perry-state reports 0 risks, perry-lint is clean and risk-migrate says already migrated. Four exits, all closed. Task tables survive only via positional fallbacks. Fix: one function, plus the reader/writer agreement test in the shape tests/test_risks.py already uses for placeholders and bullets. | — | V4 |
| TASK-051 | Migration recognizes a table by shape, not by vocabulary | Coding Agent | review | Fix landed (agent B, ec958a1): fix_tables recognizes a table by vocabulary majority — more of the schema's names present than missing — so the 1-of-6 legend is refused and the real 4-of-6 P2 is still widened; enum_candidates reads negation from schema § migration.negations, in the clause before the token; field_line shares is_header_block's span. 11/11 mutations red. Reverified independently 2026-08-18: gimegime-pmo 59 to 15 lint errors, PolyForge correctly REFUSED with its 13 findings named as Perry's own files, restore point returns every file byte-identical. AWAITING V4. | — | V4 |
| TASK-052 | The losslessness assertions ask whether the bytes survived, never what the file now says | Coding Agent | review | Three meaning checks, each with its blind spot written into its own docstring rather than one general check that does not work: reading_changed (records viewer/parsers extracts must be unchanged — blind to files with no records), prose_rewritten (a rewritten line must be a table row or the header block — blind to INSERTIONS, which is how TASK-068 got through), enum_claims (the canon must still be what the retained words say — same vocabulary as the writer, defence in depth not an independent opinion). The unguarded assertion now has test_a_cell_can_be_lost_while_every_character_survives: '\| ab \| c \|' to '\| a \| bc \|' keeps every character and is not the same two cells. AWAITING V4. | — | V4 |
| TASK-053 | route ignores --group, and the refusal that recommends it is mine | Coding Agent | review | `--group` parses into args.group and `cmd_route` never reads it, so the intake drain cannot run on a board with no `## P0`/`## P1` — which is gimegime-pmo's actual shape. `check_priority`'s message tells the user to pass the heading to `--group`, a flag route throws away. Round-5's B-3 fixed the narrow-columns half and left the missing-heading half; the guard is named `test_the_intake_drain_runs_on_a_narrow_pre_existing_board` and its fixture contains a `## P1`. | — | V4 |
| TASK-057 | evidence_paths is empty on every closed row, and nothing says so | Coding Agent | review | aiMark, 2026-08-17, doc/perry-contract-gaps.md §1. Reproduced here: 32 closed rows, all carrying an evidence cell, all with evidence_paths empty, and the files exist. The same path on the same board resolves while the row is open. conformance.evidence_not_found does not report it either, so a consumer cannot tell 'not resolved' from 'file missing' — aiMark renders the document that justifies the close as a dead link. Fix: resolve the cell for closed rows, or say in the payload that closed rows are not resolved. | — | V4 |
| TASK-063 | A dependency is not queryable — blocked says that, never on what | Coding Agent | not_started | Found by the user calling `perry-task list` and seeing a pile of rows with no way to tell which were advanceable. Three facts. (1) `add --depends` exists and writes `- **Dependencies**: <free text>` into the journal's definition block — written once at creation, never updatable, not on the board, not in the payload. So the dependency graph lives in date-sharded append-only prose, which is verbatim what DESIGN-004 section 1.3 named as the defect for the task set itself. (2) `status --status blocked` REFUSES without `--reason`, on the stated ground that `a blocked row with no named dependency is one nobody can unblock` — and then puts that named dependency in prose. The instinct is right and the answer lands somewhere unreadable. (3) `subcommands.md:87` tells triage to find `the same dependency cited in >= 2 rows`, which requires reading dependencies across rows; nothing can. A rule with no readable input. Blocked on TASK-050's agent, which owns bin/perry-task and schema/task-list-contract.md right now. | — | V4 |
| TASK-067 | The writer can destroy the table it writes to, and perry-lint cannot see it | Coding Agent | review | Fix landed: render_row refuses a value a row cannot carry (one line, reads back as itself); every reader routed through one split_row (parsers 10 sites, perry-lint, perry-state, perry-diagnose — the last two found by the category-shaped test, not by the report); ragged-row finding in both directions, placed above the missing-columns bail-out. 17 new tests, suite 955 to 971, 11/11 mutations red, no existing test edited. gimegime-pmo 59 and PolyForge 13 unchanged, zero ragged-row on either. AWAITING V4 by a fresh reviewer against evidence/2026-08/TASK-067-finding.md. | evidence/2026-08/TASK-067-finding.md | V4 |
| TASK-072 | DESIGN-006 A — Knowledge card schema + perry-lint --knowledge | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1's handoff payload, which had sat with 'TBD at handoff' in every row since the doc locked — six phases, zero board rows, while OKR v2's whole Objective 5 rests on them. Verification V3: a card missing any provenance field fails lint; a Source: resolving to nothing fails; reverting the fix must break the test. No dependencies. OUT OF SCOPE: capture points (phase B), role cards (phase C). The governing rule from the doc: a knowledge card cannot exist without provenance — that is what separates this from a notes folder. | — | V3 |

## P1

| ID | Title | Owner | Status | Next action | Evidence | Verification |
|---|---|---|---|---|---|---|
| TASK-019 | `modes/pipeline.md` | Coding Agent | blocked | Round-6 V4: FAIL. Two claims no round had tested — the missing-SLA finding is stated in modes/pipeline.md, modes/queue.md and the schema and implemented nowhere (the schema note books it as fixed in round 3), and perry-task and perry-state disagree about the same track's stages and WIP. Review filed at evidence/2026-08/TASK-019-020-round6-review.md. Fix is TASK-056. | `evidence/2026-08/TASK-019-020-v4-review.md` | V4 |
| TASK-020 | `modes/queue.md` + `BOARD.md § Intake` + triage drain | Coding Agent | blocked | Round-6 V4: FAIL. route ignores --group, so the intake drain still cannot run on a board with no ## P0/## P1 — gimegime-pmo's actual shape — and the refusal that recommends --group is one route throws away. The guard's fixture contains a ## P1, so it graded narrow columns and could not fail on a missing heading. Also the overflow signal does not name intake. Fixes are TASK-053 and TASK-062. | `evidence/2026-08/TASK-019-020-v4-review.md` | V4 |
| TASK-027 | Lane rename goals/work/decide + aliases | Coding Agent | review | UNBLOCKED 2026-08-18 — TASK-054 landed and is done. Round-5 V4 failed this for a CATEGORY, not a list: the rename reached the documents a reviewer reads and not the artifacts a user receives, and the guard it was given set TOOLS to 2 of 15 tools and discarded docstrings, which is exactly what --help prints. So the re-review must score the category: run every tool's --help and every user-facing surface, not the list round 5 happened to name. AWAITING a fresh V4. | `evidence/2026-08/TASK-027-spec.md` | V4 |
| TASK-046 | A queue track must declare an SLA at creation — no default | Coding Agent | not_started | User decision 2026-08-17: force the question at creation, give no default. Rejected a default (5d) because a number the user never declared would start producing 'overdue' verdicts — the confidently-wrong-value class this project keeps fixing. Rejected the status quo because every queue user hits the gap after the fact rather than before. Blocked on TASK-044, which owns schema + perry-lint right now. | — | V4 |
| TASK-047 | Flip the conformance gate to enforce | Coding Agent | not_started | User decision 2026-08-17: flip after TASK-044 lands, not before. The advisory default was chosen because enforcing would name a command nobody could run; TASK-044 is what removes that. Blocked on TASK-044. | — | V4 |
| TASK-048 | perry-diagnose's own output does not validate against Perry's schema | Coding Agent | not_started | Found by TASK-044: all 13 of PolyForge's lint errors are Perry's own diagnostic output failing schema/state-schema.json — status 'false_positive'/'declined' are not in finding_status, source 'read'/'read+interview' not in finding_source, ids LOAD-02/CON-03b do not match ^[A-Z]{3}-\d{2}$. Migration deliberately refuses to edit it: rewriting a finding's status to satisfy an enum is editing a diagnostic record. | — | V3 |
| TASK-056 | A claim stated in three places and implemented in none — the missing-SLA finding | Coding Agent | not_started | modes/pipeline.md:147, modes/queue.md:21 and schema/state-schema.json:64 all say triage reports a missing SLA as a finding. `grep -rn no_default bin/ viewer/ tests/` returns nothing and the triage procedure never mentions it. The schema note attributes the fix to round 3 — it was booked as done by writing the note. Either implement it or delete all three claims. Related: perry-state and perry-task disagree about the same track's stages and WIP, and `default_wip` has zero readers. | — | V4 |
| TASK-058 | asks, risks and drift have no contract — the ask from the only consumer | Coding Agent | not_started | aiMark §2. All three are written by perry-task and readable only through perry-state, the one payload with no version. The task contract names them out of scope and invites a 1.x bump if a front-end needs them; this is that ask. User Input Queue is the needs-you list the Projects dashboard sorts on. Two shape problems to fix in the same pass: a bullet-sourced risk arrives with id = the severity letter and severity = 'watch' for every row (the H/M/L a human wrote survives only in meta), and user_input_queue.items[].idle is a rendered string '9d' rather than an integer. | — | V4 |
| TASK-059 | The agents roster: aiMark's ask, as an input to DESIGN-006 | Coding Agent | not_started | Rescoped 2026-08-17 on the user's observation that this is DESIGN-006's work. That design's § 1.1 is titled 'There is no role object — only tasks and a hardcoded agent list' and names the three places the roster lives today: delegate's hardcoded Coding/Research/Review, hook_TEMPLATE's free-text 'Special agents available', and perry-dispatch-limit tracking executors as though they were roles. Adding {id, tasks} to perry-goals/list now would freeze the roster into a shape DESIGN-006 replaces, in a frozen additive contract, and file it under goals — while a roster is a fact about who executes, not about goal attribution. Blocked on DESIGN-006. aiMark keeps its DeclaredAgent path unused and its Agents view says source: owner, which is honest. | — | V5 |
| TASK-060 | mint_id does not adopt the board's own id prefix, and add has no way to ask | Coding Agent | review | aiMark section 4. On a board whose seventeen rows all carry that project's own prefix, add minted a TASK- id instead. Legitimate under the contract (ids are opaque, a board may carry several project-declared prefixes) but a user creating a task from a front-end watches their board sprout a second id family with no way to ask for the first. aiMark passes no id and cannot: nothing in the surface would let it. Fix: default to the prefix the board already uses, or accept --prefix. (Prefix examples deliberately not written out here — quoting another project's ids in a board cell puts them in this project's dangling report, which is the third time.) | — | V4 |
| TASK-062 | The board-overflow signal does not name intake, and prescribes the split queue mode forbids | Coding Agent | not_started | TASK-020's round-6 M-4. Built the fixture the rubric names (220 intake rows, board at 244 lines): perry-lint says 'Split the overflow into a sibling file' and perry-state says 'run /perry work triage', while modes/queue.md:66-69 says the correct response is a finding and explicitly not moving the section somewhere it can grow unnoticed. Neither message names intake as the cause. And perry-state carries no intake block at all — viewer/parsers.py dispatches board sections through an allowlist that ## Intake does not match — so the correlation subcommands.md demands is not computable from the payload triage is told to read, while the same document forbids obtaining it by opening the file. No test builds the over-cap fixture. | — | V4 |
| TASK-064 | SKILL.md is 62 KB and loads on every invocation — cut it under 20 KB | Coding Agent | not_started | SKILL.md is 62,398 bytes and is loaded on every single invocation. Target: under 20 KB. Sink the bodies of adopt / diagnose / relocate / config / style / hooks into reference/, and leave the router a dispatch table plus a one-line pointer per entry. MEASURED, so the plan is not a guess. The six sections the ask names total about 19 KB — not enough on their own. The two largest are `Mandatory first move: combined snapshot` at 12,707 bytes and `First-time setup` at 7,009, and getting under 20 KB means the snapshot ritual's body sinks too, leaving the router with the commands to run, the dashboard shape, and pointers for the rest. THE CONSTRAINT THAT MAKES THIS RISKY: 63 places cite `SKILL.md § <section>` by name, 17 of them the hand-off contract. TASK-054's agent declined to rewrite 37 shorthand occurrences for exactly this reason — renaming a heading dangles every `§`-citation to it, silently, because the pointer guard resolves the path half and not the section half. So: every existing citation must still resolve after the move, or be updated in the same commit, and the guard that checks section halves does not exist yet and is part of this task. THE HAND-OFF CONTRACT IS THE ONE TO THINK ABOUT SEPARATELY. It carries a V5 signature naming what the user checked, and 17 citations point at it. Moving the text does not invalidate the signature, but a signed section that moves without saying so reads as a different section. Decide and state: keep it in the router, or move it with the signature and a line recording that the move happened. DELIVERABLE: SKILL.md under 20 KB; a byte-budget test asserting it, and asserting the same for each lane SKILL.md against its own cap; every `§` citation resolving; nothing deleted that is not reachable from a pointer. | — | V4 |
| TASK-065 | bin/lib/ — six primitives, fourteen implementations, no shared module | Coding Agent | not_started | COUNT UPDATED 2026-08-18 — it grew. Was six primitives / fourteen implementations; ef16733 (perry-goals writer) re-imported fcntl, tempfile and contextlib and rebuilt three of them locally, so write_atomic is now in 4 tools (perry-decide, perry-conform, perry-goals, perry-migrate), project_lock in 3 (perry-decide, perry-goals, perry-task), load_schema in 4 (perry-decide, perry-conform, perry-goals, perry-task); resolve_state_root 2, mint_id 2 plus a third spelling mint_commitment_id, heading_re 2. THE HALF THAT WAS EXTRACTED HELD, and that is the argument for finishing it: the same commit imports render_row, split_row and squash from viewer/tables.py rather than writing a fifth cell writer. One extraction stopped one duplication; the primitives nobody extracted gained three implementations in a single commit. TASK-067 then routed every reader through the one split_row and found two more offenders (perry-state, perry-diagnose) that no report had named — because the test was written as the CATEGORY, any file carrying its own strip-and-split, not as the instances that had surfaced. That is the shape this row's guard needs. | — | V4 |
| TASK-068 | Migration joins its header block onto the author's prose — the fourth instance of one defect class | Coding Agent | not_started | FOUND BY READING THE 30 MIGRATED FILES, not by a test — TASK-052's rule 3 paying for itself. In gimegime-pmo (copy), knowledge/auto-research/auto_research_agent_全景综述_2026.md has Perry's four header fields appended to the END of the author's quoted abstract paragraph, so the metadata renders as the last four lines of somebody's seed thesis. It got in through is_header_block's 'opens immediately under the H1' branch, NOT the field-vocabulary branch — which is why narrowing the vocabulary three times did not stop it. TASK-052's prose_rewritten check cannot see it and says so in its own docstring: this is an INSERTION beside a paragraph, not a rewrite of a line, and every meaning check built so far compares lines that changed. Two smaller findings from the same read: (a) joined blocks inherit mixed colon forms and mixed languages, e.g. a bold English label with an ASCII colon beside a bold Chinese label with a fullwidth one, in one block; (b) a joined bolded source-id line does not satisfy perry-lint --provenance's anchor, which requires the id label at the start of the quote, so a migrated digest silently stops being provenance-checkable. Deliberately left by the agent that found it: narrowing that branch is a design decision with its own regression surface, and refusing the file would move the reference numbers TASK-051/052 are pinned to. | — | V4 |
| TASK-069 | The i18n alias table is read by five tools and hand-carried by the three that read state | Coding Agent | not_started | MEASURED at 34bd472, and sharper than the audit that raised it. The audit said ~20 regexes hand-carry aliases while the schema already holds the table. Both halves reproduce, but the shape is a SPLIT BRAIN, not a missing mechanism: schema i18n already declares 34 headings plus fields and columns, and FIVE tools read it (perry-lint via load_glossary, perry-conform, perry-migrate, perry-goals, perry-diagnose). The three that do not are exactly the three on the state read path — viewer/parsers.py (39 lines carrying a CJK literal, the most in the repo, and the read side of ALL THREE frozen contracts), bin/perry-task (23) and bin/perry-state (10). So perry-lint and viewer/parsers can DISAGREE about whether a Chinese heading is a heading: the linter resolves it from the table, the contract reader does not. Failure mode is silent — the section is simply absent from the payload, with nothing in conformance to say so, which is the same shape as TASK-067's escaped-pipe misread. Every new matcher needs a hand-added branch and the next one is the one that gets missed. GATE: behaviour-neutral, no test edited — the property that made the viewer/tables.py extraction safe. Candidate KR is O3.3 (content present in a project's files but absent from its contract payload, 0 dropped) — proposed, NOT assumed; left unlinked until the user says. | — | V4 |
| TASK-038 | tasks: event log becomes canonical, BOARD.md becomes a projection | Coding Agent | not_started | VERDICT 2026-08-18 (work triage): should be P1, STILL P2 — the tool cannot write a priority change. See TASK-071 (P0), found by trying to execute this. Not hand-edited: that lands with no event and perry-state reports it as unrecorded drift, the exact failure DESIGN-004 was written against. THE VERDICT ITSELF IS NOT A RE-SEQUENCING — DESIGN-005 section 6's own gate is met and nobody moved the row. Section 6 defers this behind steps 1-3 'that will have exercised the contract pattern by then'; all three landed at 34bd472: perry-decide 578 lines carrying perry-decide/list/1.0, perry-goals/list at 2.0 (past the 1.0 step 2 asked for), the perry-goals writer in ef16733 with TASK-037/042 in review. Three contracts, all frozen, version-locked by tests/test_conformance.py. P1 and not P0: this is the only step at V5 because it changes what the truth IS, so it cannot reach done without the user, and P0 holds 8 rows of which 6 are already in review. BLOCKS TASK-066 — see that row for the measurement. Carries KR-O2.2 (deadline 2026-10-31) and is its only board row. HAND-OFF FILED, NOT ACTED ON: perry/handoff/2026-08-18-design-005-step-4-gate.md asks decide two questions this lane may not answer — whether step 4's V5 rung still holds, and whether section 9 should record that its own gate opened. DESIGN-005 was not opened for writing. | — | V5 |
| TASK-073 | DESIGN-006 B — Promotion at the three capture points | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1. BLOCKED ON phase A (the card schema must exist before anything can write one). Verification V3: a real close-task run produces a card; a sourceless write is refused. OUT OF SCOPE: bulk import, and a standalone add-knowledge ritual — the doc makes that an explicit Non-Goal, because knowledge that is not captured at the moment it was learned is knowledge nobody writes down. | — | V3 |
| TASK-074 | DESIGN-006 C — Role card schema + .perry/roles/ + shipped defaults | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1. No dependencies — runs in parallel with B. Verification V3: schema tests plus the workflow-heading rejection case. THE REJECTION IS THE POINT, not a detail: the doc's decision #1 says a role card is a HIRING CONTRACT the harness instantiates, never a workflow, and a ## Workflow heading is how that distinction gets lost one card at a time. OUT OF SCOPE: delegate/dispatch wiring (phase D). | — | V3 |
| TASK-075 | DESIGN-006 D — delegate/dispatch role integration | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1. BLOCKED ON phases A and C. Verification V3: a union test — a role-added escalation term must trip the pre-flight scan — plus a lint warning when an escalation line carries zero backticks. THE UNION IS THE SAFETY PROPERTY: a role's escalation list must ADD to the project's high-stakes list, never replace it, or hiring a role quietly narrows what the project refuses to do unsupervised. OUT OF SCOPE: bin/perry-dispatch-limit, which is the executor axis and stays untouched. | — | V3 |
| TASK-077 | DESIGN-006 F — a finance-shaped role runs one real task end to end | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1. BLOCKED ON phases B, D and E. THIS IS THE PASS CONDITION in DESIGN-003's sense — the abstraction survives contact with a real non-software role, or the extraction report says why not. Candidate project: gimegime-pmo, on a copy. V5 and it cannot be otherwise: the user signs that knowledge was injected, that the escalation union blocked what it should have blocked, and that the output was accepted by the card's own Accepted by clause. An agent cannot sign any of those three. OUT OF SCOPE: cross-project role sharing, deferred in section 8. | — | V5 |

## P2

| ID | Title | Owner | Status | Next action | Evidence | Verification |
|---|---|---|---|---|---|---|
| TASK-034 | aimark integration — one call answers both of §1.3's questions | User + Agent | review | EVIDENCE REFRESHED 2026-08-18 — the lifecycle was re-run end to end on a fresh project and three things are now true that were not on 08-17. evidence_paths RESOLVES on a closed row (aiMark's first-listed finding; contract 1.4 to 1.5); a task's priority can be changed at all, via the new prioritize subcommand, which appears in the run's timeline; and prioritize's from/to are the SECTION not the status, which is the one shape in this payload that can be misread while looking correct. The re-run also found a defect in the same session that wrote it: a closed row reported priority P1 with prioritize P1 to P0 in its own timeline two lines above — fixed, and the guard now compares the field against the row's own timeline rather than a known-good value. WHAT YOUR SIGNATURE IS FOR, and it is not whether the commands work — that is the V3 and it reproduces. It is whether ONE CALL answers both of DESIGN-004 section 1.3's questions well enough for aiMark to be built on, which only aiMark's author can say. aiMark's gap report was acted on point by point and the reply is perry/handoff/2026-08-18-aimark-prompt.md. Signing means the loop closed. A V5 records NAME, DATE and WHAT WAS CHECKED — writing reviewed would make the rung a label instead of a record. | — | V5 |
| TASK-028 | diagnose/adopt mode detection + both READMEs | User + Agent | review | UNBLOCKED 2026-08-18 — TASK-055 landed and is done: Stage since and Commitment are now scored for EVERY owning mode at reduced weight, so their contribution to the margin between those owners is exactly zero, and the high gate was raised to score>=5 with margin>=3 so no verdict can reach high on shared evidence alone. Both constants are mutation-verified individually. The README half of this task is also in: README.md and README_cn.md carry 8 and 10 mode mentions respectively, against the 0 and 0 the spec measured. AWAITING a fresh V4 against evidence/2026-08/TASK-028-spec.md. | — | V4 |
| TASK-037 | perry-goals writer | Coding Agent | review | extraction landed (viewer/tables.py) and the byte-identity gate passes over 5 real OKR.md files incl. gimegime-pmo and aimark. Next: the OKR.md write path, starting with § Commitments (TASK-042). | — | V4 |
| TASK-040 | perry-task: Top risks becomes a table with id / opened / cleared | Coding Agent | blocked | V4 round 2: FAIL. B-1 is not fixed — the writer's `is this a risk table` test is still a second copy of the reader's and they disagree on a decorated header, so risk-add writes, perry-state reports 0 risks, perry-lint is clean and risk-migrate says already migrated. Root cause is two normalizations across bin/ and viewer/. Fix is TASK-050. | — | V4 |
| TASK-042 | OKR.md § Commitments — the half TASK-021 did not do | Coding Agent | review | blocked on TASK-037's extraction. Acceptance is goals/reference/phases.md § commit <promise> — the procedure shipped 2026-08-17 states the rules the tool must implement; this row closes when commit stops being prose. | — | V4 |
| TASK-045 | Retire the runtime tolerance branches, behind the conformance marker | Coding Agent | not_started | Blocked: nothing may be removed before the marker exists and the migration can produce it (ADR-004). | — | V4 |
| TASK-061 | Four things the contract docs do not say that a consumer had to discover | Coding Agent | not_started | aiMark §5. (1) stderr is not the failure channel — every successful write prints the advisory conformance line there, and a front-end treating stderr as failure reports every write as an error. (2) event_written is documented nowhere and is the difference between 'the row moved' and 'the row moved and its timeline will have a hole'. (3) two events can share a ts, so a consumer sorting by ts needs to know ties are not duplicates. (4) rows_with_no_computable_age fires per row on a project with no event log — 17 of 17 — restating has_event_log: false seventeen times rather than reporting a finding. | — | V3 |
| TASK-066 | Split perry-task by subcommand group | Coding Agent | not_started | NOW BLOCKED ON TASK-038 AS WELL AS TASK-065, and the reason is not the line count. MEASURED at 34bd472: class Board is 446 lines of perry-task's 3,291 (13.6 percent); the write side TASK-038 deletes — ensure_columns, ensure_section, ensure_section_columns, append_row, append_section_row, replace_row, remove_row, last_row — is 152 lines, 4.6 percent. The read side, 273 lines, STAYS, because KR-O2.2 requires a hand edit to raise a reconcile prompt rather than be overwritten, and detecting a hand edit means reading the board. So the audit's 'the write-side half of 531 plus 437 lines' overstates it: 152 lines alone would NOT justify blocking this row. THE REAL OVERLAP IS STRUCTURAL. This row's own gate says the three-way atomic write (board row + journal line + event) and the single project lock must stay single, and that a split giving two files their own commit path is worse than the file being long. After TASK-038 there IS no three-way write — one append, one render. Splitting 22 subcommands around an invariant that is about to be replaced means the split's one hard decision gets made twice. That is this row's own argument about TASK-065, one level up, applied to itself. | — | V4 |
| TASK-070 | Perry's own state is 19.5% of the tracked repo and grows unbounded | Coding Agent | not_started | SCOPE CUT AND ONE TENSION RESOLVED 2026-08-18, by measurement. The event log is NOT a retention candidate: with .perry/events.jsonl the payload reports 39 open + 35 closed = 74 tasks; delete it and the same call reports 39 open + 0 closed. All 35 closed tasks vanish from every contract payload. DESIGN-004 section 5.3 calls the log derived and disposable and says what is lost is history resolution and NOT TRUTH — but section 1.3 of the same doc defines the reader problem as what is the full set of tasks, and a closed row leaves the board by design, so the log is its only machine-readable record. The two sections contradict each other and 5.3 is the wrong one. Committing the log has been correct all along; the document is what is out of step. Correcting it is a decide write — raised and NOT made, in perry/handoff/2026-08-18-event-log-is-not-disposable.md, which states three candidate shapes and ranks none. SO THIS ROW IS journal/ AND evidence/ ONLY. Re-measured at fed289c: perry+.perry is 642,170 of 3,111,696 tracked (20.6 percent, up from the audit's 19.5 in a few hours). evidence/ is 174,151 across 36 files and is now the LARGEST of the three — larger than journal/ at 127,599 across 3 — which the audit did not have; it named the journal. design/ at 148,182 and decisions/ stay: architecture record. | — | V3 |
| TASK-076 | DESIGN-006 E — Task contract role field + triage staleness line | Coding Agent | not_started | Minted 2026-08-18 from DESIGN-006 section 6.1. BLOCKED ON phase C. Verification V3: the refusal case, AND the byte-identical case for a project that has declared no roles — DESIGN-006's Goal 7 is that a project which never hears of roles behaves exactly as it does today, which is the same no-op property modes/project.md holds for work modes. OUT OF SCOPE: viewer and aiMark rendering, both downstream. NOTE the contract collision: TASK-058 and TASK-063 are bumping the same contract to 1.6 tonight; this one must take the next minor after whatever they land, not race them. | — | V3 |

## Cadence (recurring; doesn't consume P0 slots)

| ID | Recurring task | Owner | Frequency | Next due | Last evidence |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## User Input Queue

| USER-id | Needed from user | Blocks | Idle | Status |
|---|---|---|---|---|
| USER-001 | Staleness threshold N | TASK-005 | — | **answered 2026-08-16: 30 days** |
| USER-002 | `--claims` vs `--strict` | — | — | **answered 2026-08-16: exempt** |

## Done this period (leaves the board at next triage)

| ID | Title | Evidence |
|---|---|---|

## Top risks (one-line; full list in `PROJECT_STATE.md`)

| ID | Risk | Opened | Status |
|---|---|---|---|
| RX-001 | Perry is half-adopted: `.perry/config.md` exists and flips `is_adopted()`, so lint demands a full state tree it does not have yet. Recorded in ADR-001 as a candidate finding for DESIGN-002. |  | open |
| RX-002 | ~~`LOAD-03` (10 decisions queued on the user)~~ — cleared 2026-08-16 when DESIGN-003's 8 rows were decided and USER-001/002 were answered. `bin/perry-diagnose --root .` now reports 0 errors and no `LOAD-*` finding. |  | cleared 2026-08-16 |
| RX-003 | The V4 review found `OKR.md § Commitments` is written by two modes that disclaim the goals cascade, with no declared owner. That is a hand-off-contract question, so TASK-026 now blocks phase D as well as phase G. |  | open |
| RX-004 | DESIGN-003 phase G rewrites `SKILL.md § The hand-off contract` — the one rule that keeps lanes composable, and `perry-lint` cannot see a bad edit to it. Mitigation is in DESIGN-003 §7: TASK-026 lands first and alone, with V5 sign-off and an ownership-refusal fixture. |  | open |
