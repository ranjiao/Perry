# Board — Perry

> Live working memory. Current open work only — closed tasks leave this file.
> History of what happened on a given day: `journal/2026-08/2026-08-16.md`
> Per-task spec / deliverable / audit: `evidence/2026-08/<TASK-ID>-*.md` (P0/P1 always have a `<TASK-ID>-spec.md`)
> Auto-dispatch a task: `/pmo dispatch <TASK-ID>` (requires spec.Dispatch mode = auto)
>
> Last updated: 2026-09-12
> Hard cap: ≤200 lines. If you're over, run `/pmo triage`.
>
> **Bootstrapped 2026-08-16** from the hand-off of DESIGN-001 and DESIGN-002, both
> locked the same day. Every row cites its design phase. Perry has no `OKR.md`, so
> every row is **declared unlinked** — not guessed into a KR.

## Intake

| Arrived | Request | Outcome |
|---|---|---|

## P0 (must finish this period)

| ID | Title | Owner | Status | Next action | Evidence | Verification | Depends on | Track | Stage | Arrived | Stage since | Parent | Commitment | Role |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|

## P1

| ID | Title | Owner | Status | Next action | Evidence | Verification | Depends on | Track | Stage | Stage since | Arrived | Parent | Commitment | Role |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TASK-129 | Agent is five strings that do not join, and role has never once been written | Coding Agent | not_started | unblocked: work owns .perry/agents.jsonl → .perry/roles/ as of the 2026-08-20 signature; needs a spec, then dispatch | — | V3 | TASK-128 | main |  |  |  |  |  |  |
| TASK-173 | an Objective is not a record, so it has no durable address | Coding Agent | not_started | DESIGN-009 drafted 2026-08-21: design/DESIGN-009-the-objective-is-a-record.md. Four User Decisions open — id shape, write-back location, how the five existing objectives get minted, and whether krs[].objective keeps the title. The key finding: the PHASE level already solved this. phase/002-linkage.md states id: O1 and the payload carries it, while okr.objectives[].id is '' for all five — and the contract already says a STATED id is legitimate where a DERIVED one is not | — | V4 | TASK-181, TASK-182, TASK-183, TASK-184, TASK-185 | main |  |  |  |  |  |  |
| TASK-177 | OKR setting is a ten-field checklist where it should be an elicitation | Coding Agent | not_started | DESIGN-011 drafted 2026-08-21: design/DESIGN-011-the-okr-is-elicited-not-collected.md. Four User Decisions open. Step 2 is the gate — run the question bank against a project with no OKR.md and run the rubric on the output; goal 3 (the rubric surfaces ZERO issues) is measured there or it is not measured | — | V4 | TASK-190, TASK-191, TASK-192, TASK-193, TASK-194 | main |  |  |  |  |  |  |
| TASK-184 | D009 step 4 — okr.objectives[].id is filled from the store and the contract moves to 2.2 | Coding Agent | not_started | — | — | V3 | TASK-183, TASK-415 | main |  |  |  |  |  |  |
| TASK-185 | D009 step 5 — an Objective id survives a rename and a reorder, proved | Coding Agent | not_started | — | — | V3 | TASK-184 | main |  |  |  |  |  |  |
| TASK-186 | D010 step 2 — a spec declares its author, and the escalation scan reports it | Coding Agent | not_started | Replace the DESIGN-010 step-2 file-field parser with a typed spec manifest/store field that callers cannot override. Preserve the provenance guarantee and update TASK-187's dependency contract to consume the structured field. | — | V3 | — | main |  |  |  |  |  |  |
| TASK-187 | D010 step 3 — a machine-authored spec is fail-closed at the escalation gate | Coding Agent | not_started | Depend on TASK-186's structured provenance and define an enumerated write-target manifest. The gate compares those targets with the high-stakes registry; document prose is handed to the agent and cannot green-light or cancel a hit. | — | V3 | TASK-186 | main |  |  |  |  |  |  |
| TASK-188 | D010 step 4 — the scout, run by hand on ten real rows and scored against what the PMO actually decided | Coding Agent | not_started | — | — | V4 | TASK-187 | main |  |  |  |  |  |  |
| TASK-189 | D010 step 5 — autopilot becomes the two-stage scout-then-build loop | Coding Agent | not_started | — | — | V4 | TASK-188 | main |  |  |  |  |  |  |
| TASK-190 | D011 step 1 — a question bank for the first-ever-OKR route | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-191 | D011 step 2 — a real transcript, scored by the rubric that stays unchanged | Coding Agent | not_started | — | — | V4 | TASK-190 | main |  |  |  |  |  |  |
| TASK-192 | D011 step 3 — routing and smart-skip, by track spine | Coding Agent | not_started | — | — | V3 | TASK-191 | main |  |  |  |  |  |  |
| TASK-193 | D011 step 4 — the escape hatch and the premise challenge | Coding Agent | not_started | — | — | V3 | TASK-191 | main |  |  |  |  |  |  |
| TASK-194 | D011 step 5 — plan-phase uses the same question bank | Coding Agent | not_started | — | — | V3 | TASK-191 | main |  |  |  |  |  |  |
| TASK-204 | Perry has no writer for a migration event, so TASK-180 hand-wrote JSON into an append-only log | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-206 | a write returns no seq, so a poll cannot tell a stale read from a fresh one | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-207 | no compare-and-set on a write, and the board demonstrably moves between a read and a write | Coding Agent | not_started | — | — | V3 | TASK-206 | main |  |  |  |  |  |  |
| TASK-212 | a locked decision that gets no task row does not ship, and nothing links a design's plan step to the work that discharges it | Coding Agent | not_started | Two directions of ONE missing edge -- read TASK-282 before starting, it carries the measurement this row has none of. This row is design -> task (a locked design's plan step has no link to the work that discharges it). TASK-282 is task -> design (an implementation row need not cite the design, so impl_refs cannot count it). Measured 2026-09-02 on d49964e for the second direction: DESIGN-001's six implementation rows are all done and verified against artifacts, wrote "DESIGN-001" into no title, next action, evidence cell or event, and left no evidence file -- so it reported pending_handoff and always would have. Filing TASK-282 then cleared that flag by text match alone, with nothing implemented, which shows impl_refs cannot tell "implements" from "mentions". Whoever picks up either row should decide first whether one declared edge answers both directions; two separate mechanisms for one relationship is how this project acquires a second translation table. | — | V3 | — | main |  |  |  |  |  |  |
| TASK-218 | thread the closing phase id through every close stage, so no stage re-reads phase/CURRENT | Coding Agent | not_started | DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. CONSEQUENCE ACCEPTED: the closing phase id is not threaded through the close stages, so whoever closes phase 003 passes it by hand at each step and nothing checks that all four stages got the same one. | evidence/2026-08/TASK-218-spec.md | V4 | TASK-217 | main |  |  |  |  |  |  |
| TASK-220 | the close-phase router subcommand, over the four unchanged lane subcommands | Coding Agent | not_started | DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. CONSEQUENCE ACCEPTED: there is no close-phase router subcommand, so closing phase 003 means running the four existing commands separately, in an order nobody has written down (which is TASK-217, deferred with it). | evidence/2026-08/TASK-220-spec.md | V4 | TASK-217, TASK-218 | main |  |  |  |  |  |  |
| TASK-221 | a phase close that stopped halfway is visible at the next snapshot, resolved from state | Coding Agent | not_started | Rewrite the five-state recovery table around a typed phase-close record/event sequence. Keep artifact existence and CURRENT pointer checks deterministic; remove phase Markdown parsing from the acceptance path and preserve the no-new-dossier decision. | evidence/2026-08/TASK-221-spec.md | V3 | TASK-217 | main |  |  |  |  |  |  |
| TASK-219 | retro-cites-phase-scores — a cross_file check that the retro cites the scores rather than re-deriving them | Coding Agent | not_started | Replace the proposed cross_file prose check with a structured phase-score/retro provenance contract. Verify that changing a typed KR status changes the generated retro input and that no Python path parses retro.md or phase prose to establish agreement. | evidence/2026-08/TASK-219-spec.md | V3 | — | main |  |  |  |  |  |  |
| TASK-231 | a measured KR number has no way into the register that does not break one of its two rules | Coding Agent | not_started | DEFERRED TO PHASE 004 by user decision 2026-09-08, at the phase 003 mid-phase review. Stale 11d, opened phase day 1, never moved. THIS ROW IS FINDING 2 OF THE MID-PHASE REVIEW, FILED ON DAY 1: a measured KR number has no way into the register that does not re-date everything else. The workaround already taken: the 2026-09-08 re-measurement of P003-O1-KR2 and P003-O2-KR1 was written to evidence/2026-09/2026-09-08-kr-remeasurement.md instead of the register, and score-phase cites that file. Same claim-surface blocker as TASK-155 and TASK-280. | evidence/2026-08/TASK-231-spec.md | V3 | TASK-155 | main |  |  |  |  |  |  |
| TASK-236 | OKR.md drops its KR tables; perry-goals renders them — and reports whether a CLI render is a good enough read surface | Coding Agent | not_started | V4 ROUND 2 FAILED 2026-09-12: evidence/2026-09/TASK-236-round2-v4-review.md. SECOND FAIL, so review.md s6 forbids a round 3 and the next step is an ask. MY FIX CLOSED FOUR OF FIVE AND MISCOUNTED THE FIFTH. The residual at bin/perry-goals:2979 scopes with `k.get("version") in wanted`, and for a record whose version is ABSENT or BLANK that is `None in wanted`, always False -- so it is placed by no objective and counted by no residual. PMO REPRODUCED IT: drop the version key from one kr and the render gives 37 of 38 at exit 0 while perry-okr diff reports identical=true AND every_line_and_cell_came_from_the_store=true and perry-lint reports 0 errors. Round 1's charged defect, same function, on the one input its five-variant sweep omitted, and none of my eight tests supplies an absent version. The reviewer also corrected my count: a blank id ALONE drops nothing, that case refused because it ALSO orphaned objective_id, so five claimed inputs were four. | evidence/2026-09/TASK-236-round2-v4-review.md, evidence/2026-09/TASK-236-round2-result.md, evidence/2026-09/TASK-236-round1-v4-review.md | V4 | TASK-235, TASK-181, TASK-182 | main |  |  |  |  |  |  |
| TASK-237 | BOARD.md stops existing; the board is what a command prints | Coding Agent | not_started | BLOCKED AGAIN, AND THE SPEC'S STOP CONDITION IS WHAT CAUGHT IT. evidence/2026-09/TASK-237-spec.md said do not start until TASK-236's verdict is in; the verdict arrived 2026-09-12 and it is a FAIL. bin/perry-goals:2943-2945, new code from TASK-236, drops a kr record that matches no objective and prints the survivor count as fact -- five user-producible corruptions render 37 of 38 at exit 0 with perry-okr diff, verify and perry-lint all silent. THAT DEFECT FALSIFIES CLAIM 5 OF THE READ-SURFACE REPORT, which is the deliverable DESIGN-013's risk table gates this row on. So the gate this row inherited is not merely unverified now, part of it is known false. Wait for TASK-236 round 2 and then re-read s3 of its result rather than the version that exists today. Everything else in the spec stands: the 142,306-byte 99%-table measurement, the Bound that gives no retirement number, and the warning that perry-task list prints four of fifteen columns. | evidence/2026-09/TASK-237-spec.md | V4 | TASK-235, TASK-236 | main |  |  |  |  |  |  |
| TASK-240 | an ADR id can be reissued, because perry-decide writes no events and has nothing to retire one with | Coding Agent | not_started | IMPLEMENTS ADR-014, NOT ADR-013 -- re-pointed 2026-09-02 and the acceptance moved with it. ADR-013 is superseded. Its measurements all stand (only the highest id reissues; a middle gap is never filled -- delete ADR-002 of five and the next mint is ADR-006; perry-decide has no delete command for option (b) to refuse in). What changed is WHERE the high-water mark lives. ADR-013 put it in .perry/events.jsonl on the reasoning that a counter beside the other stores was "foreclosed by TASK-235's own guard, tests/test_decide_writer.py:461 ADR_ONLY". That guard governs decisions/ ONLY -- a store at the state root, beside the six already there, is not an object it refuses. A local constraint was read as a global one and it excluded the right answer. The chosen home was also a file DESIGN-004 section 5.3 and perry-task --help both declare DERIVED AND DISPOSABLE, and this project has already paid for putting something load-bearing in that log once: ADR-006 exists because that disposability sentence "was false for a release, and is true again". BUILD: mint_id takes max(ADR files on disk, ceiling recorded in a store). perry-decide may still append an event as a trace, but nothing reads it to decide the next id. THE ACCEPTANCE TEST THAT CHANGED, and it is the one ADR-013 could not have passed: delete .perry/events.jsonl and reissue must NOT return. Unchanged from ADR-013: do not add a delete/refusal path (perry-decide has no delete; an ADR leaves decisions/ by a human rm), and the pin test_a_deleted_adr_number_is_reissued_and_that_disagrees_with_purge is INVERTED, not deleted. Read decisions/ADR-014-high-water-mark-lives-in-a-store.md first; ADR-013 remains readable for the measurements. | — | V4 | USER-909 | main |  |  |  |  |  |  |
| TASK-253 | bin/perry-tasks accepts --dry-run and writes anyway | Coding Agent | review | V3 round; the mutation table and the negative control are in the evidence | — | V3 | — | main |  |  |  |  |  |  |
| TASK-254 | bin/perry-lint hands back 22 commands and every one of them drops the root | Coding Agent | not_started | Premise partly decayed -- verify before starting. The row cites bin/perry-conform:360 as "the wall standard it violates"; bin/perry-conform was DELETED 2026-08-31 by TASK-261, so the standard it points at no longer exists and must either be re-derived from a surviving tool or restated in the row. The defect itself is not disproved: bin/perry-lint mentions perry- commands 90 times as of 2026-09-02 and the question is how many of those are handed back to a reader without the --root they were resolved under. Re-census by call site rather than by grep count -- a mention in a docstring is not a handed-back command, and this row's sibling TASK-255 was corrected TWICE for exactly that over-count. NOTE: bin/perry-lint is being edited concurrently by TASK-247's agent on branch coding/task-247-config-predicate; re-derive line numbers after that lands. | — | V4 | — | main |  |  |  |  |  |  |
| TASK-255 | Perry never shell-quotes a path into a command it hands a reader — shlex appears nowhere in bin/ or viewer/ | Coding Agent | not_started | Premise VERIFIED, population NOT -- 2026-09-02 on d49964e. Verified: shlex appears nowhere in bin/ or viewer/, so the row's headline claim stands as written. Not verified, and the row's own history says why to distrust it: the census was taken over FOURTEEN tools, two of which -- bin/perry-conform and bin/perry-migrate -- were deleted 2026-08-31 by TASK-261. The summary already records the population being corrected twice before that deletion (63/232 down to 42/208 handed-back commands over the other twelve), so the live number is smaller again and nobody has counted it. Re-derive the census by call site, name the instrument, and state the count before proposing a fix; a fix sized against a stale population is how this row got corrected twice already. Sibling of TASK-254, which cites the same deleted perry-conform standard. | — | V4 | TASK-234 | main |  |  |  |  |  |  |
| TASK-264 | goals has no KR writer, so adding a KR means hand-editing okr.jsonl — build the writer | Coding Agent | not_started | NOW BLOCKS TASK-155, measured 2026-09-03. TASK-155's round proved by exhaustion that a per-KR assertion date cannot be written by anything that exists: no per-KR field in phase/<NNN>-linkage.md, ZERO of 1880 events across 23 kinds carries current/target/asserted/metric, and no store holds phase KRs (okr.jsonl is OKR.md's register, not this). perry-goals:1141 says it outright -- 'Nothing here writes either field' -- and krs refuses --write, so the live values were hand-written, which is USER-912's recorded crossing. AND THE SHAPE OF THE WRITER IS NOW CONSTRAINED BY A SECOND FINDING: TASK-155 found that a genuinely re-measured KR does NOT re-date, so the assertion date must be written BY WHATEVER ASSERTS current, not by the register's writer -- no link write is involved in that direction at all. Sequencing TASK-155 established: this row, then the schema field (behind the TASK-119 section 5 gate, a user decision), then TASK-155's reader and contract change, which is then small and mechanical. | — | V4 | — | main |  |  |  |  |  |  |
| TASK-266 | Nothing tells an agent a projection is behind its store — ADR-012's rule has no surface | Coding Agent | not_started | ADR-012, decided 2026-09-01. This is the whole implementation of 'the agent detects it': a per-pair mtime comparison replacing ~3,100 lines of drift census (ADR-011 Tier B). Perry's own history says an instruction with no surface behind it does not run — goals/reference/setup.md's ten-field checklist is the standing example. | — | V4 |  | main |  |  |  |  |  |  |
| TASK-268 | viewer/parsers.py:3899-3900 builds top_risks from BOARD.md while perry/risks.jsonl exists, reached from perry-state:1631 — the task and OKR readers beside it already prefer their stores (TASK-095 V4 round 1, finding 4) | Coding Agent | not_started | — | — |  |  | intake | triaged |  | 2026-08-29 |  |  |  |
| TASK-270 | perry-config write --from-file writes a zero-record store at exit 0 on a config.md with no settings, and every perry-task/perry-goals write is then refused forever while verify/diff/lint all report zero drift — the same command is both the cause and the only offered recovery (TASK-095 round 3, finding 2) | Coding Agent | not_started | — | — |  |  | intake | triaged |  | 2026-08-29 |  |  |  |
| TASK-271 | tracks_source is on two published payloads (perry-state project.config, perry-diagnose work_modes) with four possible values and no entry in schema/ or reference/ — raised by two consecutive TASK-095 reviewers | Coding Agent | not_started | — | — |  |  | intake | triaged |  | 2026-08-29 |  |  |  |
| TASK-275 | after intake-sweep leaves the canonical intake store empty, perry-lint reports perry/intake.jsonl as NS-01 foreign state even though the intake contract says an empty file is still a store; recognize Perry's declared empty intake store without excusing a foreign same-name JSONL | Coding Agent | not_started | TASK-276's V4 ROUND ENUMERATED THIS CATEGORY ON 2026-09-07 AND HANDED THIS ROW BOTH ITS SIZE AND ITS FIX (finding F2). THE SIZE: all eight declared .jsonl claims were run through looks_like_perry_record, and intake.jsonl is the ONLY one that returns False -- events, tasks, okr, risks, asks and .perry/config all return True. So this row is not one of several; it is the last one. It was already False at 45923e7^1, which is what let that round rule the class pre-existing rather than FAIL TASK-276 for joining it. THE FIX POINTER, and it is concrete: TASK-277 shipped _matches_a_declared_store, which repaired exactly this defect for linkage.jsonl by reading stores.declared -- and intake.jsonl is still False at 1032e76 because its shape is the one not declared there. The remaining work is to declare intake's record shape in stores.declared, not to write a new recognizer. Verify the way that round did: enumerate all eight claims and show eight Trues, rather than checking intake alone and declaring victory. | — |  | — | intake | triaged |  | 2026-09-02 |  |  |  |
| TASK-265 | Thin perry-state to a query over the six JSONL stores; the payload shape does not change | Coding Agent | not_started | Inventory every perry-state dependency on viewer/parsers.py. Replace state-bearing reads with JSONL/typed records, retain only deterministic existence/count/time computations, and return raw document handles where the caller needs agent interpretation. | — | V4 | — | main |  |  |  |  |  |  |
| TASK-286 | the Bound that review.md requires is never asked for where specs are written, so its absence costs rounds instead of minutes | Coding Agent | not_started | MEASURED COST, not a style preference. review.md section 1 requires bounded criteria and perry-lint --reviews reports criteria-unbounded, but add-task -- the only place a spec is ever written -- never mentions ## Bound. So specs are authored without it and the omission surfaces rounds later. TASK-095 burned five rounds, TASK-050 eleven, and the census of 2026-09-02 found criteria-unbounded 8 times across 12 review files. review.md states the mechanism in its own words: an unbounded criterion does not fail a round, it fails to END one. The PMO hit this the same day: TASK-247's spec had no Bound and one had to be written before its V4 could be dispatched at all. | — |  |  | main |  |  |  |  |  |  |
| TASK-289 | perry-task's --actor is optional and defaults, so a write to a lane-owned file can have no owner any session can claim | Coding Agent | not_started | THE ACTIONABLE CORE IS THE OPTIONAL ACTOR, not the incident. What happened: five link-edge events at 2026-09-02T12:08:15+08:00 wrote TASK-276..279,281 to P003-O3-KR2 in phase/003-linkage.md, actor 'agent', and the session that filed this could not account for them. Only perry-goals link emits link-edge (bin/perry-goals:2069). Ruled out by measurement, so do not re-derive these: perry-task add --kr does NOT write the register (probe on a scratchpad copy, linkage md5 byte-identical); scratchpad --root did not leak (0 probe-actor events in the real log, 1 in the copy); and the PMO's own link loop ran at 12:16:32 and found all five ALREADY linked. THE EDGES ARE CORRECT and match what was intended, so this is an accountability gap and not corruption. But perry-task --help already states the open question this row is really about: '--actor ... is accepted by every subcommand and DEFAULTED rather than required -- DESIGN-004 section 8 asks whether it should be mandatory, and until that is answered an unset actor is recorded as unset rather than guessed.' An unset actor is exactly why this incident cannot be closed by reading the log. Deliverable is that answer plus whatever it implies: either --actor becomes required on writing subcommands, or the event records enough about its invocation that a write can be traced without it. Do not spend the round hunting the original five events; they are the evidence, not the work. | — |  | — | intake | triaged |  | 2026-09-02 |  |  |  |
| TASK-291 | close-task warns after the fact on a V4 whose verdict block cannot be parsed, so 10 of 27 V4 closures carry a rung nothing confirms | Coding Agent | not_started | Define the minimal typed V4 verdict contract (task id, reviewer identity, criteria ref, verdict, checked/not-checked, timestamp) and make close-task validate it. Do not add or strengthen regex parsing of review documents; keep historical repairs out of scope. | — |  | — | intake | triaged |  | 2026-09-02 |  |  |  |
| TASK-294 | every NS-01 warning Perry emits is about a file Perry itself wrote, and the remedy each one offers is a no-op | Coding Agent | not_started | REPRODUCED LIVE 2026-09-07 BY WRITING ONE FILE THE DOCUMENTED WAY. The PMO wrote a knowledge card at knowledge/verification/, which is exactly where work/SKILL.md says knowledge digests live, and perry-lint went from "knowledge/ holds 3 file(s) Perry did not write" to 4 in the same run. So the count is not a legacy of some import: it grows by one every time somebody uses the mechanism as documented. THE SECOND HALF, AND IT IS THE SHARPER ONE: knowledge/INDEX.md still reads "Active: 0 · Eternal: 0 · Stale: 0 · Archived: 0" and "(no digests yet)" while four cards sit beside it. The index calls itself "Auto-maintained by the work lane, updated on every digest, archive operation, and full rebuild during end-phase-retro" -- and nothing maintains it. That is WHY the NS-01 fires: the files are real, the index that would claim them is empty, so Perry reads its own knowledge directory as somebody else's folder. WHY THE OFFERED REMEDY IS A NO-OP HERE, concretely: the warning tells the user to run /perry relocate perry, and the state root ALREADY IS perry -- the files are at perry/knowledge/verification/. Relocating cannot move a file out of a directory it is already correctly inside. The same is true of the phase/ warning, which names seven snapshots score-phase itself writes. SO THE ROW'S CLAIM IS NOW MEASURED RATHER THAN ASSERTED: on this project every NS-01 names files Perry wrote or told a user to write -- 7 phase snapshots, 4 knowledge cards -- and the remedy is inapplicable to all eleven. TASK-222 is the phase/ half of the same defect. The fix probably has two parts, an index that is actually maintained and a claim check that consults it, and this row should say which part it owns before it is dispatched. | — |  | — | main |  |  |  |  |  |  |
| TASK-297 | parsers.py:462 says the walk is skipped when handed an exact root, and the code walks four levels anyway | Coding Agent | not_started | CLOSING THIS COSTS MORE THAN THE DOCSTRING GAP SUGGESTS, AND A CORRECT FIX WILL LOOK LIKE A REGRESSION UNLESS IT LANDS WITH ITS TRIPWIRE. TASK-292's V4 reviewer applied the minimal defect-2 fix that TASK-292's own commit message proposes -- honour an explicit project_root exactly, walk only when it is None, at viewer/parsers.py:3298-3311 -- cleared __pycache__, crossed the second boundary, and ran the module: FAILED (failures=1), test_and_the_harness_above_really_does_bite, "Lists differ: ['DESIGN-001'] != []". That test is TASK-292's ANTI-VACUITY DEVICE: it asserts pending == [] and refs['DESIGN-001'] == NOISE, i.e. it asserts the cross-project read STILL HAPPENS, so that the round's other assertions cannot pass vacuously. It is legitimate and documented and the reviewer explicitly did not charge it. But it means WHOEVER TAKES THIS ROW MUST RETIRE OR INVERT THAT TEST IN THE SAME COMMIT. TASK-292's commit says the minimal form "was measured green on the existing suite" -- true of the suite as it stood BEFORE that commit, and false of the suite it ships, because the commit installs the tripwire that fires on it. ONE THING THE REVIEWER DID NOT ESTABLISH AND THIS ROW SHOULD FIRST: it ran only test_parsers.py under that patch, not the full 108 modules, so whether the minimal defect-2 form is green suite-wide is UNVERIFIED. Establish that before anything else. Still worth doing for the reason first filed: parsers.py:462 says the walk is skipped when a caller hands over the exact root, perry-state --root <fixture> DOES pass the fixture as project_root, and the walk left it regardless -- so closing the prose/code gap fixes the contamination with no fixture change at all, which is the other direction from the one TASK-292 took. | — |  | — | main |  |  |  |  |  |  |
| TASK-301 | schema/README.md still tells a consumer to read the State root out of .perry/config.md, which is the render | Coding Agent | not_started | MEASURED 2026-09-02, and it makes the deliverable sharper: the page is not merely stale, it instructs the consumer to do a job the tool already does better. perry-state resolves the state root ITSELF and publishes it, including when there is no store. Built a project carrying only .perry/config.md with "State root: docs" and no config.jsonl: perry-state --section project returns state_root 'docs' with settings_source 'absent'. So a consumer that can run the tool NEVER needs to parse the markdown, and one that reads the store alone would get it wrong on a store-less project -- the tool's fallback is the thing that makes both cases answer the same. tests/fixtures/sample-project reports state_root '' only because its own config.md declares no such line, not because the fallback is missing. THEREFORE the page should say: read state_root off the payload; .perry/config.jsonl is the register and .perry/config.md is its projection; the tool falls back to the projection when the register is absent, so a consumer never has to. aiMark declined to implement the markdown fallback for exactly this reason and asked whether any consumer must resolve the root itself. The answer measured here is no -- only a consumer that cannot run the tools at all, and Perry has none. | — |  | — | main |  |  |  |  |  |  |
| TASK-309 | a dispatched agent's entire round lives in its context until one final commit, so any interruption costs all of it | PMO Agent | not_started | SECOND OBSERVATION 2026-09-03, and the first one where the cost is measured. TASK-155's round died at the 600s stream watchdog with its branch still at the commit it was cut from -- zero commits, whole round lost, agent still establishing a baseline when it stalled. Yesterday a batch of five died the same way when the laptop closed. THE MITIGATION THAT DID NOT WORK IS WORTH RECORDING: the dispatch brief already instructed 'commit your evidence file before any long run', and that is too late by construction when the long run IS the baseline suite. Whatever this row builds has to make the first commit happen before the agent does anything expensive, not before its LAST expensive thing. | — |  | — | main |  |  |  |  |  |  |
| TASK-334 | The header-rule guard errors instead of skipping when a file vanishes mid-walk | Coding Agent | not_started | CLOSED AND VERIFIED, kept here for the record: three sites now route through header_rule.source_of, which skips a file that vanished between the walk and the read and RAISES when git still tracks it. A SECOND, UNRELATED PARALLEL RACE has since been found by TASK-283's V4 reviewer and filed as TASK-341 -- tests/test_one_choke_point.py:532 writes a real bin/lib/rowprobe.py into the live tree while tests/test_one_primitive.py:150 asserts bin/lib holds exactly one exempt file. I confirmed both lines. Same family as this row and as TASK-326: a test observing a tree that something else is changing under it. Three instances now, in three different mechanisms -- a vanishing file, a decaying clock, and a probe written into the live tree. | — |  | TASK-258 | main |  |  |  |  |  |  |
| TASK-335 | Two contract-parity tests fail because the witness cannot make a collection observable | Coding Agent | not_started | MECHANISM FOUND, and it is not what I guessed. TASK-256's round-2 agent diagnosed it independently while measuring its own baseline, and I confirmed the gate: bin/perry-task:6303 fires when 'idle >= in_progress_limit', where in_progress_limit = threshold('in_progress_idle_hours', 4) at :6278 and idle_hours is computed against REAL TIME. So conformance.in_progress_with_no_live_run is EMPTY while an in_progress row is fresh and becomes NON-EMPTY once the event log ages past four hours -- and the two parity tests assert that the collection is empty in the blind run so that the witness fixture is what makes its keys observable. THE TEST THEREFORE BREAKS WITH THE PASSAGE OF TIME ALONE, on an unchanged tree. That is why it reproduced for me at 5601e45 in a detached worktree: that checkout's board also carries in_progress rows whose events are frozen at their commit and keep aging. MY EARLIER NOTE ON THIS ROW OFFERED TWO CANDIDATE CAUSES -- fixture defect or premise defect -- AND BOTH WERE WRONG. It is neither: the fixture is fine and the premise was true when written. The defect is that an anti-vacuity control was written against a value that decays. The agent's proof is better than mine: it re-ran the identical stub tree as a real repo at the same moment and got the same two failures with zero of its edits present. WHAT THE FIX MUST DECIDE: whether the blind run should be measured against a board frozen in time (a fixture, or a pinned clock) rather than against Perry's own live state. Any fix that just widens the assertion re-creates the vacuity TASK-132 built this class of test to remove. Perry currently has two in_progress rows, TASK-256 and TASK-263, and closing them will make the symptom disappear WITHOUT fixing anything -- do not read a green suite after that as the row being done. | — |  | TASK-132 | main |  |  |  |  |  |  |
| TASK-349 | ADR-007 census part 2 — the 71,158 lines of tests/ | Coding Agent | not_started | — | — |  | TASK-348 | main |  |  |  |  |  |  |
| TASK-352 | Records appended to okr.jsonl are invisible to the render gate that was said to cover them | Coding Agent | not_started | — | — |  | TASK-182 | main |  |  |  |  |  |  |
| TASK-355 | Every locked design now reports zero implementation references | Coding Agent | not_started | PARTIALLY DISCHARGED 2026-09-07, AND THIS ROW'S OWN HEADLINE IS NOW STALE: "every locked design reports zero implementation references" is false for six of them. WHAT CHANGED: perry-task design-link existed and NOT ONE ROW USED IT -- 0 of 145 carried a link -- which is the whole mechanical cause of the number this row reports. The PMO declared the link for 20 rows whose TITLE ALREADY NAMES ITS DESIGN, so this was transcription of an existing declaration rather than attribution anybody guessed: DESIGN-006 got 1, DESIGN-009 got 3, DESIGN-010 got 4, DESIGN-011 got 5, DESIGN-015 got 7 including the two already closed, since design-link is store-only and survives done, which is the point -- pending hand-off is a question about FINISHED work. Pending hand-off went 15 to 10. WHAT REMAINS AND WHY IT IS NOT THE SAME KIND OF WORK: TEN locked designs still report zero, and for those the rows do not name their design in the title, so linking them is a JUDGEMENT and not a transcription. Perry's own rule forbids guessing an attribution, so those need either the design document to name its implementation rows or a person to declare each one. Note DESIGN-016 already reported 9 references without any design-link record, so impl_refs has a second source this row should identify before it prescribes a fix -- otherwise the fix addresses one of two mechanisms. THE UNDERLYING DEFECT THIS ROW SHOULD NOW BE ABOUT: nothing at add time asks which design a row implements, and TASK-367 records that perry-task add ACCEPTS --design AND DROPS IT. So the field can only ever be filled by a later, separate, easily-skipped command, which is exactly why it sat at zero across 145 rows. TASK-282 is the sibling: a locked design's implementation rows need not cite it, so a finished design reports as never handed off forever. | — |  | TASK-139 | main |  |  |  |  |  |  |
| TASK-369 | DESIGN-015 stores a field that is derived at read time | Coding Agent | not_started | — | — |  | TASK-276 | main |  |  |  |  |  |  |
| TASK-371 | Nothing couples dispatching an agent to marking the row it works on | Coding Agent | not_started | EVIDENCE FROM BOTH DIRECTIONS, AND THE SECOND INSTANCE HAS BEEN RETRACTED -- READ THIS BEFORE USING IT. FIRST DIRECTION, 2026-09-04, STANDS UNCHANGED: six agents dispatched and only four marked, so the board reported 3 running against 6 live and the user caught it; then a rate limit killed all six and the board went on claiming five rows were running FOR THREE DAYS while nothing was. That is the gap in both directions and it is this row's argument. SECOND DIRECTION, RETRACTED 2026-09-07: this row previously said TASK-278's agent wrote to perry/tasks.jsonl in the primary checkout against its brief, and that its RESULT block calling that file untouched was false. BOTH CLAIMS ARE WRONG AND THE AGENT IS OWED THE CORRECTION. The write was made by a SECOND CONCURRENT PMO SESSION, not by the agent. Proof: the 11:53:41 status event carries that session's own reason string verbatim, and git diff --stat 1032e76 coding/task-278-six-read-sites -- perry/tasks.jsonl is EMPTY, so the branch never touched the file. The agent's RESULT block was accurate. THE MISREADING WAS REASONABLE AND THAT IS THE POINT: every event is stamped actor: agent whether a PMO session or a dispatched agent wrote it, so a session seeing a modified tasks.jsonl in the shared checkout had no field that could have told it otherwise. That defect is now TASK-378. WHAT THIS ROW LOSES AND WHAT IT GAINS: it loses 'the gap produces pressure on agents to reach outside their worktree', which was never observed and should not be argued from. It keeps the 09-04 evidence, which is enough on its own. Do not re-import the retracted instance from commit df269b5 or from an earlier reading of this cell. | — |  | — | main |  |  |  |  |  |  |
| TASK-372 | perry-restore-check --root cannot verify a git-archive scratch copy — the exact use review-constraints.md recommends | Coding Agent | not_started | FOUND BY TASK-276's V4 ROUND, 2026-09-07, as finding F9 -- and found by hitting it, not by reading. The reviewer followed review-constraints.md, ran the recommended call against its scratch copy, got '<ref> is not a commit in <copy>', and fell back to byte comparison against git show. THAT FALLBACK IS THE POINT: the round still verified its restores, but it verified them with a method it invented on the spot, which is exactly the N-implementations-of-one-rule defect this repository keeps finding in itself. Anchor the fix in review-constraints.md's own wording so the recommended call and the working call are the same call. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-375 | linkage.jsonl has no uniqueness or mutual-exclusion invariant — a task that is both linked and declared unlinked passes every gate | Coding Agent | not_started | FOUND BY TASK-277's V4 ROUND, 2026-09-07, finding F1, by ENUMERATING the category rather than reporting the instance: there are five ways a register can contradict itself about a row, and the round tabulated all five. Two are already caught -- a duplicate kr id is refused by a two-reader cross-check, and one task under two KRs raises linkage-task-single-kr. THE OTHER THREE PASS EVERY GATE IN THE SYSTEM. NOT A DEFECT OF ROW B, AND THE ROUND CHECKED RATHER THAN ASSUMED: it fed the same contradictory register to perry-lint at the merge base 0724fa6 and got 0 error(s) there too, so the gap is inherited. The importer is CORRECT to mirror it -- linkage_account is genuinely multiset-safe, using _multiset_minus rather than a set difference, so it faithfully reproduces whatever multiplicity the register states, which is its job. The real register is clean today. THE URGENCY IS TASK-281: once P003-O3-KR2 is computed from this store, a contradictory row silently produces a KR number with no defined meaning. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-378 | Every tool-mediated write is stamped actor: agent, so two concurrent PMO sessions cannot tell their writes apart — and one has already misattributed the other's to a dispatched agent | Coding Agent | not_started | PROVEN, NOT SUSPECTED, AND THE PROOF IS THREE FACTS. (1) The status event at 2026-09-07T11:53:41 moving TASK-278 in_progress to review carries a reason string written verbatim by the OTHER PMO session, not by any agent. (2) coding/task-278-six-read-sites never touched perry/tasks.jsonl -- "git diff --stat 1032e76 coding/task-278-six-read-sites -- perry/tasks.jsonl" is empty. (3) Every event today is stamped actor: agent regardless of origin -- 11:51:40, 11:51:52, 11:52:41 and 11:53:41 from one session, 11:54:57 and 11:56:01 from the other, all identical. THE INFERENCE THAT WENT WRONG WAS A REASONABLE ONE, which is what makes this a tooling defect and not a carelessness one: a session saw perry/tasks.jsonl modified in the shared primary checkout with an agent-stamped event beside it, and the only actor value available said agent. THE COST IS ALREADY BANKED: a false charge that a dispatched agent broke its brief and filed a dishonest RESULT block was recorded on TASK-371, on TASK-278 and in commit df269b5, and TASK-371's argument was being built on it. All retracted 2026-09-07. The field also cannot separate PMO from Coding Agent at all -- "PMO Agent" (209), "PMO" (64) and "Coding Agent" (5) exist as values but 1601 records say plain "agent", so the distinction lives in the vocabulary and not in what the writer stamps. PERRY OWN DIAGNOSTIC ALREADY RANKED THE COLLISION SURFACE AND THE INCIDENT LANDED ON ITS TOP THREE. perry-diagnose reports the many-sessions-rewrite-this finding and names them in order: perry/BOARD.md at 514 commits/90d, .perry/events.jsonl at 502, perry/tasks.jsonl at 373 -- precisely the three files the two sessions wrote today. Its stated remedy, splitting a file that holds current status into a small live file plus an append-only dated history, is a DIFFERENT fix from this row and may be the better one. It also reports the twelve-checkouts-nobody-reviews-the-combined-result finding, whose argument is exactly what happened: each piece was correct and the combined record was wrong. NEITHER IS NEW; both sat in the diagnosis BEFORE the incident, which is worth more than the incident, because the warning existed and nothing routed it to a row. | — | V3 | — | main |  |  |  |  |  |  |
| TASK-380 | main cannot have a green suite through code alone — the one red module asserts Perry's user has answered their questions | User + Agent | not_started | MEASURED ON MAIN 2026-09-07 at a9176e5: 117 modules, 3346 tests, 1 module red, and this is it. Re-run ALONE and reproduced, so it is not the parallel runner. THE ASSERTION IS assertNotIn('LOAD-03', ids(perry_root)) at tests/test_diagnose.py:561, docstring "The skill that reports this must not commit it." THE FINDING IT TRIPS ON IS TRUE AND IS NOT A CODE DEFECT: perry-diagnose reports "6 open questions are waiting on you" and names all six with file and line -- USER-917 and USER-918 at BOARD.md:205-206, plus FOUR open questions in design/DESIGN-016-the-bin-contract.md:216-219, which is a DRAFT design and the only unlocked one of sixteen. Perry is reporting its own user's real decision backlog, accurately. THE PROBLEM IS WHO CAN CLEAR IT. No code change makes this green. It goes green when the user answers six questions, four of which are open questions on an unlocked design, so main's baseline CANNOT be green through engineering alone and every agent that measures a baseline inherits a red it must reason about. THREE SEPARATE AGENTS PAID FOR THAT TODAY: TASK-276's V4, TASK-277's V4 and this session each independently re-ran the module alone, confirmed it pre-existing and wrote it up. That is the cost being paid over and over for a fact nobody has written down. THE DECISION THIS ROW EXISTS TO SURFACE, and it is the user's, not an agent's: a dogfooding assertion whose truth depends on the user's inbox is a test whose result is not a property of the code. Either (A) the assertion excludes the finding classes that measure the user rather than the tree -- LOAD-03 and anything else load-shaped -- and keeps asserting the structural ones, which is the recommendation because it restores a green baseline and keeps the dogfooding value; or (B) it stays as-is and the red is declared a permanent known-red with a named waiver the suite prints, so agents stop re-diagnosing it; or (C) the backlog is cleared and the red returns the moment a seventh question is asked, which is not a fix. Do not silently delete the assertion: it is the mechanism by which Perry is held to its own diagnosis, and DESIGN-002's whole argument is that a skill reporting a problem must not commit it. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-384 | summary is mandatory to write, reported by the linter, published by one payload and not the other, and declared in no schema | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-387 | A mitigation that lives in one writer's body was not inherited by the next writer that read the same field | Coding Agent | not_started | FOUND 2026-09-07 WHILE RECORDING TASK-155's REPRODUCTION, and it is that row's own prediction coming true one step worse. TASK-155's summary says its defect is disclosed at three layers and fixed at none, and warns that the cheapest wrong outcome is a FOURTH DISCLOSURE. What shipped instead is a fourth writer with NO disclosure. MEASURED, both sides on the same tree: bin/perry-goals:2226 builds a redated list of every KR whose assertion date the write is about to move, returns it as current_assertions_redated, and PRINTS a warning naming them -- with a comment stating why: naming it on every write is what keeps this from being discovered as a silent number months later, since the real fix is a new schema field behind the safety gate. bin/perry-tasks:1612, the linkage-write --from-register path built today by TASK-277, reads THE IDENTICAL field for declared_at, validates its FORMAT and refuses a day-only value -- and never reports what it re-dates. It rewrote declared_at on 115 records in one run with no warning at all. SO THE DEFECT WAS INHERITED AND THE DISCLOSURE WAS NOT. That is the failure mode this row is about: a mitigation that lives in one writer's body rather than in something every writer must pass through. A fifth writer will inherit the same way. WHAT TO DECIDE, and it is not just "add the warning to linkage-write": the disclosure exists because the fix is gated, so it is a standing obligation on every writer that reads updated: as a date. Either the obligation is enforced -- a shared helper that no writer can bypass, or a check that fails a writer reading that field without reporting -- or it is documentation and the next writer will skip it too. Note perry-goals' own comment says naming it is what buys time until the gated fix lands, so this row is about keeping that purchase, not about the fix itself. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-390 | A design's Linked OKR line is parsed into a field nothing reads, so a design can name a KR that does not exist and every instrument stays green | Coding Agent | not_started | FOUND AND MEASURED BY ADR-017's DOCUMENT ROUND, 2026-09-08, and it is the reason that half of the migration cannot land first. viewer/parsers.py:3397 parses the design header's Linked OKR line into a dataclass field, sets it at :3409, and NOTHING READS IT -- verified independently: no consumer anywhere in bin/, and not one reference in the whole of tests/. THE ROUND PROVED THE CONSEQUENCE RATHER THAN ARGUING IT: it renamed DESIGN-011's Linked OKR to a grammar nothing resolves and re-measured, and perry-lint stayed at 0 errors, the perry-diagnose finding set was byte-identical, no LOAD-02 appeared, and user_load.dangling -- the list test_diagnose asserts empty -- was [] BEFORE AND AFTER. So a design can point its Linked OKR at an id that does not exist and every instrument Perry has stays green. WHY THIS IS MORE THAN A DEAD FIELD: DESIGN documents declare which KR they serve in exactly this line, and that declaration is the thing a reader uses to answer "what goal is this design for". It is also adjacent to TASK-355 and TASK-282, which are about a locked design's implementation rows not citing it -- so the design-to-goal edge is unchecked in BOTH directions, and impl_refs is the only one of the two that any tool computes. THE DECISION IT FORCES IS ALREADY NAMED IN ADR-017 and is recorded as a Changes entry on that ADR: its Consequences say the seven locked design headers get a Changes entry rather than a header edit, "unless the lane decides the header field is METADATA rather than BODY". A field with no consumer is metadata by the plainest reading of that test, so the decide lane settled it that way and the header edits ride with the atomic data rename rather than becoming seven Changes entries in locked documents. WHAT THIS ROW IS FOR, and it is not the rename: either Linked OKR gets a consumer -- resolved against the OKR store, reported when it dangles, the way impl_refs is computed -- or it is deleted from the header template as decoration. Do not fix it by adding a lint rule alone: DESIGN-013's rule is that a fact with a schema lives in one store, and a design-to-KR edge is exactly that shape, which is what linkage.jsonl already does for task-to-KR. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-391 | A documented flag combination writes a linkage record that permanently reddens the suite, and the writer has no retraction | Coding Agent | not_started | FOUND BY TASK-279's V4 AS ITS NEAREST MISS, FILED RATHER THAN FAILED, AND REPRODUCED BY THE PMO BEFORE MERGING. --actor is a DOCUMENTED flag, at perry-task add --help line 150. Run add --kr <KR> --actor goals in a scratch copy of main and the writer appends {"kind":"edge","task":...,"kr":...,"actor":"goals","via":"add"} and tests/test_linkage_import.py goes from 1 failure to 2, the new one being test_no_live_record_the_goals_lane_declared_was_stamped_via_add. THERE IS NO SUPPORTED REMEDY: the linkage writer is append-only and has no retraction, so the record cannot be withdrawn by any command Perry ships. A user following the help text can therefore redden the suite permanently in one invocation. ROOT CAUSE IS ONE ASYMMETRY: bin/perry-task:2797 takes the lane from the CALLER, while the importer hardcodes LINKAGE_IMPORT_ACTOR. So the same field is caller-supplied on one path and a constant on the other, and the test that TASK-279 round 2 scoped to actor == goals reads it as if only the importer could ever write it. WHY IT WAS NOT A FAIL, and the argument is defensible: the spec never mentions actor, DESIGN-015 section 7 records that NOTHING ENFORCES THE LANE TABLE YET, the record itself is TRUE -- the goals lane really did declare it -- and it is counted correctly by the KR. The module is also already permanently red on any add --kr under TASK-383, so this adds a second failure to a module that is red either way. AND THE JUSTIFICATION FOR THE SCOPE IT EXPLOITS IS FACTUALLY WRONG. Round 2 scoped the assertion to actor == goals on the reasoning that such a record would be "a genuine lane lie". The V4 measured --actor agent against --actor goals and THE KR COUNTER SCORES THEM IDENTICALLY AT 100 PERCENT, with no disagreement anywhere. So the scope is pinned by mutation but its stated reason does not hold, which is why this row exists rather than a note on TASK-279. WHAT TO DECIDE: either the lane on a linkage record is not caller-supplied -- the writer stamps the lane it IS, the way the importer does -- or the assertion stops keying on a field a caller controls. Do not fix it by refusing --actor with --kr alone; that leaves the same field caller-supplied everywhere else it is stamped. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-393 | The shipped OKR template still mints the retired KR grammar, so a new project starts in the form ADR-017 just removed | Coding Agent | not_started | FOUND BY ADR-017 STEP 2 AND CONFIRMED BY THE PMO ON THE MERGED TREE. goals/state/OKR_TEMPLATE.md carries SIX occurrences of the retired KR-On.m grammar, and it is the template a new project's OKR.md is built from. So the migration ADR-017 just completed would be undone one project at a time: every fresh Perry project is minted in the grammar this repository stopped using. WHY IT IS NOT PART OF THE RENAME COMMIT, and this is a real distinction rather than a scoping excuse: step 2's job was the DATA of this project -- OKR.md, its projection, the phase registers and the live references in the corpus. The template is not this project's data, it is SHIPPED PRODUCT: goals/SKILL.md lists it as what OKR.md is created from, and ADR-018 part C found that test_shipped_vocabulary already checks templates copied verbatim into a user's repo, which is why it classed that module as behaviour rather than convention. SO THE FIX HAS A TEST WAITING FOR IT. Whoever takes this should check whether test_shipped_vocabulary's template assertions already pin the id shape -- if they do, the template and the assertion move together; if they do not, that is the second half of the row, because a shipped template with no pinned id grammar is how this drifts back a third time. WIDER THAN THE ONE FILE, AND THE ROUND SAID SO: 421 old-form occurrences remain repo-wide after step 2, in evidence, journal, handoff, snapshots, the event log, goals/ and code comments. MOST OF THOSE ARE HISTORY AND MUST NOT BE TOUCHED -- an evidence document records what was measured under the old grammar and a journal entry records what was written. The template is different in kind because it is not a record of the past, it is an instruction for the future. Do not let this row grow into a sweep of the other 420. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-396 | perry-task has no published write contract, so a newly required flag reaches a consumer as a refusal at runtime | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-397 | ADR-017 changed an identifier's value space and perry-goals/list stayed at 2.3, so a consumer joining on a stored KR id gets a silent miss | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-401 | every bin/ entrypoint is recompiled on every invocation because an extensionless __main__ is never bytecode-cached | Coding Agent | not_started | — | — | V4 | TASK-399 | main |  |  |  |  |  |  |
| TASK-426 | perry-restore-check's deferred import fails in exactly the deployment its docstring says it exists for | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-434 | The User Input Queue has no sweep, so 27 answered asks cannot leave the board | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-435 | perry-task add --track is accepted and dropped on a project-mode track | Coding Agent | not_started | decide whether a project-mode row carries a track cell or --track is refused there, then make one of the two true and show a mutation go red | — | V4 |  | main |  |  |  |  |  |  |
| TASK-436 | perry-diagnose reads the append-only journal as a live reference, so one mistyped id dirties a project forever | Coding Agent | in_progress | PART 1 DONE, PART 2 NOT. The harvest path is closed and merged at 9c30782a: any markdown table row's first cell was a definition point, so TASK-436's own spec defined USER-920 and silenced the finding. dangling_in_reports had the same root and went 6 to 80. WHAT IS LEFT is the round's own withdrawal: it enumerated nine append shapes after the PMO showed a case the fix misses, FOUR still move the verdict and this fix closes ONE. The uncovered one is worse -- document_reports_on_a_check is FILE-scoped, so one journal line naming perry-diagnose or any test_ exempts a live citation 52 lines away, and that line need not mention the id at all. The round recommends the file-scoped mark and the table-paragraph mark be ONE row, because fixing the latter alone masks this path without closing it. ALSO NEW AND MEASURED BY THE PMO AFTER THE MERGE: the gate now reports V4-1, V4-2 and V4-3, which are a 2026-08 review report's own numbering of its verification items written in a table's first column. The check's definition domain includes a report's internal item numbers. The row cannot close while the verdict still moves with unrelated content. | evidence/2026-09/TASK-436-result.md, evidence/2026-09/TASK-436-spec.md | V4 | — | main |  |  |  |  |  |  |
| TASK-438 | a row can close at verification V0, which the store accepts, perry-lint passes, and the writer refuses from a caller | Coding Agent | not_started | decide where a rung is validated -- at the config writer, at the close, or in the linter -- and make one place true rather than three partly true | — | V4 |  | main |  |  |  |  |  |  |
| TASK-439 | perry-task add warns instead of refusing, so the add-time linkage gate the phase's Definition of Done asks for was never built | Coding Agent | review | DELIVERED AND MERGED 2026-09-12 at c7618a96; awaiting its V4, rung V4 per the spec. VERIFIED ON THE LIVE REPO BEFORE COMMITTING, all four states: neither flag rc=1 refused, --unlinked rc=0, --kr <ID> rc=0, --kr '' rc=1 with the rewritten message. It fixed the self-contradicting advice in the same change, and argued the blank refusal's keep better than the spec did: both refuse so exit codes show one rule, the difference is the sentence, and telling a caller whose $KR was empty to 'pass one of these two' makes them plausibly write a PERMANENT un-withdrawable unlinked declaration for a row that does serve a KR. A project with NO register is not refused, because there the question has no answer and refusing would force the guess okr-linkage.md forbids. Nine callers changed; intake and route CHECKED not assumed. REPORTED NOT FIXED: route opens a main-track row with no add event and no kr key, the remaining way to open a row the KR never counts. | evidence/2026-09/TASK-439-result.md, evidence/2026-09/TASK-439-spec.md | V4 | — | main |  |  |  |  |  |  |

## P2

| ID | Title | Owner | Status | Next action | Evidence | Verification | Depends on | Track | Stage | Arrived | Stage since | Parent | Commitment | Role |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TASK-066 | Split perry-task by subcommand group | Coding Agent | not_started | NOW WAITS ON TASK-263, added 2026-09-03, and the reason is this row's own next action: it says re-size the split after the markdown reader, row renderer and cell escaping are gone, and TASK-263 is what MEASURES the category split by call site. Splitting before that measurement is the two-splits outcome this row already argues against. MEASURED 2026-09-03 on c506ca5: bin/perry-task is 7851 lines / 412,790 chars, about 103k tokens if read whole -- but it is already grep-navigable, 135 top-level functions and 26 cmd_*, largest single function 527 lines, so the three agents that changed it today all worked by targeted reads and none read it whole. The saving from splitting is real and second-order; the larger saving available today costs no code at all, which is the PMO naming the function and line in the brief. TASK-067 landed half the precondition: the row renderer and cell escaping moved to viewer/tables.py, and only 36 lines here still reference the table primitives. TIMING, and it is the stronger argument: splitting while review rounds are in flight would make every branch unmergeable and every line number in every spec and criteria file stale, on a board that cites them heavily. Sequence: reviews clear, then TASK-263 measures, then this row splits to that measurement. | evidence/2026-08/TASK-066-context.md | V4 | USER-923 | main |  |  |  |  |  |  |
| TASK-112 | the sign-off drafting guard cannot describe itself, so a true statement about it is refused | Coding Agent | not_started | Remove check_no_user_claim and its language-specific regex contract from the task scope. Specify the typed measured/restated inputs and the agent-side drafting/refusal protocol; Python may validate enum membership and selection provenance, not sentence meaning. | — | V3 | TASK-263 | main |  |  |  |  |  |  |
| TASK-137 | a new queue row is born in the second stage, not the first | Coding Agent | not_started | — | — | V2 |  | main |  |  |  |  |  |  |
| TASK-172 | four of six document collections are unreachable through any contract | Coding Agent | not_started | Re-scope the four missing collection contracts as locator/transport APIs only. For each collection, declare typed metadata and raw-content fields, then verify that no parser extracts status, decisions, meaning or quality from the body. | — | V4 | — | main |  |  |  |  |  |  |
| TASK-198 | ## Cadence becomes a store | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-222 | score-phase's own snapshots trip NS-01, because the names it writes do not match the declared pattern | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-224 | linkage-kr-exists fires only on an absent id, so a KR nested under the wrong objective lints clean | Coding Agent | not_started | — | — | V3 |  | main |  |  |  |  |  |  |
| TASK-238 | no commit on main may fail to build standalone, and nothing checks it | Coding Agent | not_started | Startable. The live test case is on main right now: git worktree add --detach <path> 0d68034 then run perry-task on a project carrying .perry/config.jsonl — it dies at :6773. Use that as the fixture rather than constructing one, and note it will STOP being reproducible once USER-908 part (b) runs, so the test must not depend on that sha surviving. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-242 | linkage-kr-exists proves SOME phase has resolvable overall edges, not that THIS phase does | Coding Agent | not_started | Blocked until TASK-157 lands, since it is that row's guard being strengthened. Start from evidence/2026-08/TASK-157-v4-review.md, which carries the measurement: at f15d234 all eight linked: values in 001-linkage.md were verbatim the retro Measured column, and 16 of 24 cells survived; after the fix, 24 of 24. The threshold that now protects that is checked >= 8, and 001 and 003 supply all eight between them. | — | V4 | TASK-157 | main |  |  |  |  |  |  |
| TASK-252 | a register write honours board rows it was never asked about, and the durable 'somebody has seen this' surface does not exist | Coding Agent | not_started | Blocked until TASK-243 lands. Start from evidence/2026-08/TASK-243-result.md sections 7.1 and 7.2 and from its V4 review, which accepted both the rejection and the close and then said each deserves a row. Note the reviewer's framing of the second: the literal property would require lint to report a disagreement that no longer exists, so the question is not how to keep drift high but where a loss lives after the write that named it. | — | V4 | TASK-243 | main |  |  |  |  |  |  |
| TASK-262 | The render must distinguish what is projected from what is canonical — TASK-199's scope on the surface ADR-010 creates | Coding Agent | not_started | Blocked in practice on TASK-236 / TASK-237, which create the render this property has to hold on. Context: USER-907 answered (a) on 2026-08-29 — restate the KR, re-scope the row. TASK-199 carried this scope and was dropped in error on 2026-09-01; the drop could not be reversed, so this row replaces it. handoff/2026-08-29-goals-lane-after-design-013.md § 1. | — | V3 | TASK-236, TASK-237 | main |  |  |  |  |  |  |
| TASK-282 | a locked design's implementation rows need not cite it, so a finished design reports as never handed off forever | Coding Agent | not_started | Startable, and WIDER than filed -- the predicted trap fired and was measured. Sequence, 2026-09-02 on d49964e: before this row existed, DESIGN-001 had impl_refs 0 and sat in pending_handoff; filing THIS row took it to impl_refs 1 and emptied pending_handoff entirely. Nothing was implemented. The only change is that this row's Next action contains the string "DESIGN-001", and impl_refs (viewer/parsers.py:3276-3311) is a literal text match over id + title + next_action + evidence plus every .perry/events.jsonl line. So a row whose next action merely says "read DESIGN-001" is counted as implementing it, and a row that IS its implementation but never names it counts for nothing -- which is exactly how TASK-001..006 left this design at zero. The defect is therefore two-sided: false negatives (the six real rows) and false positives (this one), and a check that only adds enforcement on the writing side fixes half. Read DESIGN-001 section 9's 2026-09-02 entry for the original measurement. Do NOT treat the now-empty pending_handoff as the problem being gone. | — |  | — | main |  |  |  |  |  |  |
| TASK-287 | nothing warns against writing a cross-reference to a row that has not been minted yet, and the id it guesses may land on the row doing the guessing | Coding Agent | not_started | Observed 2026-09-02 while creating DESIGN-015's six implementation rows. The PMO wrote 'unlike TASK-280' into row E's Next action, predicting that F would take 280; E itself was minted as TASK-280 and the sentence became self-referential. Repaired with perry-task next, repointing it at TASK-279. SKILL.md already forbids minting an example id that resolves to nothing, and this is NOT that case -- the id does resolve, to the wrong row -- which is why the existing rule did not catch it and a reader following the docs would repeat it. The whole class exists because add mints the id and the author is writing prose about ids that do not exist yet. | — |  |  | main |  |  |  |  |  |  |
| TASK-312 | test_tree_guard.py is now the suite's longest module at 48-124s, and its own fix is what put it there | Coding Agent | not_started | OBSERVED BY TASK-244 ROUND 2 ACROSS FOUR FULL SUITE RUNS. The floor moves rather than disappears: TASK-244 took the header-rule harness from first place, and this module is what is now at the top, at 48-124s across runs. IT IS NOT A REGRESSION AND SHOULD NOT BE READ AS ONE -- TASK-258 just PASSED V4 for making this module build its fixture from a git archive snapshot and carry its own concurrent-write loop, and both of those cost real time and both are load-bearing (that reviewer showed the mutation red on a still tree, so the concurrency is not decoration). So the question this row asks is not whether the cost is justified but whether it is the right size. The 48-124s spread is itself the first finding: this module's cost varies 2.6x with machine load, measured across runs at loads from 8.6 to 60.6. | — |  |  | main |  |  |  |  |  |  |
| TASK-340 | Three documents in the tree describe a codebase that no longer exists | Coding Agent | not_started | FIVE STALE FIGURES NOW, not three. The first three came from TASK-263's census: DESIGN-014 section 5.1's line counts are wrong for all three files it sizes (7,522/4,483/4,603 against today's 7,851/5,144/5,011); bin/perry-lint's load_glossary docstring justifies its shape by bin/perry-conform, which USER-910 deleted and which no longer exists -- three citations survive at :349, :730 and :2293; and bin/perry-task's module docstring at :43 says 'markdown is canonical' two paragraphs below its own ADR-007 note saying the opposite. I reproduced the last two directly. TWO MORE came from TASK-067's round-6 V4 reviewer, who marked them as carrying no verdict weight: TASK-067-result.md's Verification section still says '13 new tests' where the module now has 19, and it cites the branch's 3125/3125 at 267abb1 rather than main's current 3291. THE PATTERN IS THE ROW'S REAL SUBJECT: every one of these is a NUMBER OR A NAME written into prose at the moment it was true, in a file nothing re-derives. Perry has a rule for exactly this -- 'never compute a number by reading files and eyeballing it' -- and these are its mirror image: numbers computed correctly once and then frozen into a document. The round should say, in one line, whether the answer is to re-derive them, to date-stamp them, or to stop putting them in prose, because a fix that just corrects five figures leaves the sixth to be written tomorrow. | — |  | TASK-263 | main |  |  |  |  |  |  |
| TASK-350 | ADR-007 census part 3 — setup and the hook readers | Coding Agent | not_started | THE ONE QUESTION THIS ROW EXISTS TO SETTLE: TASK-339 KEPT escalation_union, hook_escalation_lines and escalation_fragments when it deleted the scanner, arguing they extract a DECLARED list of backticked spans under a named heading -- the typed side of ADR-007 rather than the prose side -- and that three surviving consumers need it, including the only check that can tell a user their high-stakes list is dead prose. Its V4 reviewer flagged that ADR-007 decision 3, 'the Python layer never parses a document at all', is being cited on BOTH SIDES of that keep. This census either confirms the argument or contradicts it, by call site. TASK-339 also reported a cost that belongs in this row's scope: perry-lint check_verification still matches a task TITLE against that same list and cannot tell a mention from an action -- the same defect class one surface over, left unfixed and outside TASK-339's Bound. SIZE: setup is 638 lines of bash; the hook readers are bin/perry-task, bin/perry-lint, bin/perry-state, bin/perry_md_store.py and viewer/parsers.py. Where a hook reader is inside TASK-348's scope, defer to that classification rather than restating it -- say which sites you deferred. | — |  | TASK-339 | main |  |  |  |  |  |  |
| TASK-374 | One malformed line in linkage.jsonl voids validation of every good record in the file | Coding Agent | not_started | FOUND BY TASK-276's V4 ROUND, 2026-09-07, as findings F5, F4 and F6, all three driven with hand-built user-producible input rather than read off the source. NONE WAS A FAIL AND THE REASON IS WORTH KEEPING: every one errs toward 'unchecked, not clean', which is the phase-003 operating rule, so the store never claims a comparison it did not perform. That makes this a robustness row and not a correctness one. F5 is the one with real consequences -- the store is about to become the authority for P003-O3-KR2 via TASK-281, and at that point a single stray line silently zeroes the evidence base for a KR rather than reporting one bad record. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-377 | perry-state's design.by_status key order depends on PYTHONHASHSEED | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-386 | A pipe-written tasks value is a hard lint error and, separately, resolves to nothing in a reader that never consults the linter | Coding Agent | not_started | SEVERITY CORRECTED 2026-09-08, AND THE CORRECTION IS TO WHAT THE PMO WROTE ON THIS ROW. This row said a pipe-written tasks value declares an edge that is dropped SILENTLY, with no KR counting it, no attribution seeing it, and NO CHECK REPORTING IT MISSING. THE LAST CLAUSE IS FALSE. Found by TASK-278 round 3's V4 and reproduced by the PMO: planting `tasks: \|` with the id on the following line draws a HARD LINT ERROR -- "perry/phase/002-linkage.md [bad-type] objectives[2].krs[1].tasks must be a list" -- so the register is refused outright rather than accepted with a lost edge. WHAT IS STILL TRUE AND IS WHY THE ROW SURVIVES: the READER still drops the edge. kr_for_task matches the id exactly and the block scalar yields the id plus a trailing newline, so the two halves disagree about the same input -- the linter calls the register malformed while the reader silently returns one fewer edge for it. A register in that state is refused by one tool and half-honoured by another, which is a smaller defect than a silent loss but is still two answers to one question. SO THE ROW IS NO LONGER ABOUT DATA LOSS. It is about a shape that fails a type check and, independently, resolves to nothing in a reader that never consults that check. Whoever takes it should decide whether the reader should refuse in the same terms the linter does, or whether being refused at lint time is sufficient and the reader's behaviour is unreachable in practice -- and MEASURE which, rather than assume, because the linter is not on every path that loads a register. DOWNGRADED FROM P1 TO P2 ON THAT BASIS. The original P1 rested on the silence, and the silence is not there. | — | V3 | — | main |  |  |  |  |  |  |
| TASK-395 | perry-okr diff reports an id drift that render --write cannot repair, because render matches rows by the id that drifted | Coding Agent | not_started | Found 2026-09-08 while removing a dead no-op in test_md_store.py. perry-okr diff REPORTS an id drift and perry-okr render --write CANNOT REPAIR IT: render matches a row to its store record by id, so a hand-edited id matches nothing and the line is passed through verbatim. Measured by writing the mutation the dead line would have been -- see evidence/2026-09/TASK-395-note.md. | — | V3 |  | main |  |  |  |  |  |  |
| TASK-398 | an advisory a program must act on is a payload field, not a stderr line, and add still writes one under --json | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-403 | four test modules use a regex to judge meaning, which ADR-007 decision 3 already ruled the Python layer never does | Coding Agent | not_started | — | — | V5 |  | main |  |  |  |  |  |  |
| TASK-413 | bound.open_total and bound.closed_total are published to dashboards and asserted by nothing | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-416 | a hand-measured zero and a template zero nobody touched are the same payload, and asserted_at empty is the whole signal | Coding Agent | not_started | — | — |  | TASK-415 | main |  |  |  |  |  |  |
| TASK-422 | a crash between the board write and the event append removes a row from both halves of P003-O3-KR2, and no desync detector names it | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-423 | three read-only tools accept an undeclared flag and exit 0; perry-goals krs refuses a positional at exit 1 not 2 | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-424 | no test compares a --compact or --json value against the store files or the schema, so a wrong build() agrees with itself | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-427 | an unknown flag does not name the legal set, while the exemplar goal 3 cites does | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-428 | three read-only subcommands accept --dry-run and drop it, and perry-config show cannot tell an unreadable config from an absent one | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-432 | --compact drops missing_defaults from project.tracks[], the one field that separates 'no honest default' from 'this mode has no such control' | Coding Agent | not_started | — | — |  |  | main |  |  |  |  |  |  |
| TASK-440 | Next action has four writers and two guards, and the comment beside one guard says there are two writers | Coding Agent | not_started | SPEC WRITTEN, NOT STARTED: evidence/2026-09/TASK-440-spec.md. Opened with --unlinked at add time, so it answers the P003-O3-KR2 question in its own creating action while serving no phase 003 KR. THE FIX MUST NOT BE 'add validate= to cmd_status and cmd_start': that is a third and fourth enforcement point for one rule, which is TASK-040's four-implementations shape, TASK-431's three-lists shape and TASK-382's four-publishers shape. The rule belongs where the FIELD is written so a fifth writer inherits it; where exactly is this row's judgement and must be argued. THE DELIVERABLE THAT MATTERS IS THE FIFTH-WRITER TEST, derived from SURFACE rather than from a hand-written list of four, and it must be shown able to fail by adding a throwaway subcommand in a scratch copy. Two false comments to correct while there: bin/perry-task:3637 says 'the other site that writes this cell' and :4913 says 'status is the only other writer' -- both were wrong when written. | evidence/2026-09/TASK-440-spec.md | V3 | — | main |  |  |  |  |  |  |

## Cadence (recurring; doesn't consume P0 slots)

| ID | Recurring task | Owner | Frequency | Next due | Last evidence |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## User Input Queue

| USER-id | Needed from user | Blocks | Idle | Status | Asked |
|---|---|---|---|---|---|
| USER-001 | Staleness threshold N | TASK-005 | — | **answered 2026-08-16: 30 days** |  |
| USER-002 | `--claims` vs `--strict` | — | — | **answered 2026-08-16: exempt** |  |
| USER-003 | Please confirm whether Perry may make tasks.jsonl the authoritative Task record, with BOARD.md becoming a generated view whose direct edits are reported instead of accepted. | TASK-038 |  | answered 2026-08-20: Already decided in ADR-007 decision 2 on 2026-08-19 (Deciders: Ran Jiao): BOARD.md becomes rendered output and a hand edit becomes drift. This row was minted the same day and duplicates that decision; recorded here so the queue matches the record. TASK-038 is unblocked, and still needs its V5 signature, which is a different act from this permission. | 2026-08-19 |
| USER-004 | When migration encounters a file the user has chmod-ed read-only, should it refuse to touch that file, or migrate it and name the overridden permission in the plan? Today it migrates silently: write_atomic renames over the target, and a rename needs write permission on the directory, not on the file. | TASK-079 |  | answered 2026-08-20: Migrate and name the override; do not refuse. Reasoning recorded because the row was minted for it: refusing would block a whole migration for a reason unrelated to shape, and migration is the one road ADR-004 gives an undeclared project — a refusal there is the wall with no door that this project rejects everywhere else. The override is also reversible: the restore point carries the file's original bytes, verified under TASK-079. The 'at least name it' half of the ADR-004 posture is already satisfied by what shipped in PR #6, and TASK-115 added the guard that keeps that wording an observation rather than advice. The read-only bit stays a signal Perry reports and does not act on. | 2026-08-20 |
| USER-015 | hand perry/evidence/2026-08/TASK-114-delegation-prompt.md to an aiMark coding agent and paste its result back | TASK-114 |  | answered 2026-08-21: aiMark agent ran the v2 prompt and returned 2026-08-21. CONTRACT_TESTED is {task 1.14, goals 2.1, decide 1.0}; suite 672 pass / 0 fail verified here. Four findings came back, all four check out — see evidence/2026-08/TASK-114-result.md | 2026-08-21 |
| USER-016 | declare risks.jsonl in schema/state-schema.json § claims — {"path": "risks.jsonl", "kind": "file", "owner": "work", "anchor": "state"} — so perry-tasks risks-write --from-board can be enabled | TASK-040 |  | answered 2026-08-21: declared 2026-08-21: claims[] now carries okr.jsonl (goals/state), risks.jsonl (work/state) and .perry/config.jsonl (perry/project). The declaration alone does not enable risks-write — cmd_risks_write was never built; the refusal now reads the claim and names the gap that is actually open | 2026-08-21 |
| USER-903 | Should .perry/config.md become a rendered projection of .perry/config.jsonl? Running 'perry-config write --from-file' costs one command and moves P002-O1-KR2 from 1 of 2 to 2 of 2. The cost: a hand edit to your own config file becomes reported drift at warn. SKILL.md promises this file is 'a tier-1 file the user owns and edits directly' — OKR.md was never promised that, which is why the OKR half was uncontroversial. TASK-092 shipped the capability and deliberately left the store uncreated so the promise is not broken until you choose. See evidence/2026-08/2026-08-28-a-kr-with-no-open-task.md | — |  | answered 2026-08-28: 决定 2026-08-28：变。跑 perry-config write --from-file，.perry/config.md 成为 .perry/config.jsonl 的渲染投影，手改被报成 drift（warn）。这是对 SKILL.md「这是你手写的一等文件」承诺的有意修改，由用户做出。P002-O1-KR2 因此可以从 1/2 走到 2/2。迁移命令由用户执行，不由 Perry 代跑。 | 2026-08-28 |
| USER-904 | TASK-050 has now failed SEVEN V4 rounds and needs a decision, not a round 8. Each round's fix moved the same defect rather than closing it: round 5's reviewer defeated a regex, round 6 replaced it with an AST walk, and round 7 showed the walk's gate is still an allowlist of variable names (ROW_NAMES, 11 entries). Measured: of 829 mapping constructs in the 18 readers, 59 are classified as row-cell sources and 35 of those are the bare name 'header'; FOUR LIVE header resolutions (viewer/parsers.py:1827, bin/perry-task:6029 and :6200, bin/perry-tasks:925) can be reverted to the exact historical defect with the whole 2793-test suite green, and parsers.py:1827 silently drops a KR when reverted. In the other direction the check now reports CORRECT code — 6 of 8 legitimate shapes flagged, including the exact latent risk round 5 recorded. Blind to four of the tree's own header resolutions AND loud about a keyword tokenizer: both failure modes the spec names, in one artefact. THE CHOICE. (A) Round 8, same shape — widen the source-expression recognition. The record says this is the fourth time that has moved the defect. (B) Invert the burden: flag EVERY case-folding map in a reader, and require the ~30 legitimate value normalizers to carry a one-line opt-out marker. Correct code declares itself once; anything new is caught by default. Cost: touching 30 live sites and a new convention. (C) RECOMMENDED — make it structurally impossible: one header_index() function becomes the only thing allowed to fold a header, and the guard becomes 'nothing outside it calls squash on a row', which is a one-symbol surface instead of a shape. This is the move ADR-007 already made for stores. (D) Accept the guard as advisory rather than a gate, close the row at a lower rung, and document the limitation. My recommendation is C, with B as the fallback. All four are design decisions with blast radius beyond this row, which is why this is an ask and not a dispatch. Evidence: evidence/2026-08/TASK-050-round7-v4-review.md. | TASK-050 |  | answered 2026-08-29: 决定 2026-08-29（用户拍板，Perry 推荐 C）：选 C —— 结构上不可能。一个 header_index() 成为唯一被允许折叠表头的函数，守卫从「识别一种形状」变成「它之外没有东西对行单元格调用 squash」，一个符号的检查面。这是 ADR-007 对 store 已经做过的同一个动作：不要更聪明的检测器，要更小的表面。代价接受：改动 18 个 reader 的表头解析入口。不做第 8 轮的白名单拓宽 —— 记录显示那已经是第四次把缺陷挪一步。分支 coding/task-050-header-harness (c67e5a4) 上的 AST 遍历不再是交付物；它作为迁移期间的脚手架可以保留，但完成标准是 header_index() 加上那条单符号守卫。 | 2026-08-29 |
| USER-905 | TASK-095 has now failed FIVE V4 rounds and needs a decision, not a round 6. I caused three of the five, and every one is the same shape: two situations answered as one, one step to the left of the last. Round 1 collapsed four None-returns. Round 2 collapsed 'no-track-record' into unusable and hard-blocked three of this repo's own fixtures. Round 3 collapsed the two default cases. Round 4 filtered on the NAME 'main' instead of on whether the table DECLARED it. Round 5 compares on names over records, so a record that CONTRADICTS a declared row counts as carrying it. THE DECISION, and the reviewer states it cleanly: two principles are each defensible applied once, and round 5 applies one to the synthesised main and the other to the recorded main. (A) 'A declared row the register contradicts is drift' — then a table declaring queue/4/3d beside a store recording project must WARN, and perry-lint already computes exactly that. (B) 'The store is truth and the table is a stale projection' — then the trackless case must be SILENT too, because the register answered there as well. Pick one and it applies everywhere; the current code cannot be right because it holds both. SECOND, SEPARATE DECISION — the refusal WIDTH, and it is urgent because I made it worse: I widened the write refusal from source=store-default to source=store, and the reviewer measured three ordinary hand-edit workflows now hard-blocked that wrote at 45a355d AND at round 4. On the third — derive the store from a two-track table, then hand-swap one row — 'perry-config write --from-file', the ONLY command both refusal messages name, exits 1. The block cannot be cleared by the documented remedy. Options: revert to round 4's narrower width; make it a warning rather than a refusal; or fix perry-config so the remedy works. THIRD: the perry-goals half of the guard is a tautology — deleting it leaves the full 2875-test suite at exactly the baseline, which is the same defect TestTheGoalsLaneRefusesToo's own docstring records against round 2. My recommendation: (A) for the principle, because perry-lint already owns that rule and the root cause across three rounds has been re-deriving it differently; plus revert the refusal width to round 4's until perry-config's remedy is fixed. All of this is on an UNMERGED branch, so nothing is harmed in production. Evidence: evidence/2026-08/TASK-095-round5-v4-review.md. | TASK-095 |  | answered 2026-08-29: 决定 2026-08-29（用户拍板，Perry 推荐 A + 回退）。两个决定。(1) 原则：选 A —— 一条表里声明、store 里被反驳的轨道就是 drift。处处适用：一张声明 queue/4/3d 的表配一个只记 project 的 store 必须 WARN，无论那条被反驳的轨道是 main 还是别的。理由：perry-lint 已经在算这条规则，而三轮的根因正是在写入侧反复重新推导它 —— 交给已经拥有它的那一方，不要第二份实现。第 5 轮 have 用名字集合比较必须改成按记录比较。(2) 拒绝宽度：回退到第 4 轮的窄宽度（source=store-default），立即恢复那三条被硬挡的普通手改流程。perry-config write --from-file 退出 1 的缺陷单独一行（已在 Intake），修好之前不再谈放宽。全部在未合并分支上，生产未受影响。 | 2026-08-29 |
| USER-906 | TASK-203 has now failed THREE V4 rounds, all three mine, and every one has ended with the same defect: an ordinary command silently truncates a canonical register store. I said I would escalate rather than attempt a fourth, so here it is. ROUND 3's FAIL: the gate is read at a moment the command controls. cmd_add's queue-mode branch calls ensure_section('Intake') BEFORE commit() asks the gate, so the gate sees a freshly created, readable, EMPTY table, answers yes, derives [] and writes zero bytes. Measured: a 291-byte 3-record intake.jsonl goes to 0 on 'perry-task add --track ops' with rc 0, byte-identical on 45a355d, and perry-lint reports '0 row(s) drifted'. It is round 1's blocking finding word for word — round 2 closed it for the project-mode track and never asked the queue-mode track, which is the mode ## Intake exists for. Three more doors of the same shape: intake 3->1, ask 3->1, risk-add 3->1, all rc 0, all preserved on base. THE DECISION. (A) Evaluate the gate against the board AS IT WAS AT COMMAND ENTRY, not after the command mutated it — snapshot the shape before any board write. Principled and small, but it is the fourth 'move the question' fix on this row and the first three all looked principled too. (B) RECOMMENDED — make it structurally impossible: an ordinary write may never SHRINK a canonical store. Only an explicit removal command (purge, resolve-intake, intake-sweep) may reduce the record count, and any derivation that would produce fewer records than the store holds is a refusal, not a write. That is one invariant covering every door found in three rounds — the command name, the non-unique tuple, the four section shapes, and the ensure_section ordering — instead of a fourth predicate. (C) Revert TASK-203 entirely and reconsider the row. It has introduced a store-truncation regression in all three rounds; before it, intake.jsonl did not exist and could not be wrong. That is a real 'should we do this at all' question and it deserves an answer, not an assumption. (D) Narrow the scope to the risks register only, which is the one that already existed, and defer intake/asks. NOTE THIS AFFECTS THE PHASE: TASK-203 is the ONLY row under P003-O1-KR1, and DoD Must-Have 2 names intake.jsonl and asks.jsonl explicitly, so (C) or (D) means the phase misses that Must-Have deliberately rather than by accident. Also filed from this round: my third shape test is VACUOUS (the legend table lands under ## Top risks because ensure_section anchors ## Intake before ## P0, so the foreign shape has no test on any register); the uniqueness test cannot distinguish uniqueness from adjacency; load_register_records lets a JSONDecodeError escape as an uncaught traceback where every other failure in that file is a Refused. Evidence: evidence/2026-08/TASK-203-round3-v4-review.md. | TASK-203 |  | answered 2026-08-29: 决定 2026-08-29（用户拍板，Perry 推荐 B）：选 B —— 一条不变量取代第四个谓词。普通写入永远不得缩小一个 canonical store：只有显式的移除命令（purge、resolve-intake、intake-sweep）可以减少记录数，任何会产出比 store 现有记录更少的推导都是 refusal 而不是写入。这一条覆盖三轮里找到的全部四扇门 —— 命令名、非唯一元组、四种 section 形状、ensure_section 的顺序 —— 而不是再加一个「门在什么时刻被读」的判断。不选 A：那是这一行上第四次「把问题挪一步」，前三次看上去也都有原则。不选 C/D：DoD Must-Have 2 明文点名 intake.jsonl 和 asks.jsonl，这条 Must-Have 保留，phase 003 不放弃它。同轮附带的三项一并修：第三个 shape 测试是空测（legend 落在 ## Top risks 之下，foreign 形状在任何 register 上都没有测试）；唯一性测试分不清唯一性与相邻；load_register_records 让 JSONDecodeError 以裸 traceback 逃逸，而该文件里其他每个失败都是 Refused。 | 2026-08-29 |
| USER-907 | ADR-010 deletes BOARD.md, which makes P003-O2-KR3 unmeetable mid-phase — the KR is 'BOARD.md's two truth models are marked in the file' and TASK-199 is its only row. A boundary cannot be marked in a file that is gone. This needs your decision because dropping a KR changes what phase 003's Definition of Done MEANS, and the phase is live. THREE OPTIONS. (a) RESTATE the KR as something ADR-010 can satisfy — 'the render distinguishes what is projected from what is canonical' — which is the same reader-facing property the KR was actually buying, on a surface that will exist; TASK-199 is re-scoped rather than dropped. This is my recommendation: the KR was never really about the file, it was about a reader being able to tell truth from projection, and that need survives. (b) DROP the KR and TASK-199, recording that phase 003 closes with one KR withdrawn by a decision made DURING the phase. Honest, and it makes the phase score mean what it says. (c) Keep both and mark the boundary on a file scheduled for deletion — cheapest to do, hardest to defend. NOTE ON PROCESS: TASK-199 has been left not_started and untouched on purpose. Dropping the row is the visible half of dropping the KR, and doing the visible half first would make the record say the KR FAILED rather than that it was WITHDRAWN. The goals lane owns the edit either way; this ask is the decision, not the write. Full context: handoff/2026-08-29-goals-lane-after-design-013.md | TASK-199 |  | answered 2026-08-29: 决定 2026-08-29（用户拍板，Perry 推荐 a）：选 (a) 重述这条 KR。P003-O2-KR3 改成 ADR-010 能满足的东西 —— 「渲染能区分哪些是投影、哪些是 canonical」。理由：这条 KR 买的从来不是「文件里有标记」这个实现，而是「读者能分辨真相和投影」这个读者可见的属性，而那个需求在新表面上原样成立。TASK-199 因此重定范围而不是作废，phase 003 也不需要记录一条被撤回的 KR。写 KR 是 goals lane 的权，已在 handoff/2026-08-29-goals-lane-after-design-013.md 交接；这条 ask 是决定，不是那次写入。 | 2026-08-29 |
| USER-908 | May Perry rewrite unpushed local history to repair commit 0d68034? MEASURED 2026-08-29: at that commit, every perry-task write on a project carrying .perry/config.jsonl dies with AttributeError: module 'perry_state' has no attribute 'defaulted_over_a_declaring_table' — bin/perry-task at :6773 calls a function that arrives one commit later. The TASK-213 commit also carries the bin/perry-task half of TASK-095 round 4. Its own message's suite claim is FALSE at that commit. The branch tip was whole and main is whole; only this one commit does not build. It is now in main's history via the 777d021 merge, so a git bisect across the 20 commits after it gets a false 'broken' verdict there. WHY THIS IS AN ASK: .perry/hook.md lists git history rewrites — push --force, --force-with-lease, rebase onto main, tag deletion — as high-stakes operations requiring your explicit authorization. I filed it rather than doing it, and a V4 reviewer endorsed that: 'That reasoning is correct and I endorse it — do not rewrite.' WHAT CHANGED SINCE: origin/main is at 45a355d and local main is at 91e5351, so all 27 commits including this one are UNPUSHED. Nobody has seen this history. That materially lowers the risk the hook rule is written for — the rule protects shared history, and this is not shared yet. OPTIONS. (a) LEAVE IT, and document the bad commit so a future bisect knows to skip it. Zero risk, permanent small cost, and the record keeps an honest scar. (b) REWRITE the unpushed history to move the perry-task hunk into the commit that owns it, then verify every commit in the range builds. My recommendation IF you want a clean history, because unpushed is the only moment this is cheap — after a push it becomes a shared-history rewrite and the answer should be (a) forever. (c) Leave the history and add a test that every commit on main builds standalone, so this class is caught at the next merge rather than by a person. Slower, and it does not fix this commit. My recommendation is (b) THEN (c) — repair it while it is still free, and add the guard so the next one is caught by a machine. But this is your call and I will not touch history without it. | — |  | answered 2026-08-29: 决定 2026-08-29（用户拍板，Perry 推荐 b 然后 c）：授权重写未推送的本地历史修复 0d68034，并加守卫。授权已给出。但执行顺序必须倒过来，理由是测量出来的，不是我改主意：重写 0d68034 会改掉它之后每一个 commit 的 SHA，包括 6c0d041 和 8abd30d —— 而那正是当前四个在飞分支的 merge base（coding/task-050-header-index、coding/task-203-round4、coding/task-095-round6 都在 6c0d041，coding/task-157-kr-declared-once 在 8abd30d），其中三个还在跑。现在重写会让它们的 merge base 消失，把已完成的工作推进一次不必要的 rebase，而这些行正是 phase 003 的 Must-Have。所以：(c) 现在做 —— 一行守卫，让下一次这类问题在合并时被机器抓住；(b) 在四个分支落地之后立刻做，那时 origin/main 仍然在 45a355d，未推送这个便宜窗口还开着。如果在那之前发生了 push，(b) 作废，答案永远变成 (a)：那条 hook 规则保护的是共享历史，一旦共享就不该动。 | 2026-08-29 |
| USER-909 | perry-decide REISSUES a retired ADR id, and perry-task does not — two tools, one contract, opposite answers. Measured by the TASK-235 agent 2026-08-29: delete ADR-011's file and the next mint hands out 011 again. perry-task purge retires an id through the append-only event log so it is never reissued; perry-decide writes NO events at all, so it has nothing to retire an id with. Worse, before TASK-235 the behaviour was NON-DETERMINISTIC: on main an unrelated write re-rendered the index and the next mint reissued anyway. THE STAKE: an ADR id is an address. ADR-007 is cited by name in ADR-010, in DESIGN-013, in three task rows and in this project's own commit messages. If 011 can name two different decisions, every one of those citations becomes ambiguous the moment an ADR is deleted and a new one minted. This did not matter while nobody deleted ADRs; TASK-235 is the row that made deletion ordinary. THE OPTIONS. (a) RECOMMENDED — perry-decide writes events like every other writer, and an id is retired the way perry-task retires one. This is the answer that makes the two tools agree rather than documenting why they differ, and DESIGN-004's whole posture is that every write goes through a tool and leaves a trace. Cost: perry-decide gains an event surface it does not have today. (b) Refuse the delete instead: an ADR file may not be removed, only superseded — which is arguably what a decision record SHOULD mean, since ADR-005 and ADR-007 are cited as history and history is not deleted. Cheaper, and it may be more correct in principle. (c) Accept reissue, and document that an ADR id is a slot rather than an address. Honest but it silently breaks every existing citation. (d) Leave it declared as it is now — the TASK-235 agent pinned the disagreement with a named test rather than resolving it silently, so nothing is hidden and the row can close. My recommendation is (b) THEN (a): stop the deletion that creates the problem, then give perry-decide the event surface so the two tools stop disagreeing on principle rather than by accident. But (b) changes what a decision record IS, which is yours and not mine. | — |  | answered 2026-09-02: (a), scoped to a high-water mark. perry-decide appends a mint event to .perry/events.jsonl and mint_id takes max(files union events), so an id once issued is never reissued -- the same rule as perry-task's minting_records. Three measurements on d49964e reshaped the choice and belong to the implementing row. (1) Only the HIGHEST id reissues; a middle gap is never filled -- delete ADR-002 of five and the next mint is ADR-006 -- so what is needed is a durable high-water mark, not an event surface on every command. (2) Option (b) "refuse the delete" was rejected because perry-decide has no delete command to refuse: bootstrap/new/supersede/status/list only, and an ADR leaves decisions/ by a human rm, so (b) could only ever be documentation plus a lint detector, never enforcement. (3) The cheap counter-file fix is foreclosed by TASK-235's own guard, tests/test_decide_writer.py:461 ADR_ONLY = ^decisions/ADR-\d+-[^/]+\.md$, which permits nothing but ADR bodies in decisions/. The remaining durable home is .perry/events.jsonl, whose schema owner is "perry" rather than a lane, so decide writing there does not breach the hand-off contract. Current exposure: ADR-012 is the top id (8 citations); ADR-007 (321) and ADR-004 (237) are reachable only after every id above them is deleted first. | 2026-08-29 |
| USER-910 | perry-migrate cannot survive Tier A — its output IS the deleted ledger (C.declare, 14 sites). A: delete migration too (0 records ever carried route:migrate; TASK-097 never started) — recommended. B: restore ~200 ledger lines for migrate alone, keep the write-path gate deleted, make TASK-097 the next phase. Full form: evidence/2026-08/2026-08-31-TASK-261-migration-fork.md | TASK-261 |  | answered 2026-08-31: A — delete migration too. perry-migrate, perry_schema.py and test_migrate.py are out; TASK-097 drops with them. | 2026-08-31 |
| USER-911 | Should the adoption reader be fenced at all, now that it has no caller? P003-O2-KR2's metric asks for 'the adoption reader fenced into one named module, with a mechanical guard shown able to go red'. Its own re-baseline of 2026-09-01 then removed the ground under it: bin/perry-migrate was DELETED 2026-08-31 by TASK-261, TASK-097 was dropped with it, and /perry adopt is a user-facing command whose implementation went too. So viewer/parsers.py's 4,603 lines now serve ONE role, and the KR text says in its own words that 'whether an unreachable reader is still worth fencing is the open question TASK-099 carries into the pivot rather than an assumption this KR may keep making'. The phase file lists this under User Commitments as 'a product decision rather than a review'. THE OPTIONS. (a) FENCE IT ANYWAY -- a boundary that exists before the caller returns is cheaper than one retrofitted after, and /perry adopt is a documented command someone will re-implement. Cost: a guard around code nothing calls, which is also a guard nothing exercises, and an unexercised guard is the class of check this project keeps finding rotten. (b) DO NOT FENCE; delete the adoption half instead, on the ground that ADR-011 already condemns viewer/parsers.py as the representation layer and a reader with no caller is dead code by definition. Cost: /perry adopt cannot be re-implemented without restoring it, and the deletion is not this phase's declared scope. (c) FENCE ONLY WHAT THE STORE READERS TOUCH -- draw the boundary from the live side rather than the dead side, so the guard is exercised by code that actually runs. My recommendation, because it is the only one of the three whose guard can be shown able to go red by a test of live behaviour. (d) DEFER the whole KR to phase 004 and score P003-O2-KR2 as withdrawn, since its premise was deleted mid-phase. NOTE ON SEQUENCING: the SWEEP half of TASK-099 -- censusing bin/, viewer/ and tests/ for document handling ADR-007 made dead -- does NOT depend on this answer and is being dispatched separately, so the census will be on the table when you answer | TASK-099 |  | answered 2026-09-02: WITHDRAW P003-O2-KR2, with the reason recorded. Decided 2026-09-02 after the ask's own four options were measured and three of them collapsed. THE PREMISE WAS WRONG, and this is the finding rather than the decision: the KR asks for "the adoption reader fenced into ONE NAMED MODULE", and that module does not exist and never did. Measured on 185c208: viewer/parsers.py is 4,606 lines and EIGHT tools import it -- perry-task, perry-lint, perry-goals, perry-decide, perry-diagnose, perry-knowledge, perry_store.py, perry_md_store.py. It carries no adopt- or migrate-specific top-level definition at all; its mentions of adoption are READ SEMANTICS ("there is legitimately no store, this is the adoption path", "a project with NO store has not been adopted, its markdown IS its state"), not a separable half. The re-baseline of 2026-09-01 was right that the file serves ONE role -- but that role is Perry's shared reader, not adoption. Two further corrections to what the ask asserted. (1) /perry adopt is NOT unimplemented: reference/adoption.md is 26,944 bytes, SKILL.md:27 routes it, and first-run step 4 sends an existing project to it. What was deleted was perry-migrate, and adoption.md already says so in the past tense at line 441. (2) /perry adopt does not go through parsers.py at all -- the agent reads README, CHANGELOG, docs/ and git history directly, so the reader's callers are the eight tools above and not adoption. THEREFORE: (a) "fence it anyway" has nothing to fence; (b) "delete the adoption half" would delete the shared read layer eight binaries stand on; (c) "fence what the store readers touch" describes a real boundary -- tolerant parsing versus strict store reading, 25 tolerance sites inside one file -- but eight callers straddle it and the work is far larger than the KR ever contemplated, on phase day 6. The KR describes a structure that does not exist, so it is a KR that cannot be met rather than work that was not done. Phase 003 scores on 6 KRs, not 7, and the record must show WITHDRAWN and not FAILED. THE WRITE IS THE GOALS LANE'S: phase/003-linkage.md and phase/003-storage-code.md are goals-owned, perry-goals krs is READ-ONLY and is the only surface for KRs, so there is no tool path -- that gap is TASK-264. TASK-099 is freed by this and needs re-scoping: its fence half is withdrawn with the KR, and whether its sweep half still earns a row is now an open question rather than a blocked one. | 2026-09-02 |
| USER-912 | Recording the P003-O2-KR2 withdrawal is blocked by two walls, and both need your call because both are deliberate design constraints rather than bugs. THE DECISION IS ALREADY MADE (USER-911, answered 2026-09-02, withdraw the KR because its metric describes a module that does not exist). What is blocked is WRITING IT DOWN. WALL 1 -- the phase document is FULL. phase/003-storage-code.md is 300 lines against a cap of 300 declared cap_kind: hard in schema/state-schema.json, and tier-1 hard caps are one of only three hard blocks in Perry. goals/reference/pivots.md step 3 requires a '## Changes' line in that file with the old text left as strikethrough; there is no room for one. The tier-1 rule's own answer is to force overflow into a sibling appendix, leaving the main file a section index plus one-paragraph summaries -- but that is an editorial restructuring of a live phase document, mid-phase, and it is yours to authorise rather than mine to perform. WALL 2 -- the register has no write path and forbids hand edits. The phase KRs live in phase/<NNN>-linkage.md; perry-goals krs is READ-ONLY and calls itself 'the only surface for them'; goals/reference/phases.md:212 says 'nothing in this file is edited by hand once it exists'. The precedent cuts both ways: P003-O3-KR1 was withdrawn 2026-08-31 and its record is simply gone from the register today, so it was either a hand edit against that rule or an unwritten path. Recorded on TASK-264, which is the missing KR writer. THE OPTIONS. (a) COMPACT the phase document first -- move superseded strikethrough passages and older Changes entries into evidence/2026-09/phase-003-appendix.md, which the pre-pivot state already preserves in two snapshots taken today, then write the pivot entry normally. My recommendation: it obeys the cap rule as written and the history is already safe. (b) WRITE THE PIVOT ENTRY ANYWAY and let the document go to 301+, accepting a hard-cap breach that perry-lint will report at every snapshot. Honest and visible, but it makes the one cap the project treats as unbreakable breakable by convenience. (c) RECORD THE WITHDRAWAL ONLY IN THE REGISTER and skip the phase document entry, accepting that the phase's own narrative will not say why it scores on 6 KRs. Cheapest and worst -- the strikethrough plus reason is the whole point of pivots.md's friction. (d) DEFER the write until the phase closes and let score-phase record the withdrawal, leaving the KR live-but-known-dead in the meantime. NOTE: whichever you pick, step 4 of pivots.md hands off to the decide lane for an ADR recording the pivot rationale, and that half is unblocked | TASK-099 |  | answered 2026-09-02: (a) COMPACT FIRST — done 2026-09-02, with one correction to the option as I wrote it. The ask proposed moving overflow to evidence/2026-09/phase-003-appendix.md. That was WRONG and was not done: `evidence/` is owned by `work` in schema/state-schema.json § claims[], so goals writing there would breach the hand-off contract. The archive that was used instead already existed and is goals-owned — the two snapshots taken today under phase/snapshots/, which hold the complete pre-compaction document and register. No new file was created. WHAT WAS COMPACTED: the two existing `## Changes / Pivots` entries, each of which already NAMED its own snapshot, were reduced to summaries that keep the decision, the who, and the KR-count delta, and point at those snapshots for the account. 300 -> 278 lines, 22 freed. The pivot entry then went in and the User Commitments line was struck through with its reason. Final: 294 of 300, back under the hard cap with room. WALL 2 was crossed and is recorded rather than hidden. The KR record was removed from phase/003-linkage.md BY HAND, because perry-goals krs is READ-ONLY and there is no write path, while goals/reference/phases.md:212 says nothing in that file is edited by hand once it exists. The procedure prescribes an operation the tooling cannot perform. `updated` was re-stamped to a full ISO datetime the way perry-goals link does it, so the register's freshness field does not lie about when it last changed. perry-goals krs now prints 6 KRs and O2 shows KR1 and KR3 only; perry-lint is at 0 errors. That hand edit is TASK-264's gap made concrete and is noted on that row. STILL OPEN: pivots.md step 4 hands off to the decide lane for a Process ADR recording the pivot rationale. Not done here — decide owns decisions/ and that is a separate lane action. | 2026-09-02 |
| USER-914 | TASK-285 has FAILed two V4 rounds and perry-lint says another round is not the next step. UNUSUALLY, THE PRINCIPLE HAS ALREADY BEEN NAMED -- by the round 2 reviewer, not by me -- and I have already implemented it. This ask exists to have you confirm or overrule that, because a PMO who has just fixed something is the worst judge of whether the principle is settled. WHAT HAPPENED. The row writes one rule into three shipped files: a dispatched agent works in its own git worktree, the primary checkout is never switched by an agent and merges the branch afterwards. Round 1 defeated the guard by softening the rule to 'MAY ... where convenient' -- every assertion pinned the presence of words, none pinned modality. I fixed that with a denylist of eight hedge words. Round 2 then kept the pinned imperative VERBATIM, used none of the eight, and added one sentence inside the rule's own blockquote: 'Where a worktree is impractical, sharing the primary checkout instead is an acceptable alternative.' All nine tests reported OK with the headline deliverable retracted. It also walked past the push check with two synonyms (publishes / raises a pull request). Its summary: A DENYLIST OVER ENGLISH HAS NOW LOST THIS ARGUMENT TWICE. THE OPTIONS. (a) IMPLEMENTED AND MY RECOMMENDATION -- an allowlist. The rule block is compared literally and four normative regions are pinned by sha256; any addition, removal or rewording reddens. Rationale prose is deliberately NOT pinned, so the observed-failure account and the local-merge argument can still be improved without touching a test; what an agent is TOLD TO DO cannot change silently. Thirteen mutations, thirteen red, including both that defeated the old design and a single extra space. Cost: changing the rule now requires re-pinning a digest in the same commit, which is friction on purpose. This is the same move you chose in USER-904 (one header_index over a smarter detector) and USER-906 (one invariant over a fourth predicate). (b) Keep a denylist and widen it again. The record says this is the third widening and the first two each looked principled. (c) Accept the guard as advisory, close at a lower rung, document the limit. (d) Drop the guard entirely and rely on the fact that the rule is now in three files a reader sees. STATED LIMIT OF (a), because it is real: an allowlist pins what EXISTS and cannot cover a fifth normative region someone adds later in a file it does not read. Evidence: evidence/2026-09/TASK-285-round2-v4-review.md and TASK-285-result.md. | TASK-285 |  | answered 2026-09-03: 决定 2026-09-03（用户拍板，Perry 推荐 a）：选 (a) —— 白名单。规则块逐字比对，四个规范性区域按 sha256 钉死；任何增删改写都变红，包括没人预料到的那种。理由散文故意不钉：观察记录和本地 merge 的论证可以继续改进而不动测试，不能悄悄变的是 agent 被要求做什么。代价接受：以后改这条规则必须在同一个提交里重新钉一次摘要，这个摩擦是有意的。不选 (b) 再拓宽黑名单 —— 那已经输了两次，第三次也只是猜下一个没被列进去的词。已实现，13 个突变 13 红。声明的限制保留在记录里：白名单只能钉住已存在的规范性文字，日后有人在它读不到的文件里新加第五处，它看不见。 | 2026-09-03 |
| USER-915 | TASK-067 has FAILed two V4 rounds and needs a principle chosen, not a third round. THE CENSUS THAT MAKES THIS ANSWERABLE NOW EXISTS: TASK-323 delivered a bound and a census and PASSED V4 round 2 on 2026-09-03, so unlike the last two rounds this choice is against measured numbers rather than a recollection. Both failed rounds carried criteria-unbounded because the criteria file had no bound; it now has one -- 89 members (27 W1 / 18 W2 / 38 R1 / 6 R2), last element viewer/tables.py:280, generated by a runnable script at evidence/2026-09/TASK-323-bound.py. Census: 20 rows, 12 caught, 8 uncaught, plus 13 shape rows with 4 red controls. THE TWO READINGS, with what each would actually have to cover. (A) EVERY WRITE PATH MUST REFUSE AN UNRENDERABLE CELL. Population is all writers. To close it must cover all 27 W1 members, already refusing, PLUS the 7 uncaught separator-row builders -- perry-task:985, :1034, :1112, :5174, perry_md_store.py:774, perry-goals:327-328, perry-goals:3093 -- PLUS bin/perry-decide:332, which guards with a weaker third spelling that misses a trailing newline, PLUS perry-task:7431. It would NOT cover the five uncaught read shapes, which are not writes. Its cost is visible in the bound itself: the population has no last element other than the current line count, which is exactly why this file had no bound for two rounds and why each round found a member the previous one never reached. (B) ONE CHOKE POINT PLUS ONE DETECTOR, MY RECOMMENDATION. To close it must cover ONE SYMBOL -- nothing outside render_row builds a row -- with ragged-row as the backstop. The census says precisely what that buys and what it does not: it covers the 7 separator builders by construction IF separator rows are routed through the choke point too, which they are not today; its backstop is MEASURED to work on w4-w7 and MEASURED not to exist for r2/r3/r4/r7/r8; ragged-row cannot cover the read side at all and .split('\|', 6) proves no count-based check ever will; its detector has four demonstrated blind spots on the read half because SPLIT_RE matches none of maxsplit, re.split, a SEP constant, or .rsplit/.partition; and it does not reach perry-decide:332, which builds no rows. TWO MORE FACTS THE ROUNDS DID NOT HAVE. First, ragged-row fires ONLY inside a table the schema recognises -- round 1 confirmed with a control that the identical 8-cell row is silent in an unrecognised section -- so on a project filing work under its own headings via add --group, which Perry explicitly supports, (B)'s backstop does not exist. Second, both readings leave perry-task:7431 open; it is a message-quality defect rather than a corruption path. PRECEDENT, OFFERED AS PRECEDENT AND NOT AS AN ARGUMENT: you chose shape (B) in USER-904 after seven rounds and USER-906 after three. The census neither confirms nor contradicts that -- (B)'s one-symbol surface is real and measured, and so is the fact that its detector is green on nine shapes, five of which have no backstop of any kind. Evidence: evidence/2026-08/TASK-067-finding.md sections Bound, Census and The two readings; evidence/2026-09/TASK-323-round2-v4-review.md. | TASK-067 |  | answered 2026-09-03: 决定 2026-09-03（用户拍板，Perry 推荐乙/B）：选 (B) —— 一个收口点。除 render_row 外没有东西构造表格行，守卫变成一个符号的检查面。这是本项目第三次选这个形状（USER-904 的 header_index、USER-906 的 shrink 不变量）。THE THIRD OPTION WAS OFFERED AND NOT TAKEN: 我另外提过「(B) 收口 + 把甲那 9 处里真正会写坏数据的 8 处也做掉」，用户选的是 (B) 本身，所以那 8 处不在这次范围内。因此实现时必须明确以下三点，它们是 (B) 买到的和没买到的，普查已经量过：(1) 7 个分隔行构造点只有在分隔行也被路由进收口点时才被覆盖 —— 今天没有，所以路由分隔行是 (B) 的一部分而不是附加项；(2) bin/perry-decide:332 不构造行，(B) 够不着它，它仍然开着；(3) bin/perry-task:7431 两种读法都不覆盖，是消息质量缺陷不是损坏路径。最重要的一条，按 Perry 的推荐记录：不要把 ragged-row 当成 (B) 的卖点。普查测出它在 w4-w7 有效、在 r2/r3/r4/r7/r8 不存在，读取侧有四个已证实盲点（maxsplit、re.split、SEP 常量、.rsplit/.partition），而且只在 schema 认得的表格里触发 —— round 1 用对照确认，同样的 8 格行放在不被识别的 section 里报 0 错误，所以用 add --group 的项目上这张网根本不存在。这些洞要写成声明的限制，不能让下一轮以为有兜底。 | 2026-09-03 |
| USER-916 | Should bin/perry-state --escalation-scan exist at all, or should the dispatching agent judge a spec against the hook's high-stakes list itself? | TASK-099 |  | answered 2026-09-04: 答复 2026-09-04（用户拍板）：去掉。'bin/perry-state --escalation-scan 这个命令有问题啊，之前我们已经讨论过，不要用python代码来检查文件语义。应该去掉这个检查逻辑，让agent自己来判断gate。' 这是 ADR-007 决策 3 直接适用到派发闸门上：Python 层根本不解析文档，判断交给 agent。它推翻了 Perry 建议的另一条路 —— TASK-333 提的'把闸门输入换成类型化字段'不再是方向，因为那仍然是 Python 在读 spec，只是读得更窄。PERRY 量过的删除面（2026-09-04, main 7f890f9）：viewer/parsers.py 的 scan_spec_escalations 是 152 行，加上 _PATH_CHAR / _NAME_EDGE / escalation_occurrences / matching_escalations 这一串；调用点在 bin/perry-state（11 处）、bin/perry-lint（4 处）、work/reference/dispatch.md 和 autopilot.md 各 1 处；测试 tests/test_escalation_boundaries.py 1120 行 + test_escalation_union.py 719 行 + test_spec_scannability.py 里 7 处。ONE BOUNDARY THE DECISION DOES NOT SETTLE, and Perry is flagging it rather than assuming: 扫描 spec 的那一半明确要删，但 escalation_union / hook_escalation_lines 是从 .perry/hook.md 里抽出'什么算高风险'这份清单 —— agent 自己判断时仍然需要这份清单，只是它可以直接读那份文档。这一半是跟着删、还是留着当数据源，是实现行要回答的第一个问题。THE REAL RISK, stated plainly: 闸门武装着这个项目的每一次派发。删掉检查而不同时写下 agent 要遵循的判断程序，等于把闸门换成真空。所以实现行的交付物必须包含 work/reference/dispatch.md 里一段可执行的人写程序，而不只是删代码。WHAT THIS RETIRES: TASK-290（修 cite-vs-write）、TASK-329（markdown 斜体那个洞）、TASK-333（类型化字段）全部被这条取代；TASK-306 早已 dropped。TASK-099 由此解封 —— 它卡的就是 setup 这个词的误报。 | 2026-09-04 |
| USER-917 | DESIGN-016 Decision 1 — how the standup gets a small structured read. MEASURED 2026-09-04 on 5d19d83: SKILL.md:130 mandates perry-state --json every invocation and that payload is 186,856 bytes (~47k tokens) on this repository; its largest section, board, is 122,828 on its own; the only compact answer, --dashboard, is 1,132 bytes and text-only. perry-context-budget reports 99.1% of this project's 8.43B tokens is context re-read per turn, so this is the expensive kind of cost. A: add a --compact payload derived in-process from the full one and point SKILL.md step 3 at it (recommended — --json keeps its published shape, and the narrow mode can be tested as a strict projection). B: add --fields selection instead, so the caller names what it wants (more flexible, but every caller now has to know the field names). C: change nothing in the tool and narrow SKILL.md to a list of --section calls (cheapest, but board alone is still 31k tokens, so it does not reach the goal). | TASK-362 |  | answered 2026-09-08: 决定 2026-09-08：选 A —— 新增 --compact payload，从完整 payload 在进程内派生。--json 保持已发布的形状不变，所以 aiMark 和任何现有消费者不受影响，而窄模式可以作为一个严格投影来测试。重新测量后这个决定比立项时更紧迫：payload 从 09-04 的 186,856 bytes 涨到今天的 258,989 bytes（约 65k tokens），+39%，其中 board 一个 section 占 159,648 bytes = 72%。涨的是本会话加的十几行 task。第一手证据：今天开场跑 perry-state --json 输出 248.8KB 被系统直接转存到文件，因为塞不进上下文，此后整个会话都在从那个文件里抠字段。选项 C（只改 SKILL.md 用 --section）明确排除：board 单独就是 72%，达不到目标。 | 2026-09-04 |
| USER-918 | DESIGN-016 Decision 2 — where the --help rationale goes. MEASURED 2026-09-04: perry-task answers --help with 160 lines / 10,059 bytes and its Usage block starts at line 51; perry-lint 135 lines, perry-goals 106, and four more tools put Usage past line 36. The prose above the fold is ADR numbers, the bug that motivated the tool and why each refusal is a refusal — it is good content and it is not what a caller asking for a flag list wants first. A: usage-first --help, rationale behind --help --verbose (recommended — nothing is lost and the default call gets cheap). B: move the rationale into bin/README.md and leave only usage in the tool (README is already 20KB and would grow). C: leave as-is and accept the ~2.5k token discovery cost per tool. | TASK-365 |  | answered 2026-09-08: 决定 2026-09-08：选 B —— 论证搬进 bin/README.md，--help 只留 Usage 和子命令。重新测量：perry-task --help 169 行 10,690 字节，Usage 在第 51 行；perry-goals 107 行，Usage 在第 58 行；perry-decide 62 行，Usage 在第 41 行。而 perry-lint 同样长（136 行）Usage 却在第 8 行，说明这不是论证太多，是排版顺序。PMO 提出的风险，用户已知并接受：论证离开它解释的代码之后两边会各自漂移，而那正是 ADR-019 今天删掉的那一类重复。缓解办法应与搬迁同批交付，不是后补：bin/README.md 目前覆盖 19 个工具中的 19 个，但没有任何测试引用它，覆盖是手工维持的。搬迁应带一个机械检查——每个 bin/ 工具在 README 里有对应小节，且其 Usage 块与工具实际的 --help 一致——把漂移从静默变成被报告，这是 perry-lint 对其它投影已经在用的模式。 | 2026-09-04 |
| USER-919 | ADR-017 step 1 is blocked on two ADDITIVE edits to schema/state-schema.json, which .perry/hook.md names as the claim surface and therefore yours. Line 1303 is id_pattern ^KR-O\d+\.\d+$ on the OKR table's Id column, enforced per row at bin/perry-lint:1203, and it is the definition of an overall KR id; it would become ^(?:KR-O\d+\.\d+\|O\d+-KR\d+)$. Line 286 is a prose list of ID shapes naming KR-O1.2; it would name the new form alongside it. NEITHER REMOVES THE OLD FORM, so nothing on the board or in the corpus breaks the moment they land -- they only stop the linter refusing the new grammar. The round declined to widen the parsers without this on a substantive ground worth your attention: 1303 IS the definition, so widening the parsers while it stands would leave Perry's parsers accepting an id Perry's own linter calls an error, which is the two-readers-one-grammar defect ADR-017 exists to close. Approving unblocks step 1; the data rename and the 74 test occurrences follow it. | TASK-388 |  | answered 2026-09-07: APPROVED 2026-09-08. Both additive edits made inline and verified with a three-armed differential: new grammar against the OLD schema draws 38 bad-id findings, against the NEW schema draws 0, and the OLD grammar against the new schema also draws 0 -- so nothing on the board or in the corpus breaks, which is what additive was supposed to mean. schema:1303 id_pattern is now ^(?:KR-O\d+\.\d+\|O\d+-KR\d+)$ and the invariant list at 286 names O<n>-KR<m> alongside KR-O1.2. JSON still parses. The gate is open for ADR-017 steps 1 through 3. | 2026-09-07 |
| USER-921 | FOUND AND VERIFIED 2026-09-08 BY TASK-281 ROUND 2, AND CONFIRMED BY THE PMO ON THE MERGED TREE. bin/lib/__init__.py:741 computes half the P003-O3-KR2 numerator from records matching kind unlinked AND via add. NO WRITER ANYWHERE PRODUCES THAT SHAPE. via:add is hardcoded at exactly one site, bin/perry-task:2798, which writes EDGE records; bin/perry-goals:2022 appends an unlinked record carrying NO via field at all, and it is the goals lane, whose declarations are via link by definition. THE CONSEQUENCE IS ON THE PHASE, NOT ON A TOOL. Definition of Done item 5, as restated 2026-08-31, reads: every main-track row opened after the gate lands carries A KR EDGE OR AN UNLINKED DECLARATION WRITTEN BY ITS OWN add. The second half has no implementation, so a row that genuinely serves no KR CANNOT satisfy item 5 the way item 5 describes it. Its only available action is to omit --kr, which the computation counts as not-answered and puts in the denominator. SO EVERY HONEST no-KR ROW PERMANENTLY LOWERS THIS KR, and the 100 percent target is unreachable by construction rather than by not having done the work yet. THIS IS WHY THE NUMBER LOOKS THE WAY IT DOES. P003-O3-KR2 read 50 percent, then 33, then 15.38, and none of those moves was a regression -- the denominator grew as rows were filed and most of them legitimately serve no KR. The KR is measuring honestly and the target it is measured against cannot be reached. TWO RESOLUTIONS AND THEY ARE NOT EQUIVALENT. (A) GIVE add A WRITER for the declaration: a row can say at add time that it serves no KR, producing kind unlinked with via add, and it then counts as ANSWERED. This is what DoD item 5 already describes and it makes the 100 percent target reachable. (B) RESTATE THE KR so that omitting --kr counts as answered-with-none. Cheaper, but it destroys the distinction the KR exists to measure -- never-asked and declared-no-KR become the same reading, which is exactly the state DESIGN-015 section 5.2 built the derivation to tell apart. THE PMO RECOMMENDS (A), and the reason is that (B) makes the number un-interpretable rather than merely lower: after (B) a KR at 100 percent would tell you nothing about whether anybody was ever asked. This is a phase-scoring decision and phase/003-storage-code.md lists phase-scoring participation as a User Commitment, which is why it is an ask rather than a row the PMO takes. | TASK-281 |  | answered 2026-09-07: (A) — give add a writer for the declaration. Chosen 2026-09-08. The KR keeps the distinction it exists to measure: never-asked and declared-no-KR stay two readings, which is what DESIGN-015 section 5.2 built the derivation to tell apart, and 100 percent becomes reachable rather than being redefined into reach. Filed as TASK-394 and dispatched. | 2026-09-07 |
| USER-922 | Is the bin/ tool boundary drawn on the mechanism or on the object? DESIGN-016 section 8 asks it, section 1.4 is the evidence, and Decision 3 explicitly did not rule on it. Four registers -- tasks, risks, intake, asks -- are each named twice, once by perry-task for their lane verbs and once by perry-tasks for their store verbs, in two tools that differ by one letter and disagree about singular/plural and about which side the verb goes on. Option A: keep the mechanism split as it is, and TASK-066's per-subcommand-group split of perry-task proceeds. Option B: redraw by object, one tool per register holding both its lane and store verbs, which makes TASK-066's split the wrong cut. Option C: neither now -- record the axis as decided-to-defer with a date, so it stops blocking. THIS IS A BREAKING CHANGE TO EVERY CALL SITE in the skill and in every project already using Perry, which is why DESIGN-016 section 1.4 calls itself a finding and not a proposal, and why it is the user's call rather than a task. NOT COSTED ANYWHERE YET: no one has measured how many call sites a rename touches. | — |  | answered 2026-09-10: SUPERSEDED BY USER-923, and this answer is bookkeeping rather than the user's decision on the substance. The option set here was incomplete: it listed keep / redraw-by-object / defer and omitted the fourth, which the PMO recommends and which the user asked to have written in. An ask cannot be re-worded once filed -- retitle and purge both refuse a row in the User Input Queue -- so it is superseded instead. USER-923 carries all four options and the two facts that changed the premise. Nothing about the boundary is decided by this. | 2026-09-10 |
| USER-923 | Is the bin/ tool boundary drawn on the mechanism or on the object? Supersedes USER-922, which listed three options and was missing the one the PMO recommends. THE PROBLEM, in one sentence: there are two tools one letter apart -- perry-task writes rows the way you work every day, perry-tasks rebuilds and compares the stores behind them -- and to pick one you must know which KIND of operation you want before you can say WHICH THING you want it done to. The four things are tasks, risks, intake and asks. OPTION A -- leave it. TASK-066's per-subcommand-group split of perry-task then proceeds. OPTION B -- one tool per thing: perry-risk holds both its everyday verbs and its repair verbs. This is what DESIGN-016 section 8 asks. Costs 475 live call sites plus 623 in bin/, 200 in tests, 56 in viewer/, and every project already using Perry; the 4589 mentions in journal and evidence are history and must not be touched. OPTION C -- decide to defer, with a date, so it stops blocking TASK-066. OPTION D, RECOMMENDED BY THE PMO -- keep the two tools and let BOTH take the thing as a parameter. C5 already did this on the repair side: risks-build became perry-tasks build --register risks, with the register list read from schema section claims and the twelve old names kept as aliases for one release. The everyday side still spells the thing into the verb -- risk-add, intake-sweep, ask, answer -- eight subcommands. Finishing the same move there makes both tools symmetric and costs eight subcommands plus aliases instead of a rename. TWO FACTS THAT CHANGED THE PREMISE SINCE SECTION 1.4 WAS WRITTEN: (1) C5 already answered half the question in the object direction, so the prefix table is asymmetric because the work is half done, not because the design is confused; (2) section 1.4's complaint was that the names do not carry the distinction, and perry list and perry describe now carry it, so nobody has to read it out of a name. THE ARGUMENT FOR KEEPING THE MECHANISM SPLIT: the everyday side takes a lock, stamps an event, writes the journal and the board; the repair side does one-way imports and byte comparisons. Different risk classes. Under option B, perry-risk render --write sits one tab-completion from perry-risk add -- and render --write is the one place in this repository that was found reporting a write it had not performed. | — |  | answered 2026-09-10: OPTION B, AND ALL THREE PAIRS. Decided by the user 2026-09-10. The reason given is architectural rather than economic: two commands that differ by one character cannot coexist, and the cost of many external references is acceptable BECAUSE AGENTS DO THE WORK -- which is the premise that makes B affordable now and did not hold when section 1.4 was written. Scope is all three pairs section 1.4 names, not just the acute one: perry-task/perry-tasks differ by 1 character, perry-state/perry-state-cost by 5, perry-goals/perry-okr are the same shape with a different word. Renaming in three passes would break downstream projects three times and each later pass would be harder to justify than the first. WHAT B MEANS CONCRETELY: the boundary is redrawn on the OBJECT. perry-task keeps tasks alone; perry-risk, perry-intake and perry-ask appear, each holding both its everyday verbs and its store verbs; perry-tasks disappears and the confusable pair goes with it. THE PMO'S RESERVATION, RECORDED AND OVERRULED: under B a destructive maintenance verb sits beside an everyday one -- perry-risk render --write next to perry-risk add -- and render --write is the one command in this repository found reporting a write it had not performed. The user's answer is that the near-identical names are themselves the worse hazard. That is a decision to carry into the new design as a constraint on how the verbs are grouped, not a reason to revisit the axis. This does not go into DESIGN-016. That document is locked and decide/SKILL.md says a structural pivot needs a new doc; the redraw gets DESIGN-018 and DESIGN-016 section 9 records that its open question was answered there. | 2026-09-10 |
| USER-924 | What independent source of truth does the --compact projection test have, and if the answer is none, where does criterion 5 stop? TASK-362 has now FAILED FOUR V4 ROUNDS -- the limit is 2 -- and perry-lint reports review-rounds-exhausted on it. FOUR ROUNDS, FOUR DIFFERENT LAYERS OF ONE SHAPE, each found only because the previous fix removed the layer above it: round 4, the RULE was verified by calling the function under test; round 5, the DATA was cardinality one so a projection keeping only the first of anything was green; round 6, the PAIRING resolved each expectation through the very path it was checking, 46 of 53 pairs repointable while green; round 7, the same pairing defect one level down in the subdict children, plus _at itself being shared by the tool and by the test's expectation. ROUND 7 NAMED THE PRINCIPLE PLAINLY AND IT IS A REAL FORK: no test in tests/test_compact_payload.py reads .perry/config.jsonl, okr.jsonl, linkage.jsonl or schema/. --json is the reference on both sides and BOTH COME FROM ONE build(). If build() is wrong, both agree and every test in the module passes. READING 1 -- the criterion's ceiling is that the projection is applied CONSISTENTLY over its whole declaration, an independent source is out of reach for a projection by definition, and the row closes when every declared pair is compared from the key: the ten subdict triples, the eleven arg names, and _at. Bounded, finite, and the fix is already specified by round 7. READING 2 -- criterion 5 says every key carries the same VALUE, so it needs a source outside build(): the store files and schema/ read independently. That is a larger piece of work and would give the module the property it has never had. THE PMO RECOMMENDS READING 1 for this row and READING 2 as a separate row, because the four rounds converged rather than diverged -- each went one level deeper and the category is now enumerated and finite -- and because the rounds-exhausted rule exists to stop a search with no last element, which this no longer is. But the rule is the rule and this is the decision it asks for. WHAT IS ALREADY TRUE regardless of the answer: criterion 13 has held under three independent rounds of attack including five directions round 7 invented, and the code has been correct at every round -- what failed each time was the evidence that it was correct. | TASK-362 |  | answered 2026-09-10: READING 1. Decided by the user 2026-09-10. A projection is checked for whether it is applied CONSISTENTLY over its whole declaration; whether the value underneath is right belongs to the function that produces it, not to the projection of it. TASK-362 closes when every declared pair is compared from the KEY side: the ten subdict inner triples at bin/perry-state:2280-2289, the eleven arg-tuple names, and _at itself, which the tool and the test's expectation share. Bounded, finite, and round 7 specified the fix. THE FACT THAT DECIDES IT, measured rather than argued: --json is ALREADY pinned to the schema, just not in this module. tests/test_wip_and_stages.py:40 loads schema/state-schema.json, and round 7's MUT-2 (default_stages_for returns []) is GREEN on the compact module and RED on the full suite. So the chain holds by composition -- the schema pins --json, the projection walk pins --compact -- and the blind spot is in one module rather than in the coverage. THE RISK THIS READING ACCEPTS, stated so nobody discovers it later as a surprise: if build() reads the wrong source for a field, --json and --compact carry the SAME wrong number, the standup shows it, both payloads agree and every test passes. That is not criterion 5's subject under this reading, and it is real. It is filed as its own row rather than left implicit. WHY THIS IS NOT THE ROUNDS-EXHAUSTED RULE BEING WAIVED: that rule stops a search with no last element. Four rounds went one layer deeper each and the category is now counted -- ten triples, eleven names, one function. The code has been correct at every round; what failed each time was the evidence that it was correct. | 2026-09-10 |
| USER-925 | AUTHORIZE OR DECLINE a claim-surface dispatch against schema/state-schema.json. It is on .perry/hook.md High-stakes operations, so no agent may touch it without you saying so, and it is the sole blocker under both blocked rows. TASK-155 needs a new per-KR assertion date field there; TASK-280 needs krs[].title required:true flipped, measured in a sandbox copy to take that round from 6 errors to 0. The 2026-09-08 mid-phase review found this is ONE authorization, not three rows, and called it the highest-leverage single decision on the board. TASK-155 was separately deferred to phase 004 on 2026-09-08, so a yes here may still leave its work in 004. | TASK-155, TASK-280 |  | answered 2026-09-10: answered 2026-09-10: ALREADY AUTHORIZED — no second authorization needed. ADR-019 (2026-09-08, active, Deciders: Ran Jiao) records it in its Consequences: schema/state-schema.json is on .perry/hook.md High-stakes operations under the claim surface, and the user authorized that edit in session on 2026-09-08. ADR-019 chose Option B, so phase/<NNN>-linkage.md stops existing rather than shedding half, which dissolves TASK-155 and unblocks TASK-280. Verified on main, not only on a branch: the linkage document is absent from git ls-tree main perry/phase/, _linkage_drift_rows is absent from git grep main -- bin/, and bin/perry-goals states TASK-155 is unable to happen. USER-925 was minted 2026-09-10 against two rows whose next actions were written 2026-09-08 before the ADR landed; recorded here so the queue matches the record, the same way USER-003 was. Evidence: evidence/2026-09/TASK-280-closed-by-adr-019.md | 2026-09-10 |
| USER-927 | TASK-236 has FAILed V4 twice and review.md s6 says two FAILs is a decision, not a third round. Both FAILs are the same shape in bin/perry-goals: a kr record the render cannot place is emitted nowhere while the count beside it is printed as fact. Round 1 charged five corruption shapes; round 2 closed four of them by computing a residual and refusing, and left the fifth because the residual is scoped by `k.get("version") in wanted` and a record whose version is ABSENT or BLANK is never in wanted. PMO reproduced it: drop the version key from one kr and the render gives 37 of 38 at exit 0 while perry-okr diff says identical=true AND every_line_and_cell_came_from_the_store=true and perry-lint says 0 errors. Every gate green, one key result gone. TWO READINGS. (A) FIX THE RENDER: treat blank, absent and unknown as STRANDED rather than out-of-scope. Cheap, one function, and it leaves every OTHER consumer of okr.jsonl with the same blind spot -- the reviewer's own objection. (B) MAKE version AND objective_id REQUIRED in perry_store validate_records, so every reader and perry-lint refuse the record rather than each reader guarding itself. Addresses the category; costs a schema change, and schema/state-schema.json is on hook.md High-stakes operations so it needs your authorisation. The reviewer recommends B with A as the stopgap. I agree, and add one thing it could not know: this is the second time today a rule had one enforcement point where it needed to be inherited (TASK-440 is the same shape on the board cell cap), which is an argument for B on its own. WHAT IS BLOCKED MEANWHILE: TASK-237 is gated on this row's read-surface report, and TASK-262 and P003-O2-KR3 sit behind 237. That is the whole remaining chain of phase 003's Objective 2. | TASK-236, TASK-237, TASK-262, P003-O2-KR3 |  | pending | 2026-09-12 |

## Done this period (leaves the board at next triage)

| ID | Title | Evidence |
|---|---|---|

## Top risks (one-line; full list in `PROJECT_STATE.md`)

| ID | Risk | Opened | Status |
|---|---|---|---|
| RX-001 | Perry is half-adopted: `.perry/config.md` exists and flips `is_adopted()`, so lint demands a full state tree it does not have yet. Recorded in ADR-001 as a candidate finding for DESIGN-002. |  | cleared 2026-09-10 — The condition cannot occur: ADR-019 deleted .perry/config.md, which this risk is entirely about — it said that file EXISTS and flips is_adopted(), so lint demands a state tree the project has not got. Cleared 2026-09-10 by DESIGN-019 phase A, and the clearing is the point of that design rather than housekeeping beside it: perry-state --compact published this as risks.top on every read and work/SKILL.md:169 rendered it into every standup as the project's top risk, for however long it has been false. Nothing noticed because nothing brings a risk back. This is the first risk this project has ever cleared through the tool -- all four rows were imported from the markdown table by risks-write --from-board, which is why every opened cell is blank and why .perry/events.jsonl holds zero risk writes. |
| RX-002 | ~~`LOAD-03` (10 decisions queued on the user)~~ — cleared 2026-08-16 when DESIGN-003's 8 rows were decided and USER-001/002 were answered. `bin/perry-diagnose --root .` now reports 0 errors and no `LOAD-*` finding. |  | cleared 2026-08-16 |
| RX-003 | The V4 review found `OKR.md § Commitments` is written by two modes that disclaim the goals cascade, with no declared owner. That is a hand-off-contract question, so TASK-026 now blocks phase D as well as phase G. |  | open |
| RX-004 | DESIGN-003 phase G rewrites `SKILL.md § The hand-off contract` — the one rule that keeps lanes composable, and `perry-lint` cannot see a bad edit to it. Mitigation is in DESIGN-003 §7: TASK-026 lands first and alone, with V5 sign-off and an ownership-refusal fixture. |  | open |
