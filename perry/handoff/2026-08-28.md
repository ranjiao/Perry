# Handoff — 2026-08-28, overnight

> Read this, then `perry/BOARD.md`. Every number below is a command's output.

## Seven things need you

Nothing overnight was blocked on them, but none can be done by me.

1. **`git push`.** `feat/work-modes` is **605 commits ahead of `origin/main`**.
   Every merge has been local, which is the workflow you set — but the remote
   has seen none of it, and neither has CI.

2. **`merge-check` as a *required* status check.** The workflow exists —
   `.github/workflows/merge-check.yml` — so this is not "build it"; it is the
   branch-protection setting that makes it blocking, and that needs admin. It earned its keep
   again tonight — it caught a real textual conflict between two changes to
   `schema/README.md` made hours apart.

3. **Four User Decisions on DESIGN-009** (`an Objective is a title string…`) —
   id shape, write-back location, how the five existing Objectives get minted,
   and whether `krs[].objective` keeps its title. You chose option A; the RFC is
   drafted and locked behind those four rows.

4. **Four on DESIGN-010** (`autopilot cannot dispatch anything…`). Decision 2 is
   the one that matters: what a machine-authored spec's `## Out of scope` is
   worth to the escalation gate. My recommendation is **nothing — fail closed**,
   and § 5.2 argues why.

5. **Four on DESIGN-011** (`the OKR is collected as ten fields…`). Decision 1
   would make DESIGN-008's spine/flow axis its first consumer.

6. **A release for TASK-155** — it is schema-gated and still is.

7. **`USER-903` — one command, one promise.** Should `.perry/config.md` become
   a rendered projection? `perry-config write --from-file` finishes `P-O1.2`
   (1 of 2 → 2 of 2). The cost is that hand edits to your own config file become
   reported drift at `warn`. `SKILL.md` promises you own that file directly,
   which is why TASK-092 built the capability and deliberately did not run it.
   **The only item here that is genuinely one command.**

## Where the tree is

```
84 modules · 2510 tests · 1 red        (PYTHONNOUSERSITE=1 /usr/bin/python3)
perry-lint  0 errors · 3 warnings · 0 rows drifted
KR-O2.4     0 across all six contracts
26 open rows · 150 closed  —  eighteen closed overnight
```

**The red count went 2 → 1**, and this line is a measurement taken after the
last merge, not carried forward. `test_contract_invariance` is green:
**TASK-145** made the contract *page* decide a type instead of a captured
sample, so a key declared `int | null` is correct on either branch.

The single red is `test_diagnose`, two failures, both TASK-179's subject.

**Four contracts moved**, each with a `semantics` entry: `perry-task/list`
1.15 → **1.18**, `perry-goals/list` 2.1 → **2.2**, `perry-events/list` 1.1 →
**1.2**, and `perry-knowledge/list` gained the page it never had.

**The one red has an owner and is not new:** `test_diagnose`, two failures,
both about ids this project pays for having written about. **TASK-165 closing
did not fix them** — see *The thing that keeps happening*. **TASK-179** is the
open decision.

**Perry is stdlib-only now, in fact rather than in intent.** The suite reads
identically on `/usr/bin/python3` and on `PYTHONNOUSERSITE=1
/usr/bin/python3` — it no longer cares whether jinja2 exists. That was the
point of deleting the viewer, and it closes yesterday's "the red set changes
with the interpreter" finding.

## One decision that is one command, and one mistake of mine

**`P-O1.2` reads 1.0 of 2.0 and can be finished tonight if you want it
finished.**

It is *"`OKR.md` and `.perry/config.md` likewise become stores"*. You ran the
OKR half at the start of this session. The config half is **`perry-config write
--from-file`** — one command, and `bin/perry-config` is complete and runs today
(`build` returns 9 records without writing).

**The reason it has not been run is a promise, and the promise is about you.**
`SKILL.md` says `.perry/config.md` is *"a tier-1 file the user owns and edits
directly"*. Making it a projection turns your hand edits into reported drift, at
the `warn` severity you chose. TASK-092 shipped the capability and deliberately
left the store uncreated so that promise is not broken until you choose to break
it. `OKR.md` was never promised that, which is why its half was uncontroversial.

**Filed as `USER-903`** — a decision belongs in the ask register, not on a task
board.

### And I got this wrong first

For about an hour this document told you the config store was **missing work
that nobody owned**. It is not, and the way I got there is worth more than the
finding: I read three artifacts that agreed — the KR's number, the absent file,
and a `claims[]` entry pointing at nothing — and treated agreement as
corroboration. **All three are views of the same fact, and all three are equally
consistent with "the command has not been run."** `bin/perry-config` was one
grep away and is listed in `bin/README.md`.

Corrected in `e545ed6`; the wrong intake row is discharged with its reason. The
original text is in git at `47352c0` if you want to see what I actually claimed.

`perry/risks.jsonl` is declared-and-absent too, for a genuinely different
reason: `cmd_risks_write` was never built (confirmed, 0 matches), and that gap is
recorded in `USER-016`'s answer — which is where it belongs. **Two absent
stores, two different reasons, both written down. The system worked; I did not
read it.**

### The signal underneath is still worth building

Nothing joins a KR's linked-task statuses to its progress, so *a KR short of
target whose every linked task is terminal* is invisible. But this case sets the
guard's hardest requirement: **it must tell "stalled" from "waiting on a
decision that is correctly yours"** — fired here, a naive version would have
reported `P-O1.2` as broken when the row is behaving exactly as designed, and
would have been wrong in the same way I was. `USER-` asks are the register that
answers it.

Plus phase-scoping: the sweep returns four rows and three are phase 001, which
is closed and *allowed* to end unmet. **A naive guard would be wrong on four of
four rows on this project today.** Full measurement:
`evidence/2026-08/2026-08-28-a-kr-with-no-open-task.md`.

I did not touch the linkage register. **1 of 2 is the correct number.**

## The thing that keeps happening

**Three times tonight, documenting a fix broke the thing the fix fixed.**

`perry-diagnose` reports an id that is referenced and never defined. An
evidence record that *quotes a checker's output* mentions such an id, and the
project is then charged for it. TASK-165 fixed the queue-register half — and
its own result record, merged an hour later, re-broke the test:

```
queue reconcile: 2 != 0
dangling: TASK-007, TASK-9999, USER-900, USER-902
```

Measured, not guessed: `document_reports_on_a_check` says the record **is** a
report, but `report_lines` finds only 3, and the fourth mark needs *every* live
mention to be a report line. The check-name mark is scoped to the **paragraph**,
deliberately and with an argument in its docstring, and my mentions sit in
paragraphs about the fixture rather than about a check.

**The rule is working as specified.** Five ids now — `TASK-007`, `TASK-9999`,
`USER-900`, `USER-902`, `WIT-404`.

**I re-measured this after writing the section above, and two of its three
options are wrong.**
I had counted *documents* — 46 touches across 14 evidence records — which made
it look like an evidence-record policy question. It is not:

```
exempt evidence/ + journal/ + handoff/ entirely
  → the dangling list shrinks by ZERO ids
  → all five survive in perry/BOARD.md alone
```

`BOARD.md` is rendered from `perry/tasks.jsonl`, so the whole population is
**two `next_action` cells** — and **TASK-179's own supplies five of the five**.
The row that exists to describe the defect is the row committing it.

**My recommendation is now a fourth option I had not offered: move the prose out
of `next_action`** into the evidence record it belongs in. Two `perry-task`
calls, no exemption, no widening, and the check keeps `BOARD.md` — which is the
surface a *genuine* dangling reference would appear on. Full measurement and the
cost of each option: `evidence/2026-08/TASK-179-the-cost-is-two-store-cells.md`.

**And `test_diagnose` is red twice, not once.** The second failure is a queue
register disagreeing with `perry-task` about open asks — `2 != 0` — which is not
this question and was riding along on TASK-179's back. Filed as its own intake
row, so closing TASK-179 alone will not turn the module green.

## What landed

| row | |
|---|---|
| **TASK-168** | `perry-task events` returned the log's **head** while four texts promised its tail. Now `perry-events/list/1.1`, first page is the tail, `semantics` announces it. Verified by paging all 733 events and reassembling: exact, no gaps, no duplicates. |
| **TASK-169** | `perry-knowledge/list/1.0` had **no contract page**, which is why aiMark asked for a surface that already existed. Page written; the `stale` predicate mutation-proved three ways. |
| **TASK-170** | An answered `USER-` ask was in **no** register a consumer could query. Now `tasks[].depends_on_resolved` at `1.15` — one field read resolves it. |
| **TASK-171** | The event key table was missing **eleven** kinds, not the three my spec claimed. It is now pinned to what the *writer* can emit, by two independent derivations. |
| **TASK-176** | A key table can now name the collections it documents. |
| **TASK-178** | The web viewer is deleted — **3,977 lines**. |
| **TASK-130** | `schema/README.md` pinned **three** stale contract versions. The numbers are gone; the table names families, and a test keeps them gone. |
| **TASK-165** | The queue register now reads the quoted verdict instead of guessing — and its agent **corrected my diagnosis**, which was wrong about the mechanism. |
| **TASK-166** | A closed row's name is repairable, and `TASK-029` has one again after twelve days. Its `done` event is **the first event in the log and the only one of 777 with no `title` key**; the key entered the schema six minutes later, added by the row that built the log. |
| **TASK-132** | 15 keys nothing had ever verified are now verified — by a **witness project** the real tools read through `--root`, not a hand-written payload. All 15 were correctly documented; that is now proved. |
| **TASK-117** | `drift: 0` beside `checked: false` is gone. And it **inverted my framing**: `perry-lint` was the tool in the wrong, calling 175 of 175 rows drifted when the only thing missing was the log. |
| **TASK-174** | A procedure may **explain** a contract field and may not **restate** it — a two-tier guard, and it found the defect in a second place on the page my spec had located once. |
| **TASK-154** | A title now loses the subject's own id, not every id in the heading. Eleven titles gained back a word; five `DESIGN-006 phase A…E` rows stopped reading as unattached fragments. **My spec named one call site; there were two, and 8 of the 11 came from the one I missed.** |
| **TASK-102** | Evidence is a typed list — 139 cells, 223 entries, **0 characters lost**. And the row's own `{path, kind, round}` shape **did not fit**: `round` exists only inside filenames, on 8 cells of 139. |
| **TASK-164** | Each root global gets the root its name means — **and the enumeration inverted my claim**: there are zero call-time readers, so the write was dead. It also found a second defect I never named: `STATE_ROOT` was never assigned at all. |
| **TASK-144** | The event log stamps its offset now, and `ts_moment` is the only reader. **My spec had the error's direction backwards**, and `_ts_key` was not the only place both shapes met — there were three more, one of which would have raised `TypeError` on the first offset-bearing event. |
| **TASK-145** | The contract **page** decides a type now, and element zero stops deciding. **This turned `test_contract_invariance` green.** Re-sorting the board alone used to invent two fresh contract violations. |
| **TASK-163** | A dash is not a clock, to either reader. **My spec named one call site; there were two** — the third time tonight. |
| **TASK-156** | A linkage edge to a task no record carries is now a finding. **`perry-goals link` writes exactly that edge today** — returns 0, appends it, then signs off recommending `perry-lint`, which was blind to it. My spec said no writer could produce the case; the agent disproved it with a pin that **passes**, not skips. |
| **TASK-175** | **Closed without a dispatch — aiMark had already built it**, and built it to the restraint you specified. Its own test is the row's title restated. Closed at **V2, not the declared V3**, because I read the source and never ran it; the record names the one-minute step that earns V3. |

## What is in flight

**One, and I am deliberately leaving the second slot empty.**

- **TASK-167** — the three smoke rows are `TASK-081/082/083`, title `t`, six
  seconds apart. They are in **3 of 176 contract-payload rows and 0 board rows**,
  so they are invisible to you and present to every consumer. The real finding:
  **`perry-task` has no removal path** — `drop` sets a status, which is what put
  them in this state. The deliverable is the mechanism; the sweep is its first
  use. The blank line is `.perry/events.jsonl:67`, append-only, so that half is
  "prove every reader tolerates it", not a fix.

**Why the slot is empty rather than filled.** Every remaining startable row I
priced — TASK-112, TASK-118, TASK-158 — touches `bin/perry-task`, which TASK-167
is actively rewriting. I resolved three merge conflicts earlier tonight;
dispatching into a fourth on purpose is not throughput, it is work I would spend
twice. The slot frees itself the moment TASK-167 merges.

**Two rows closed without a dispatch of their own, and that is now a pattern
worth a signal.** TASK-161 was resolved by TASK-176 and TASK-132 arriving from
opposite directions; **TASK-175 was resolved by the other repository**. Perry has
no way to notice either. Filed as intake.

**TASK-161 closed without a dispatch of its own** — TASK-176 made the page
writable and TASK-132 made it checkable, from opposite directions, and together
they are exactly that row. Verified on its own case rather than argued: two
collections that are empty on this project right now have their keys documented
*and* compared.

## One contract moved, by my overrule

`perry-task/list` **1.15 → 1.16**. TASK-117's agent recorded its change as *"not
a version"* and flagged it as the judgement worth overruling. I overruled it: the
page said in as many words *"Everything else is then zero or empty"*, which is a
documented guarantee on a versioned payload, and 1.5 — the precedent the agent
cited itself — is a meaning change with no key change that moved the minor.

**Two things my own bump broke, both caught by tests that already existed**: I
prepended the `semantics` entry where the list is ordered oldest-first, and I
tripped an assertion that had encoded a moment (*"the last entry is 1.14"*)
rather than its rule (*"there is no 1.15 entry"*). The second is now rewritten to
say what it means.

## Three design docs, drafted

`DESIGN-009` · `DESIGN-010` · `DESIGN-011`, in the order you asked for. Each has
its User Decisions table unresolved — that is the gate, and it is yours.

## What I got wrong

- **I reported two agents as running when they were dead.** I read
  `perry-dispatch-limit list`, which is my own bookkeeping file and observes no
  process. You caught it. The evidence that settled it was transcript mtimes,
  empty worktrees and an absent process — filed as intake.
- **The self-matching `pgrep` hazard bit me one dispatch after an agent reported
  it.** `ps aux | grep "[t]ests/parallel"` matched my own shell.
- **Everything I hand-wrote after the session resumed was dated six days
  stale.** The event log dates the resume exactly — `2026-08-21T18:39:04` then
  `2026-08-27T23:19:11`. Corrected; and the blanket fix over-corrected a
  filename, which is also corrected.
- **I diagnosed the `USER-900` red with the wrong mechanism**, confidently, in a
  commit message and on a row. I said `TASK-153-result.md:50` *"became the
  definition point"*; `perry-explain USER-900` says `defined NOWHERE`. The real
  cause is the inverse — `only_an_example` short-circuits an empty location to
  "not an example". TASK-165's agent refused my version and produced the right
  one; corrected in `5e6257e`.
- **My TASK-171 spec measured the log where it should have measured the
  writer**, so it put the gap at 3 where it was 11.
- **My TASK-178 spec undercounted `serve.py`'s references.** There was a fifth,
  in a module that read it *as text* — invisible to an import grep.
- **My TASK-156 spec was wrong twice**, and the agent followed the code both
  times: *"the case cannot be produced by any writer"* (`perry-goals link`
  produces it, pinned by a test that passes) and *"nothing proves a task the
  graph names exists"* (true of `perry-lint`, not of `perry-diagnose`, which asks
  a different question and gets a different answer on every `done` row).
- **I turned a green test red and told you the count was 1.** Filing `USER-903`
  reddened `test_answered_ask_is_legible`, which asserted *"this board's asks are
  all answered"* — a moment, not a rule. **merge-check caught it**, by baselining
  two reds where I had claimed one. The class containing that assertion carries a
  docstring, four methods below, explaining that this exact defect is the one
  this repository has spent the most nights on. Fixed and mutation-proved: the
  precondition is that `USER-015` and `USER-016` are *answered*, not that nobody
  has asked anything new.

Every one of these was caught by an agent or by you, not by me, and each is
recorded where the next person will trip over the same thing.

## The register I have been filling all night

```
perry-task list --json → intake.undischarged   37
```

**Thirty-seven undischarged intake rows**, and I added **five** tonight (one
of which I have since discharged as wrong — see above):
the `ABSENT` unification follow-up, the dead-agent bookkeeping note, the
`test_diagnose` queue disagreement, the missing config store, and the stalled-KR
signal.

Every one of them is a real finding and filing it was right. But *filing* is the
cheap half, and a register that grows every night and is drained by nobody is
the shape of a queue that has stopped meaning anything. It is also **not**
mode-managed — `## Intake` has no SLA, unlike a declared `queue` track.

I am flagging it rather than draining it, because triage is a prioritisation
call and that is yours. **`/perry work triage` is the drain.** If you would
rather I stop filing and start folding findings into existing rows, say so and
I will.

## Housekeeping I did not do

- **14 merged `coding/*` branches** are undeleted. Safe to remove; I left them
  because you did not ask.
- **`coding/task-086-lint-ns01`** is unmerged and superseded by its own `-v2`.
- `.claude/worktrees/` holds many harness-managed agent worktrees.
