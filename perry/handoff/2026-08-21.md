# Handoff — 2026-08-21, overnight

> Read this, then `perry/BOARD.md`. Everything below is a command's output.

## Two things need you, and only two

1. **`git push`.** `feat/work-modes` is well ahead of origin. Every merge
   overnight was local, which is the workflow you set — but nothing is on the
   remote, and CI has therefore not seen any of it. Related: **CI has come back
   failure on essentially every PR since 2026-08-20 05:41**, and TASK-143's agent
   established that *a check that cannot be green is not a gate*.

2. **Two safety-gate releases**, both per-task, neither borrowable from an
   earlier one:
   - **TASK-153** — refuses on `diagnose`. Same false-positive shape you
     released for TASK-126 (the fragment matches the *filename*
     `bin/perry-diagnose`, not the diagnose **execute stage**). It is the last
     red in the suite. Spec is written and waiting.
   - **`merge-check` as a required status check** — TASK-143 shipped the
     mechanism and stopped at saying so, because branch protection needs admin.
     Until you apply it, it reports; it cannot refuse a merge.

## Where the tree is

**72 modules · 2078 tests · 1 red** — and that one red is TASK-153, above.
`perry-lint` 0 errors, **0 rows drifted**, 150 records.

The suite was **fully green** for a stretch overnight (70 · 2042 · 0 red); the
one red returned when the last of Perry's own queue rows was answered, which
exposed a fixture that had always been counted. See TASK-153.

## What landed overnight — eight rows, all merged locally

| row | what it settled |
|---|---|
| **TASK-143** | a differential merge check that **names the conflicting pair** |
| **TASK-121** | the live-state sweep became a mechanism; floor of 7, each entry judged |
| **TASK-135** | an existing row can change track; `Arrived` carried, never restamped |
| **TASK-148** | the `startable` rule is stated **once**, guarded by an AST scan |
| **TASK-149** | a heading **names its subject**; it does not define what it argues about |
| TASK-120, 122, 126, 140, 141 | closed after their code merged and the tree verified |

## The three findings worth your attention

**TASK-143 disproved its own premise.** CI **never ran** for PR #14 or #15 —
`actions/runs?head_sha=` returns `total_count 0` for both, verified twice.
*"Green on its own base"* was each agent's local run, never a CI verdict. And
`ci.yml` already tests the merge result, so the suspected fix would have
prevented nothing. The two real defects: the merge is computed against the base
**as it stood when the event fired**, and nothing ever tests two **open** changes
together.

**merge-check earned its keep on its first two uses.** It caught TASK-121 as
`BROKEN ON ITS OWN` — the base had moved under the branch — rather than blaming a
pair. And on the second pair it correctly attributed `test_diagnose` **to the
base, to nobody**, which without the baselining step would have been a false
accusation against two innocent branches.

**Three agents reported a baseline red rather than absorbing it, and two of them
were correcting me.** I told them the tree was all green; it was not. The reading
I quoted was taken at an earlier commit. One was mid-run and was sent a
correction.

## What I got wrong overnight, recorded because the records are the point

- **I gave three agents a wrong baseline.** Corrected above and in TASK-148's
  evidence.
- **I created a dangling id while documenting the dangling-id defect** — the
  anti-vacuity example `ZZZ-404`, quoted in two records I wrote.
- **I then defined an id by writing a heading about it.** `## Its diagnosis of
  REL-00 is wrong` made that evidence file the id's definition point, and it
  vanished from *both* the dangling list and the exemption list. That became
  TASK-149, which fixed the **rule** rather than the prose — and the heading is
  now restored, naming the id on purpose, as the regression case.
- **I speculated about the cause of TASK-153 and was wrong** (`USER-014` appears
  0 times under `perry/`). Two agents had it right independently.

## Later overnight — six more rows, and the last red is still yours

**77 modules · 2216 tests · 1 red**, and that red is TASK-153, which needs the
`diagnose` release. `perry-lint` 0 errors, 0 rows drifted, **115 rows closed.**

| row | what it settled |
|---|---|
| **TASK-119** | the linkage register has a writer, in place, byte-proved, refusing to guess an attribution |
| **TASK-142** | stranded-row checks — and one that says what it might *mean* rather than what it matched |
| **TASK-146** | the KR chain card, built so it **cannot show the number alone** |
| **TASK-150/151/152** | the live-state floor 7 → 4, every remaining entry a named false positive |
| **TASK-159** | one answer to what a project root is — the viewer was rendering **completely blank** on Perry's own project |
| **TASK-136** | the queue clock finally has a consumer |

**KR-O1.2 has its first of three.** `intake` — 14 rows, `sla: 5d`, runnable, 0
breaches, **first one falls due 2026-08-26.** `main`, having no SLA, correctly
reports `runnable: false` rather than zero breaches.

**KR-O2.4: 17 → 14.**

### Three things the agents caught that I had wrong

- I told three agents the tree was all green. **It was not** — the reading was
  from an earlier commit. Two caught it independently.
- I said `intake` was "declared and empty" in a spec. **By the time the agent ran
  it held 12 rows**, because I had spent the night adding them.
- TASK-120 reported that the viewer "renders the chain from `viewer/parsers.py`".
  **It rendered no KR `current` anywhere** — no template had ever read `linkage`.

### And one I caused while documenting

Blocking TASK-114 on an ask made `depends_on_unknown` non-empty for the first
time — which let the parity check finally compare that collection and find two
keys undocumented, **and then a second checker rejected the table I wrote to fix
it.** Both now agree. TASK-161 is open for the general defect: *a contract page
cannot be checked against a collection nothing populates, so the check is silent
until the day it is not.*

## In flight

TASK-119 (the linkage writer — documented as machine-written, and no tool writes
it), TASK-142 (stranded-row checks in `conformance`), TASK-146 (the viewer's
missing provenance). All three merge locally; none opens a PR.

## Still yours, unchanged from yesterday

**TASK-094** is the only row in `review`. Its PR merged long ago, but
verification item 1 asked for **0** header-resolution call sites across all three
stores and `BOARD.md` keeps **13 splits / 87 resolutions** on four registers that
have no store. Either its target moves or it stays open behind DESIGN-007. That
is a scope decision, not a closing note.

**DESIGN-008's § 7 risk 2** asks step 1 to enumerate the illegal spine/flow
pairs. TASK-140 enumerated **none**, argued that all sixteen compose, and asked
for a human to confirm that reading. Still open.
