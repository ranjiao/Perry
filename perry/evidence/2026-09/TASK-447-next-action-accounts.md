# TASK-447 — Preserved Next action accounts

PMO reviewed all 33 summaries against these verbatim accounts and compared originals with the complete live task-list contract immediately before each tool write.
Source: captured next-actions-source.json, 2026-09-17. Historical claims below remain historical; only Next action text is migrated. Current task state is not changed by this archive.

## TASK-066

NOW WAITS ON TASK-263, added 2026-09-03, and the reason is this row's own next action: it says re-size the split after the markdown reader, row renderer and cell escaping are gone, and TASK-263 is what MEASURES the category split by call site. Splitting before that measurement is the two-splits outcome this row already argues against. MEASURED 2026-09-03 on c506ca5: bin/perry-task is 7851 lines / 412,790 chars, about 103k tokens if read whole -- but it is already grep-navigable, 135 top-level functions and 26 cmd_*, largest single function 527 lines, so the three agents that changed it today all worked by targeted reads and none read it whole. The saving from splitting is real and second-order; the larger saving available today costs no code at all, which is the PMO naming the function and line in the brief. TASK-067 landed half the precondition: the row renderer and cell escaping moved to viewer/tables.py, and only 36 lines here still reference the table primitives. TIMING, and it is the stronger argument: splitting while review rounds are in flight would make every branch unmergeable and every line number in every spec and criteria file stale, on a board that cites them heavily. Sequence: reviews clear, then TASK-263 measures, then this row splits to that measurement.

## TASK-173

DESIGN-009 drafted 2026-08-21: design/DESIGN-009-the-objective-is-a-record.md. Four User Decisions open — id shape, write-back location, how the five existing objectives get minted, and whether krs[].objective keeps the title. The key finding: the PHASE level already solved this. phase/002-linkage.md states id: O1 and the payload carries it, while okr.objectives[].id is '' for all five — and the contract already says a STATED id is legitimate where a DERIVED one is not

## TASK-212

Two directions of ONE missing edge -- read TASK-282 before starting, it carries the measurement this row has none of. This row is design -> task (a locked design's plan step has no link to the work that discharges it). TASK-282 is task -> design (an implementation row need not cite the design, so impl_refs cannot count it). Measured 2026-09-02 on d49964e for the second direction: DESIGN-001's six implementation rows are all done and verified against artifacts, wrote "DESIGN-001" into no title, next action, evidence cell or event, and left no evidence file -- so it reported pending_handoff and always would have. Filing TASK-282 then cleared that flag by text match alone, with nothing implemented, which shows impl_refs cannot tell "implements" from "mentions". Whoever picks up either row should decide first whether one declared edge answers both directions; two separate mechanisms for one relationship is how this project acquires a second translation table.

## TASK-240

IMPLEMENTS ADR-014, NOT ADR-013 -- re-pointed 2026-09-02 and the acceptance moved with it. ADR-013 is superseded. Its measurements all stand (only the highest id reissues; a middle gap is never filled -- delete ADR-002 of five and the next mint is ADR-006; perry-decide has no delete command for option (b) to refuse in). What changed is WHERE the high-water mark lives. ADR-013 put it in .perry/events.jsonl on the reasoning that a counter beside the other stores was "foreclosed by TASK-235's own guard, tests/test_decide_writer.py:461 ADR_ONLY". That guard governs decisions/ ONLY -- a store at the state root, beside the six already there, is not an object it refuses. A local constraint was read as a global one and it excluded the right answer. The chosen home was also a file DESIGN-004 section 5.3 and perry-task --help both declare DERIVED AND DISPOSABLE, and this project has already paid for putting something load-bearing in that log once: ADR-006 exists because that disposability sentence "was false for a release, and is true again". BUILD: mint_id takes max(ADR files on disk, ceiling recorded in a store). perry-decide may still append an event as a trace, but nothing reads it to decide the next id. THE ACCEPTANCE TEST THAT CHANGED, and it is the one ADR-013 could not have passed: delete .perry/events.jsonl and reissue must NOT return. Unchanged from ADR-013: do not add a delete/refusal path (perry-decide has no delete; an ADR leaves decisions/ by a human rm), and the pin test_a_deleted_adr_number_is_reissued_and_that_disagrees_with_purge is INVERTED, not deleted. Read decisions/ADR-014-high-water-mark-lives-in-a-store.md first; ADR-013 remains readable for the measurements.

## TASK-242

Blocked until TASK-157 lands, since it is that row's guard being strengthened. Start from evidence/2026-08/TASK-157-v4-review.md, which carries the measurement: at f15d234 all eight linked: values in 001-linkage.md were verbatim the retro Measured column, and 16 of 24 cells survived; after the fix, 24 of 24. The threshold that now protects that is checked >= 8, and 001 and 003 supply all eight between them.

## TASK-252

Blocked until TASK-243 lands. Start from evidence/2026-08/TASK-243-result.md sections 7.1 and 7.2 and from its V4 review, which accepted both the rejection and the close and then said each deserves a row. Note the reviewer's framing of the second: the literal property would require lint to report a disagreement that no longer exists, so the question is not how to keep drift high but where a loss lives after the write that named it.

## TASK-254

Premise partly decayed -- verify before starting. The row cites bin/perry-conform:360 as "the wall standard it violates"; bin/perry-conform was DELETED 2026-08-31 by TASK-261, so the standard it points at no longer exists and must either be re-derived from a surviving tool or restated in the row. The defect itself is not disproved: bin/perry-lint mentions perry- commands 90 times as of 2026-09-02 and the question is how many of those are handed back to a reader without the --root they were resolved under. Re-census by call site rather than by grep count -- a mention in a docstring is not a handed-back command, and this row's sibling TASK-255 was corrected TWICE for exactly that over-count. NOTE: bin/perry-lint is being edited concurrently by TASK-247's agent on branch coding/task-247-config-predicate; re-derive line numbers after that lands.

## TASK-255

Premise VERIFIED, population NOT -- 2026-09-02 on d49964e. Verified: shlex appears nowhere in bin/ or viewer/, so the row's headline claim stands as written. Not verified, and the row's own history says why to distrust it: the census was taken over FOURTEEN tools, two of which -- bin/perry-conform and bin/perry-migrate -- were deleted 2026-08-31 by TASK-261. The summary already records the population being corrected twice before that deletion (63/232 down to 42/208 handed-back commands over the other twelve), so the live number is smaller again and nobody has counted it. Re-derive the census by call site, name the instrument, and state the count before proposing a fix; a fix sized against a stale population is how this row got corrected twice already. Sibling of TASK-254, which cites the same deleted perry-conform standard.

## TASK-282

Startable, and WIDER than filed -- the predicted trap fired and was measured. Sequence, 2026-09-02 on d49964e: before this row existed, DESIGN-001 had impl_refs 0 and sat in pending_handoff; filing THIS row took it to impl_refs 1 and emptied pending_handoff entirely. Nothing was implemented. The only change is that this row's Next action contains the string "DESIGN-001", and impl_refs (viewer/parsers.py:3276-3311) is a literal text match over id + title + next_action + evidence plus every .perry/events.jsonl line. So a row whose next action merely says "read DESIGN-001" is counted as implementing it, and a row that IS its implementation but never names it counts for nothing -- which is exactly how TASK-001..006 left this design at zero. The defect is therefore two-sided: false negatives (the six real rows) and false positives (this one), and a check that only adds enforcement on the writing side fixes half. Read DESIGN-001 section 9's 2026-09-02 entry for the original measurement. Do NOT treat the now-empty pending_handoff as the problem being gone.

## TASK-286

MEASURED COST, not a style preference. review.md section 1 requires bounded criteria and perry-lint --reviews reports criteria-unbounded, but add-task -- the only place a spec is ever written -- never mentions ## Bound. So specs are authored without it and the omission surfaces rounds later. TASK-095 burned five rounds, TASK-050 eleven, and the census of 2026-09-02 found criteria-unbounded 8 times across 12 review files. review.md states the mechanism in its own words: an unbounded criterion does not fail a round, it fails to END one. The PMO hit this the same day: TASK-247's spec had no Bound and one had to be written before its V4 could be dispatched at all.

## TASK-287

Observed 2026-09-02 while creating DESIGN-015's six implementation rows. The PMO wrote 'unlike TASK-280' into row E's Next action, predicting that F would take 280; E itself was minted as TASK-280 and the sentence became self-referential. Repaired with perry-task next, repointing it at TASK-279. SKILL.md already forbids minting an example id that resolves to nothing, and this is NOT that case -- the id does resolve, to the wrong row -- which is why the existing rule did not catch it and a reader following the docs would repeat it. The whole class exists because add mints the id and the author is writing prose about ids that do not exist yet.

## TASK-294

REPRODUCED LIVE 2026-09-07 BY WRITING ONE FILE THE DOCUMENTED WAY. The PMO wrote a knowledge card at knowledge/verification/, which is exactly where work/SKILL.md says knowledge digests live, and perry-lint went from "knowledge/ holds 3 file(s) Perry did not write" to 4 in the same run. So the count is not a legacy of some import: it grows by one every time somebody uses the mechanism as documented. THE SECOND HALF, AND IT IS THE SHARPER ONE: knowledge/INDEX.md still reads "Active: 0 · Eternal: 0 · Stale: 0 · Archived: 0" and "(no digests yet)" while four cards sit beside it. The index calls itself "Auto-maintained by the work lane, updated on every digest, archive operation, and full rebuild during end-phase-retro" -- and nothing maintains it. That is WHY the NS-01 fires: the files are real, the index that would claim them is empty, so Perry reads its own knowledge directory as somebody else's folder. WHY THE OFFERED REMEDY IS A NO-OP HERE, concretely: the warning tells the user to run /perry relocate perry, and the state root ALREADY IS perry -- the files are at perry/knowledge/verification/. Relocating cannot move a file out of a directory it is already correctly inside. The same is true of the phase/ warning, which names seven snapshots score-phase itself writes. SO THE ROW'S CLAIM IS NOW MEASURED RATHER THAN ASSERTED: on this project every NS-01 names files Perry wrote or told a user to write -- 7 phase snapshots, 4 knowledge cards -- and the remedy is inapplicable to all eleven. TASK-222 is the phase/ half of the same defect. The fix probably has two parts, an index that is actually maintained and a claim check that consults it, and this row should say which part it owns before it is dispatched.

## TASK-297

CLOSING THIS COSTS MORE THAN THE DOCSTRING GAP SUGGESTS, AND A CORRECT FIX WILL LOOK LIKE A REGRESSION UNLESS IT LANDS WITH ITS TRIPWIRE. TASK-292's V4 reviewer applied the minimal defect-2 fix that TASK-292's own commit message proposes -- honour an explicit project_root exactly, walk only when it is None, at viewer/parsers.py:3298-3311 -- cleared __pycache__, crossed the second boundary, and ran the module: FAILED (failures=1), test_and_the_harness_above_really_does_bite, "Lists differ: ['DESIGN-001'] != []". That test is TASK-292's ANTI-VACUITY DEVICE: it asserts pending == [] and refs['DESIGN-001'] == NOISE, i.e. it asserts the cross-project read STILL HAPPENS, so that the round's other assertions cannot pass vacuously. It is legitimate and documented and the reviewer explicitly did not charge it. But it means WHOEVER TAKES THIS ROW MUST RETIRE OR INVERT THAT TEST IN THE SAME COMMIT. TASK-292's commit says the minimal form "was measured green on the existing suite" -- true of the suite as it stood BEFORE that commit, and false of the suite it ships, because the commit installs the tripwire that fires on it. ONE THING THE REVIEWER DID NOT ESTABLISH AND THIS ROW SHOULD FIRST: it ran only test_parsers.py under that patch, not the full 108 modules, so whether the minimal defect-2 form is green suite-wide is UNVERIFIED. Establish that before anything else. Still worth doing for the reason first filed: parsers.py:462 says the walk is skipped when a caller hands over the exact root, perry-state --root <fixture> DOES pass the fixture as project_root, and the walk left it regardless -- so closing the prose/code gap fixes the contamination with no fixture change at all, which is the other direction from the one TASK-292 took.

## TASK-301

MEASURED 2026-09-02, and it makes the deliverable sharper: the page is not merely stale, it instructs the consumer to do a job the tool already does better. perry-state resolves the state root ITSELF and publishes it, including when there is no store. Built a project carrying only .perry/config.md with "State root: docs" and no config.jsonl: perry-state --section project returns state_root 'docs' with settings_source 'absent'. So a consumer that can run the tool NEVER needs to parse the markdown, and one that reads the store alone would get it wrong on a store-less project -- the tool's fallback is the thing that makes both cases answer the same. tests/fixtures/sample-project reports state_root '' only because its own config.md declares no such line, not because the fallback is missing. THEREFORE the page should say: read state_root off the payload; .perry/config.jsonl is the register and .perry/config.md is its projection; the tool falls back to the projection when the register is absent, so a consumer never has to. aiMark declined to implement the markdown fallback for exactly this reason and asked whether any consumer must resolve the root itself. The answer measured here is no -- only a consumer that cannot run the tools at all, and Perry has none.

## TASK-309

SECOND OBSERVATION 2026-09-03, and the first one where the cost is measured. TASK-155's round died at the 600s stream watchdog with its branch still at the commit it was cut from -- zero commits, whole round lost, agent still establishing a baseline when it stalled. Yesterday a batch of five died the same way when the laptop closed. THE MITIGATION THAT DID NOT WORK IS WORTH RECORDING: the dispatch brief already instructed 'commit your evidence file before any long run', and that is too late by construction when the long run IS the baseline suite. Whatever this row builds has to make the first commit happen before the agent does anything expensive, not before its LAST expensive thing.

## TASK-312

OBSERVED BY TASK-244 ROUND 2 ACROSS FOUR FULL SUITE RUNS. The floor moves rather than disappears: TASK-244 took the header-rule harness from first place, and this module is what is now at the top, at 48-124s across runs. IT IS NOT A REGRESSION AND SHOULD NOT BE READ AS ONE -- TASK-258 just PASSED V4 for making this module build its fixture from a git archive snapshot and carry its own concurrent-write loop, and both of those cost real time and both are load-bearing (that reviewer showed the mutation red on a still tree, so the concurrency is not decoration). So the question this row asks is not whether the cost is justified but whether it is the right size. The 48-124s spread is itself the first finding: this module's cost varies 2.6x with machine load, measured across runs at loads from 8.6 to 60.6.

## TASK-334

CLOSED AND VERIFIED, kept here for the record: three sites now route through header_rule.source_of, which skips a file that vanished between the walk and the read and RAISES when git still tracks it. A SECOND, UNRELATED PARALLEL RACE has since been found by TASK-283's V4 reviewer and filed as TASK-341 -- tests/test_one_choke_point.py:532 writes a real bin/lib/rowprobe.py into the live tree while tests/test_one_primitive.py:150 asserts bin/lib holds exactly one exempt file. I confirmed both lines. Same family as this row and as TASK-326: a test observing a tree that something else is changing under it. Three instances now, in three different mechanisms -- a vanishing file, a decaying clock, and a probe written into the live tree.

## TASK-340

FIVE STALE FIGURES NOW, not three. The first three came from TASK-263's census: DESIGN-014 section 5.1's line counts are wrong for all three files it sizes (7,522/4,483/4,603 against today's 7,851/5,144/5,011); bin/perry-lint's load_glossary docstring justifies its shape by bin/perry-conform, which USER-910 deleted and which no longer exists -- three citations survive at :349, :730 and :2293; and bin/perry-task's module docstring at :43 says 'markdown is canonical' two paragraphs below its own ADR-007 note saying the opposite. I reproduced the last two directly. TWO MORE came from TASK-067's round-6 V4 reviewer, who marked them as carrying no verdict weight: TASK-067-result.md's Verification section still says '13 new tests' where the module now has 19, and it cites the branch's 3125/3125 at 267abb1 rather than main's current 3291. THE PATTERN IS THE ROW'S REAL SUBJECT: every one of these is a NUMBER OR A NAME written into prose at the moment it was true, in a file nothing re-derives. Perry has a rule for exactly this -- 'never compute a number by reading files and eyeballing it' -- and these are its mirror image: numbers computed correctly once and then frozen into a document. The round should say, in one line, whether the answer is to re-derive them, to date-stamp them, or to stop putting them in prose, because a fix that just corrects five figures leaves the sixth to be written tomorrow.

## TASK-350

THE ONE QUESTION THIS ROW EXISTS TO SETTLE: TASK-339 KEPT escalation_union, hook_escalation_lines and escalation_fragments when it deleted the scanner, arguing they extract a DECLARED list of backticked spans under a named heading -- the typed side of ADR-007 rather than the prose side -- and that three surviving consumers need it, including the only check that can tell a user their high-stakes list is dead prose. Its V4 reviewer flagged that ADR-007 decision 3, 'the Python layer never parses a document at all', is being cited on BOTH SIDES of that keep. This census either confirms the argument or contradicts it, by call site. TASK-339 also reported a cost that belongs in this row's scope: perry-lint check_verification still matches a task TITLE against that same list and cannot tell a mention from an action -- the same defect class one surface over, left unfixed and outside TASK-339's Bound. SIZE: setup is 638 lines of bash; the hook readers are bin/perry-task, bin/perry-lint, bin/perry-state, bin/perry_md_store.py and viewer/parsers.py. Where a hook reader is inside TASK-348's scope, defer to that classification rather than restating it -- say which sites you deferred.

## TASK-355

PARTIALLY DISCHARGED 2026-09-07, AND THIS ROW'S OWN HEADLINE IS NOW STALE: "every locked design reports zero implementation references" is false for six of them. WHAT CHANGED: perry-task design-link existed and NOT ONE ROW USED IT -- 0 of 145 carried a link -- which is the whole mechanical cause of the number this row reports. The PMO declared the link for 20 rows whose TITLE ALREADY NAMES ITS DESIGN, so this was transcription of an existing declaration rather than attribution anybody guessed: DESIGN-006 got 1, DESIGN-009 got 3, DESIGN-010 got 4, DESIGN-011 got 5, DESIGN-015 got 7 including the two already closed, since design-link is store-only and survives done, which is the point -- pending hand-off is a question about FINISHED work. Pending hand-off went 15 to 10. WHAT REMAINS AND WHY IT IS NOT THE SAME KIND OF WORK: TEN locked designs still report zero, and for those the rows do not name their design in the title, so linking them is a JUDGEMENT and not a transcription. Perry's own rule forbids guessing an attribution, so those need either the design document to name its implementation rows or a person to declare each one. Note DESIGN-016 already reported 9 references without any design-link record, so impl_refs has a second source this row should identify before it prescribes a fix -- otherwise the fix addresses one of two mechanisms. THE UNDERLYING DEFECT THIS ROW SHOULD NOW BE ABOUT: nothing at add time asks which design a row implements, and TASK-367 records that perry-task add ACCEPTS --design AND DROPS IT. So the field can only ever be filled by a later, separate, easily-skipped command, which is exactly why it sat at zero across 145 rows. TASK-282 is the sibling: a locked design's implementation rows need not cite it, so a finished design reports as never handed off forever.

## TASK-371

EVIDENCE FROM BOTH DIRECTIONS, AND THE SECOND INSTANCE HAS BEEN RETRACTED -- READ THIS BEFORE USING IT. FIRST DIRECTION, 2026-09-04, STANDS UNCHANGED: six agents dispatched and only four marked, so the board reported 3 running against 6 live and the user caught it; then a rate limit killed all six and the board went on claiming five rows were running FOR THREE DAYS while nothing was. That is the gap in both directions and it is this row's argument. SECOND DIRECTION, RETRACTED 2026-09-07: this row previously said TASK-278's agent wrote to perry/tasks.jsonl in the primary checkout against its brief, and that its RESULT block calling that file untouched was false. BOTH CLAIMS ARE WRONG AND THE AGENT IS OWED THE CORRECTION. The write was made by a SECOND CONCURRENT PMO SESSION, not by the agent. Proof: the 11:53:41 status event carries that session's own reason string verbatim, and git diff --stat 1032e76 coding/task-278-six-read-sites -- perry/tasks.jsonl is EMPTY, so the branch never touched the file. The agent's RESULT block was accurate. THE MISREADING WAS REASONABLE AND THAT IS THE POINT: every event is stamped actor: agent whether a PMO session or a dispatched agent wrote it, so a session seeing a modified tasks.jsonl in the shared checkout had no field that could have told it otherwise. That defect is now TASK-378. WHAT THIS ROW LOSES AND WHAT IT GAINS: it loses 'the gap produces pressure on agents to reach outside their worktree', which was never observed and should not be argued from. It keeps the 09-04 evidence, which is enough on its own. Do not re-import the retracted instance from commit df269b5 or from an earlier reading of this cell.

## TASK-372

FOUND BY TASK-276's V4 ROUND, 2026-09-07, as finding F9 -- and found by hitting it, not by reading. The reviewer followed review-constraints.md, ran the recommended call against its scratch copy, got '<ref> is not a commit in <copy>', and fell back to byte comparison against git show. THAT FALLBACK IS THE POINT: the round still verified its restores, but it verified them with a method it invented on the spot, which is exactly the N-implementations-of-one-rule defect this repository keeps finding in itself. Anchor the fix in review-constraints.md's own wording so the recommended call and the working call are the same call.

## TASK-374

FOUND BY TASK-276's V4 ROUND, 2026-09-07, as findings F5, F4 and F6, all three driven with hand-built user-producible input rather than read off the source. NONE WAS A FAIL AND THE REASON IS WORTH KEEPING: every one errs toward 'unchecked, not clean', which is the phase-003 operating rule, so the store never claims a comparison it did not perform. That makes this a robustness row and not a correctness one. F5 is the one with real consequences -- the store is about to become the authority for P003-O3-KR2 via TASK-281, and at that point a single stray line silently zeroes the evidence base for a KR rather than reporting one bad record.

## TASK-375

FOUND BY TASK-277's V4 ROUND, 2026-09-07, finding F1, by ENUMERATING the category rather than reporting the instance: there are five ways a register can contradict itself about a row, and the round tabulated all five. Two are already caught -- a duplicate kr id is refused by a two-reader cross-check, and one task under two KRs raises linkage-task-single-kr. THE OTHER THREE PASS EVERY GATE IN THE SYSTEM. NOT A DEFECT OF ROW B, AND THE ROUND CHECKED RATHER THAN ASSUMED: it fed the same contradictory register to perry-lint at the merge base 0724fa6 and got 0 error(s) there too, so the gap is inherited. The importer is CORRECT to mirror it -- linkage_account is genuinely multiset-safe, using _multiset_minus rather than a set difference, so it faithfully reproduces whatever multiplicity the register states, which is its job. The real register is clean today. THE URGENCY IS TASK-281: once P003-O3-KR2 is computed from this store, a contradictory row silently produces a KR number with no defined meaning.

## TASK-378

PROVEN, NOT SUSPECTED, AND THE PROOF IS THREE FACTS. (1) The status event at 2026-09-07T11:53:41 moving TASK-278 in_progress to review carries a reason string written verbatim by the OTHER PMO session, not by any agent. (2) coding/task-278-six-read-sites never touched perry/tasks.jsonl -- "git diff --stat 1032e76 coding/task-278-six-read-sites -- perry/tasks.jsonl" is empty. (3) Every event today is stamped actor: agent regardless of origin -- 11:51:40, 11:51:52, 11:52:41 and 11:53:41 from one session, 11:54:57 and 11:56:01 from the other, all identical. THE INFERENCE THAT WENT WRONG WAS A REASONABLE ONE, which is what makes this a tooling defect and not a carelessness one: a session saw perry/tasks.jsonl modified in the shared primary checkout with an agent-stamped event beside it, and the only actor value available said agent. THE COST IS ALREADY BANKED: a false charge that a dispatched agent broke its brief and filed a dishonest RESULT block was recorded on TASK-371, on TASK-278 and in commit df269b5, and TASK-371's argument was being built on it. All retracted 2026-09-07. The field also cannot separate PMO from Coding Agent at all -- "PMO Agent" (209), "PMO" (64) and "Coding Agent" (5) exist as values but 1601 records say plain "agent", so the distinction lives in the vocabulary and not in what the writer stamps. PERRY OWN DIAGNOSTIC ALREADY RANKED THE COLLISION SURFACE AND THE INCIDENT LANDED ON ITS TOP THREE. perry-diagnose reports the many-sessions-rewrite-this finding and names them in order: perry/BOARD.md at 514 commits/90d, .perry/events.jsonl at 502, perry/tasks.jsonl at 373 -- precisely the three files the two sessions wrote today. Its stated remedy, splitting a file that holds current status into a small live file plus an append-only dated history, is a DIFFERENT fix from this row and may be the better one. It also reports the twelve-checkouts-nobody-reviews-the-combined-result finding, whose argument is exactly what happened: each piece was correct and the combined record was wrong. NEITHER IS NEW; both sat in the diagnosis BEFORE the incident, which is worth more than the incident, because the warning existed and nothing routed it to a row.

## TASK-380

MEASURED ON MAIN 2026-09-07 at a9176e5: 117 modules, 3346 tests, 1 module red, and this is it. Re-run ALONE and reproduced, so it is not the parallel runner. THE ASSERTION IS assertNotIn('LOAD-03', ids(perry_root)) at tests/test_diagnose.py:561, docstring "The skill that reports this must not commit it." THE FINDING IT TRIPS ON IS TRUE AND IS NOT A CODE DEFECT: perry-diagnose reports "6 open questions are waiting on you" and names all six with file and line -- USER-917 and USER-918 at BOARD.md:205-206, plus FOUR open questions in design/DESIGN-016-the-bin-contract.md:216-219, which is a DRAFT design and the only unlocked one of sixteen. Perry is reporting its own user's real decision backlog, accurately. THE PROBLEM IS WHO CAN CLEAR IT. No code change makes this green. It goes green when the user answers six questions, four of which are open questions on an unlocked design, so main's baseline CANNOT be green through engineering alone and every agent that measures a baseline inherits a red it must reason about. THREE SEPARATE AGENTS PAID FOR THAT TODAY: TASK-276's V4, TASK-277's V4 and this session each independently re-ran the module alone, confirmed it pre-existing and wrote it up. That is the cost being paid over and over for a fact nobody has written down. THE DECISION THIS ROW EXISTS TO SURFACE, and it is the user's, not an agent's: a dogfooding assertion whose truth depends on the user's inbox is a test whose result is not a property of the code. Either (A) the assertion excludes the finding classes that measure the user rather than the tree -- LOAD-03 and anything else load-shaped -- and keeps asserting the structural ones, which is the recommendation because it restores a green baseline and keeps the dogfooding value; or (B) it stays as-is and the red is declared a permanent known-red with a named waiver the suite prints, so agents stop re-diagnosing it; or (C) the backlog is cleared and the red returns the moment a seventh question is asked, which is not a fix. Do not silently delete the assertion: it is the mechanism by which Perry is held to its own diagnosis, and DESIGN-002's whole argument is that a skill reporting a problem must not commit it.

## TASK-386

SEVERITY CORRECTED 2026-09-08, AND THE CORRECTION IS TO WHAT THE PMO WROTE ON THIS ROW. This row said a pipe-written tasks value declares an edge that is dropped SILENTLY, with no KR counting it, no attribution seeing it, and NO CHECK REPORTING IT MISSING. THE LAST CLAUSE IS FALSE. Found by TASK-278 round 3's V4 and reproduced by the PMO: planting `tasks: |` with the id on the following line draws a HARD LINT ERROR -- "perry/phase/002-linkage.md [bad-type] objectives[2].krs[1].tasks must be a list" -- so the register is refused outright rather than accepted with a lost edge. WHAT IS STILL TRUE AND IS WHY THE ROW SURVIVES: the READER still drops the edge. kr_for_task matches the id exactly and the block scalar yields the id plus a trailing newline, so the two halves disagree about the same input -- the linter calls the register malformed while the reader silently returns one fewer edge for it. A register in that state is refused by one tool and half-honoured by another, which is a smaller defect than a silent loss but is still two answers to one question. SO THE ROW IS NO LONGER ABOUT DATA LOSS. It is about a shape that fails a type check and, independently, resolves to nothing in a reader that never consults that check. Whoever takes it should decide whether the reader should refuse in the same terms the linter does, or whether being refused at lint time is sufficient and the reader's behaviour is unreachable in practice -- and MEASURE which, rather than assume, because the linter is not on every path that loads a register. DOWNGRADED FROM P1 TO P2 ON THAT BASIS. The original P1 rested on the silence, and the silence is not there.

## TASK-387

FOUND 2026-09-07 WHILE RECORDING TASK-155's REPRODUCTION, and it is that row's own prediction coming true one step worse. TASK-155's summary says its defect is disclosed at three layers and fixed at none, and warns that the cheapest wrong outcome is a FOURTH DISCLOSURE. What shipped instead is a fourth writer with NO disclosure. MEASURED, both sides on the same tree: bin/perry-goals:2226 builds a redated list of every KR whose assertion date the write is about to move, returns it as current_assertions_redated, and PRINTS a warning naming them -- with a comment stating why: naming it on every write is what keeps this from being discovered as a silent number months later, since the real fix is a new schema field behind the safety gate. bin/perry-tasks:1612, the linkage-write --from-register path built today by TASK-277, reads THE IDENTICAL field for declared_at, validates its FORMAT and refuses a day-only value -- and never reports what it re-dates. It rewrote declared_at on 115 records in one run with no warning at all. SO THE DEFECT WAS INHERITED AND THE DISCLOSURE WAS NOT. That is the failure mode this row is about: a mitigation that lives in one writer's body rather than in something every writer must pass through. A fifth writer will inherit the same way. WHAT TO DECIDE, and it is not just "add the warning to linkage-write": the disclosure exists because the fix is gated, so it is a standing obligation on every writer that reads updated: as a date. Either the obligation is enforced -- a shared helper that no writer can bypass, or a check that fails a writer reading that field without reporting -- or it is documentation and the next writer will skip it too. Note perry-goals' own comment says naming it is what buys time until the gated fix lands, so this row is about keeping that purchase, not about the fix itself.

## TASK-390

FOUND AND MEASURED BY ADR-017's DOCUMENT ROUND, 2026-09-08, and it is the reason that half of the migration cannot land first. viewer/parsers.py:3397 parses the design header's Linked OKR line into a dataclass field, sets it at :3409, and NOTHING READS IT -- verified independently: no consumer anywhere in bin/, and not one reference in the whole of tests/. THE ROUND PROVED THE CONSEQUENCE RATHER THAN ARGUING IT: it renamed DESIGN-011's Linked OKR to a grammar nothing resolves and re-measured, and perry-lint stayed at 0 errors, the perry-diagnose finding set was byte-identical, no LOAD-02 appeared, and user_load.dangling -- the list test_diagnose asserts empty -- was [] BEFORE AND AFTER. So a design can point its Linked OKR at an id that does not exist and every instrument Perry has stays green. WHY THIS IS MORE THAN A DEAD FIELD: DESIGN documents declare which KR they serve in exactly this line, and that declaration is the thing a reader uses to answer "what goal is this design for". It is also adjacent to TASK-355 and TASK-282, which are about a locked design's implementation rows not citing it -- so the design-to-goal edge is unchecked in BOTH directions, and impl_refs is the only one of the two that any tool computes. THE DECISION IT FORCES IS ALREADY NAMED IN ADR-017 and is recorded as a Changes entry on that ADR: its Consequences say the seven locked design headers get a Changes entry rather than a header edit, "unless the lane decides the header field is METADATA rather than BODY". A field with no consumer is metadata by the plainest reading of that test, so the decide lane settled it that way and the header edits ride with the atomic data rename rather than becoming seven Changes entries in locked documents. WHAT THIS ROW IS FOR, and it is not the rename: either Linked OKR gets a consumer -- resolved against the OKR store, reported when it dangles, the way impl_refs is computed -- or it is deleted from the header template as decoration. Do not fix it by adding a lint rule alone: DESIGN-013's rule is that a fact with a schema lives in one store, and a design-to-KR edge is exactly that shape, which is what linkage.jsonl already does for task-to-KR.

## TASK-391

FOUND BY TASK-279's V4 AS ITS NEAREST MISS, FILED RATHER THAN FAILED, AND REPRODUCED BY THE PMO BEFORE MERGING. --actor is a DOCUMENTED flag, at perry-task add --help line 150. Run add --kr <KR> --actor goals in a scratch copy of main and the writer appends {"kind":"edge","task":...,"kr":...,"actor":"goals","via":"add"} and tests/test_linkage_import.py goes from 1 failure to 2, the new one being test_no_live_record_the_goals_lane_declared_was_stamped_via_add. THERE IS NO SUPPORTED REMEDY: the linkage writer is append-only and has no retraction, so the record cannot be withdrawn by any command Perry ships. A user following the help text can therefore redden the suite permanently in one invocation. ROOT CAUSE IS ONE ASYMMETRY: bin/perry-task:2797 takes the lane from the CALLER, while the importer hardcodes LINKAGE_IMPORT_ACTOR. So the same field is caller-supplied on one path and a constant on the other, and the test that TASK-279 round 2 scoped to actor == goals reads it as if only the importer could ever write it. WHY IT WAS NOT A FAIL, and the argument is defensible: the spec never mentions actor, DESIGN-015 section 7 records that NOTHING ENFORCES THE LANE TABLE YET, the record itself is TRUE -- the goals lane really did declare it -- and it is counted correctly by the KR. The module is also already permanently red on any add --kr under TASK-383, so this adds a second failure to a module that is red either way. AND THE JUSTIFICATION FOR THE SCOPE IT EXPLOITS IS FACTUALLY WRONG. Round 2 scoped the assertion to actor == goals on the reasoning that such a record would be "a genuine lane lie". The V4 measured --actor agent against --actor goals and THE KR COUNTER SCORES THEM IDENTICALLY AT 100 PERCENT, with no disagreement anywhere. So the scope is pinned by mutation but its stated reason does not hold, which is why this row exists rather than a note on TASK-279. WHAT TO DECIDE: either the lane on a linkage record is not caller-supplied -- the writer stamps the lane it IS, the way the importer does -- or the assertion stops keying on a field a caller controls. Do not fix it by refusing --actor with --kr alone; that leaves the same field caller-supplied everywhere else it is stamped.

## TASK-393

FOUND BY ADR-017 STEP 2 AND CONFIRMED BY THE PMO ON THE MERGED TREE. goals/state/OKR_TEMPLATE.md carries SIX occurrences of the retired KR-On.m grammar, and it is the template a new project's OKR.md is built from. So the migration ADR-017 just completed would be undone one project at a time: every fresh Perry project is minted in the grammar this repository stopped using. WHY IT IS NOT PART OF THE RENAME COMMIT, and this is a real distinction rather than a scoping excuse: step 2's job was the DATA of this project -- OKR.md, its projection, the phase registers and the live references in the corpus. The template is not this project's data, it is SHIPPED PRODUCT: goals/SKILL.md lists it as what OKR.md is created from, and ADR-018 part C found that test_shipped_vocabulary already checks templates copied verbatim into a user's repo, which is why it classed that module as behaviour rather than convention. SO THE FIX HAS A TEST WAITING FOR IT. Whoever takes this should check whether test_shipped_vocabulary's template assertions already pin the id shape -- if they do, the template and the assertion move together; if they do not, that is the second half of the row, because a shipped template with no pinned id grammar is how this drifts back a third time. WIDER THAN THE ONE FILE, AND THE ROUND SAID SO: 421 old-form occurrences remain repo-wide after step 2, in evidence, journal, handoff, snapshots, the event log, goals/ and code comments. MOST OF THOSE ARE HISTORY AND MUST NOT BE TOUCHED -- an evidence document records what was measured under the old grammar and a journal entry records what was written. The template is different in kind because it is not a record of the past, it is an instruction for the future. Do not let this row grow into a sweep of the other 420.

## TASK-436

PART 1 DONE, PART 2 NOT. The harvest path is closed and merged at 9c30782a: any markdown table row's first cell was a definition point, so TASK-436's own spec defined USER-920 and silenced the finding. dangling_in_reports had the same root and went 6 to 80. WHAT IS LEFT is the round's own withdrawal: it enumerated nine append shapes after the PMO showed a case the fix misses, FOUR still move the verdict and this fix closes ONE. The uncovered one is worse -- document_reports_on_a_check is FILE-scoped, so one journal line naming perry-diagnose or any test_ exempts a live citation 52 lines away, and that line need not mention the id at all. The round recommends the file-scoped mark and the table-paragraph mark be ONE row, because fixing the latter alone masks this path without closing it. ALSO NEW AND MEASURED BY THE PMO AFTER THE MERGE: the gate now reports V4-1, V4-2 and V4-3, which are a 2026-08 review report's own numbering of its verification items written in a table's first column. The check's definition domain includes a report's internal item numbers. The row cannot close while the verdict still moves with unrelated content.

## TASK-440

SPEC WRITTEN, NOT STARTED: evidence/2026-09/TASK-440-spec.md. Opened with --unlinked at add time, so it answers the P003-O3-KR2 question in its own creating action while serving no phase 003 KR. THE FIX MUST NOT BE 'add validate= to cmd_status and cmd_start': that is a third and fourth enforcement point for one rule, which is TASK-040's four-implementations shape, TASK-431's three-lists shape and TASK-382's four-publishers shape. The rule belongs where the FIELD is written so a fifth writer inherits it; where exactly is this row's judgement and must be argued. THE DELIVERABLE THAT MATTERS IS THE FIFTH-WRITER TEST, derived from SURFACE rather than from a hand-written list of four, and it must be shown able to fail by adding a throwaway subcommand in a scratch copy. Two false comments to correct while there: bin/perry-task:3637 says 'the other site that writes this cell' and :4913 says 'status is the only other writer' -- both were wrong when written.

