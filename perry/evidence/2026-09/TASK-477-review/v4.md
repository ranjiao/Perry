# TASK-477 — V4, 2026-09-21

Fresh-context reviewer (Claude subagent, own worktree), criteria
`perry/evidence/2026-09/TASK-477-spec.md`, range
`558ca2ac4a29893c5f8a7b1b16a413c18ba74f85..2e690db4dccb27b1dca604ce97015b2ba2e8c06f`.
Copied by the PMO from the reviewer's returned text.

## Findings

1. **Consumers of the Perry-owned set** (`bin/perry-diagnose:2641-2647`):
   `scan_docs` (1204-1287) — index detection (1267, `BOARD.md`/`PROJECT_STATE.md`,
   state-anchored, unaffected) and orphans (1281-1287, covered); `scan_archetype`
   (1521, same membership, observed through orphans). `scan_user_load` (908) and
   `scan_namespace` (1723) do not receive it; report text (2440-2449, 2710) reads
   `docs["orphans"]` only.
2. **Other state-root prefixes of schema paths:** `scan_namespace`'s claim loop
   (1744-1747) skips `project`-anchored and `file` claims; the one code-anchored
   claim is a file. `column_aliases` (1899) reads column names; `scan_tracking`
   uses hard-coded lists. No code-anchored path is still state-prefixed.
3. **13 CLI layouts** (state root `perry`/`.`; code root unset, `code`, `.`,
   `./code/`, absolute inside, absolute outside, `../elsewhere`, missing, equal to
   the state root both ways, symlink): all matched the expected ownership;
   `OKR.md`/`phase/*`, extra directories and `.perry/hook.md` stay at the state
   root. The base fails the split code-root case (reports `code/ARCHITECTURE.md`
   as an orphan).
4. **Mutations** (scratch copy, pycache cleared, 1.2 s waits, restores checked
   against `git show 2e690db4:bin/perry-diagnose`): killed — M1 revert the
   anchor-aware prefix (2645), M2 drop the `"."` exemption (2646), M3 ignore the
   schema anchor (1027), M4 code root = project root (2643), M7 everything at the
   code root (2645). Survived — M5 drop the `.perry` exemption (2646;
   pre-existing, untested anywhere), M6 extra directories default to `code`
   (1040; behaviour correct per fixtures, unguarded by the new test).
5. Affected tier 33 modules / 985 tests green; `git diff --check` clean; net +15.

Notes, not a FAIL: glob characters in a root name are not escaped (`my[app]`,
same as the base for state roots); schema-unreadable fallback keeps
ARCHITECTURE.md state-anchored; `<component>/ARCHITECTURE.md` matches nothing;
`scan_namespace` reads the schema via `HERE.parent` where the rest uses
`PERRY_HOME`.

=== VERDICT ===
task: TASK-477
rung: V4
result: PASS
criteria: perry/evidence/2026-09/TASK-477-spec.md
checked: bash tests/run --tier affected --base 558ca2ac (green, 33 modules / 985 tests) in my worktree detached at 2e690db4, PERRY_HOME=worktree, PERRY_PROJECT unset, TMPDIR under the scratch derivation; git diff --check clean; numstat +23/-8 = net +15; enumerated the consumers of the Perry-owned set (scan_docs indexes and entry_points/orphans, scan_archetype subject_md; not scan_user_load or scan_namespace) and the schema-path prefixes (scan_namespace claims skip file claims, the only code-anchored claim is a file); 13 CLI fixtures on a git-archive scratch copy (state root perry and "."; code root unset, code, ".", ./code/, absolute inside, absolute outside, ../elsewhere, missing, equal to the state root in both directions, symlink) observed through documents.orphans; extra-directory and .perry fixtures; the same fixtures on a base copy (base fails the split code root); 7 line-anchored mutants on the copy with __pycache__ purged and a 1.2 s wait, each restore checked against git show 2e690db4:bin/perry-diagnose: M1, M2, M3, M4, M7 killed; M5 (.perry exemption, pre-existing) and M6 (extra directories default to code) survived test_diagnose, test_ns_collision, test_board_less_project_is_recognised and test_work_modes, both test gaps with correct candidate behaviour
not-checked: full or slow suite (the integrator's job); the archetype output directly (only through the shared set); symlinked project roots via in-process diagnose() callers (the CLI resolves the root); Windows paths; code_repo_path with ~ expansion; bin/perry-restore-check not used because the scratch copy is not a git repo (restores hand-checked against git show instead); the schema-unreadable fallback with a split code root (author notes it stays state-anchored, out of scope); prefixes containing glob characters (my[app], st[1]) not glob-escaped, same as the base, noted and not failed
proof: —
=== END VERDICT ===
