# TASK-271 + TASK-275 — dispatch record

> Date: 2026-09-16 · Executor: claude-subagent (async, isolated worktree), one round for two rows · Cycle time: 21 min
> Branch: `coding/task-271-275` · Base: `b424dafe` · Tip: `c9788912` · Merged: `7039d365` (`--no-ff`)
> Spec: `evidence/2026-09/TASK-271-275-spec.md` (both rows re-measured by the PMO before dispatch) · Result: `evidence/2026-09/TASK-271-275-result.md` (on the branch)

## What landed

**TASK-271.** The code emits **six** `tracks_source` values, not the four the row counted: `perry-state` emits `store`, `store-default`, `absent`, `unreadable`, `invalid` (at `project.config.tracks_source`, and `project.tracks_source` under `--compact`); `perry-diagnose` emits those five plus `unavailable`, when its work-mode scan fails. `no-track-record` is still defined in `bin/perry-state` and never emitted. Neither payload has a `schema/*-contract.md` page, so each value is documented where the payload is already described — `reference/snapshot.md` step 3b and `reference/diagnose.md` — with what it means and what a reader should do. `tests/test_tracks_source_documented.py` runs both tools over seven store states, parses each page's table, and fails naming any value present on one side only. Its stated limit: a value reachable only on a path none of its projects reaches would go unnoticed.

**TASK-275.** An empty file at a declared canonical store path under the state root counts as Perry's, in the same `if` of `perry-lint § looks_like_perry_state` that TASK-270 added for `.perry/`-anchored claims, with the store list from `parsers.canonical_store_names()`. A non-empty file is still judged by its first record. No schema change — declaring intake's record shape would only have helped a file that has a record, and an empty file has none.

**Two changes beyond the brief, both recorded:** TASK-270's scope test asserted that an empty `tasks.jsonl` is foreign — the exact thing this row widens — and was replaced by one asserting a foreign record is still flagged at both kinds of path. And `test_empty_config_store` gained its `tests/durations.json` entry, whose absence since TASK-270's first merge had left `test_durations_provenance` red on main.

## A PMO verification gap this round exposed

`test_durations_provenance` is a **slow-tier** module. Every merge preview this week ran the full tier only, so from `81d5dda1` (TASK-270 round 1) main's slow tier was red — confirmed by running the module alone on main — and no preview saw it. From this record on, merge previews run the full **and** slow tiers, and every spec asks the agent for both.

## PMO verification

1. **Merge preview against main `9156ea1c`**, merge exit status checked: `MERGE OK`. Then on the merge result: `bash tests/run` → `✓ all green`; `bash tests/run --tier slow` → `✓ all green`; tree guard `✓` on both.
2. **PMO mutation:** `intake` removed from the store set `perry-lint` consults for the empty-file excuse → **red** on four named tests in `tests/test_empty_declared_store.py`, including `test_an_empty_file_at_every_declared_store_is_perrys` and `test_a_foreign_intake_file_is_still_a_collision`.
3. **The agent's four mutations**, each red: an undocumented emitted value (3), a documented value never emitted (2), excusing non-empty files (6 across three modules), reverting the rule (4).

## Architecture review

**PASS.** The excuse covers only files with no non-blank line, at a claim anchored in `.perry/` or listed by `canonical_store_names()`, which reads the schema's claims — no copy in `perry-lint` (NN-1). A foreign empty file does get through, but holds nothing to lose or misread. The replaced scope test and `test_the_criterion_is_the_declaration_not_the_suffix` together still cover what the old one guarded. Documenting on `reference/` pages without a version bump is consistent with § 5, which lists these payloads without a contract page. The parity test genuinely reads both sides: values from running the tools, documented values from the pages' tables, compared both ways.

**Two § 7 candidates:** `tracks_source` is a value consumers act on — a stand-in value means "do not trust the list" — yet it lives in payloads no contract page versions; and the NN-01 ownership rule for empty files depends on path matching that the architecture document does not name.

**Noticed by the agent, not changed:** `perry-diagnose` raises no finding of its own when the config store is `unreadable` or `invalid`, although `perry-state` warns about the same store.
