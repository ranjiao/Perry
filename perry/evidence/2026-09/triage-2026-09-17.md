# Triage — 2026-09-17

Actor: Codex PMO. Source: live perry-task list --all --limit 0 --json and perry-state --json. Read-only census plus the explicit writes below. No implementation, dispatch, close, or verification-rung award.

## Disposition

- TASK-191 — real first-OKR interview: changed not_started to blocked through perry-task. USER-955 is already pending and remains the only unsatisfied prerequisite. No new interview choice was made.
- TASK-270 — empty-config inconsistency: retained review. Read rounds 2–3 dispatch evidence and USER-948/949. Merged fixes and architecture PASS do not erase the explicitly retained external-truncation hazard. SLA remains 5d; arrival remains 2026-08-29.
- TASK-291 — unparseable V4 verdicts: retained not_started and the phase-005 deferral authorized in USER-950. startable=true is dependency readiness, not authority to override that deferral. SLA remains 5d; arrival remains 2026-09-02.
- TASK-436 — journal/reference separation: partial implementation remains in_progress. No registered dispatch and about 141 idle hours are a liveness question, not proof the executor is gone. USER-956 requests ownership confirmation before resetting status; no redispatch.

## Register and contract checks

- Intake: no rows to drain or sweep. Recovery clear; no interrupted pipeline.
- No off-enum or missing statuses, unknown dependencies, dependency cycles, missing ages, or blocked rows whose dependencies are all terminal.
- No cadence records, overdue recurrence, undated recurrence, or unreadable frequency. No recurring-request conversion inferred from the empty intake.
- Queue: three active rows; two SLA breaches (14 and 10 days over). No queue row without an arrival clock. No trend claimed without a comparable prior depth snapshot.
- Tool reports zero event/store drift. No BOARD.md is required; missing projection is not a task-state failure.
- Existing evidence_not_found entries include command strings, symbols, historical file names and commit hashes; they are not automatic evidence-less closures. No historical closure was reopened from this lexical finding alone.
- No new repository-wide quality claim: product tests were not run for this state-only triage. Architecture payload says absent at its resolved location; this does not establish that the code-root architecture is absent.

## Stale task flags

Thresholds: P0 >=3d, P1 >=7d, P2 >=14d from task updated timestamp. Staleness is not an instruction to drop or reprioritize.

| Task | Title | Priority | Idle days |
|---|---|---|---|
| TASK-129 | Agent is five strings that do not join, and role has never once been written | P1 | 13.9 |
| TASK-173 | an Objective is not a record, so it has no durable address | P1 | 13.9 |
| TASK-185 | D009 step 5 — an Objective id survives a rename and a reorder, proved | P1 | 9.9 |
| TASK-186 | D010 step 2 — a spec declares its author, and the escalation scan reports it | P1 | 9.9 |
| TASK-187 | D010 step 3 — a machine-authored spec is fail-closed at the escalation gate | P1 | 9.9 |
| TASK-188 | D010 step 4 — the scout, run by hand on ten real rows and scored against what the PMO actually decided | P1 | 9.9 |
| TASK-189 | D010 step 5 — autopilot becomes the two-stage scout-then-build loop | P1 | 9.9 |
| TASK-204 | Perry has no writer for a migration event, so TASK-180 hand-wrote JSON into an append-only log | P1 | 13.9 |
| TASK-206 | a write returns no seq, so a poll cannot tell a stale read from a fresh one | P1 | 13.9 |
| TASK-207 | no compare-and-set on a write, and the board demonstrably moves between a read and a write | P1 | 13.9 |
| TASK-212 | a locked decision that gets no task row does not ship, and nothing links a design's plan step to the work that discharges it | P1 | 13.9 |
| TASK-218 | thread the closing phase id through every close stage, so no stage re-reads phase/CURRENT | P1 | 9.1 |
| TASK-219 | retro-cites-phase-scores — a cross_file check that the retro cites the scores rather than re-deriving them | P1 | 13.7 |
| TASK-220 | the close-phase router subcommand, over the four unchanged lane subcommands | P1 | 9.1 |
| TASK-221 | a phase close that stopped halfway is visible at the next snapshot, resolved from state | P1 | 13.7 |
| TASK-222 | score-phase's own snapshots trip NS-01, because the names it writes do not match the declared pattern | P2 | 19.8 |
| TASK-224 | linkage-kr-exists fires only on an absent id, so a KR nested under the wrong objective lints clean | P2 | 19.8 |
| TASK-238 | no commit on main may fail to build standalone, and nothing checks it | P2 | 19.0 |
| TASK-240 | an ADR id can be reissued, because perry-decide writes no events and has nothing to retire one with | P1 | 14.9 |
| TASK-242 | linkage-kr-exists proves SOME phase has resolvable overall edges, not that THIS phase does | P2 | 18.5 |
| TASK-252 | a register write honours board rows it was never asked about, and the durable 'somebody has seen this' surface does not exist | P2 | 18.1 |
| TASK-254 | bin/perry-lint hands back 22 commands and every one of them drops the root | P1 | 15.0 |
| TASK-255 | Perry never shell-quotes a path into a command it hands a reader — shlex appears nowhere in bin/ or viewer/ | P1 | 15.0 |
| TASK-265 | Thin perry-state to a query over the six JSONL stores; the payload shape does not change | P1 | 13.7 |
| TASK-266 | Nothing tells an agent a projection is behind its store — ADR-012's rule has no surface | P1 | 13.9 |
| TASK-286 | the Bound that review.md requires is never asked for where specs are written, so its absence costs rounds instead of minutes | P1 | 13.9 |
| TASK-294 | every NS-01 warning Perry emits is about a file Perry itself wrote, and the remedy each one offers is a no-op | P1 | 9.9 |
| TASK-297 | parsers.py:462 says the walk is skipped when handed an exact root, and the code walks four levels anyway | P1 | 13.9 |
| TASK-301 | schema/README.md still tells a consumer to read the State root out of .perry/config.md, which is the render | P1 | 13.9 |
| TASK-309 | a dispatched agent's entire round lives in its context until one final commit, so any interruption costs all of it | P1 | 13.9 |
| TASK-334 | The header-rule guard errors instead of skipping when a file vanishes mid-walk | P1 | 13.1 |
| TASK-349 | ADR-007 census part 2 — the 71,158 lines of tests/ | P1 | 13.0 |
| TASK-352 | Records appended to okr.jsonl are invisible to the render gate that was said to cover them | P1 | 13.0 |
| TASK-355 | Every locked design now reports zero implementation references | P1 | 9.9 |
| TASK-369 | DESIGN-015 stores a field that is derived at read time | P1 | 9.9 |
| TASK-371 | Nothing couples dispatching an agent to marking the row it works on | P1 | 10.1 |
| TASK-372 | perry-restore-check --root cannot verify a git-archive scratch copy — the exact use review-constraints.md recommends | P1 | 10.1 |
| TASK-375 | linkage.jsonl has no uniqueness or mutual-exclusion invariant — a task that is both linked and declared unlinked passes every gate | P1 | 10.1 |
| TASK-378 | Every tool-mediated write is stamped actor: agent, so two concurrent PMO sessions cannot tell their writes apart — and one has already misattributed the other's to a dispatched agent | P1 | 10.0 |
| TASK-380 | main cannot have a green suite through code alone — the one red module asserts Perry's user has answered their questions | P1 | 10.0 |
| TASK-384 | summary is mandatory to write, reported by the linter, published by one payload and not the other, and declared in no schema | P1 | 9.9 |
| TASK-387 | A mitigation that lives in one writer's body was not inherited by the next writer that read the same field | P1 | 9.9 |
| TASK-390 | A design's Linked OKR line is parsed into a field nothing reads, so a design can name a KR that does not exist and every instrument stays green | P1 | 9.9 |
| TASK-391 | A documented flag combination writes a linkage record that permanently reddens the suite, and the writer has no retraction | P1 | 9.8 |
| TASK-393 | The shipped OKR template still mints the retired KR grammar, so a new project starts in the form ADR-017 just removed | P1 | 9.8 |
| TASK-396 | perry-task has no published write contract, so a newly required flag reaches a consumer as a refusal at runtime | P1 | 9.1 |
| TASK-397 | ADR-017 changed an identifier's value space and perry-goals/list stayed at 2.3, so a consumer joining on a stored KR id gets a silent miss | P1 | 9.1 |
| TASK-401 | every bin/ entrypoint is recompiled on every invocation because an extensionless __main__ is never bytecode-cached | P1 | 8.9 |

## Shared unsatisfied dependencies

| Dependency | Title | Dependent open tasks |
|---|---|---|
| TASK-184 | D009 step 4 — okr.objectives[].id is filled from the store and the contract moves to 2.2 | TASK-173, TASK-185 |
| TASK-191 | DESIGN-020 phase C — the SkyTonight interview, scored by the unchanged rubric | TASK-177, TASK-192, TASK-193, TASK-194, TASK-444 |
| TASK-192 | D011 step 3 — routing and smart-skip, by track spine | TASK-177, TASK-465 |
| TASK-193 | D011 step 4 — the escape hatch and the premise challenge | TASK-177, TASK-465 |
| TASK-194 | D011 step 5 — plan-phase uses the same question bank | TASK-177, TASK-465 |
| TASK-264 | DESIGN-022 B — goals writes KR records, their checks and their measurements | TASK-231, TASK-444 |

## Long next actions

Proposed follow-up: move explanatory detail to per-task evidence only after scope agreement; preserve dependency and prior-decision information. The 400-character comparison is a triage heuristic, not a claim the pending enforcement task has shipped.

- TASK-066 — Split perry-task by subcommand group: 1290 characters.
- TASK-173 — an Objective is not a record, so it has no durable address: 473 characters.
- TASK-212 — a locked decision that gets no task row does not ship, and nothing links a design's plan step to the work that discharges it: 969 characters.
- TASK-240 — an ADR id can be reissued, because perry-decide writes no events and has nothing to retire one with: 1702 characters.
- TASK-242 — linkage-kr-exists proves SOME phase has resolvable overall edges, not that THIS phase does: 410 characters.
- TASK-252 — a register write honours board rows it was never asked about, and the durable 'somebody has seen this' surface does not exist: 435 characters.
- TASK-254 — bin/perry-lint hands back 22 commands and every one of them drops the root: 853 characters.
- TASK-255 — Perry never shell-quotes a path into a command it hands a reader — shlex appears nowhere in bin/ or viewer/: 835 characters.
- TASK-282 — a locked design's implementation rows need not cite it, so a finished design reports as never handed off forever: 1065 characters.
- TASK-286 — the Bound that review.md requires is never asked for where specs are written, so its absence costs rounds instead of minutes: 668 characters.
- TASK-287 — nothing warns against writing a cross-reference to a row that has not been minted yet, and the id it guesses may land on the row doing the guessing: 649 characters.
- TASK-294 — every NS-01 warning Perry emits is about a file Perry itself wrote, and the remedy each one offers is a no-op: 1730 characters.
- TASK-297 — parsers.py:462 says the walk is skipped when handed an exact root, and the code walks four levels anyway: 1749 characters.
- TASK-301 — schema/README.md still tells a consumer to read the State root out of .perry/config.md, which is the render: 1272 characters.
- TASK-309 — a dispatched agent's entire round lives in its context until one final commit, so any interruption costs all of it: 692 characters.
- TASK-312 — test_tree_guard.py is now the suite's longest module at 48-124s, and its own fix is what put it there: 811 characters.
- TASK-334 — The header-rule guard errors instead of skipping when a file vanishes mid-walk: 738 characters.
- TASK-340 — Three documents in the tree describe a codebase that no longer exists: 1413 characters.
- TASK-350 — ADR-007 census part 3 — setup and the hook readers: 1189 characters.
- TASK-355 — Every locked design now reports zero implementation references: 1816 characters.
- TASK-371 — Nothing couples dispatching an agent to marking the row it works on: 1672 characters.
- TASK-372 — perry-restore-check --root cannot verify a git-archive scratch copy — the exact use review-constraints.md recommends: 629 characters.
- TASK-374 — One malformed line in linkage.jsonl voids validation of every good record in the file: 641 characters.
- TASK-375 — linkage.jsonl has no uniqueness or mutual-exclusion invariant — a task that is both linked and declared unlinked passes every gate: 1011 characters.
- TASK-378 — Every tool-mediated write is stamped actor: agent, so two concurrent PMO sessions cannot tell their writes apart — and one has already misattributed the other's to a dispatched agent: 2194 characters.
- TASK-380 — main cannot have a green suite through code alone — the one red module asserts Perry's user has answered their questions: 2194 characters.
- TASK-386 — A pipe-written tasks value is a hard lint error and, separately, resolves to nothing in a reader that never consults the linter: 1658 characters.
- TASK-387 — A mitigation that lives in one writer's body was not inherited by the next writer that read the same field: 1847 characters.
- TASK-390 — A design's Linked OKR line is parsed into a field nothing reads, so a design can name a KR that does not exist and every instrument stays green: 2191 characters.
- TASK-391 — A documented flag combination writes a linkage record that permanently reddens the suite, and the writer has no retraction: 2218 characters.
- TASK-393 — The shipped OKR template still mints the retired KR grammar, so a new project starts in the form ADR-017 just removed: 1787 characters.
- TASK-436 — perry-diagnose reads the append-only journal as a live reference, so one mistyped id dirties a project forever: 1147 characters.
- TASK-440 — Next action has four writers and two guards, and the comment beside one guard says there are two writers: 963 characters.

## References to terminal tasks

These need semantic review; references may mean completed prerequisites, historical context, or stale waiting text. No bulk rewrite or automatic unblock.

- TASK-066 — Split perry-task by subcommand group: cites TASK-067 (done).
- TASK-066 — Split perry-task by subcommand group: cites TASK-263 (done).
- TASK-192 — D011 step 3 — routing and smart-skip, by track spine: cites TASK-464 (done).
- TASK-220 — the close-phase router subcommand, over the four unchanged lane subcommands: cites TASK-217 (dropped).
- TASK-231 — a measured KR number has no way into the register that does not break one of its two rules: cites TASK-155 (dropped).
- TASK-240 — an ADR id can be reissued, because perry-decide writes no events and has nothing to retire one with: cites TASK-235 (done).
- TASK-242 — linkage-kr-exists proves SOME phase has resolvable overall edges, not that THIS phase does: cites TASK-157 (done).
- TASK-252 — a register write honours board rows it was never asked about, and the durable 'somebody has seen this' surface does not exist: cites TASK-243 (done).
- TASK-254 — bin/perry-lint hands back 22 commands and every one of them drops the root: cites TASK-247 (done).
- TASK-254 — bin/perry-lint hands back 22 commands and every one of them drops the root: cites TASK-261 (done).
- TASK-255 — Perry never shell-quotes a path into a command it hands a reader — shlex appears nowhere in bin/ or viewer/: cites TASK-261 (done).
- TASK-282 — a locked design's implementation rows need not cite it, so a finished design reports as never handed off forever: cites TASK-001 (done).
- TASK-286 — the Bound that review.md requires is never asked for where specs are written, so its absence costs rounds instead of minutes: cites TASK-050 (done).
- TASK-286 — the Bound that review.md requires is never asked for where specs are written, so its absence costs rounds instead of minutes: cites TASK-095 (done).
- TASK-286 — the Bound that review.md requires is never asked for where specs are written, so its absence costs rounds instead of minutes: cites TASK-247 (done).
- TASK-287 — nothing warns against writing a cross-reference to a row that has not been minted yet, and the id it guesses may land on the row doing the guessing: cites TASK-279 (done).
- TASK-287 — nothing warns against writing a cross-reference to a row that has not been minted yet, and the id it guesses may land on the row doing the guessing: cites TASK-280 (done).
- TASK-297 — parsers.py:462 says the walk is skipped when handed an exact root, and the code walks four levels anyway: cites TASK-292 (done).
- TASK-309 — a dispatched agent's entire round lives in its context until one final commit, so any interruption costs all of it: cites TASK-155 (dropped).
- TASK-312 — test_tree_guard.py is now the suite's longest module at 48-124s, and its own fix is what put it there: cites TASK-244 (done).
- TASK-312 — test_tree_guard.py is now the suite's longest module at 48-124s, and its own fix is what put it there: cites TASK-258 (done).
- TASK-334 — The header-rule guard errors instead of skipping when a file vanishes mid-walk: cites TASK-283 (done).
- TASK-334 — The header-rule guard errors instead of skipping when a file vanishes mid-walk: cites TASK-326 (dropped).
- TASK-334 — The header-rule guard errors instead of skipping when a file vanishes mid-walk: cites TASK-341 (done).
- TASK-340 — Three documents in the tree describe a codebase that no longer exists: cites TASK-067 (done).
- TASK-340 — Three documents in the tree describe a codebase that no longer exists: cites TASK-263 (done).
- TASK-350 — ADR-007 census part 3 — setup and the hook readers: cites TASK-339 (done).
- TASK-350 — ADR-007 census part 3 — setup and the hook readers: cites TASK-348 (done).
- TASK-355 — Every locked design now reports zero implementation references: cites TASK-367 (done).
- TASK-371 — Nothing couples dispatching an agent to marking the row it works on: cites TASK-278 (done).
- TASK-372 — perry-restore-check --root cannot verify a git-archive scratch copy — the exact use review-constraints.md recommends: cites TASK-276 (done).
- TASK-374 — One malformed line in linkage.jsonl voids validation of every good record in the file: cites TASK-276 (done).
- TASK-374 — One malformed line in linkage.jsonl voids validation of every good record in the file: cites TASK-281 (done).
- TASK-375 — linkage.jsonl has no uniqueness or mutual-exclusion invariant — a task that is both linked and declared unlinked passes every gate: cites TASK-277 (done).
- TASK-375 — linkage.jsonl has no uniqueness or mutual-exclusion invariant — a task that is both linked and declared unlinked passes every gate: cites TASK-281 (done).
- TASK-378 — Every tool-mediated write is stamped actor: agent, so two concurrent PMO sessions cannot tell their writes apart — and one has already misattributed the other's to a dispatched agent: cites TASK-278 (done).
- TASK-380 — main cannot have a green suite through code alone — the one red module asserts Perry's user has answered their questions: cites TASK-276 (done).
- TASK-380 — main cannot have a green suite through code alone — the one red module asserts Perry's user has answered their questions: cites TASK-277 (done).
- TASK-386 — A pipe-written tasks value is a hard lint error and, separately, resolves to nothing in a reader that never consults the linter: cites TASK-278 (done).
- TASK-387 — A mitigation that lives in one writer's body was not inherited by the next writer that read the same field: cites TASK-155 (dropped).
- TASK-387 — A mitigation that lives in one writer's body was not inherited by the next writer that read the same field: cites TASK-277 (done).
- TASK-391 — A documented flag combination writes a linkage record that permanently reddens the suite, and the writer has no retraction: cites TASK-279 (done).
- TASK-391 — A documented flag combination writes a linkage record that permanently reddens the suite, and the writer has no retraction: cites TASK-383 (dropped).
- TASK-440 — Next action has four writers and two guards, and the comment beside one guard says there are two writers: cites TASK-040 (done).
- TASK-440 — Next action has four writers and two guards, and the comment beside one guard says there are two writers: cites TASK-382 (done).
- TASK-440 — Next action has four writers and two guards, and the comment beside one guard says there are two writers: cites TASK-431 (done).

## Remaining scope

Agent-owned not_started rows remain backlog flags, not dispatch authorization. Deferred work stays deferred. Broad stale-row reprioritization, detailed historical-evidence reconciliation, recurrence trend reconstruction and ownership-dependent transitions remain unapproved follow-up; this report does not certify them resolved.
