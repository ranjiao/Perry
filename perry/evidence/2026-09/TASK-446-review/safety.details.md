# Safety interruption details
Source: explicitly synthetic typed `safety.fixture.json`, recovery gate only.
recovery.blocking = true; pending_transactions = []; malformed_dossiers contains exactly /fixture/.perry/adopt/run.md with error “invalid stage”.
No normal dashboard, compact capture, next selector or interrupted-state read is permitted beyond this gate. Do not infer a healthy phase, task count, pending count or recommendation. Repair must follow the existing startup procedure; this example performs no repair and authorizes no resume. If the later interrupted gate finds a run, its existing card and explicit choice remain mandatory.
