# TASK-448 — dispatch record

> Date: 2026-09-15 · Executor: claude-subagent (async, isolated worktree) · Cycle time: 27 min (agent-reported)
> Branch: `coding/task-448-covers-and-replay` · Base: `bb178072` · Tip: `e76cb084` · **Not merged** (see § Merge)
> Spec: `evidence/2026-09/TASK-448-spec.md` · Result: `evidence/2026-09/TASK-448-result.md` · Replay: `evidence/2026-09/TASK-448-replay.md`

## Agent RESULT (verbatim)

```
=== RESULT ===
Branch: coding/task-448-covers-and-replay
PR URL: n/a — push is escalated on this project
Files changed: 149
  - tests/selection.py (new)
  - tests/test_selection.py (new)
  - tests/run
  - tests/durations.json
  - tests/test_*.py (143 modules — COVERS declaration, plus `from selection import ALL` in one)
  - perry/evidence/2026-09/TASK-448-replay.md
  - perry/evidence/2026-09/TASK-448-result.md
Tests: 3958/3968 (command: env -u PERRY_PROJECT -u PERRY_HOME bash tests/run, on e76cb084) — the same 2 modules / 10 tests red as at base
Cycle time: 27
Notes: Base check: HEAD 0b5bf99e was a strict ancestor of bb178072 with a clean tree; fast-forwarded, then created the branch. Read ARCHITECTURE.md in full, the TASK-448 spec in full, and DESIGN-021 in full.
  Baseline at bb178072: 140 modules / 3934 tests, 2 modules and 10 tests red:
  - test_md_store: 9 failures, including TestThisRepositoryIsReproducedByteForByte.test_okr
  - test_okr_krs_render: 1 failure, TestTheShippedOkr.test_the_shipped_okr_md_carries_no_kr_table_rows
  Both reproduce when run alone. Both assert against the live perry/OKR.md and okr.jsonl that 15369956 (OKR v4) rewrote, so they are not from this change. The final run has exactly the same reds and nothing else; the tree guard is green.
  python3 -m unittest tests.test_selection: 34 tests, OK. All three mutations went red on named tests; none stayed green.
  Replay verdict: FAIL. Median share 100.0%, mean 75.1%, 30 of 50 merges full. Reasons: tests/ helper 27, schema/ 10, bin/lib/ 9. Counterfactual with durations.json treated as covered: median 76.0%.
  Dry run `--tier affected --base HEAD~1` exits 0 and runs no test. At 3da8ad1e it widens to the full suite on tests/durations.json; at e76cb084 it selects 32 modules (33.7%). Both outputs are quoted in the result.
  Unusual: selection.py was briefly in tests/ during the baseline run (moved out, guard green); the scratch path was spelled out because the harness refused `$(git …)`; one durations.json entry added for the new module.
=== END RESULT ===
=== ARCHITECTURE COMPLIANCE ===
Touched sections: §2 (tests/), §3 Forbidden (second reader), §6 NN-4, §6 NN-5
Compliance check:
- §2 tests/: the change adds a helper and a module inside tests/ and declarations in test modules; no component's ownership moves, though the "136 modules" count in §2 and §3 is now stale at 144 (not edited, out of scope).
- §3 Forbidden "a second reader of any state file": tests/selection.py reads durations.json through tests/parallel § load_durations rather than parsing it again, and reads no Perry state file.
- §6 NN-4: the selector makes only mechanical judgements (string-prefix matching, a fixed widening list, an ast literal read); what each module covers was decided by an agent and written as a declaration, not inferred by code.
- §6 NN-5: the dry run runs inside tests/run's tree-guard snapshot and EXIT trap and writes nothing; the replay only reads git from the checkout; mutation copies lived under $PERRY_SCRATCH; tree_guard is green on every run.
New §7 questions opened:
- The replay fails DESIGN-021's gate because 40 modules assert against this repository's own live perry/ state, which nearly every merge changes. Should those modules move to frozen or pinned copies, or should selection treat perry/evidence/ differently? This is a redesign of § 5.2, and phase A's gate hands it to the user. — recommended USER-id
=== END COMPLIANCE ===
```

## Objective verification, run by the PMO

1. **Base and scope.** `git merge-base` with main → `bb178072`. 149 files, 2,043 insertions, 1 deletion. Outside `tests/test_*.py` the branch changes only `tests/selection.py`, `tests/run`, `tests/durations.json` and the two TASK-448 evidence files — nothing in `bin/`, `viewer/`, `schema/` or the lanes. In the 143 modules, `git diff -U0` shows no added or removed line other than the `COVERS` tuple and, in one module, `from selection import ALL` with its comment.
2. **`tests/run`.** The new code runs only when `$1 = --tier`, refuses every spelling but `--tier affected --base <ref> --dry-run` with exit 2, and exits inside the tree guard. The only other hunk is the banner branch, also `--tier`-only. Bare `tests/run`, `--lint`, `--serial`, `--only`, `--slow` are unchanged.
3. **Full suite on the tip, re-run by the PMO** in the agent's worktree (HEAD `e76cb084`, clean), `env -u PERRY_PROJECT -u PERRY_HOME bash tests/run`:
   `✗ test_md_store.py — 9 of 76` · `✗ test_okr_krs_render.py — 1 of 40` · `✗ 2 of 141 MODULE(S) red` · `✗ 10 of 3968 TEST(S) failed` · tree guard `✓ nothing … moved`. Identical to the base reds.
4. **PMO mutations**, each on a `git archive` copy of the branch, `python3 -m unittest tests.test_selection`:

   | Mutation | Result |
   |---|---|
   | new `tests/test_zz_pmo_mutation.py` with no `COVERS` | **red**: `test_no_module_is_without_covers` — `['test_zz_pmo_mutation.py'] != []` |
   | `if path.startswith("schema/"):` → `if False and …` in `tests/selection.py` | **red**: `test_a_path_under_schema_selects_the_full_suite` — the path came back `unmatched` |

5. **Replay report** has its 50 rows, the median and the verdict (agent-verified; the PMO read the verdict line and reasons).

## Architecture review

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-15

PASS

**What I checked:** The diff touches only `tests/` and two TASK-448 evidence files, with no change to `bin/`, `viewer/`, `schema/`, the lanes or `ARCHITECTURE.md`. The new `tests/run` code runs only when the first argument is `--tier`, so bare `tests/run`, `--lint`, `--serial`, `--only`, `--slow` and `tests/parallel` run the same tests as before. The only other `tests/run` change adds a banner branch that also fires only for `--tier`. Across the 143 modules the only lines added are a `COVERS` tuple and, in `test_blank_cell_is_one_rule.py` alone, `from selection import ALL` plus a comment saying why; no module lost a line.

**Against the architecture document:**
- **§3, second reader:** `tests/selection.py` reads no Perry state file. It reads `git diff`/`git log` output, reads `COVERS` from module sources with `ast`, and gets durations by loading `tests/parallel` and calling its `load_durations`. That file's command-line runner only starts under `if __name__ == "__main__"` (line 896), so loading it runs nothing.
- **§2 ownership:** no component takes on new state.
- **§5 contracts:** nothing in the `bin/`, payload or schema contracts is touched.
- **NN-4:** it applies to `bin/`, and the selector's decisions are prefix matches and fixed widening rules anyway.
- **NN-5:** the dry run happens after `tree_guard`'s snapshot and under the EXIT trap. The replay only reads git. `TestTheRunEntry` works in a throwaway temporary repository. The one new side effect, `__pycache__` from importing `selection`, is excluded by `tree_guard` (`IGNORE_DIRS`).
- **§7:** the question about 40 modules asserting against the repository's live `perry/` state is a DESIGN-021 §5.2 design question. The result evidence (§5, finding 1) hands it to the user through phase A's gate. The architecture document is silent here, and NN-6 lets an agent add to §7 but does not require it.

**Not a violation:** §2 and the §3 diagram still say "136 modules" while the tree has 144. That is descriptive drift an agent may fix under NN-6. The result evidence (finding 3) records it as left for a later update.

## The gate outcome

The round did what it was asked; the thing it measured failed. `DESIGN-021 § 6`
phase A: the replay median must be ≤ 50% of module-seconds before `TASK-449`
touches any brief. **Measured 100.0%** (mean 75.1%, 30 of 50 merges widen to the
full suite; with `durations.json` treated as covered, 76.0%). The floor is the
40 modules that read this repository's own `perry/` state: an evidence-only
merge still selects 32 modules (33.7%), and `perry/evidence/` changes in 46 of
50 merges. `TASK-449` does not start until § 5.2 is redesigned — a user decision.

## Merge

Held for the user, for two reasons:

1. **Whether to land phase A's machinery before § 5.2 is redesigned.** Merging
   puts a `COVERS` obligation on every future test module (a guard reddens
   without one) for a selector whose rules are about to change.
2. **Integration with main.** Main is at `ba11da7a` (TASK-453 merged). A merge
   conflicts in `tests/durations.json`, and TASK-453's
   `tests/test_architecture_rules.py` (and, once merged, TASK-442's
   `tests/test_next_section.py`) carry no `COVERS`, so
   `test_no_module_is_without_covers` would redden. Both belong to the agent's
   branch: it merges main, resolves the file and declares the new modules, and
   the PMO re-verifies the merge result.

## Subjective verification

(none — the spec declares none)
