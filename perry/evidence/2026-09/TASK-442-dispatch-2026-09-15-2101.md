# TASK-442 — dispatch record

> Date: 2026-09-15 · Executor: claude-subagent (async, isolated worktree) · Cycle time: 47 min (agent-reported)
> Branch: `coding/task-442-next-section` · Base: `bb178072` · Tip: `384f27ac` · **Not merged: architecture review FAIL, re-dispatched** (see § Re-dispatch)
> Spec: `evidence/2026-09/TASK-442-spec.md` · Result: `evidence/2026-09/TASK-442-result.md`

> **Open-question ids in the quoted blocks below are written as `§ 7 (n)`.**
> `ARCHITECTURE.md` (§ 1's note) says an open question is referred to by its
> SECTION and never by its id, because `OQ-` is not a citation family and
> `perry-diagnose` reports such a reference as dangling. The agents' and the
> reviews' words are otherwise verbatim.

## Agent RESULT (verbatim)

```
=== RESULT ===
Branch: coding/task-442-next-section
PR URL: n/a — push is escalated on this project
Files changed: 17
  - SKILL.md
  - bin/perry-state
  - decide/SKILL.md
  - goals/SKILL.md
  - perry/evidence/2026-09/TASK-442-result.md
  - reference/next-rules.json
  - reference/next.md
  - reference/snapshot.md
  - schema/README.md
  - schema/next-contract.md
  - tests/durations.json
  - tests/fixtures/contract-key-parity.json
  - tests/fixtures/shipped-semantics.json
  - tests/test_contract_key_parity.py
  - tests/test_next_section.py
  - tests/test_semantics_on_every_payload.py
  - work/SKILL.md
Tests: 3964/3974 (command: bash tests/run, with PERRY_PROJECT and PERRY_HOME unset)
Cycle time: 47
Notes: Base check: HEAD 0b5bf99e was a strict ancestor of bb178072 on a clean tree; fast-forwarded and branched there. Read ARCHITECTURE.md, bin/ARCHITECTURE.md, the spec, DESIGN-020 and USER-934 in full first. Baseline reds at bb178072: test_md_store 9/76 and test_okr_krs_render 1/40, identical when re-run alone; the final run's 10 failing test ids match them exactly, and the tree guard reports nothing moved. Always unknown: week.planned, drafts.drafted (TASK-444) and commitments.due (not computed). Out of scope: test_contract_key_parity.py and test_semantics_on_every_payload.py plus two fixture baselines, in their own commit cbd1ea8b. The "alternate repeats a command" mutation was green until a test was added. The first full run caught --help over its 3,000-byte cap (fixed in bf9a1d9a). Commits: 1791a8c3, 00fc5a5c, cbd1ea8b, 1970b6c2, bf9a1d9a, 384f27ac.
=== END RESULT ===
=== ARCHITECTURE COMPLIANCE ===
Touched sections: ARCHITECTURE.md §2 (bin/, schema/, lanes), §3 Forbidden, §4 read path, §5 perry-state --json/--compact contract, §6 NN-1, NN-4, NN-5, NN-6; bin/ARCHITECTURE.md §5 SURFACE, §6 NN-B1, NN-B2, NN-B4
Compliance check:
- §2 bin/: perry-state computes every number in the block and the lanes only render it, so lanes still own procedure and bin/ owns figures.
- §2 schema/: one contract page was added, which leaves the §2 sentence "Seven contract pages" stale; that descriptive line in ARCHITECTURE.md was not updated because the file is outside this row's scope.
- §3 Forbidden: build_next reads only the payload build() returns plus the clock and the rule file, and a test shows the block is unchanged after the project is deleted, so there is no second reader of state; the lane step-5 lines now call the tool instead of counting.
- §4: the read path gains one more section of the single perry-state entry point, and nothing on the write path changes.
- §5 perry-state contract: next is additive on both branches and COMPACT is untouched, with a test asserting --compact carries no next key.
- §6 NN-1: facts come only from values perry-state already computes, and anything needing a new parse (week.planned, drafts.drafted, commitments.due) is emitted as unknown.
- §6 NN-4: predicates are typed comparisons over fact paths, a kind mismatch is a rule error rather than a coercion, and no rule reads prose; the "a target of 0 is met only at 0" convention uses numbers only.
- §6 NN-5: every test builds its project under a temporary root, every mutation ran on a scratch copy outside the repository, and the tree guard reports nothing moved.
- §6 NN-6: ARCHITECTURE.md was not edited.
- bin §5 / NN-B2: --after and --lane are declared in SURFACE, and are refused with exit 2 when used without --section next or with a lane outside the three.
- bin NN-B1 / NN-B4: --help still prints before any read, and it was cut back to 2,924 bytes under test_bin_surface's cap.
New §7 questions opened:
- Should a key-result record carry a typed direction (up or down), so "met" stops relying on the target-0 convention? — recommended USER-id
- For TASK-443, may perry-state accept --compact together with --section next, or should the closing step drop --compact? — recommended USER-id
- Should the contract registry (CONTRACT_ID in tests/contract_key_parity.py, and the §2 page count) recognise contract families that are not /list? — recommended USER-id
=== END COMPLIANCE ===
```

## Objective verification, run by the PMO

1. **Base and scope.** `git merge-base` with main → `bb178072`. 17 files, 2,244 insertions, 46 deletions. Four paths are outside the spec's `Files in scope`: `tests/test_contract_key_parity.py`, `tests/test_semantics_on_every_payload.py`, `tests/fixtures/contract-key-parity.json`, `tests/fixtures/shipped-semantics.json`, all in commit `cbd1ea8b`. The edits make the two registries accept a contract that is not `perry-<x>/list` (`perry-next/1.0`, page `next-contract.md`) and add that one entry to each fixture. None touches the spec's forbidden surfaces (`schema/state-schema.json`, existing `perry-state` keys, `--compact`, the stores). **Accepting them is the user's call; the PMO recommends accepting** — the spec asked for `schema/next-contract.md`, and without these edits it reddens two existing tests.
2. **Full suite on the tip, re-run by the PMO** in the agent's worktree (HEAD `384f27ac`, clean), `env -u PERRY_PROJECT -u PERRY_HOME bash tests/run`:
   `✗ test_md_store.py — 9 of 76` · `✗ test_okr_krs_render.py — 1 of 40` · `✗ 2 of 141 MODULE(S) red` · `✗ 10 of 3974 TEST(S) failed` · tree guard `✓ nothing … moved`. Identical to the base reds.
3. **Named modules alone** in the worktree: `tests.test_next_section tests.test_contract_key_parity tests.test_semantics_on_every_payload tests.test_bin_surface` → `Ran 158 tests … OK`. On a `git archive` copy: `tests.test_router_budget` → `OK` (verification 4).
4. **`perry-state --section next` on a copy of the branch** (verification 5): `contract perry-next/1.0`; primary `R-sla-breach` → `/perry work triage` ("5 queue item(s) are past their SLA, the longest-waiting being TASK-270"); alternates `R-review-due` → `/perry work friday-review`, `R-handoff-stale` → `/perry work handoff`; `unknown` names `drafts.drafted`, `phase.kr_progress.met_ratio`, `week.planned`, `commitments.due`.
5. **PMO mutations** on `git archive` copies, `python3 -m unittest tests.test_next_section`:

   | Mutation to `reference/next-rules.json` | Result |
   |---|---|
   | swap the order of `R-no-okr` and `R-no-phase` | **red**, 2: `test_every_rule_id_is_explained_on_the_page_and_nothing_else_is`, `test_the_bound_three_overlays_ten_rules_and_the_replacements` |
   | `R-no-okr`'s `command` `/perry goals init` → `/perry work triage` | **green — a finding** |

   **Finding:** no test pins the command a rule recommends. The fixtures assert which rule fires, not what the user is told to run, so a rule can recommend the wrong command with the suite green. Sent back with the re-dispatch.
6. **Merge preview.** `git merge-tree` against main `ba11da7a` conflicts in `tests/durations.json` only.

## Architecture review

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-15

FAIL: ARCHITECTURE.md §2 (ownership). Also the rule in the file's header that a change contradicting a section stops and asks the user.

**The issue.** §2 gives the decision about what to do next to the lanes and explicitly denies it to `bin/`. This diff moves that decision into `bin/perry-state` and forbids the lanes from changing it. The architecture documents were not updated, and no §7 question records the move. The attestation covers this with "lanes still own procedure and bin/ owns figures". That skips the §2 line that actually conflicts.

**What the architecture documents say, on the branch:**
- `ARCHITECTURE.md` §2, `bin/`: "**Doesn't own**: what to do next. The `SKILL.md` files decide; these tools execute and refuse."
- `ARCHITECTURE.md` §2, lanes: "**Owns**: procedure — what an agent does, in what order".
- `bin/ARCHITECTURE.md` §1: "It does not decide what to do."

**What the diff says:**
- `bin/perry-state § build_next` docstring: "The `next` block: the command decides, the agent renders it."
  - It picks `primary` and up to two `alternates` from the 17 declared rules in `reference/next-rules.json`.
- `reference/next.md`: "`perry-state --section next` decides what Perry recommends. The agent puts that answer into words and never chooses it."
- `schema/next-contract.md`: "**The command decides and the renderer does not.**"
- `goals/SKILL.md` step 5, `work/SKILL.md` step 7 and `decide/SKILL.md` step 5 each now read "The command decides; never add, drop or reorder a recommendation." Each one replaces a list the lane used to judge for itself.

**To pass:**
1. **Record the new ownership.** The §2 and `bin/ARCHITECTURE.md` §1 lines are descriptive, so NN-6 lets the agent edit them.
   - In `ARCHITECTURE.md` §2, `bin/` owns the next-step recommendation (evaluated by `perry-state` from the declared rule table), and the lanes render it.
   - Make the matching change to the "does not decide what to do" sentence in `bin/ARCHITECTURE.md` §1.
   - Cite the user decision that authorises the move: DESIGN-020 § 9, the 2026-09-15 entry. If that entry is not user-confirmed, ask the user before merging.
2. **Fix the other descriptive lines this diff makes stale in the same edit:**
   - §2 `schema/`: "Seven contract pages" is now eight.
   - §2 `reference/`: it is described as "reference pages loaded on demand", and it now also holds a rule table that `bin/` evaluates.
   - §4 read path: "`--section <name>` → one key, in full". With `--after` or `--lane`, `--section next` now prints a narrowed block that differs from the `next` key in `--json`.
3. **Add these to §7, marked `proposed`:**
   - For TASK-443: should `--compact` be allowed together with `--section next`? The two are refused together today.
   - Should the contract registry (`CONTRACT_ID`) recognise contract families that are not `/list`?
   - May `--section <name>` print something other than the payload's key for that name?

**Checked and not violated:**
- **No second reader (§3, NN-1).** `build_next` reads only the payload, the clock, the shipped rule file and `schema/state-schema.json`. `perry-state` already read that schema at line 1620. "State changed today" is `history.latest_journal_days`, which `build()` already computes from `snap.journal`; no journal file is opened again.
- **COMPACT untouched (§5).** `COMPACT` itself is unchanged, and `--compact` together with `--section` was already refused before this diff.
- **Rules read no prose (NN-4).** Predicates are typed comparisons, and a kind mismatch is reported as a rule error.
- **Tests (NN-5).** They build their projects under `tempfile.mkdtemp`.
- **New flags (NN-B1, NN-B2).** `--after` and `--lane` are declared in `SURFACE`, and a misuse exits 2.

## Re-dispatch

The authorising decision exists and is the user's: the 2026-09-15 `DESIGN-020 § 9`
entry records that the next step comes from a deterministic command and the agent
only renders it, confirmed by Ran Jiao in session before hand-off. So the fix is
the descriptive edits the review lists — which NN-6 lets an agent make — not a
design change. Sent back to the same agent on 2026-09-15 with: the review's three
items; a test pinning each rule's recommended command; merging main (`ba11da7a`)
into the branch and resolving `tests/durations.json`; a fresh full suite.

## Subjective verification

- [user-verify] Do the `reason` strings in the sample output (verification 4) read as plain
  language a user would act on? **Accepted as written, Ran Jiao, 2026-09-16 (`USER-941`)** —
  the three strings quoted in round 1's verification 4 read plainly; no change asked for.

## Round 2 — 2026-09-15

> Agent tip `8c779cca` (merges main `ba11da7a` at `9a3b7c10`; `905fb1bf` pins fixture commands; `81b6c43d` edits the architecture documents; `8c779cca` records round 2). Cycle time 22 min (69 total). `USER-938` accepted the round-1 out-of-scope test and fixture edits.

### PMO verification

1. **Base.** `main` (`ba11da7a`) is an ancestor of the tip; `git diff --stat main coding/task-442-next-section`: 19 files, 2,435 insertions, 59 deletions. `ARCHITECTURE.md` hunks fall at main lines 64–98 (§ 2), 172–176 (§ 4) and 280–283 (§ 7, § 8); none inside § 6 (lines 206–262).
2. **Full suite on the tip, re-run by the PMO** (`8c779cca`, clean worktree, env unset): `✗ test_diagnose.py — 1 of 158` · `✗ test_md_store.py — 8 of 76` · `✗ 2 of 142 MODULE(S) red` · `✗ 9 of 4008 TEST(S) failed` · tree guard `✓`.
   - `test_md_store` 8: the base reds on main, `TASK-459`.
   - **`test_diagnose` 1 is the branch's.** `TestUserLoadFindings.test_perry_itself_passes_its_own_id_checks`: `['DESIGN-022', '§ 7 (4)', '§ 7 (5)', '§ 7 (6)', 'USER-937'] != []`. Red alone in the agent's worktree; **green alone on a `git archive` copy of main `ba11da7a`** (158 tests OK). `DESIGN-022` and `USER-937` are cited in `TASK-442-result.md:42`, and both exist only uncommitted in the primary checkout — the PMO's round-2 brief named them. `§ 7 (4)`–`§ 7 (6)` are the new § 7 entries. The agent's RESULT reported `test_md_store` as the only red; its run was on `81b6c43d`, before the result-file commit.
3. **PMO mutations** on `git archive` copies of the tip, `tests.test_next_section`: `R-no-okr` command → `/perry work triage`: **red** (2, `TestEachFixtureOffersItsCommand`); `R-sla-breach` command → `/perry work nudge`: **red** (2, fixture `queue_track`). The agent's own finding stands: commands are pinned only for the five fixture primaries.

### Architecture review, round 2

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-15

FAIL: ARCHITECTURE.md §2 (the lanes component: "Owns" and "Doesn't own")

Round 1's items are fixed. Both documents now record the move with a DESIGN-020 § 9 citation. The stale lines are corrected: eight contract pages, the rule table in `reference/`, and the narrowed read path in §4. § 7 (4), § 7 (5) and § 7 (6) are in §7, marked Proposed. The diff leaves §1, §3's Forbidden lines, §5 and §6 of `ARCHITECTURE.md` untouched. In `bin/ARCHITECTURE.md` only §1, §2 and §8 changed, and none of those are user-confirmed there. Neither new §8 entry claims a confirmation it lacks: both are labelled descriptive, and DESIGN-020 § 9 (2026-09-15) does record the user's request. `build_next` works only from the payload `build()` returns, the clock, `reference/next-rules.json` and the schema's thresholds block. It opens no state file, so NN-1 and §3 hold. `next` is additive on both payload branches and `COMPACT` is unchanged.

**The issue.** The new §2 wording says something the branch does not do. It says a lane "never reorders, adds or drops a recommendation" and does not own "which step to recommend". `goals/SKILL.md` is in this diff, and on the branch it still tells the lane to make recommendations of its own:

- Line 104, the context line directly above the step 5 the diff rewrote: "If no current phase exists: render only the overall OKR block, then suggest `/okr plan-phase <slug>`." That is a lane adding a recommendation outside `--section next`.
- Lines 42–43 still describe two automatic prompts as lane behaviour:
  - "**KR-progress prompt**: when ≥80% of commit KRs are achieved, OKR standup suggests `score-phase` and starting the next." The rule table's `R-phase-closable` recommends `/perry work end-phase-retro` instead.
  - "**Heartbeat prompt**: … OKR standup suggests `/okr snapshot`." No rule in `reference/next-rules.json` and no entry in `NEXT_FACTS` covers a snapshot heartbeat.

The diff removed the step 5 text that fired the heartbeat (`- **Heartbeat prompt** (auto): if days-since-last-snapshot ≥ phase_heartbeat_days …`). No rule replaced it and nothing records the drop. So one of two things is true, and both contradict §2:
- the goals lane still owns recommendations (lines 42–43, 104);
- or the move silently dropped a recommendation the lane used to make, and §2 describes lanes as unable to drop one.

**To pass:**
1. Make `goals/SKILL.md` match §2:
   - Line 104: remove "then suggest `/okr plan-phase <slug>`", since `R-no-phase` already recommends it through the next block.
   - Lines 42–43: say both prompts now come from `perry-state --section next` and name the rules, or delete the prompts.
2. Decide the heartbeat prompt explicitly:
   - either add a rule, and its fact, to `reference/next-rules.json` (explained in `reference/next.md`);
   - or record it as dropped: a *Proposed* §7 question ("does the phase heartbeat survive the move to `--section next`?") plus a line in the §8 TASK-442 entry.
3. Optional: the router's first-time setup (`SKILL.md:172`, "Recommend the order — `/perry goals init` → …") also recommends steps without the command. Either say in §2 that first-time setup is outside the next block, where `R-setup` stops at `/perry`, or add it to the §7 question.

### Round 3

Re-dispatched to the same agent: the review's items, with the PMO's default for
the heartbeat — keep it as a rule if `perry-state` already computes the fact,
otherwise declare the rule with its fact unknown and add the *Proposed* § 7
question, so nothing is dropped silently; the `test_diagnose` red (no citation of
an id that does not exist on the branch; § 7 ids resolved the way `§ 7 (1)`–`§ 7 (3)`
are); a fresh full suite on the final commit.

## Round 3 — 2026-09-15

> Agent tip `58c73b86` (`08929751` result file cites no dangling id; `eacba06c` goals-lane prompts point at rules, `R-phase-heartbeat` declared with its fact unknown; `9db8d934` § 2 first-time-setup sentence, § 7 heartbeat question, § 8; `58c73b86` round-3 result). Cycle time 20 min (89 total).

### PMO verification

1. **Base.** `main` (`ba11da7a`) is an ancestor; 19 files, 2,619 insertions, 63 deletions against main. `ARCHITECTURE.md` hunks at main lines 64–98 (§ 2), 172–176 (§ 4), 280–283 (§ 7/§ 8); none in § 5 (177–205) or § 6 (206–262). Router `SKILL.md` 20,457 bytes on both sides.
2. **Full suite on the tip, re-run by the PMO** (`58c73b86`, clean worktree, env unset): `✗ test_md_store.py — 8 of 76` · `✗ 1 of 142 MODULE(S) red` · `✗ 8 of 4009 TEST(S) failed` · tree guard `✓`. Only the base reds `TASK-459` fixes. `test_diagnose` alone on a `git archive` copy of the tip: 158 tests OK.
3. **PMO mutation:** `R-phase-heartbeat` deleted from `reference/next-rules.json` on a copy → **red** (1 error, 2 failures: the heartbeat test, rule/page parity, the rule-file bound).

### Architecture review, round 3

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-15

FAIL: ARCHITECTURE.md §2, `SKILL.md` + `goals/` `work/` `decide/` — the lanes

The heartbeat is fine and §1, §3, §5 and §6 are unchanged against main. The FAIL is lane text outside the diff hunks.

**What was verified.**
- **§1, §3, §5, §6:** the section text on the branch is identical to main. `ARCHITECTURE.md` changes only in §2, §4, §7 and §8.
- **No second reader:** `build_next` reads only the payload, the clock, `reference/next-rules.json` and the schema thresholds.
- **Heartbeat, NN-1:** no payload value dates the last snapshot, and `perry-state` never lists `phase/snapshots/`. `R-phase-heartbeat` reports `phase.days_since_snapshot` as unknown instead of reading it.
- **Heartbeat, NN-4:** its predicate is a typed `ge` against `{"threshold": "phase_heartbeat_days"}`.
- **Heartbeat, records:** §7 § 7 (7) and the §8 entry say plainly that the rule never fires.
- **Round-2 items:** `goals/SKILL.md` lines 42–43 and 104 are fixed. The first-time setup carve-out is stated in §2.

**The issue.** The new §2 says a lane "never reorders, adds or drops a recommendation" and does not own "which step to recommend". The round-2 fix was to make `goals/SKILL.md` match that. On the branch, the lane standups still tell the agent to name a step of its own in the TL;DR. The TL;DR is rendered at step 4, before the next block at step 5, so it is a recommendation the command did not return:

- **`goals/SKILL.md:77`:** `TL;DR: No current phase — run /okr plan-phase to set the next tactical commitment.` This is the same recommendation, for the same state, that round 2 required removed at line 104. It survives one screen up. It can also contradict the block: `R-draft-waiting` (`/perry plan`) and the `R-recovery` / `R-interrupted` overlays all rank above `R-no-phase`.
- **`goals/SKILL.md:76`:** `TL;DR: Phase #002 commit KRs hit 80% — time to score and start the next.` This is the score-first order round 2 flagged. `R-phase-closable` recommends `/perry work end-phase-retro` first.
- **`decide/SKILL.md:52–53`:** `TL;DR: DESIGN-002 is locked but has no implementation tasks yet — run handoff.` and `TL;DR: DESIGN-003 has 3 open user decisions blocking lock — run decide.` Both are recommendations the decide lane builds itself, from counts it reads off the dashboard.
- **`goals/SKILL.md:177` (Bootstrap):** `"Overall OKR found (v<N>), no current phase. Run plan-phase <slug>?"`. This again recommends `R-no-phase`'s step outside the block. §2 carves out only first-time setup.

**To pass.**
1. Rewrite the TL;DR examples in `goals/SKILL.md` and `decide/SKILL.md` so they describe the state and name no command. For example: `TL;DR: No current phase is set.` Also say that the step to take is the next block's primary. `work/SKILL.md:148` ("time to consider closing the phase") should be checked the same way.
2. Remove the command prompt from the goals Bootstrap block for the no-current-phase case. Either defer to the next block, or add a carve-out to §2 like the first-time setup one.
3. Optional, not required to pass: `goals/SKILL.md:120` ("Suggests next `plan-phase`") and `goals/reference/phases.md:284` ("Suggest `/okr plan-phase <new-slug>`") are a lane suggesting a step after a subcommand. That is the `--after` closing step TASK-443 builds. Either mark them as awaiting TASK-443 or add a Proposed §7 question saying so.

### Not re-dispatched automatically

Third FAIL, each round on lane prose the previous review had not reached. The
code has been objectively verified at every round; the remaining gap is the
absolute § 2 sentence against every place a lane document names a step. Held
at `review` for the user to choose a round 4 with an enumerated bound, or an
override.

## Round 4 — 2026-09-16

> Agent tip `6d819989` (`99b03e9a` narrows the § 2 rule, fixes five TL;DR examples and the bootstrap annotation, marks the two after-subcommand lines, adds the § 7 question and the § 8 entry; `6d819989` records round 4). Cycle time 15 min. Bound: `USER-939`, the PMO's enumeration.

### PMO verification

1. **Base.** Merge base with main is still `ba11da7a`; main has since moved to `a2028df8` with PMO-document commits only. `git merge-tree` reports **no conflict**. 34 files against main, because main's own commits are counted.
2. **Full suite on the MERGE PREVIEW**, not the tip: a detached worktree at the branch tip with main merged in, `env -u PERRY_PROJECT -u PERRY_HOME bash tests/run` — `✗ test_diagnose.py — 1 of 158` · `✗ test_md_store.py — 8 of 76` · `✗ 2 of 142 MODULE(S) red` · `✗ 9 of 4009` · tree guard `✓`.
   - **The `test_diagnose` red is the PMO's, not the branch's.** `TestUserLoadFindings.test_perry_itself_passes_its_own_id_checks`: `['OQ-4','OQ-5','OQ-6','OQ-7'] != []`. Reproduced on main's own tip `a2028df8` alone. Commit `a959901c` (this file) quoted the reviews verbatim with their `OQ-` ids, and `ARCHITECTURE.md § 1` says an open question is cited by SECTION, never by id. Fixed on main by rewriting those citations; `tests.test_diagnose` is green alone afterwards. The same defect the branch hit at round 3.
   - `test_md_store` 8: `TASK-459`, dispatched 2026-09-16.

### Architecture review, round 4

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-16 · open-question ids written as `§ 7 (n)`

FAIL: §2 (lanes)

The narrowed rule does not match the lane documents as they now stand. §2 on the branch says that **in a standup's TL;DR and in its next-step position** a lane "does not add, drop or reorder a recommendation", and names four kinds of line outside it. Three lines sit inside the rule's own scope and in none of the four carve-outs:

1. **`work/SKILL.md:146`** — `TL;DR: BOARD is 240 lines, over the 200-line cap — triage before adding new work.` One of the five TL;DR examples in the same list; line 148 below it was fixed this round. `triage` is a `work` subcommand and this exact case is a declared rule — `R-board-over-cap`, `"command": "/perry work triage"` — so the TL;DR pre-empts a recommendation the next block owns.
2. **`decide/reference/decisions.md:310`** — "surface the migration suggestion in the standup's *next actions* list".
3. **`work/reference/digests.md:242,244`** — "include *drain inputs/ via /pmo digest* in the suggested next actions", and the stale-inputs line below it. `digests.md`'s dashboard block at 238–241 IS a row hint and legitimately outside the rule; 242 is a separate instruction about the next-actions list.

Meanwhile `reference/snapshot.md` step 5 and the three lanes' step 5/7 all now say "The command decides; never add, drop or reorder a recommendation", so the branch contradicts itself, not only § 2. This also falsifies the round-4 claim that the PMO's enumeration covered every case: **the PMO's list was built by grep and missed all three.**

**To pass:** either fix them — `work/SKILL.md:146` describes state, and the two reference pages hand the next-actions position to the next block or are marked as awaiting their rule, the way the `score-phase` row was marked — or ask the user to widen the carve-out, which is a change to the rule the user just settled and so is a question, not an agent's edit.

**Verified clean:** § 1, § 3, § 5 and § 6 are byte-identical to the merge base, section by section; the new § 7 entry is marked *Proposed*; the new § 8 entry claims no user confirmation; `bin/ARCHITECTURE.md § 1` genuinely carries no absolute lane rule to narrow; the four carve-outs are honest and bounded.

**Secondary inaccuracy, not the FAIL:** § 8 and the agent's result say "six TL;DR examples" changed; five did, plus the bootstrap-prompt annotation.

### Round 5 — queued

Three lines and one wording fix, and the reviewer states the set is closed.
Waiting on a dispatch slot: `claude-subagent` is at its cap of 2 with
`TASK-448` and `TASK-459`.

## Round 5 — 2026-09-16

> Agent tip `c585b252` (`d449d9e4` merges main `d597fdbc`; `5d053662` the three lane lines, the § 7 widening and the § 8 count; `c585b252` the round-5 result). Cycle time 12 min. Bound: the three lines round 4's review named, plus the six-vs-five wording.

### PMO verification

1. **Merge preview against main `1c23b9d3`:** no conflict; `env -u PERRY_PROJECT -u PERRY_HOME bash tests/run` on the merge result — `✗ test_md_store.py — 8 of 76` · `✗ 1 of 142 MODULE(S) red` · `✗ 8 of 4009` · tree guard `✓`. Only `TASK-459`'s reds.
2. Prose-only round: nothing under `bin/`, `reference/next-rules.json` or the tests changed, so rounds 1–3's mutation evidence still stands.

### Architecture review, round 5

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-16

FAIL: §2 (the lanes' next-step rule), with a consequent §7 defect.

**1. A line still sits inside the rule's scope.** `decide/reference/decisions.md § Sunset / expiration auto-check` (lines 231–235) tells PMO, at every standup, to `Suggest action: "ADR-NNN sunset date passed; run /perry decide adr --expire ADR-NNN …"`. Its first bullet is already the dashboard row hint, so the second is a separate next-step item, and none of § 2's four carve-outs covers it. It is ~70 lines above the line round 5 did edit in the same file.

**2. The reason the markings rest on is false.** The round's § 7 entry and result say no payload fact exists for the marked states. Two of the three do exist, and the PMO confirmed both in the branch's own `bin/perry-state`:

| Claimed missing | Actually |
|---|---|
| an expired ADR sunset | `expired_sunsets()` at `bin/perry-state:1524`, published under `decisions` at `:2259` |
| how many files sit in `inputs/` | `operations.inputs` / `inputs_oldest` / `inputs_oldest_days`, published at `:2274` and printed on `--dashboard` at `:3137` |

Only the third — an old-style `DECISIONS.md` migration — is genuinely factless. The review also notes `work/reference/digests.md:232` still says "surface in dashboard + suggest `digest`" ten lines above the line this round rewrote to say the opposite, so that page now contradicts itself.

**Verified clean by the review:** `ARCHITECTURE.md` §1, §3, §5 and §6 byte-identical to main; no §3 boundary crossed and no second reader; §5 contract versions untouched; the new §8 entry claims no user confirmation; every `OQ-` id appears only in §7's own headings; and `SKILL.md`, the three lanes and `reference/snapshot.md` are clean end to end — every standup step delegates to `--section next`, all six TL;DR examples describe state, and the bootstrap prompt and first-run order are correctly outside the rule.

### Round 6 — queued

Three items, and the fix is a reason rather than a rule. `DESIGN-020 § 5.3`'s
table is this row's Bound, and rules it does not name are out of scope — so the
answer is NOT to add `R-sunset-expired` or an inputs rule here:

1. Mark `decisions.md`'s sunset bullet the way lines 307 and `digests.md` 242/244
   are marked.
2. Correct § 7 and the result: the facts DO exist for the expired sunset and for
   `inputs/`; what is missing is a rule in `DESIGN-020 § 5.3`'s table, which this
   row may not add.
3. Reconcile `digests.md:232` with 242.

Waiting on a dispatch slot: `claude-subagent` is at its cap of 2 with `TASK-448`
and `TASK-459`.

## Round 6 — 2026-09-16 · PASS, merged

> Agent tip `79a660cb` (`0bffadac` merges main `1c23b9d3`; `5eba50eb` the sunset marking, the corrected reasons, the digests contradiction, § 7 and § 8; `fb5c6388` the round-6 result; `79a660cb` the re-pinned suppression line). Cycle time 18 min. Merged into main as `fb64e30b` (`--no-ff`).

### What round 6 changed

1. `decide/reference/decisions.md § Sunset / expiration auto-check`'s second bullet is marked like the other three; its dashboard alert bullet is byte-identical.
2. **The correction.** Every marking now names the fact that exists and gives the row's Bound — `DESIGN-020 § 5.3`'s rule table — as why no rule was written:

   | State | Fact | |
   |---|---|---|
   | sunset date passed | `decisions.expired_sunsets` | exists |
   | undigested `inputs/` | `operations.inputs`, `inputs_oldest`, `inputs_oldest_days` | exists |
   | stale digests | none — only per-card staleness under `roles.cards[].knowledge[]` | a fact is needed too |
   | `DECISIONS.md` migration | none | a fact is needed too |

3. `work/reference/digests.md`'s bullet no longer suggests `digest`; the dashboard row stays.

**A red of the agent's own, caught by its first full run and fixed:** `tests/test_procedures_call_the_tool.py` pins the line of a suppression in `decisions.md`, which the sunset marking moved 292 → 297. Red alone, so attributed to the branch. Only the pinned number and a comment changed.

### PMO verification

1. **Merge preview against main `363c5397`** — no conflict — `env -u PERRY_PROJECT -u PERRY_HOME bash tests/run` on the merge result: **`✓ all green`**, tree guard `✓`.
2. Prose and one pinned line only; nothing under `bin/`, `reference/next-rules.json` or the next-block tests changed since round 3, so rounds 1–3's mutation evidence stands. The PMO's own mutations from rounds 3 and 5 (`R-no-okr`'s command, `R-sla-breach`'s command, deleting `R-phase-heartbeat`) were red on named tests.

### Architecture review, round 6

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-16

**PASS.** It verified each stated fact against the branch's own code rather than the agent's table: `expired_sunsets()` at `bin/perry-state:1524` published at `:2254-2259`; `OpsCounts` at `viewer/parsers.py:1382-1386`, set at `:4570-4572`, published at `bin/perry-state:2274`; no digest-staleness count anywhere, only the per-card boolean at `bin/perry-state:1378`; and no `DECISIONS` reference in `bin/perry-state` at all. All four markings are correct, which is precisely what FAILed at round 5.

It read all eight lane and reference documents in full and found no line left inside the rule: the remaining command-naming lines are dashboard row hints, bootstrap prompts, first-time setup, or the two after-subcommand lines already marked and tracked in § 7. The one TL;DR still carrying an imperative names no command, so it is not a recommendation as `reference/next.md § Rendering` defines one.

It confirmed the test change is a re-pin only (292 → 297 plus a comment, the suppression untouched, and the arithmetic checks out), that `ARCHITECTURE.md` § 1, § 3, § 5 and § 6 have no hunks, that all five § 7 entries are marked *Proposed*, that the § 8 entry claims no user confirmation, and that `reference/next-rules.json` declares 18 rules and none for the four marked states.

### The record of six rounds

| Round | Verdict | What it was |
|---|---|---|
| 1 | FAIL § 2 | the ownership move was not recorded in the document |
| 2 | FAIL § 2 | `goals/SKILL.md` still recommended for itself; the heartbeat was dropped silently |
| 3 | FAIL § 2 | TL;DR examples and a bootstrap prompt still named steps |
| 4 | FAIL § 2 | three lines the PMO's grep-built bound missed |
| 5 | FAIL § 2 | one unmarked standup suggestion, and a marking reason the payload contradicted |
| 6 | **PASS** | verified fact by fact, documents read in full |

The code was objectively verified at every round; every FAIL was about the
document telling the truth about the code. Two process notes worth keeping: a
bound built by grep is not a bound (round 4), and an agent's "no fact exists" is
a claim to check against the payload, not to accept (round 5).
