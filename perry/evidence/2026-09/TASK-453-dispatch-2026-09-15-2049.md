# TASK-453 — dispatch record

> Date: 2026-09-15 · Executor: claude-subagent (async, isolated worktree) · Cycle time: 28 min (agent-reported)
> Branch: `coding/task-453-architecture-rules` · Base: `bb178072` · Tip: `abb5a9c2` · Merged: `ba11da7a` (`--no-ff`, primary checkout)
> Spec: `evidence/2026-09/TASK-453-spec.md` · Result: `evidence/2026-09/TASK-453-result.md`
> Decisions in force: `USER-935` (S1 lands as an expected failure), `USER-936` (S2 not done; NN-1's grep check too crude to become a test)

## Agent RESULT (verbatim)

```
=== RESULT ===
Branch: coding/task-453-architecture-rules
PR URL: n/a — push is escalated on this project
Files changed: 3
  - tests/test_architecture_rules.py
  - tests/durations.json
  - perry/evidence/2026-09/TASK-453-result.md
Tests: 3956/3966 (command: bash tests/run, Perry env vars confirmed unset) — the 10 failures are identical by name to the baseline's 10; new module alone 32 tests: 30 pass, 1 skip (S7), 1 expected failure (S1 live)
Cycle time: 28
Notes: Base check: HEAD 0b5bf99e was a strict ancestor of bb178072 with a clean tree; fast-forwarded and branched. I read ARCHITECTURE.md and bin/ARCHITECTURE.md in full, plus the spec, DESIGN-017 § 1.4/5.1/5.2/6 and USER-934/935/936. Baseline reds: test_md_store (9) and test_okr_krs_render (1), both reproduced alone, both on the live OKR.md rewritten by 15369956. S1 is an expected failure (USER-935, TASK-458). S2 is not done, by decision (USER-936). S3 is green and skips on 3.9 with a stated reason, with tests/fixtures excluded as data. S4 and S6 are green. S5 is green with a declared exempt list; state/ is flagged. S7 skips with the specified reason. All implemented mutations went red; S2's mutation stayed green by decision. The sandbox refused the scratch-derivation block, env -u and heredocs, so those were derived or run by hand. Commits: 9e3cf9d4, c9725643, abb5a9c2.
=== END RESULT ===
=== ARCHITECTURE COMPLIANCE ===
Touched sections: § 2, § 3, § 6 (read and encoded as tests; no document text changed)
Compliance check:
- § 2: S5 checks that every top-level directory and bin/ executable is named in a § 2 heading or in bin/perry list, or is exempt with a reason; § 2 itself is unchanged.
- § 3: S1 encodes "parsers imports nothing from bin/" exactly as written, as an expected failure per USER-935; the Forbidden lines are not edited, only hashed by S7.
- § 6: S4 encodes NN-5 and S7 encodes NN-6 without editing any rule; NN-1 has no test by decision (USER-936); the proposed Check: lines are in the result file only; the tests read typed facts only, consistent with NN-4.
New §7 questions opened:
- Is state/ (the diagnosis and adoption report templates) a component § 2 should name, or a confirmed exemption from S5? — recommended USER-id: new ask
- Does S3's "every import in tests/" include frozen fixture snapshots under tests/fixtures/ (two import the deleted gate module)? — recommended USER-id: new ask
=== END COMPLIANCE ===
```

## Objective verification, run by the PMO

1. **Base.** `git merge-base HEAD coding/task-453-architecture-rules` → `bb178072`, main's HEAD at dispatch. `git diff --stat bb178072 abb5a9c2`: 3 files, 1,097 insertions, 0 deletions — the three the RESULT names. None is in the spec's Out of scope (`TASK-454`, `TASK-455`, `TASK-456` surfaces).
2. **Full suite on the branch tip, re-run by the PMO** in the agent's worktree (clean, HEAD `abb5a9c2`, Python 3.11.15), `bash tests/run`:
   `✗ 2 of 141 MODULE(S) red` · `✗ 10 of 3966 TEST(S) failed` · tree guard `✓ nothing … moved`.
   The two modules are `test_md_store.py` (9 of 76) and `test_okr_krs_render.py` (1 of 40) — the same modules and counts as the agent's baseline at `bb178072`. Both were reproduced alone on main before this run; both read the live `perry/OKR.md` / `okr.jsonl` that `15369956` changed. `06e437ba` has since fixed `test_okr_krs_render` and one of the nine; the other eight are `TASK-459`.
3. **Control and mutations of the PMO's own**, each on a `git archive` copy of `coding/task-453-architecture-rules`, `python3 -m unittest tests.test_architecture_rules`:

   | Copy | Mutation | Result |
   |---|---|---|
   | control | none | `OK (skipped=1, expected failures=1)`, 32 tests |
   | S3 | `import numpy` appended to `viewer/parsers.py` | **red**: `viewer/parsers.py:5289 imports numpy` (S5 also red, because `bin/perry list` could not load) |
   | S6 | `bin/ARCHITECTURE.md` grown by 700 lines | **red**: `bin/ARCHITECTURE.md is 895 lines; the module-document cap is 600` |
   | S5 (a) | executable `bin/perry-unnamed-tool` added | **green — a finding, below** |
   | S5 (b) | executable `bin/stray-tool` added | **red**: `` `bin/stray-tool` is in no § 2 heading … `` |

4. **Scope of the durations change.** Two additions: a `test_architecture_rules.py` entry (0.78 s) and a `2026-09-15-task453` source entry recording how it was timed.

## Finding — S5's `bin/` half cannot fail for a `perry-*` tool

`perry_list_names` returns `payload["tools"] | set(payload["undeclared"])`, and
`bin/perry list` puts every `perry-*` file in `undeclared` by filename alone
(mutation S5 (a): `perry-unnamed-tool` appears under "not yet declaring a
surface"). So the rule as the spec words it — "appears in a § 2 heading or in
`bin/perry list`" — is satisfied by the name of any `perry-*` executable. It
catches directories and non-`perry-` executables only.

Narrowing it to `tools` (surface-declaring) would redden 13 tools at `ba11da7a`,
none of which `ARCHITECTURE.md` names as `bin/<tool>`: `perry-churn`,
`perry-codex-preflight`, `perry-context-budget`, `perry-decide`,
`perry-detect-host`, `perry-dispatch-limit`, `perry-explain`, `perry-goals`,
`perry-knowledge`, `perry-lint`, `perry-restore-check`, `perry-state-cost`,
`perry-update-check`. The agent implemented the spec as written; the gap is the
rule's wording in `DESIGN-017 § 5.2`, the same class as S2. Merged as is, with
this recorded; no row opened.

## Architecture review

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-15

PASS

I checked all 917 lines of `tests/test_architecture_rules.py`, the two added `tests/durations.json` entries and the result file against `ARCHITECTURE.md` on the branch, and none of them breaks a rule. The new module edits no document, imports nothing from `bin/` (it only runs `bin/perry list --json` with cwd set to the repository root, and that command writes nothing), writes only to temporary roots, and adds no store beyond the eight in § 4; S1 is encoded as § 3's allowed-directions line says, as a marked expected failure. Two things the coordinator hasn't seen and should raise as § 7 candidates, not failures: S7 hashes all of § 6, so an addition that NN-6 lets an agent make without asking ("may ADD to §6 … marked `proposed`") would turn it red (the hash also leaves out § 5's contract versions, which NN-6 makes the user's), and the `state/` exemption says "§ 2's list is the user's", when NN-6 calls § 2 descriptive and § 3's `Forbidden` line says an agent may not write the file at all, so the document contradicts itself here and the diff stays safe by editing nothing.

## Open for the user — four § 7 candidates, none blocking

1. `state/` — a component § 2 should name, or a confirmed S5 exemption? (agent; review adds that § 2 contradicts itself on whose list it is)
2. `tests/fixtures/` — inside S3's "every import in `tests/`"? Two frozen snapshots import the deleted `gate` module. (agent)
3. S7 hashes all of § 6, so an agent's NN-6-permitted `proposed` addition turns it red, and § 5's contract versions are outside the hash. `TASK-454` must settle the hashed text. (review)
4. S5's `bin/` half, above. (PMO)

## Subjective verification

(none — the spec declares none)

## Post-merge suite

`bash tests/run` (PERRY_PROJECT and PERRY_HOME unset) on `ba11da7a` in a clean
detached worktree under the PMO's scratch directory:

`✗ test_md_store.py — 8 of 76 test(s) failed` · `✗ 1 of 141 MODULE(S) red` ·
`✗ 8 of 3966 TEST(S) failed` · tree guard `✓ nothing … moved`.

One module fewer and two tests fewer than the branch tip. The difference is
`06e437ba` on main (the `OKR.md` retro reformat), not this merge. The eight
that remain are `TASK-459`. TASK-453 adds no red to main.

Cleanup: the PMO's scratch worktree was removed. The agent worktree
(`.claude/worktrees/agent-a435e369f332c0c74`) is locked by the harness
("claude agent … pid 1882"), so it and `coding/task-453-architecture-rules`,
which it has checked out, are kept until the harness releases them. The branch
is fully merged at `ba11da7a`.
