# TASK-459 — dispatch record

> Date: 2026-09-16 · Executor: claude-subagent (async, isolated worktree) · Cycle time: ~50 min (agent-reported)
> Branch: `coding/task-459-md-store-counts` · Base: `a2028df8` · Tip: `0f6bfc10` · Merged: `363c5397` (`--no-ff`)
> Spec: `evidence/2026-09/TASK-459-spec.md` · Result: `evidence/2026-09/TASK-459-result.md`

## What landed

Eight tests pinned this repository's live OKR counts and went red when `15369956`
added 4 `objective` and 14 `kr` records. Each pin is replaced by the relation it
stood for, read from the store the test itself loaded:

| Was pinned | Now derived |
|---|---|
| `10` objective records | `sum(1 for r in p.okr_records() if r["kind"] == "objective")` → 14 |
| `38` KRs | the `objective_id` fields the mint must move, off the unminted store → 52 |
| the minted set `{O-1…O-6}` | `{f"O-{n}" for n in range(1, len(titles)+1)}`, plus a contiguity assertion the base never had |
| the next id `"O-7"` | `f"O-{highest + 1}"` → `O-11` |
| the synthetic `"v4: 2026-10-01"` block | `LATER_VERSION = "v99: 2099-10-01"`, with its reason in a comment |

`perry/okr.jsonl`, `perry/OKR.md` and every other state file are byte-identical to
the base: the store moved, so the test was corrected and the store was not
(`NN-2`). `tests/durations.json` untouched — nine timed runs gave 4.5–5.1 s
against a recorded 4.9, so no move is attributable.

## The agent's own two findings, reported rather than hidden

1. It first asserted the KR↔Objective join in **both** directions; its own
   mutation (an Objective appended with no KRs yet) turned that red, which is
   exactly the fragility this row exists to remove. Narrowed to KR→Objective,
   with the reason in the test's comment.
2. **The spec's mutation M2 cannot reach the case it names.** Every case in
   `TestTheObjectiveIdIsMinted` runs `_unminted`, which strips every
   `objective_id` before the mint, so a store-level deletion is re-minted away.
   The agent added M2b and M2c, which do reach it. The spec was wrong; the row
   said so instead of quietly passing.

## Objective verification, run by the PMO

1. **Scope.** Two files against `a2028df8`: `tests/test_md_store.py` and the
   result. `git diff … -- perry/okr.jsonl perry/OKR.md` is empty. No `bin/`,
   `viewer/`, `schema/`, `durations.json` or other test module.
2. **Merge preview against main `1c23b9d3`** — no conflict — then
   `env -u PERRY_PROJECT -u PERRY_HOME bash tests/run` on the merge result:
   **`✓ all green`**, tree guard `✓`. The first fully green suite since
   `15369956`.
3. **PMO mutations**, each on a `git archive` copy of the branch:

   | Mutation | Result |
   |---|---|
   | one `kr` record's `objective_id` removed | **red**: `test_the_shipped_store_carries_an_id_for_every_objective` |
   | an `objective` + its `kr` appended to `okr.jsonl` only, then `perry-okr render --write` | **red** (4), `diff's census disagrees with the store on disk about how many objective records it holds` |

   The second is **correct behaviour, not a pin**: `render --write` says outright
   it cannot restore missing lines, so the copy holds a store and a document that
   disagree — which is what those cases exist to catch. The agent's own M1a,
   which appends to both, stays green. The acceptance criterion in the spec —
   "a fifth objective can be appended without reddening" — holds for a
   consistent repository, and the row does not hide an inconsistent one.

## Architecture review

> Executor: `Agent(general-purpose)`, synchronous, isolated worktree · 2026-09-16

PASS. It checked all eight cases one by one for weakening — the central risk of
this row — and found none: the census cases now compare the subprocess against
an independent read of the copied file and still fail on a miscount; the mint set
still reddens on a per-record mint, a gap, a restart or a shared id, at any store
size; the byte gate's subtraction stays exact and the `returncode == 3` contract
survives; case 7 replaced a bare fixture guard with a real premise and claim.
No live count remains pinned anywhere in the module. State files, `bin/`,
`viewer/`, `schema/` and `durations.json` are byte-identical; `NN-1`, `NN-4`,
`NN-5`, `NN-6` and §5's contract versions are untouched; no §7 question is owed.

**Three non-blocking observations**, recorded here rather than sent back:

1. `tests/test_md_store.py:1428` — `assertEqual(removed, sum(1 for r in records if r["kind"] == "objective"))` is a tautology by construction, so that line cannot fail. The adjacent `assertTrue(removed)` and the load-bearing `len(out["lines_verbatim"]) == removed` keep the case honest; the line asserts nothing.
2. Two stale comments the row's own theme would have swept: the `TestTheObjectiveIdIsMinted` docstring still says `O-1 … O-6` on "all ten Objectives" (`:1576`, `:1579`), and the message at `:1449` still says "the ten headings".
3. The result file attributes its full-suite run to `177937e5`, which is not on the branch — the pre-amend tip. `git diff 177937e5 0f6bfc10 -- tests/` is empty, so the figure holds for the branch's code.

## Subjective verification

(none — the spec declares none)
