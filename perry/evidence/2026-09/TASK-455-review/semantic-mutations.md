# Independent procedure mutations

Only the explicitly authorized isolated review checkout was mutated. Both mutations target exact numbered lines in work/reference/dispatch.md at 779e74675515a6d669e4c8317c3f583201d86ff2. No Python logic was changed; Python cache invalidation is therefore inapplicable.

1. Pointer loss: replace line 495's instruction to use review.md § Integration architecture reviewer brief with “review the candidate.” Exact diff: pointer-mutation.diff. Independent semantic verdict: FAIL. The dispatch step no longer directs its executor to the normative brief carrying rule-by-rule answers and the concrete output contract (review.md:483–530). A reviewer following that shortened step can miss the required brief. Other pack references do not restore this required direct connection. This is the bounded pointer mutation required by review.md's negative checks. No claim that a lexical guard understands this omission. Restore: pointer-restore.json verifies bytes against git show of the tested head, not against a cached baseline. Restored semantic verdict: PASS; the explicit brief reference is present again.

2. Reverting fix / author self-attestation: restore line 331 to exactly the corresponding Claude Build prompt line at base 3034c249b8f116c5e1ae975685bbf9358a4471b2. Exact diff: self-attestation-mutation.diff. Independent semantic verdict: FAIL. A supported author is again told to inject the whole architecture and return its own mandatory compliance block, contradicting acceptance criterion 3 and the independent role boundary at dispatch.md:456–457,499–501. This is the same procedure regression the existing governed-region hash must detect. One affected-tier run is retained in mutation-affected.log. The runner checks lexical/pinned contracts, not semantic meaning.

Restoration and targeted confirmation are recorded in restore.json and restored-targeted.log. Both mutated behaviors are rejected by this fresh reviewer; no test-only semantic authority is claimed.
