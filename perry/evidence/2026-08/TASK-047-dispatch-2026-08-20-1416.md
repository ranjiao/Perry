# TASK-047 — dispatch record

> Date: 2026-08-20 · Executor: claude-subagent · Async: yes
> Cycle time: ~55 min (agent-reported)
> Worktree: `.claude/worktrees/agent-af6e5e9d7ae3c3f2d`
> Branch: `coding/task-047-conformance-enforce`, based on `feat/work-modes` (f58394a)
> PR: https://github.com/ranjiao/Perry/pull/8

## The high-stakes override this dispatch ran under

The pre-flight safety scan matched `state-schema.json` against
`.perry/hook.md § High-stakes operations` ("The claim surface"). The user was
shown that the match was substantive rather than a substring artifact, was told
the flip changes upgrade behaviour for every existing Perry project and not only
this one, and cleared it — limited to the conformance default value, with no
entry in `claims[]` to be touched. Recorded in `TASK-047-spec.md § Changes`.

**The limit held, verified rather than accepted:**

| Boundary | Check | Result |
|---|---|---|
| `claims[]` untouched | `git diff f58394a..<branch> -- schema/state-schema.json \| grep -i claim` | **no lines** |
| `state-schema.json` limited to the default | `git diff --stat` on that path | **1 file, 1 insertion, 1 deletion** — the conformance `note` only |
| `perry/` untouched | `git diff --stat f58394a..<branch> -- perry/` | **empty** |
| No file declared on the user's behalf | `perry-conform status` | same 3 undeclared, same 13 conformant |

## Objective verification — run by the PMO in the agent's worktree

| Spec item | Command | Result |
|---|---|---|
| V4.5 `perry-lint` | `python3 bin/perry-lint --root .` | clean — 103 records, 0 drifted |
| V4.5 `perry-conform status` | same verdicts as before the change | **13 conformant, 3 undeclared** — unchanged, and `gate: enforce` |
| V4.6 full suite | `python3 tests/parallel` | **1693/1695** · 58 modules · 132.2s · 2 modules red |
| V4.6 `git diff --check` | | clean |

**The two red modules are pre-existing, verified independently.** The agent
claimed it, but its earlier sibling made the same claim with a vacuous method,
so it was re-measured here on a detached worktree at `f58394a` with none of this
change present:

```
test_router_budget  FAIL  SKILL.md: 21036 bytes > 20480 cap
test_board_render   FAIL  cells_verbatim {'Depends on': 1} != {}
```

Both fail on the clean base. `SKILL.md` was **21036** bytes at `f58394a` and is
**21030** on this branch — the agent's edit made it 6 bytes shorter, so it did
not cause the overrun and slightly reduced it. The file is 550 bytes over its
cap and that is a standing problem this task did not create.

**Why the main checkout showed only one red earlier.** The uncommitted
working-tree changes present since before this session mask both: `SKILL.md`
there is 20416 bytes, under the cap, and `tests/test_board_render.py` is
likewise modified. Meanwhile the PMO's own triage prose in `perry/` trips
`LOAD-03`, which the agent's worktree does not contain. Three different red
sets across three trees, all explained by uncommitted state — worth knowing
before anyone reads a suite result as a property of the branch.

## Scope cross-check

Spec `Files in scope`: `bin/perry-conform`, `schema/state-schema.json` (default
only), the writers that consult the gate, focused conformance tests.

**In scope**: `bin/perry-conform`, `bin/perry-task`, `bin/perry-goals`,
`bin/perry-decide`, `schema/state-schema.json`, `tests/test_conformance.py`.

**Outside the declared list, and defensible**: `SKILL.md`, `reference/config.md`
and `bin/README.md` each contained a sentence stating the gate is advisory,
which the flip makes false. Leaving them would ship documentation contradicting
behaviour. This is the spec's `Files in scope` being incomplete, not the agent
exceeding it — an amendment worth making to the spec, not a violation.

**Outside the declared list, and worth a reviewer's eye**: `tests/gate.py` (new)
plus fixture opt-outs in 14 suites. Read it before judging — the reasoning is
written down there and holds up: after the flip every undeclared fixture write is
correctly refused, and a large share of those fixtures are *deliberately*
malformed (`test_prioritize` widens a board with 4 shape errors; `test_row_integrity`
corrupts rows on purpose), so `perry-conform declare` refuses them by
construction. The opt-out uses `- Conformance gate: advisory`, the documented
user-facing control, per fixture. Both gate branches, both precedence paths and
both exemptions remain exercised in `tests/test_conformance.py`, and
`tests/test_work_modes.py` still declares for real rather than opting out.

## The decision this dispatch does NOT settle — read this one

The spec's `Subjective verification` line was: *whether `enforce` is the right
default now, versus keeping `advisory` and enforcing only under an explicit
opt-in.* The agent found the repository had **already attempted this flip on
2026-08-18** and recorded two blockers in `bin/README.md § The switch-over
checklist`. It re-measured both against the landed TASK-044 rather than trusting
the note. **Both are still live:**

1. **Migration still does not reach zero on a real board.** On a copy of
   `~/proj/gimegime-pmo`, `BOARD.md` goes 3 errors → 1. The residue is a
   `Status` cell reading `半解`, which migration deliberately refuses to coerce.
   Much improved — the note recorded 59→15 project-wide with the board
   byte-identical — but the class survives.
2. **A brand-new zero-error project is still `undeclared`**, so its very first
   write asks the user for one `perry-conform declare`.

It flipped anyway, arguing the gate made itself conditional on *a road existing*
rather than on the road being paved end to end, and that both halves of the
population now have one the refusal names. It pinned both costs as tests that go
red if either is fixed, and states that reverting is one line.

**That argument is the user's to accept or reject.** It is exactly what the
subjective-verification line exists for, and it is why this row goes to `review`
and not to `done`.

## Two latent bugs found in passing

Both were unreachable while `gate.ok` was always true under `advisory`:
`perry-goals` would have double-printed under `enforce` + `--migrate`
(announcing the exemption *and* calling itself advisory), and all three writers
hardcoded the literal string `"advisory"` in the write-through message.

## Architecture review

**Skipped** per the skip conditions: `Touches architecture: (none)` and the
primary's `Touched sections` empty. The primary nonetheless named the thing
worth naming — this change alters no rule and no file class; it *activates* a
rule that was decided in ADR-004, fully implemented, and shipped switched off.

## Status

`review`. Not `done`: the subjective verification above is unanswered and the
branch is unmerged.
