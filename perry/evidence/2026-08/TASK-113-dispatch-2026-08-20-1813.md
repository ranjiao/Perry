# TASK-113 — dispatch record

> Date: 2026-08-20 · Executor: claude-subagent · PR: https://github.com/ranjiao/Perry/pull/18
> Branch: `coding/task-113-checks-read-live-state` · Cycle time: ~110 min, including a ~10-minute stall
> 4 files: `bin/perry-diagnose`, `tests/test_diagnose.py`, `tests/test_one_line_break_rule.py`, `tests/test_v5_signoff.py`

## The suite is green for the first time today

**63 modules · 1864 tests · 0 red**, verified by the PMO in the agent's own
worktree. `git diff -- perry/` is empty, which was the row's hard bound: the
signed V5 record naming `REL-00` was never edited, and the fix is in the checker.

## Five instances, not three

The three dispatched, the fourth handed over mid-run, and a **fifth the agent
found itself**: merging the moved base locally surfaced `DESIGN-900`, charged as
dangling because a V4 review under `perry/evidence/` **demonstrates a check going
red** by quoting a probe document. Same defect as `REL-00`, one document over —
prose *about* a check counted as the thing the check measures. Fixed in the same
rule rather than patched separately.

## Not vacuous — verified independently in both directions

The PMO re-ran the anti-vacuity case rather than accepting it:

```
a temp project whose only content is "Blocked on ZZZ-404 until Friday."
  → dangling: ['ZZZ-404']   ✅ LOAD-02 fires

this repository
  → dangling: []   dangling_in_reports: ['DESIGN-900', 'REL-00']
```

The two exempted ids are **visible, not deleted**. And the deliberate cost is
named and tested by the agent: bare prose reading `REL-00 is gone`, carrying none
of the three structural marks — inside a signed record, inside a blockquote, or
in a paragraph naming a check — **still counts**.

## Each check proved to still fire, by reintroducing its defect

Not "a test exists". Four mutations of a V5 record, each with its actual message:
heading renamed → *"'accepted' is not a disposition this format defines"*;
provenance rewritten; signer blanked → *"the signer's name is gone"*; an item
deleted → caught by journal↔event count reconciliation. Disabling the value→flag
match in `bin/perry-task` reproduces the original anonymous refusal and reddens
the flag test. Relaxing `split_dangling` to always-exempt reddens 4 of 11.

It also added `test_the_fixture_row_is_reachable_before_the_refusal_is_asserted`,
which asserts the row is writable **before** asserting the refusal — because a
vanished fixture and a broken refusal look identical from outside, and that is
exactly how the original red misnamed its own cause for hours.

## Whether TASK-108's move applied — the answer is better than yes or no

*"Not literally, and `bin/perry-diagnose` says so where the rule is written."*
LOAD-03 switched from mentions to records because a decision has identity only in
a register row. LOAD-02 **already** counts one entry per id — `REL-00` was
reported once for three mentions — so deduplicating buys it nothing. What
transfers is the move underneath: **ask what the number is for, and count only
what the answer admits.** LOAD-03's number is *questions waiting on a person*;
LOAD-02's is *codes a reader cannot look up*, so only a line asking a reader to
look one up counts. Both replace a filter on which prose with a rule about what
the measurement means — which is why neither adds a sixth carve-out.

## Verification 4 — the sweep, which is the row's real deliverable

A two-stage AST pass over `tests/test_*.py`: stage 1 finds tests reading live
project state, following one level of `setUp`; stage 2 keeps those asserting
against a **literal**, excluding exit codes. **70 set aside · 137 read live state
· 52 assert a literal over it (60 assertions)**, hand-triaged to four real hits:

| test | what it asserts | breaks when |
|---|---|---|
| `test_risks.py:506` | `len(cleared) == 1` **and** a specific date, over the live Top risks table | a second risk is cleared — **the highest-risk one left** |
| `test_md_store.py:127` | `len(krs) > 20` over the live `OKR.md` | KRs are retired at a phase boundary — **literally the `rows_from_store > 20` shape `c9018ae` fixed**, on a different file |
| `test_track_attribution.py:172` | the track list equals exactly `['main']` | a second track is declared |
| `test_one_primitive.py:150` | exact file list under `bin/lib/` | any file is added — **deliberate**, its docstring says so |

For `test_risks.py` the agent notes the property it actually wants — *a cleared
risk leaves the open set* — is already the third assertion in the same test and
needs no count. For `test_md_store.py`, the line above already asserts the real
property, so the `> 20` is a redundant bound whose threshold is a snapshot.

## It caught itself

One of the sweep's hits was **its own new test**:
`test_perrys_own_repository_reports_the_exemption_it_used` originally asserted
`dangling == []` on the live repo — this row's exact defect, committed by this
row. It now asserts only that `REL-00` moved between the two lists.

## Close

`done` at **V3** — reproducible run, and the anti-vacuity case re-run by the PMO
rather than accepted on report.
