# TASK-115 — dispatch record

> Date: 2026-08-20 · Executor: claude-subagent · PR: https://github.com/ranjiao/Perry/pull/17
> Branch: `coding/task-115-readonly-guard-coverage` · Commit `217ceb2` · Cycle time: ~28 min

## Closed with tests only

`bin/perry-migrate` is **byte-identical** — `git diff 57ba64b..<branch> -- bin/perry-migrate`
reports **0 changed lines**. The row's premise was that nothing was broken and
only the guard was missing, and the diff says exactly that: one file,
`tests/test_migrate.py`, +112 / −4.

## The two mutations now die — reproduced independently by the PMO

Not taken from the RESULT block. Injected into a copy of the worktree's
`bin/perry-migrate` and run:

```
"observed": f"read-only for its owner; you should chmod it. {READ_ONLY_MECHANISM}",

FAIL: test_the_json_wording_states_what_was_observed_too
AssertionError: 'should' unexpectedly found in '{"mode": "0444", "observed":
  "read-only for its owner; you should chmod it. the write is a rename over
  this path, ..."}' : the JSON override for BOARD.md takes a position
Ran 133 tests — FAILED (failures=1)
```

Restored afterwards; `git diff --stat bin/perry-migrate` clean. This is the
mutation that was **silent** before this row — the one the task's own fourth
deliverable most depended on.

The agent reports the second mutation — deleting
`read_only_mode=owner_read_only(store_path)` from `_plan_task_store` — now fails
two tests, and re-ran the four the earlier reviewer had already caught,
confirming each still dies. All six accounted for.

## It avoided the trap the brief named

The prompt warned that a banned-word guard needs one list, not two, or it becomes
the same defect this project has hit repeatedly. `POLICY_WORDS` is defined once
(`tests/test_migrate.py:2190`), read by both guards (`:2238`), with a comment at
`:2382` saying so in as many words: *"one `POLICY_WORDS` above rather than a copy,
so the two surfaces cannot"* end up guarded against different lists. The eight
words are unchanged, so none of the ten original tests was weakened to make room.

## A fixture note worth keeping

The store test needed a `tasks.jsonl` **waiting to be rewritten**, or there is no
override to report. The agent writes the store with the shipped
`perry-tasks write --from-board`, then ages it to a pre-TASK-106 shape by
dropping the `summary` key. That record stays valid, projects identically to the
board so nothing is refused, and acquires the explicit empty summary at the
validation boundary — so migration plans the canonical rewrite. `BOARD.md` keeps
an ordinary mode there, making the store the only file in the run with the bit
cleared. Constructing the precondition out of the project's own history rather
than hand-writing a store is what makes that fixture trustworthy.

## Objective verification

`tests/test_migrate.py` 130 → **133**, green. Full suite 63 modules / **1849**
tests with the same 3 pre-existing reds — `test_diagnose`,
`test_one_line_break_rule`, `test_v5_signoff`, the last two being what TASK-113
is fixing right now. `perry-lint` 0 errors / 3 warnings, `git diff --check` clean.

The agent corrected the brief on its own baseline: it expected `test_host_support`
as the third red and found `test_diagnose` instead, noting that it inspects
Perry's own `perry/` state, which other agents were changing concurrently. That
is the right reading.

## The PR shows more than it changed, and the agent said so rather than acting

PR #17 displays 9 inherited commits and their `perry/` files because remote
`feat/work-modes` is behind the local tip. The agent named it and explicitly did
not push someone else's branch to tidy its own PR. That restraint is correct: the
files drop off the moment `feat/work-modes` is pushed, which is the PMO's to do.

## Close

`done` at **V3** — reproducible run, command and output re-runnable, and the
central one re-run by the PMO rather than accepted on report.
