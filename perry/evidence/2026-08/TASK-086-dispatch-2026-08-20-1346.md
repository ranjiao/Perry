# TASK-086 — dispatch record

> Date: 2026-08-20 · Executor: claude-subagent (re-routed from codex) · Async: yes
> Cycle time: ~35 min (agent-reported)
> Worktree: `.claude/worktrees/agent-aa62958e04053113e`
> Branch: `coding/task-086-lint-ns01` · Commit: `5865212`
> PR: https://github.com/ranjiao/Perry/pull/7

## Verdict — the work does not apply to this branch

**The dispatch is a failure, and the agent is not at fault.** Its worktree was
created from `f3c6712`, which is `main`. The working branch is
`feat/work-modes` at `f58394a`, **279 commits ahead**. The agent therefore
edited a `bin/perry-lint` that predates, among much else:

```
34b08d9 feat: make task store the write target
2af97e4 feat(tasks): read canonical task store
3750451 fix(lint): make store drift observable
16df2cf fix(lint): report terminal store-only drift
```

`git merge-base --is-ancestor f58394a coding/task-086-lint-ns01` → **not an
ancestor.** Every verification the agent ran — including its central claim that
default lint output is byte-identical on a collision-free project — was measured
against that older binary, so none of it carries over. PR #7 cannot be assessed
on its merits until it is rebased onto `feat/work-modes` and re-verified.

This is a PMO defect, not an executor one. `work/reference/dispatch.md` says a
subagent shares the parent cwd, and `git-boundaries.md` tells every dispatched
agent to cut its own branch; when those collide the fix used here was worktree
isolation, and the isolation was taken from the default branch rather than from
the branch the work was based on. Nothing in the dispatch procedure says which
base a worktree gets, which is exactly why it went unnoticed.

## Agent RESULT block, verbatim

```
=== RESULT ===
PR URL: https://github.com/ranjiao/Perry/pull/7
Files changed: 2
  - bin/perry-lint
  - tests/test_lint_namespace.py
Tests: 195/195 (command: bash tests/run — includes `python3 -m unittest discover -s tests`,
17 of them new; plus `python3 bin/perry-lint` exit 0 and `git diff --check` clean)
Cycle time: ~35 minutes
Notes: `tests/parallel` from V2 step 6 does not exist in this repo (not on this branch, not on main)
— substituted the unittest discovery `tests/run` actually invokes. No file under `perry/` was touched.
The sample fixture's two pre-existing false-positive collisions are now visible in default lint output;
pinned by test and filed as a follow-up rather than fixed, since fixing them would move `--claims`
output, which deliverable 5 forbids. `--strict` no longer promotes `NS-01` — a behavior change to
`--strict` I judged required by deliverable 2 plus DESIGN-002's "a collision never sets a non-zero
exit", flagging it explicitly in case the reviewer scopes it differently.
=== END RESULT ===

=== ARCHITECTURE COMPLIANCE ===
Touched sections: (none)
Compliance check:
- No architecture document in this project; the governing locked decision is DESIGN-002, and this change
  implements its decision #4 row for `perry-lint` default mode without altering any locked decision.
- One design-level invariant is adjacent and worth naming rather than papering over: `--strict` previously
  meant "every warning is a failure", and after this change it means "every warning except `NS-01` is a
  failure". I read that as mandated by deliverable 2 and by DESIGN-002 § Changes, but it is a change to a
  tool-wide contract, not just an added finding, so the reviewer should confirm it.
New §7 questions opened:
- Should `looks_like_perry_state` / `_perry_shaped` recognise files Perry wrote under names outside its
  regex list (`evidence/REL-001-spec.md`, `inputs/vendor-api.md`)?
- DESIGN-002 § 8 left open "Is `NS-01` a diagnose finding, a lint finding, or both?", leaning both. This PR
  makes it both in fact. The catalog entry in `reference/diagnose.md` still reads as diagnose-only.
=== END COMPLIANCE ===
```

## Objective verification — not run

Spec V2 step 6 requires `python3 tests/parallel`. **The agent's report that this
file does not exist is true of its worktree and false of this branch**:
`tests/parallel` is tracked, present on `feat/work-modes`, and absent from
`main` — which is precisely the evidence that the worktree came off `main`. It
ran on this branch during the TASK-079 verification an hour later: 58 modules,
1700 tests.

Running the spec's verification against PR #7 as it stands would measure the
wrong tree, so it was not run. Nothing here is a judgment on the change's
quality.

## Three things the agent raised that survive the rebase

These are about the task, not the base, and are worth deciding before a re-run:

1. **`--strict` changes meaning.** It stops promoting `NS-01`. The agent argues
   DESIGN-002's "a collision never sets a non-zero exit" plus deliverable 2
   require it. That is a tool-wide contract change the spec did not authorise in
   so many words, and it flagged it rather than burying it — correctly.
2. **Two false-positive collisions become user-visible.** `sample-project`'s
   the two `REL-` spec files under `evidence/2026-08/` and `inputs/vendor-api.md` are files
   Perry itself wrote that the shape check does not recognise. Not new —
   `perry-diagnose` has emitted them since it shipped — but default lint puts
   them in front of every user on every run. DESIGN-002 § 7 anticipates this
   misfire.
3. **The catalog entry reads as diagnose-only.** A one-sentence doc follow-up,
   correctly left out of scope.

## Architecture review

**Skipped**, per the skip conditions: spec declares `Touches architecture:
(none)` and the primary's `Touched sections` is empty. Note that the primary
nonetheless named an adjacent tool-wide contract change, which is the behaviour
the compliance block exists to elicit.

## Status

`review`, with a `wrong-base` annotation. No auto-retry, per the failure-handling
rule. The re-dispatch, if the user wants one, must pin the worktree base to
`feat/work-modes`.
