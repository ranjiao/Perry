## PMO Status — 2026-08-28

> The first weekly report this project has written. `perry-state` reports
> `history.latest_weekly: ""`, so there is no prior week to compare against and
> every number below is a first reading rather than a delta.

### Summary

- **Overall status**: `on_track`
- **Main progress**: **31 rows closed this week**, 27 of them today. Every one of
  the five stores `claims[]` declares now exists — three were created today —
  and the phase-KR id migration landed with no compatibility left behind.
- **Main risk**: **the intake register is 58 undischarged and nobody drains it.**
  Its oldest entry is 2026-08-21, seven days old. Roughly half were filed today
  by the PMO, all from real findings. A queue that only grows stops being a
  queue.
- **User decisions needed**: **0 open asks.** `USER-903` was answered today. The
  twelve design decisions that were open this morning are resolved and
  DESIGN-009/010/011 are locked.
- **Cost ceiling status**: not tracked on this project — no ceiling is declared
  in `.perry/config.md`.

### P0

- **[TASK-050] `not_started`, startable** — "One normalization for a header cell,
  not two". Unblocked today when TASK-094 closed after eight days at `review`.
  Four V4 review rounds already exist for it; its own note says the fifth should
  be a mutation harness, not another regex. **Next action**: read the four rounds
  before writing a spec — it was not dispatched today for exactly that reason.
- **[TASK-067] `blocked`** — "The writer can destroy the table it writes to, and
  `perry-lint` cannot see it". Genuinely blocked on TASK-095, which is now
  startable. **Next action**: TASK-095 first.

### P1

31 open. The ones that moved or are moving:

- **[TASK-202] in flight** — the hook side of the escalation union has no
  not-extractable check. Measured on `~/proj/gimegime-pmo`: **5 bullets → 3
  fragments, three contribute nothing, unwarned**, including the one naming
  `max_daily_loss` and `max_position` on a project handling real money.
- **[TASK-197] merge-check running** — `## User Input Queue` becomes a store.
  Byte gate load-bearing here (a duplicate `USER-` id collapses two rows),
  unlike intake where it was a tautology.
- **[TASK-204…207]** — opened from aiMark round 5, all four verified here before
  filing. **[TASK-206]** (`seq` on a write result) and **[TASK-207]**
  (`--if-seq` compare-and-set) are the two the front-end is actually blocked on.
- **[TASK-077] startable** — DESIGN-006's pass condition. **Do not run it yet**:
  `Files in scope` / `Deliverable` / `Out of scope` gained `zh` aliases only
  today (TASK-201), and before that a Chinese-headed spec scanned clean. The
  target project's document language is 中文.
- **[TASK-095, TASK-097, TASK-129, TASK-155, TASK-173, TASK-177, TASK-179]** —
  startable, none dispatched today.

### Decisions Needed

- **Decision**: does the intake register get drained, or does the PMO stop
  filing into it?
  **Why it matters**: 58 undischarged, ~half filed today, every one a real
  finding. Both answers are defensible; continuing to file without draining is
  not. `/perry work triage` is the drain.
  **Deadline**: none, and that is the problem.

- **Decision**: `perry/intake.jsonl` and `perry/asks.jsonl` imports.
  **Why it matters**: both stores are built and byte-gated; neither import has
  been run, because the import is the user's, as it was for risks and config.
  `perry-lint` will keep reporting them as unchecked until it is.
  **Deadline**: none — the tools are correct either way.

- **Decision**: four phase-KR ids in TASK-180's migration were resolved on a
  date-and-batch prior alone, with the KR title confirming neither phase.
  **Why it matters**: the agent offered to revert them to unresolved. My reading
  is that the original attribution was loose rather than the phase ambiguous.
  **Deadline**: none; reverting later costs the same as reverting now.

### Next 3 Actions

1. **Merge TASK-197 and TASK-202**, then close them. Both are near done.
2. **`/perry work triage`** — drain the intake register, or decide it is not a
   queue. It is the largest un-owned number on this board.
3. **Read TASK-050's four V4 rounds** and write its spec. It is the oldest P0
   and the only one that is startable.

---

### Two things the dashboard reported that are worth naming

**`BOARD.md`'s `Last updated` header reads `2026-08-16 (21st pass — DESIGN-004
handed off, 6 tasks)`** while the board has been re-rendered dozens of times
today. `perry-state` publishes it as `board.last_updated` and nothing refreshes
it. Filed.

**`perry-dispatch-limit` never registered a single slot this session.** The
subcommand is `register <task-id> <executor>`; the PMO called `acquire`, which
is not one, so the tool printed usage and exited 0 every time. **The concurrency
cap was enforced by nothing but the caller's own counting.** It happened to be
correct — never more than two — but the protection was zero, and this is the
same tool that reported two dead agents as running on 2026-08-27. Filed.

### One warning from `perry-state`

```
P002-O2-KR2 — a KR `current` was asserted before a linked task moved
              and can no longer be trusted; re-measure the metric
```

Note the id: the migration is live in the payload, not only in the files.
