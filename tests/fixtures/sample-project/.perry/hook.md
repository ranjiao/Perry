# Perry hook — Sample Project

## High-stakes operations

- Production deploys — `deploy`, `release`, `promote`, `production`, `prod`
- Credentials and secrets — `.env`, `secrets`, `credentials`, `token`, `apikey`
- Destructive data operations — `DROP TABLE`, `migrate --down`, `rm -rf`
- Git history rewrites — `push --force`, tag deletion

## Project specifics

If the project is **Sample Project**:
- Roadmap source-of-truth: —
- Decision tag types: Process | Architecture | Tooling
