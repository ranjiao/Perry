# Auto-update timestamp written by bin/perry-update-check
.update-check

# perry-task's write lock — lives beside BOARD.md because that is what it
# protects, and is pure runtime state with nothing to record.
.board.lock

# macOS
.DS_Store
.gstack/

# Python bytecode from bin/ + viewer/
__pycache__/
*.pyc
